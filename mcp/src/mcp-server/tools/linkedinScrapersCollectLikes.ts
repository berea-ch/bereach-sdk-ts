/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinScrapersCollectLikes } from "../../funcs/linkedinScrapersCollectLikes.js";
import { CollectLinkedInLikesRequest$zodSchema } from "../../models/collectlinkedinlikesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CollectLinkedInLikesRequest$zodSchema,
};

export const tool$linkedinScrapersCollectLikes: ToolDefinition<typeof args> = {
  name: "linkedin-scrapers-collect-likes",
  description: `Scrape LinkedIn post likes

Authenticates the requester, validates LinkedIn credentials, and returns up to 200 profiles per page that reacted to the specified post. Supports pagination.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinScrapersCollectLikes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
