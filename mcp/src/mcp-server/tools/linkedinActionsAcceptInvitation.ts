/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinActionsAcceptInvitation } from "../../funcs/linkedinActionsAcceptInvitation.js";
import { AcceptLinkedInInvitationRequest$zodSchema } from "../../models/acceptlinkedininvitationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AcceptLinkedInInvitationRequest$zodSchema,
};

export const tool$linkedinActionsAcceptInvitation: ToolDefinition<typeof args> =
  {
    name: "linkedin-actions-accept-invitation",
    description: `Accept a LinkedIn connection invitation

Accepts a pending connection invitation. Requires the invitationId and sharedSecret obtained from the list invitations endpoint. No daily/weekly cap — only a 5-second minimum interval between calls. Costs 1 credit per request.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await linkedinActionsAcceptInvitation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
