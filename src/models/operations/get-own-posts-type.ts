/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type FollowProfileRequest = {
  /**
   * Accepts full LinkedIn profile URLs (e.g. linkedin.com/in/username), vanity names (e.g. john-doe), or profile URNs (e.g. urn:li:fsd_profile:ACoAAA...).
   */
  profile: string;
};

export type FollowProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type FollowProfileMeta = {
  credits: FollowProfileCredits;
};

/**
 * Profile followed
 */
export type FollowProfileResponse = {
  success: true;
  message: string;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: FollowProfileMeta | undefined;
};

export type UnfollowProfileRequest = {
  /**
   * Accepts full LinkedIn profile URLs (e.g. linkedin.com/in/username), vanity names (e.g. john-doe), or profile URNs (e.g. urn:li:fsd_profile:ACoAAA...).
   */
  profile: string;
};

export type UnfollowProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UnfollowProfileMeta = {
  credits: UnfollowProfileCredits;
};

/**
 * Profile unfollowed
 */
export type UnfollowProfileResponse = {
  success: true;
  message: string;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UnfollowProfileMeta | undefined;
};

export type EditPostRequest = {
  /**
   * LinkedIn post URL or URN
   */
  postUrl: string;
  /**
   * New text content for the post
   */
  text: string;
};

export type EditPostCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type EditPostMeta = {
  credits: EditPostCredits;
};

/**
 * Post edited
 */
export type EditPostResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: EditPostMeta | undefined;
};

export type EditCommentRequest = {
  /**
   * urn:li:fsd_comment:(COMMENT_ID,urn:li:ugcPost:POST_ID) - from comment API response
   */
  fsdCommentUrn?: string | undefined;
  /**
   * Legacy comment URN format
   */
  commentUrn?: string | undefined;
  /**
   * New comment text
   */
  text: string;
};

export type EditCommentCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type EditCommentMeta = {
  credits: EditCommentCredits;
};

/**
 * Comment edited
 */
export type EditCommentResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: EditCommentMeta | undefined;
};

export type EditProfileRequest = {
  /**
   * New LinkedIn headline (max 220 chars)
   */
  headline?: string | undefined;
  /**
   * New LinkedIn about/summary section (max 2600 chars)
   */
  summary?: string | undefined;
};

export type EditProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type EditProfileMeta = {
  credits: EditProfileCredits;
};

/**
 * Profile updated
 */
export type EditProfileResponse = {
  success: true;
  /**
   * Fields that were updated on LinkedIn
   */
  updated: Array<string>;
  /**
   * Fields that were synced back to the local database
   */
  synced: Array<string>;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: EditProfileMeta | undefined;
};

export type UnlikePostRequest = {
  /**
   * LinkedIn post URL or URN to unlike
   */
  postUrl: string;
};

export type UnlikePostCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UnlikePostMeta = {
  credits: UnlikePostCredits;
};

/**
 * Reaction removed
 */
export type UnlikePostResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UnlikePostMeta | undefined;
};

export type UnlikeCommentRequest = {
  /**
   * Full comment URN to unlike (e.g. urn:li:comment:(urn:li:activity:123,456))
   */
  commentUrn: string;
};

export type UnlikeCommentCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UnlikeCommentMeta = {
  credits: UnlikeCommentCredits;
};

/**
 * Reaction removed
 */
export type UnlikeCommentResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UnlikeCommentMeta | undefined;
};

export type DeleteCommentRequest = {
  /**
   * Full comment URN to delete (e.g. urn:li:comment:(activity:7327726660852629504,7460399586222727168))
   */
  commentUrn: string;
};

export const DeleteCommentType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
export type DeleteCommentType = OpenEnum<typeof DeleteCommentType>;

export type Deleted = {
  type: DeleteCommentType;
  parentId: string;
  commentId: string;
  alreadyGone: boolean;
};

export type DeleteCommentCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type DeleteCommentMeta = {
  credits: DeleteCommentCredits;
};

/**
 * Comment deleted (or already gone)
 */
export type DeleteCommentResponse = {
  success: true;
  deleted: Deleted;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: DeleteCommentMeta | undefined;
};

export type FollowCompanyRequest = {
  /**
   * LinkedIn company URL or URN
   */
  company: string;
};

export type FollowCompanyCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type FollowCompanyMeta = {
  credits: FollowCompanyCredits;
};

/**
 * Company followed
 */
export type FollowCompanyResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: FollowCompanyMeta | undefined;
};

export type UnfollowCompanyRequest = {
  /**
   * LinkedIn company URL or URN
   */
  company: string;
};

export type UnfollowCompanyCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UnfollowCompanyMeta = {
  credits: UnfollowCompanyCredits;
};

/**
 * Company unfollowed
 */
export type UnfollowCompanyResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UnfollowCompanyMeta | undefined;
};

export type GetProfilePositionStartDate = {
  month: number | null;
  year: number;
};

export type GetProfilePositionEndDate = {
  month: number | null;
  year: number;
};

export type GetProfilePosition = {
  companyName: string | null;
  title: string | null;
  /**
   * Position description / responsibilities text (always fetched on visit_profile; may be null if LinkedIn has none)
   */
  description?: string | null | undefined;
  companyUrl: string | null;
  companyLogo: string | null;
  /**
   * Short description of the company (always fetched on visit_profile; may be null)
   */
  companyDescription?: string | null | undefined;
  startDate: GetProfilePositionStartDate | null;
  endDate: GetProfilePositionEndDate | null;
  isCurrent: boolean;
};

export type GetProfileEducationStartDate = {
  month: number | null;
  year: number;
};

export type GetProfileEducationEndDate = {
  month: number | null;
  year: number;
};

export type GetProfileEducation = {
  schoolName: string | null;
  degreeName: string | null;
  fieldOfStudy: string | null;
  schoolUrl: string | null;
  schoolLogo: string | null;
  startDate: GetProfileEducationStartDate | null;
  endDate: GetProfileEducationEndDate | null;
};

/**
 * Profile location
 */
export type GetProfileLocation = {
  countryCode: string;
  postalCode?: string | undefined;
};

/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export const GetProfileType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export type GetProfileType = OpenEnum<typeof GetProfileType>;

/**
 * Type of media attached to the post.
 */
export const GetProfileMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type GetProfileMediaType = OpenEnum<typeof GetProfileMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type GetProfileMedia = {
  /**
   * Type of media attached to the post.
   */
  type: GetProfileMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type GetProfileReactionTypeCount = {
  reactionType: string;
  count: number;
};

export type GetProfileLastPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
   */
  type: GetProfileType;
  /**
   * True when the post is a repost/reshare of another post. Absent for original posts.
   */
  isRepost?: boolean | undefined;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: GetProfileMedia | undefined;
  /**
   * Display name of the post's author.
   */
  authorName?: string | undefined;
  /**
   * Author's profile URL.
   */
  authorProfileUrl?: string | undefined;
  /**
   * Author's vanity slug, when they have one.
   */
  authorPublicIdentifier?: string | null | undefined;
  /**
   * Author's profile URN.
   */
  authorProfileUrn?: string | null | undefined;
  /**
   * Author's headline as shown on the post.
   */
  authorHeadline?: string | undefined;
  /**
   * Author's avatar URL.
   */
  authorImageUrl?: string | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<GetProfileReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
};

export type GetProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetProfileMeta = {
  credits: GetProfileCredits;
};

/**
 * Stored LinkedIn profile data
 */
export type GetProfileResponse = {
  success: true;
  /**
   * LinkedIn vanity name (e.g., 'alexandre-sarfati')
   */
  linkedinProfileId: string | null;
  /**
   * Full name from LinkedIn
   */
  linkedinName: string | null;
  /**
   * Email from LinkedIn profile
   */
  linkedinEmail: string | null;
  /**
   * LinkedIn profile URN
   */
  profileUrn: string | null;
  /**
   * LinkedIn headline
   */
  headline: string | null;
  /**
   * Profile picture URL
   */
  profilePic: string | null;
  /**
   * Full LinkedIn profile URL
   */
  profileUrl: string | null;
  /**
   * About section text. Present only when an earlier enrichment stored it; no current call populates it, so treat it as optional.
   */
  summary?: string | null | undefined;
  /**
   * Work positions. Present only when an earlier enrichment stored them; no current call populates them, so treat them as optional.
   */
  positions?: Array<GetProfilePosition> | undefined;
  /**
   * Education entries. Present only when an earlier enrichment stored them; no current call populates them, so treat them as optional.
   */
  educations?: Array<GetProfileEducation> | undefined;
  /**
   * Total LinkedIn connections
   */
  connectionsCount: number | null;
  /**
   * Total number of followers
   */
  followersCount?: number | null | undefined;
  /**
   * Profile location
   */
  location: GetProfileLocation | null;
  /**
   * LinkedIn verification status
   */
  isVerified: boolean | null;
  /**
   * Last 5 posts (populated when posts have been fetched via /me/linkedin/posts)
   */
  lastPosts?: Array<GetProfileLastPost> | undefined;
  /**
   * Last 20 comments by the user (populated after /me/linkedin/activity?tabType=COMMENTS)
   */
  lastComments?: Array<any> | undefined;
  /**
   * Last reactions by the user (populated after /me/linkedin/activity?tabType=REACTIONS)
   */
  lastReactions?: Array<any> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetProfileMeta | undefined;
};

/**
 * Proxy status for this account. Full config is masked; only enabled flag is exposed.
 */
export type ListAccountsProxyConfig = {
  enabled: boolean;
};

export type ListAccountsAccount = {
  id: string;
  label: string | null;
  isDefault: boolean;
  /**
   * Whether this account is the one used by the current API token.
   */
  isCurrent: boolean;
  linkedinProfileId: string | null;
  linkedinName: string | null;
  linkedinEmail: string | null;
  profileUrl: string | null;
  headline: string | null;
  profilePic: string | null;
  profileUrn: string | null;
  isActive: boolean;
  isValid: boolean;
  /**
   * Account plan tier (e.g. 'free', 'pro', 'team').
   */
  accountPlan: string | null;
  /**
   * Whether this account has unlimited credits.
   */
  isUnlimited: boolean;
  /**
   * Proxy status for this account. Full config is masked; only enabled flag is exposed.
   */
  proxyConfig: ListAccountsProxyConfig | null;
  /**
   * The user this account belongs to. Differs from the caller on a workspace-wide list.
   */
  ownerUserId?: string | null | undefined;
  /**
   * That user's display name, when known.
   */
  owner?: string | null | undefined;
  /**
   * True when this account has lost its session repeatedly, which is the signal to reconnect it rather than retry.
   */
  repeatDisconnect?: boolean | undefined;
  /**
   * True when writes on this account are simulated rather than sent.
   */
  simulateWrites?: boolean | undefined;
  /**
   * What the last seat probe concluded. 'unknown' means it has not been established, which is different from established-as-absent.
   */
  salesNavSeatStatus?: string | null | undefined;
  /**
   * When that probe ran, so a stale 'unknown' can be told from a fresh one.
   */
  salesNavSeatLastProbedAt?: string | null | undefined;
  createdAt: string;
  updatedAt: string;
};

export type ListAccountsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListAccountsMeta = {
  credits: ListAccountsCredits;
};

/**
 * List of LinkedIn accounts
 */
export type ListAccountsResponse = {
  success: true;
  accounts: Array<ListAccountsAccount>;
  total: number;
  /**
   * True when the caller owns the workspace, in which case `accounts` spans the whole workspace rather than just their own.
   */
  isOwner: boolean;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListAccountsMeta | undefined;
};

export type ProxyConfigIsp = {
  mode: "isp";
  /**
   * ISO country code (reference only).
   */
  country: string;
  /**
   * Dedicated ISP IPv4 address from Decodo.
   */
  staticIp: string;
  /**
   * Port for the static IP.
   */
  staticPort: number;
};

export type ProxyConfigResidential = {
  mode: "residential";
  /**
   * ISO 3166-1 alpha-2 country code.
   */
  country: string;
  /**
   * City for geo-targeting.
   */
  city?: string | undefined;
  /**
   * Sticky IP rotation in hours (default 1).
   */
  rotationHours?: number | undefined;
};

/**
 * Proxy config. Null = disable proxy. Object = enable with mode.
 */
export type ProxyConfig = ProxyConfigResidential | ProxyConfigIsp;

export type UpdateAccountRequest = {
  /**
   * ID of the LinkedIn account to update.
   */
  accountId: string;
  /**
   * User-facing label (e.g. 'Personal', 'Company SDR').
   */
  label?: string | null | undefined;
  /**
   * Set as the default account. Clears default from other accounts.
   */
  isDefault?: boolean | undefined;
  /**
   * Enable or disable BeReach access to this LinkedIn session. Disabling pauses all new data collection and actions on this account; the stored session is kept until the account is removed.
   */
  isActive?: boolean | undefined;
  /**
   * Proxy config. Null = disable proxy. Object = enable with mode.
   */
  proxyConfig?: ProxyConfigResidential | ProxyConfigIsp | null | undefined;
};

export type UpdateAccountProxyConfigResponse = {
  city: string | null;
  mode: string | null;
  country: string | null;
  rotationHours: number | null;
};

export type UpdateAccountAccount = {
  id: string;
  label: string | null;
  isDefault: boolean;
  linkedinProfileId: string | null;
  linkedinName: string | null;
  headline: string | null;
  profilePic: string | null;
  isValid: boolean;
  isActive: boolean;
  proxyConfig: UpdateAccountProxyConfigResponse | null;
};

export type UpdateAccountCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UpdateAccountMeta = {
  credits: UpdateAccountCredits;
};

/**
 * Updated account
 */
export type UpdateAccountResponse = {
  success: true;
  account: UpdateAccountAccount;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UpdateAccountMeta | undefined;
};

export type GetFollowersRequest = {
  /**
   * Pagination offset (default 0).
   */
  start?: number | undefined;
  /**
   * Number of followers to fetch per page (default 10, max 50).
   */
  count?: number | undefined;
};

export type Follower = {
  name: string;
  headline: string | null;
  profileUrl: string | null;
  profilePicture: string | null;
  followerCount: number | null;
  /**
   * Whether you follow this person back
   */
  following: boolean;
  /**
   * LinkedIn profile URN (e.g. urn:li:fsd_profile:ACoAAA...) when available
   */
  profileUrn: string | null;
  /**
   * Vanity slug from profile URL (e.g. john-doe) when not URN-based
   */
  publicIdentifier: string | null;
};

export type GetFollowersCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetFollowersMeta = {
  credits: GetFollowersCredits;
};

/**
 * Paginated list of followers
 */
export type GetFollowersResponse = {
  success: true;
  followers: Array<Follower>;
  count: number;
  /**
   * Total reported by LinkedIn (capped at ~1000)
   */
  totalFollowers: number;
  start: number;
  hasMore: boolean;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetFollowersMeta | undefined;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type ConnectionRequestDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type ConnectionRequestWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for sending LinkedIn connection requests
 */
export type ConnectionRequest = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: ConnectionRequestDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: ConnectionRequestWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type MessageDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type MessageWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for sending DMs
 */
export type GetLimitsMessage = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: MessageDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: MessageWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type ProfileVisitDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type ProfileVisitWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for visiting LinkedIn profiles and company pages
 */
export type ProfileVisit = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: ProfileVisitDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: ProfileVisitWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type ScrapingDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type ScrapingWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for data collection: search, collecting posts/likes/comments, fetching followers, listing chats
 */
export type Scraping = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: ScrapingDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: ScrapingWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type PostDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type PostWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for publishing LinkedIn posts
 */
export type GetLimitsPost = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: PostDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: PostWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type AcceptInvitationDaily2 = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type AcceptInvitationWeekly2 = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for accepting connection invitations
 */
export type AcceptInvitation = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: AcceptInvitationDaily2 | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: AcceptInvitationWeekly2 | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type CommentPostDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type CommentPostWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for commenting on posts
 */
export type CommentPost = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: CommentPostDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: CommentPostWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type ReplyCommentDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type ReplyCommentWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for replying to comments
 */
export type ReplyComment = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: ReplyCommentDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: ReplyCommentWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type LimitsDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type LimitsWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for structured profile reads.
 */
export type GetLimitsProfile = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: LimitsDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: LimitsWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

/**
 * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
 */
export type EngagementDaily = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
 */
export type EngagementWeekly = {
  /**
   * Number of actions used in this window
   */
  current: number;
  /**
   * Maximum allowed actions in this window (with multiplier applied)
   */
  limit: number;
  /**
   * Actions remaining before hitting the limit
   */
  remaining: number;
};

/**
 * Limits for likes and reactions.
 */
export type Engagement = {
  /**
   * Daily usage counter (resets at midnight UTC). Null if not configured for this action type.
   */
  daily: EngagementDaily | null;
  /**
   * Weekly usage counter (resets Monday 00:00 UTC). Null if no weekly cap for this action type.
   */
  weekly: EngagementWeekly | null;
  /**
   * Minimum delay in seconds required between two consecutive actions of this type
   */
  minIntervalSeconds: number;
  /**
   * ISO 8601 timestamp of the next daily counter reset. Null if not configured.
   */
  nextResetDaily: string | null;
  /**
   * ISO 8601 timestamp of the next weekly counter reset. Null if no weekly cap.
   */
  nextResetWeekly: string | null;
};

export type Limits = {
  /**
   * Limits for sending LinkedIn connection requests
   */
  connectionRequest: ConnectionRequest;
  /**
   * Limits for sending DMs
   */
  message: GetLimitsMessage;
  /**
   * Limits for visiting LinkedIn profiles and company pages
   */
  profileVisit: ProfileVisit;
  /**
   * Limits for data collection: search, collecting posts/likes/comments, fetching followers, listing chats
   */
  scraping: Scraping;
  /**
   * Limits for publishing LinkedIn posts
   */
  post?: GetLimitsPost | undefined;
  /**
   * Limits for accepting connection invitations
   */
  acceptInvitation?: AcceptInvitation | undefined;
  /**
   * Limits for commenting on posts
   */
  commentPost?: CommentPost | undefined;
  /**
   * Limits for replying to comments
   */
  replyComment?: ReplyComment | undefined;
  /**
   * Limits for structured profile reads.
   */
  profile?: GetLimitsProfile | undefined;
  /**
   * Limits for likes and reactions.
   */
  engagement?: Engagement | undefined;
};

export type GetLimitsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetLimitsMeta = {
  credits: GetLimitsCredits;
};

/**
 * Rate limit status for all action types
 */
export type GetLimitsResponse = {
  success: true;
  /**
   * Workspace limit multiplier applied to all base limits (default 1.0)
   */
  multiplier: number;
  limits: Limits;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetLimitsMeta | undefined;
};

/**
 * What the account is doing: nothing_queued means nobody is waiting, sending means invitations are going out, spacing means it is between two invitations, blocked means nothing is going out.
 */
export const State = {
  NothingQueued: "nothing_queued",
  Sending: "sending",
  Spacing: "spacing",
  Blocked: "blocked",
} as const;
/**
 * What the account is doing: nothing_queued means nobody is waiting, sending means invitations are going out, spacing means it is between two invitations, blocked means nothing is going out.
 */
export type State = OpenEnum<typeof State>;

/**
 * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
 */
export const UpcomingStatus = {
  Next: "next",
  Waiting: "waiting",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
/**
 * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
 */
export type UpcomingStatus = OpenEnum<typeof UpcomingStatus>;

/**
 * Why this person has not gone out yet, as the sender itself answers it. A block on the account outranks the place in line.
 */
export type UpcomingWhy = {
  /**
   * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
   */
  status: UpcomingStatus;
  /**
   * For blocked, the account's own sentence, safe to show as written. Otherwise a plain phrase about the wait.
   */
  detail: string;
  /**
   * Only while blocked: whether waiting is enough.
   */
  clearsItself: boolean | null;
  /**
   * Only while blocked and a person has to act: where to send them.
   */
  fixUrl: string | null;
};

export type Upcoming = {
  /**
   * Identifier of the person waiting
   */
  contactId: string;
  /**
   * The person's name, when known
   */
  name: string | null;
  /**
   * Their profile picture, for rendering a row. Never something to say.
   */
  avatarUrl: string | null;
  /**
   * Their headline, when known
   */
  headline: string | null;
  /**
   * Their profile, for linking a row. Never paste it.
   */
  linkedinUrl: string | null;
  /**
   * When they were last seen doing something on LinkedIn, or null when nothing we hold says. The order is sorted on this within one ask.
   */
  lastActivityAt: string | null;
  /**
   * True when we looked at the profile and found nothing recent to read: no post, comment, reaction, about text or education. A flag to point at, never a reason the order changed. False when we have not looked.
   */
  thinProfile: boolean;
  /**
   * Why this person has not gone out yet, as the sender itself answers it. A block on the account outranks the place in line.
   */
  why: UpcomingWhy;
  /**
   * Their place in line, counting from 1. A fact about the order, not a promise of a time.
   */
  position: number;
  /**
   * Rough days until their invitation goes out, estimated from how fast this account has actually been sending. Null when nothing is going out, or when the account has no sending history to estimate from.
   */
  etaDays: number | null;
};

/**
 * Why nothing is going out. The ordinary spacing between two invitations is state 'spacing', never a block.
 */
export const GetConnectionStatusKind = {
  Challenge: "challenge",
  SessionDead: "session_dead",
  NoCredit: "no_credit",
  InviteCap: "invite_cap",
  RateLimited: "rate_limited",
} as const;
/**
 * Why nothing is going out. The ordinary spacing between two invitations is state 'spacing', never a block.
 */
export type GetConnectionStatusKind = OpenEnum<typeof GetConnectionStatusKind>;

/**
 * Set only while something is stopping this account. Never set for the ordinary spacing between two invitations, which is not a fault: that is state 'spacing'. If this is null, nothing is wrong with the account.
 */
export type GetConnectionStatusBlocked = {
  /**
   * Why nothing is going out. The ordinary spacing between two invitations is state 'spacing', never a block.
   */
  kind: GetConnectionStatusKind;
  /**
   * A plain sentence explaining it, suitable to show as written
   */
  message: string;
  /**
   * When it lifts. Null when it needs a person to act rather than time to pass.
   */
  resumesAt: string | null;
  /**
   * Whether waiting is enough, or somebody has to do something
   */
  clearsItself: boolean;
  /**
   * Where to send the account owner to fix it, when a person has to act. Null for the kinds that clear on their own.
   */
  fixUrl: string | null;
};

/**
 * Set only while LinkedIn's own invitation ceiling is what stopped this account
 */
export type CapProbe = {
  /**
   * When LinkedIn is asked again
   */
  nextProbeAt: string | null;
  /**
   * How far the wait between checks has widened
   */
  rung: number;
  /**
   * The widest it gets. Checks continue at that spacing until sending is allowed again.
   */
  finalRung: number;
};

export type GetConnectionStatusSkipped = {
  reason: string;
  count: number;
};

export type GetConnectionStatusAccount = {
  /**
   * The LinkedIn account this describes
   */
  linkedinCredentialsId: string;
  /**
   * Human-readable name of the LinkedIn account
   */
  accountLabel: string;
  /**
   * What the account is doing: nothing_queued means nobody is waiting, sending means invitations are going out, spacing means it is between two invitations, blocked means nothing is going out.
   */
  state: State;
  /**
   * The people at the front of the line, nearest first: five unless the caller asked for more (up to twenty-five). waitingTotal says how many are in line altogether.
   */
  upcoming: Array<Upcoming>;
  /**
   * When the next invitation can go out, when that instant is actually known. Null when nothing is holding the account back, since there is then no specific moment to name.
   */
  nextSendAt: string | null;
  /**
   * Set only while something is stopping this account. Never set for the ordinary spacing between two invitations, which is not a fault: that is state 'spacing'. If this is null, nothing is wrong with the account.
   */
  blocked: GetConnectionStatusBlocked | null;
  /**
   * Set only while LinkedIn's own invitation ceiling is what stopped this account
   */
  capProbe: CapProbe | null;
  /**
   * Everyone still to be invited from this account, counted once per person
   */
  waitingTotal: number;
  /**
   * How long the person waiting longest has been waiting
   */
  oldestWaitingHours: number | null;
  /**
   * Invitations sent from this account that nobody has answered yet
   */
  awaitingResponse: number;
  /**
   * Invitations sent in the last 24 hours
   */
  sentLast24h: number;
  /**
   * Invitations accepted in the last 24 hours
   */
  acceptedLast24h: number;
  /**
   * How many a day this account has actually been sending, averaged over the last 7 days. An observed rate, never a configured limit: there is no daily or weekly cap on connection requests. Do not call this number a limit, maximum, or quota, and do not treat it as contradicted by a single day's count being higher or lower than it.
   */
  sendsPerDay: number;
  /**
   * How many to expect over the next 24 hours. Null when no honest estimate exists: something is stopping the account, or it has no sending history yet. Ordinary spacing between invitations does NOT suppress it. Never report null as zero.
   */
  expectedNext24h: number | null;
  /**
   * Why people are set aside right now, and how many for each reason. Up to eight reasons, commonest first.
   */
  skipped: Array<GetConnectionStatusSkipped>;
};

/**
 * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
 */
export const NextStatus = {
  Next: "next",
  Waiting: "waiting",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
/**
 * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
 */
export type NextStatus = OpenEnum<typeof NextStatus>;

/**
 * Why this person has not gone out yet, as the sender itself answers it. A block on the account outranks the place in line.
 */
export type NextWhy = {
  /**
   * next is first in line, not sent; waiting names the place; blocked carries the account's own sentence.
   */
  status: NextStatus;
  /**
   * For blocked, the account's own sentence, safe to show as written. Otherwise a plain phrase about the wait.
   */
  detail: string;
  /**
   * Only while blocked: whether waiting is enough.
   */
  clearsItself: boolean | null;
  /**
   * Only while blocked and a person has to act: where to send them.
   */
  fixUrl: string | null;
};

/**
 * Who goes next overall, named only when exactly one account is sending. With several sending at once there is no single next person.
 */
export type Next = {
  /**
   * Identifier of the person waiting
   */
  contactId: string;
  /**
   * The person's name, when known
   */
  name: string | null;
  /**
   * Their profile picture, for rendering a row. Never something to say.
   */
  avatarUrl: string | null;
  /**
   * Their headline, when known
   */
  headline: string | null;
  /**
   * Their profile, for linking a row. Never paste it.
   */
  linkedinUrl: string | null;
  /**
   * When they were last seen doing something on LinkedIn, or null when nothing we hold says. The order is sorted on this within one ask.
   */
  lastActivityAt: string | null;
  /**
   * True when we looked at the profile and found nothing recent to read: no post, comment, reaction, about text or education. A flag to point at, never a reason the order changed. False when we have not looked.
   */
  thinProfile: boolean;
  /**
   * Why this person has not gone out yet, as the sender itself answers it. A block on the account outranks the place in line.
   */
  why: NextWhy;
  /**
   * Their place in line, counting from 1. A fact about the order, not a promise of a time.
   */
  position: number;
  /**
   * Rough days until their invitation goes out, estimated from how fast this account has actually been sending. Null when nothing is going out, or when the account has no sending history to estimate from.
   */
  etaDays: number | null;
  accountLabel: string;
};

/**
 * Everything summed. Null when no account is connected.
 */
export type GetConnectionStatusTotals = {
  waitingTotal: number;
  awaitingResponse: number;
  sentLast24h: number;
  acceptedLast24h: number;
  expectedNext24h: number | null;
  /**
   * True when some accounts could not be estimated, so the expected figure is a floor rather than a total.
   */
  expectedIsPartial: boolean;
  oldestWaitingHours: number | null;
  accountsSending: number;
  accountsBlocked: number;
  accountsUnreadable: number;
  /**
   * Who goes next overall, named only when exactly one account is sending. With several sending at once there is no single next person.
   */
  next: Next | null;
};

export type GetConnectionStatusCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetConnectionStatusMeta = {
  credits: GetConnectionStatusCredits;
};

/**
 * Current state of the connection-request lane
 */
export type GetConnectionStatusResponse = {
  success: true;
  /**
   * One entry per connected LinkedIn account. Accounts send independently and in parallel, each with its own pacing and its own limits.
   */
  accounts: Array<GetConnectionStatusAccount>;
  /**
   * Everything summed. Null when no account is connected.
   */
  totals: GetConnectionStatusTotals | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetConnectionStatusMeta | undefined;
};

export type GetCreditsCredits = {
  /**
   * Number of credits used in the current billing period
   */
  current: number;
  /**
   * Maximum credits available for the workspace, or null if unlimited
   */
  limit: number | null;
  /**
   * Credits remaining before hitting the limit, or null if unlimited
   */
  remaining: number | null;
  /**
   * Percentage of credits used (0-100, capped at 100, rounded to 2 decimal places). 0 when unlimited
   */
  percentage: number;
  /**
   * Whether the workspace has unlimited credits (Pro plan). When true, limit and remaining are null
   */
  isUnlimited: boolean;
};

export type GetCreditsMetaCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetCreditsMeta = {
  credits: GetCreditsMetaCredits;
};

/**
 * Credit balance for the workspace
 */
export type GetCreditsResponse = {
  success: true;
  credits: GetCreditsCredits;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetCreditsMeta | undefined;
};

export type SwitchAccountRequest = {
  /**
   * The LinkedIn credentials ID to switch to (from listAccounts)
   */
  credentialsId: string;
};

/**
 * Switched account summary
 */
export type SwitchAccountAccount = {
  id: string;
  name: string;
  avatar: string | null;
  plan: string;
};

export type SwitchAccountCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SwitchAccountMeta = {
  credits: SwitchAccountCredits;
};

/**
 * Account switched
 */
export type SwitchAccountResponse = {
  success: true;
  /**
   * Switched account summary
   */
  account: SwitchAccountAccount;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SwitchAccountMeta | undefined;
};

/**
 * Sort order (default RECENTLY_ADDED)
 */
export const SortType = {
  RecentlyAdded: "RECENTLY_ADDED",
} as const;
/**
 * Sort order (default RECENTLY_ADDED)
 */
export type SortType = ClosedEnum<typeof SortType>;

export type GetConnectionsRequest = {
  /**
   * Pagination offset
   */
  start?: number | undefined;
  /**
   * Results per page (max 40)
   */
  count?: number | undefined;
  /**
   * Sort order (default RECENTLY_ADDED)
   */
  sortType?: SortType | undefined;
  /**
   * Filter connections by name
   */
  keywords?: string | undefined;
};

/**
 * Connection date (Unix timestamp ms or ISO string)
 */
export type ConnectedAt = number | string;

export type Connection = {
  name: string;
  headline: string | null;
  profileUrl: string | null;
  profileUrn: string | null;
  publicIdentifier: string | null;
  /**
   * Profile picture URL
   */
  profilePicture: string | null;
  /**
   * Connection date (Unix timestamp ms or ISO string)
   */
  connectedAt: number | string | null;
};

export type GetConnectionsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetConnectionsMeta = {
  credits: GetConnectionsCredits;
};

/**
 * Connections list
 */
export type GetConnectionsResponse = {
  success: true;
  connections: Array<Connection>;
  count: number;
  start: number;
  total: number;
  hasMore: boolean;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetConnectionsMeta | undefined;
};

/**
 * Type of activity to fetch
 */
export const QueryParamTabType = {
  Comments: "COMMENTS",
  Reactions: "REACTIONS",
} as const;
/**
 * Type of activity to fetch
 */
export type QueryParamTabType = ClosedEnum<typeof QueryParamTabType>;

export type GetOwnActivityRequest = {
  /**
   * Type of activity to fetch
   */
  tabType?: QueryParamTabType | undefined;
  /**
   * Number of items per page (max 50)
   */
  count?: number | undefined;
  /**
   * Pagination offset
   */
  start?: number | undefined;
  /**
   * Pagination token from previous response
   */
  paginationToken?: string | undefined;
};

export const TabTypeResponse = {
  Comments: "COMMENTS",
  Reactions: "REACTIONS",
} as const;
export type TabTypeResponse = OpenEnum<typeof TabTypeResponse>;

export type GetOwnActivityCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetOwnActivityMeta = {
  credits: GetOwnActivityCredits;
};

/**
 * Activity list
 */
export type GetOwnActivityResponse = {
  success: true;
  /**
   * Always 'personal' — user activity
   */
  actor: string;
  tabType: TabTypeResponse;
  /**
   * Activity items — comments or reactions depending on tabType
   */
  items: Array<any>;
  count: number;
  total: number;
  start: number;
  paginationToken: string | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetOwnActivityMeta | undefined;
};

export type GetOwnPostsRequest = {
  /**
   * Number of posts to fetch (default 20, max 100).
   */
  count?: number | undefined;
  /**
   * Pagination offset (default 0).
   */
  start?: number | undefined;
  /**
   * Pagination token from a previous response to fetch the next page.
   */
  paginationToken?: string | undefined;
};

/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export const GetOwnPostsType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export type GetOwnPostsType = OpenEnum<typeof GetOwnPostsType>;

/** @internal */
export type FollowProfileRequest$Outbound = {
  profile: string;
};

/** @internal */
export const FollowProfileRequest$outboundSchema: z.ZodMiniType<
  FollowProfileRequest$Outbound,
  FollowProfileRequest
> = z.object({
  profile: z.string(),
});

export function followProfileRequestToJSON(
  followProfileRequest: FollowProfileRequest,
): string {
  return JSON.stringify(
    FollowProfileRequest$outboundSchema.parse(followProfileRequest),
  );
}

/** @internal */
export const FollowProfileCredits$inboundSchema: z.ZodMiniType<
  FollowProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function followProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<FollowProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowProfileCredits' from JSON`,
  );
}

/** @internal */
export const FollowProfileMeta$inboundSchema: z.ZodMiniType<
  FollowProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => FollowProfileCredits$inboundSchema),
});

export function followProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<FollowProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowProfileMeta' from JSON`,
  );
}

/** @internal */
export const FollowProfileResponse$inboundSchema: z.ZodMiniType<
  FollowProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => FollowProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function followProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<FollowProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowProfileResponse' from JSON`,
  );
}

/** @internal */
export type UnfollowProfileRequest$Outbound = {
  profile: string;
};

/** @internal */
export const UnfollowProfileRequest$outboundSchema: z.ZodMiniType<
  UnfollowProfileRequest$Outbound,
  UnfollowProfileRequest
> = z.object({
  profile: z.string(),
});

export function unfollowProfileRequestToJSON(
  unfollowProfileRequest: UnfollowProfileRequest,
): string {
  return JSON.stringify(
    UnfollowProfileRequest$outboundSchema.parse(unfollowProfileRequest),
  );
}

/** @internal */
export const UnfollowProfileCredits$inboundSchema: z.ZodMiniType<
  UnfollowProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function unfollowProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowProfileCredits' from JSON`,
  );
}

/** @internal */
export const UnfollowProfileMeta$inboundSchema: z.ZodMiniType<
  UnfollowProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UnfollowProfileCredits$inboundSchema),
});

export function unfollowProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowProfileMeta' from JSON`,
  );
}

/** @internal */
export const UnfollowProfileResponse$inboundSchema: z.ZodMiniType<
  UnfollowProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => UnfollowProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function unfollowProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowProfileResponse' from JSON`,
  );
}

/** @internal */
export type EditPostRequest$Outbound = {
  postUrl: string;
  text: string;
};

/** @internal */
export const EditPostRequest$outboundSchema: z.ZodMiniType<
  EditPostRequest$Outbound,
  EditPostRequest
> = z.object({
  postUrl: z.string(),
  text: z.string(),
});

export function editPostRequestToJSON(
  editPostRequest: EditPostRequest,
): string {
  return JSON.stringify(EditPostRequest$outboundSchema.parse(editPostRequest));
}

/** @internal */
export const EditPostCredits$inboundSchema: z.ZodMiniType<
  EditPostCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function editPostCreditsFromJSON(
  jsonString: string,
): SafeParseResult<EditPostCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditPostCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditPostCredits' from JSON`,
  );
}

/** @internal */
export const EditPostMeta$inboundSchema: z.ZodMiniType<EditPostMeta, unknown> =
  z.object({
    credits: z.lazy(() => EditPostCredits$inboundSchema),
  });

export function editPostMetaFromJSON(
  jsonString: string,
): SafeParseResult<EditPostMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditPostMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditPostMeta' from JSON`,
  );
}

/** @internal */
export const EditPostResponse$inboundSchema: z.ZodMiniType<
  EditPostResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => EditPostMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function editPostResponseFromJSON(
  jsonString: string,
): SafeParseResult<EditPostResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditPostResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditPostResponse' from JSON`,
  );
}

/** @internal */
export type EditCommentRequest$Outbound = {
  fsdCommentUrn?: string | undefined;
  commentUrn?: string | undefined;
  text: string;
};

/** @internal */
export const EditCommentRequest$outboundSchema: z.ZodMiniType<
  EditCommentRequest$Outbound,
  EditCommentRequest
> = z.object({
  fsdCommentUrn: z.optional(z.string()),
  commentUrn: z.optional(z.string()),
  text: z.string(),
});

export function editCommentRequestToJSON(
  editCommentRequest: EditCommentRequest,
): string {
  return JSON.stringify(
    EditCommentRequest$outboundSchema.parse(editCommentRequest),
  );
}

/** @internal */
export const EditCommentCredits$inboundSchema: z.ZodMiniType<
  EditCommentCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function editCommentCreditsFromJSON(
  jsonString: string,
): SafeParseResult<EditCommentCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditCommentCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditCommentCredits' from JSON`,
  );
}

/** @internal */
export const EditCommentMeta$inboundSchema: z.ZodMiniType<
  EditCommentMeta,
  unknown
> = z.object({
  credits: z.lazy(() => EditCommentCredits$inboundSchema),
});

export function editCommentMetaFromJSON(
  jsonString: string,
): SafeParseResult<EditCommentMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditCommentMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditCommentMeta' from JSON`,
  );
}

/** @internal */
export const EditCommentResponse$inboundSchema: z.ZodMiniType<
  EditCommentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => EditCommentMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function editCommentResponseFromJSON(
  jsonString: string,
): SafeParseResult<EditCommentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditCommentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditCommentResponse' from JSON`,
  );
}

/** @internal */
export type EditProfileRequest$Outbound = {
  headline?: string | undefined;
  summary?: string | undefined;
};

/** @internal */
export const EditProfileRequest$outboundSchema: z.ZodMiniType<
  EditProfileRequest$Outbound,
  EditProfileRequest
> = z.object({
  headline: z.optional(z.string()),
  summary: z.optional(z.string()),
});

export function editProfileRequestToJSON(
  editProfileRequest: EditProfileRequest,
): string {
  return JSON.stringify(
    EditProfileRequest$outboundSchema.parse(editProfileRequest),
  );
}

/** @internal */
export const EditProfileCredits$inboundSchema: z.ZodMiniType<
  EditProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function editProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<EditProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditProfileCredits' from JSON`,
  );
}

/** @internal */
export const EditProfileMeta$inboundSchema: z.ZodMiniType<
  EditProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => EditProfileCredits$inboundSchema),
});

export function editProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<EditProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditProfileMeta' from JSON`,
  );
}

/** @internal */
export const EditProfileResponse$inboundSchema: z.ZodMiniType<
  EditProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    updated: z.array(types.string()),
    synced: z.array(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => EditProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function editProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<EditProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EditProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EditProfileResponse' from JSON`,
  );
}

/** @internal */
export type UnlikePostRequest$Outbound = {
  postUrl: string;
};

/** @internal */
export const UnlikePostRequest$outboundSchema: z.ZodMiniType<
  UnlikePostRequest$Outbound,
  UnlikePostRequest
> = z.object({
  postUrl: z.string(),
});

export function unlikePostRequestToJSON(
  unlikePostRequest: UnlikePostRequest,
): string {
  return JSON.stringify(
    UnlikePostRequest$outboundSchema.parse(unlikePostRequest),
  );
}

/** @internal */
export const UnlikePostCredits$inboundSchema: z.ZodMiniType<
  UnlikePostCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function unlikePostCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UnlikePostCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikePostCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikePostCredits' from JSON`,
  );
}

/** @internal */
export const UnlikePostMeta$inboundSchema: z.ZodMiniType<
  UnlikePostMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UnlikePostCredits$inboundSchema),
});

export function unlikePostMetaFromJSON(
  jsonString: string,
): SafeParseResult<UnlikePostMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikePostMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikePostMeta' from JSON`,
  );
}

/** @internal */
export const UnlikePostResponse$inboundSchema: z.ZodMiniType<
  UnlikePostResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => UnlikePostMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function unlikePostResponseFromJSON(
  jsonString: string,
): SafeParseResult<UnlikePostResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikePostResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikePostResponse' from JSON`,
  );
}

/** @internal */
export type UnlikeCommentRequest$Outbound = {
  commentUrn: string;
};

/** @internal */
export const UnlikeCommentRequest$outboundSchema: z.ZodMiniType<
  UnlikeCommentRequest$Outbound,
  UnlikeCommentRequest
> = z.object({
  commentUrn: z.string(),
});

export function unlikeCommentRequestToJSON(
  unlikeCommentRequest: UnlikeCommentRequest,
): string {
  return JSON.stringify(
    UnlikeCommentRequest$outboundSchema.parse(unlikeCommentRequest),
  );
}

/** @internal */
export const UnlikeCommentCredits$inboundSchema: z.ZodMiniType<
  UnlikeCommentCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function unlikeCommentCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UnlikeCommentCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikeCommentCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikeCommentCredits' from JSON`,
  );
}

/** @internal */
export const UnlikeCommentMeta$inboundSchema: z.ZodMiniType<
  UnlikeCommentMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UnlikeCommentCredits$inboundSchema),
});

export function unlikeCommentMetaFromJSON(
  jsonString: string,
): SafeParseResult<UnlikeCommentMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikeCommentMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikeCommentMeta' from JSON`,
  );
}

/** @internal */
export const UnlikeCommentResponse$inboundSchema: z.ZodMiniType<
  UnlikeCommentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => UnlikeCommentMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function unlikeCommentResponseFromJSON(
  jsonString: string,
): SafeParseResult<UnlikeCommentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnlikeCommentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnlikeCommentResponse' from JSON`,
  );
}

/** @internal */
export type DeleteCommentRequest$Outbound = {
  commentUrn: string;
};

/** @internal */
export const DeleteCommentRequest$outboundSchema: z.ZodMiniType<
  DeleteCommentRequest$Outbound,
  DeleteCommentRequest
> = z.object({
  commentUrn: z.string(),
});

export function deleteCommentRequestToJSON(
  deleteCommentRequest: DeleteCommentRequest,
): string {
  return JSON.stringify(
    DeleteCommentRequest$outboundSchema.parse(deleteCommentRequest),
  );
}

/** @internal */
export const DeleteCommentType$inboundSchema: z.ZodMiniType<
  DeleteCommentType,
  unknown
> = openEnums.inboundSchema(DeleteCommentType);

/** @internal */
export const Deleted$inboundSchema: z.ZodMiniType<Deleted, unknown> = z.object({
  type: DeleteCommentType$inboundSchema,
  parentId: types.string(),
  commentId: types.string(),
  alreadyGone: types.boolean(),
});

export function deletedFromJSON(
  jsonString: string,
): SafeParseResult<Deleted, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Deleted$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Deleted' from JSON`,
  );
}

/** @internal */
export const DeleteCommentCredits$inboundSchema: z.ZodMiniType<
  DeleteCommentCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function deleteCommentCreditsFromJSON(
  jsonString: string,
): SafeParseResult<DeleteCommentCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteCommentCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteCommentCredits' from JSON`,
  );
}

/** @internal */
export const DeleteCommentMeta$inboundSchema: z.ZodMiniType<
  DeleteCommentMeta,
  unknown
> = z.object({
  credits: z.lazy(() => DeleteCommentCredits$inboundSchema),
});

export function deleteCommentMetaFromJSON(
  jsonString: string,
): SafeParseResult<DeleteCommentMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteCommentMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteCommentMeta' from JSON`,
  );
}

/** @internal */
export const DeleteCommentResponse$inboundSchema: z.ZodMiniType<
  DeleteCommentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    deleted: z.lazy(() => Deleted$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => DeleteCommentMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function deleteCommentResponseFromJSON(
  jsonString: string,
): SafeParseResult<DeleteCommentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteCommentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteCommentResponse' from JSON`,
  );
}

/** @internal */
export type FollowCompanyRequest$Outbound = {
  company: string;
};

/** @internal */
export const FollowCompanyRequest$outboundSchema: z.ZodMiniType<
  FollowCompanyRequest$Outbound,
  FollowCompanyRequest
> = z.object({
  company: z.string(),
});

export function followCompanyRequestToJSON(
  followCompanyRequest: FollowCompanyRequest,
): string {
  return JSON.stringify(
    FollowCompanyRequest$outboundSchema.parse(followCompanyRequest),
  );
}

/** @internal */
export const FollowCompanyCredits$inboundSchema: z.ZodMiniType<
  FollowCompanyCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function followCompanyCreditsFromJSON(
  jsonString: string,
): SafeParseResult<FollowCompanyCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowCompanyCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowCompanyCredits' from JSON`,
  );
}

/** @internal */
export const FollowCompanyMeta$inboundSchema: z.ZodMiniType<
  FollowCompanyMeta,
  unknown
> = z.object({
  credits: z.lazy(() => FollowCompanyCredits$inboundSchema),
});

export function followCompanyMetaFromJSON(
  jsonString: string,
): SafeParseResult<FollowCompanyMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowCompanyMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowCompanyMeta' from JSON`,
  );
}

/** @internal */
export const FollowCompanyResponse$inboundSchema: z.ZodMiniType<
  FollowCompanyResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => FollowCompanyMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function followCompanyResponseFromJSON(
  jsonString: string,
): SafeParseResult<FollowCompanyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FollowCompanyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FollowCompanyResponse' from JSON`,
  );
}

/** @internal */
export type UnfollowCompanyRequest$Outbound = {
  company: string;
};

/** @internal */
export const UnfollowCompanyRequest$outboundSchema: z.ZodMiniType<
  UnfollowCompanyRequest$Outbound,
  UnfollowCompanyRequest
> = z.object({
  company: z.string(),
});

export function unfollowCompanyRequestToJSON(
  unfollowCompanyRequest: UnfollowCompanyRequest,
): string {
  return JSON.stringify(
    UnfollowCompanyRequest$outboundSchema.parse(unfollowCompanyRequest),
  );
}

/** @internal */
export const UnfollowCompanyCredits$inboundSchema: z.ZodMiniType<
  UnfollowCompanyCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function unfollowCompanyCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowCompanyCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowCompanyCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowCompanyCredits' from JSON`,
  );
}

/** @internal */
export const UnfollowCompanyMeta$inboundSchema: z.ZodMiniType<
  UnfollowCompanyMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UnfollowCompanyCredits$inboundSchema),
});

export function unfollowCompanyMetaFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowCompanyMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowCompanyMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowCompanyMeta' from JSON`,
  );
}

/** @internal */
export const UnfollowCompanyResponse$inboundSchema: z.ZodMiniType<
  UnfollowCompanyResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => UnfollowCompanyMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function unfollowCompanyResponseFromJSON(
  jsonString: string,
): SafeParseResult<UnfollowCompanyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UnfollowCompanyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UnfollowCompanyResponse' from JSON`,
  );
}

/** @internal */
export const GetProfilePositionStartDate$inboundSchema: z.ZodMiniType<
  GetProfilePositionStartDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function getProfilePositionStartDateFromJSON(
  jsonString: string,
): SafeParseResult<GetProfilePositionStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfilePositionStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfilePositionStartDate' from JSON`,
  );
}

/** @internal */
export const GetProfilePositionEndDate$inboundSchema: z.ZodMiniType<
  GetProfilePositionEndDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function getProfilePositionEndDateFromJSON(
  jsonString: string,
): SafeParseResult<GetProfilePositionEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfilePositionEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfilePositionEndDate' from JSON`,
  );
}

/** @internal */
export const GetProfilePosition$inboundSchema: z.ZodMiniType<
  GetProfilePosition,
  unknown
> = z.object({
  companyName: types.nullable(types.string()),
  title: types.nullable(types.string()),
  description: z.optional(z.nullable(types.string())),
  companyUrl: types.nullable(types.string()),
  companyLogo: types.nullable(types.string()),
  companyDescription: z.optional(z.nullable(types.string())),
  startDate: types.nullable(
    z.lazy(() => GetProfilePositionStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => GetProfilePositionEndDate$inboundSchema),
  ),
  isCurrent: types.boolean(),
});

export function getProfilePositionFromJSON(
  jsonString: string,
): SafeParseResult<GetProfilePosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfilePosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfilePosition' from JSON`,
  );
}

/** @internal */
export const GetProfileEducationStartDate$inboundSchema: z.ZodMiniType<
  GetProfileEducationStartDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function getProfileEducationStartDateFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileEducationStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileEducationStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileEducationStartDate' from JSON`,
  );
}

/** @internal */
export const GetProfileEducationEndDate$inboundSchema: z.ZodMiniType<
  GetProfileEducationEndDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function getProfileEducationEndDateFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileEducationEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileEducationEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileEducationEndDate' from JSON`,
  );
}

/** @internal */
export const GetProfileEducation$inboundSchema: z.ZodMiniType<
  GetProfileEducation,
  unknown
> = z.object({
  schoolName: types.nullable(types.string()),
  degreeName: types.nullable(types.string()),
  fieldOfStudy: types.nullable(types.string()),
  schoolUrl: types.nullable(types.string()),
  schoolLogo: types.nullable(types.string()),
  startDate: types.nullable(
    z.lazy(() => GetProfileEducationStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => GetProfileEducationEndDate$inboundSchema),
  ),
});

export function getProfileEducationFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileEducation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileEducation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileEducation' from JSON`,
  );
}

/** @internal */
export const GetProfileLocation$inboundSchema: z.ZodMiniType<
  GetProfileLocation,
  unknown
> = z.object({
  countryCode: types.string(),
  postalCode: types.optional(types.string()),
});

export function getProfileLocationFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileLocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileLocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileLocation' from JSON`,
  );
}

/** @internal */
export const GetProfileType$inboundSchema: z.ZodMiniType<
  GetProfileType,
  unknown
> = openEnums.inboundSchema(GetProfileType);

/** @internal */
export const GetProfileMediaType$inboundSchema: z.ZodMiniType<
  GetProfileMediaType,
  unknown
> = openEnums.inboundSchema(GetProfileMediaType);

/** @internal */
export const GetProfileMedia$inboundSchema: z.ZodMiniType<
  GetProfileMedia,
  unknown
> = z.object({
  type: GetProfileMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function getProfileMediaFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileMedia' from JSON`,
  );
}

/** @internal */
export const GetProfileReactionTypeCount$inboundSchema: z.ZodMiniType<
  GetProfileReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function getProfileReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const GetProfileLastPost$inboundSchema: z.ZodMiniType<
  GetProfileLastPost,
  unknown
> = z.object({
  postUrl: types.string(),
  text: types.string(),
  date: types.number(),
  likesCount: types.number(),
  commentsCount: types.number(),
  sharesCount: types.number(),
  postUrn: types.string(),
  postId: types.string(),
  type: GetProfileType$inboundSchema,
  isRepost: types.optional(types.boolean()),
  media: types.optional(z.lazy(() => GetProfileMedia$inboundSchema)),
  authorName: types.optional(types.string()),
  authorProfileUrl: types.optional(types.string()),
  authorPublicIdentifier: z.optional(z.nullable(types.string())),
  authorProfileUrn: z.optional(z.nullable(types.string())),
  authorHeadline: types.optional(types.string()),
  authorImageUrl: types.optional(types.string()),
  repostedFromName: types.optional(types.string()),
  reactionTypeCounts: types.optional(
    z.array(z.lazy(() => GetProfileReactionTypeCount$inboundSchema)),
  ),
  numImpressions: z.optional(z.nullable(types.number())),
  highlightedReactorName: z.optional(z.nullable(types.string())),
  viewerLiked: z.optional(z.nullable(types.boolean())),
});

export function getProfileLastPostFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileLastPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileLastPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileLastPost' from JSON`,
  );
}

/** @internal */
export const GetProfileCredits$inboundSchema: z.ZodMiniType<
  GetProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileCredits' from JSON`,
  );
}

/** @internal */
export const GetProfileMeta$inboundSchema: z.ZodMiniType<
  GetProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetProfileCredits$inboundSchema),
});

export function getProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileMeta' from JSON`,
  );
}

/** @internal */
export const GetProfileResponse$inboundSchema: z.ZodMiniType<
  GetProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    linkedinProfileId: types.nullable(types.string()),
    linkedinName: types.nullable(types.string()),
    linkedinEmail: types.nullable(types.string()),
    profileUrn: types.nullable(types.string()),
    headline: types.nullable(types.string()),
    profilePic: types.nullable(types.string()),
    profileUrl: types.nullable(types.string()),
    summary: z.optional(z.nullable(types.string())),
    positions: types.optional(
      z.array(z.lazy(() => GetProfilePosition$inboundSchema)),
    ),
    educations: types.optional(
      z.array(z.lazy(() => GetProfileEducation$inboundSchema)),
    ),
    connectionsCount: types.nullable(types.number()),
    followersCount: z.optional(z.nullable(types.number())),
    location: types.nullable(z.lazy(() => GetProfileLocation$inboundSchema)),
    isVerified: types.nullable(types.boolean()),
    lastPosts: types.optional(
      z.array(z.lazy(() => GetProfileLastPost$inboundSchema)),
    ),
    lastComments: types.optional(z.array(z.any())),
    lastReactions: types.optional(z.array(z.any())),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetProfileResponse' from JSON`,
  );
}

/** @internal */
export const ListAccountsProxyConfig$inboundSchema: z.ZodMiniType<
  ListAccountsProxyConfig,
  unknown
> = z.object({
  enabled: types.boolean(),
});

export function listAccountsProxyConfigFromJSON(
  jsonString: string,
): SafeParseResult<ListAccountsProxyConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAccountsProxyConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAccountsProxyConfig' from JSON`,
  );
}

/** @internal */
export const ListAccountsAccount$inboundSchema: z.ZodMiniType<
  ListAccountsAccount,
  unknown
> = z.object({
  id: types.string(),
  label: types.nullable(types.string()),
  isDefault: types.boolean(),
  isCurrent: types.boolean(),
  linkedinProfileId: types.nullable(types.string()),
  linkedinName: types.nullable(types.string()),
  linkedinEmail: types.nullable(types.string()),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePic: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
  isActive: types.boolean(),
  isValid: types.boolean(),
  accountPlan: types.nullable(types.string()),
  isUnlimited: types.boolean(),
  proxyConfig: types.nullable(
    z.lazy(() => ListAccountsProxyConfig$inboundSchema),
  ),
  ownerUserId: z.optional(z.nullable(types.string())),
  owner: z.optional(z.nullable(types.string())),
  repeatDisconnect: types.optional(types.boolean()),
  simulateWrites: types.optional(types.boolean()),
  salesNavSeatStatus: z.optional(z.nullable(types.string())),
  salesNavSeatLastProbedAt: z.optional(z.nullable(types.string())),
  createdAt: types.string(),
  updatedAt: types.string(),
});

export function listAccountsAccountFromJSON(
  jsonString: string,
): SafeParseResult<ListAccountsAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAccountsAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAccountsAccount' from JSON`,
  );
}

/** @internal */
export const ListAccountsCredits$inboundSchema: z.ZodMiniType<
  ListAccountsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listAccountsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListAccountsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAccountsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAccountsCredits' from JSON`,
  );
}

/** @internal */
export const ListAccountsMeta$inboundSchema: z.ZodMiniType<
  ListAccountsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListAccountsCredits$inboundSchema),
});

export function listAccountsMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListAccountsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAccountsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAccountsMeta' from JSON`,
  );
}

/** @internal */
export const ListAccountsResponse$inboundSchema: z.ZodMiniType<
  ListAccountsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    accounts: z.array(z.lazy(() => ListAccountsAccount$inboundSchema)),
    total: types.number(),
    isOwner: types.boolean(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListAccountsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listAccountsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAccountsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAccountsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAccountsResponse' from JSON`,
  );
}

/** @internal */
export type ProxyConfigIsp$Outbound = {
  mode: "isp";
  country: string;
  staticIp: string;
  staticPort: number;
};

/** @internal */
export const ProxyConfigIsp$outboundSchema: z.ZodMiniType<
  ProxyConfigIsp$Outbound,
  ProxyConfigIsp
> = z.object({
  mode: z.literal("isp"),
  country: z.string(),
  staticIp: z.string(),
  staticPort: z.int(),
});

export function proxyConfigIspToJSON(proxyConfigIsp: ProxyConfigIsp): string {
  return JSON.stringify(ProxyConfigIsp$outboundSchema.parse(proxyConfigIsp));
}

/** @internal */
export type ProxyConfigResidential$Outbound = {
  mode: "residential";
  country: string;
  city?: string | undefined;
  rotationHours: number;
};

/** @internal */
export const ProxyConfigResidential$outboundSchema: z.ZodMiniType<
  ProxyConfigResidential$Outbound,
  ProxyConfigResidential
> = z.object({
  mode: z.literal("residential"),
  country: z.string(),
  city: z.optional(z.string()),
  rotationHours: z._default(z.int(), 1),
});

export function proxyConfigResidentialToJSON(
  proxyConfigResidential: ProxyConfigResidential,
): string {
  return JSON.stringify(
    ProxyConfigResidential$outboundSchema.parse(proxyConfigResidential),
  );
}

/** @internal */
export type ProxyConfig$Outbound =
  | ProxyConfigResidential$Outbound
  | ProxyConfigIsp$Outbound;

/** @internal */
export const ProxyConfig$outboundSchema: z.ZodMiniType<
  ProxyConfig$Outbound,
  ProxyConfig
> = z.union([
  z.lazy(() => ProxyConfigResidential$outboundSchema),
  z.lazy(() => ProxyConfigIsp$outboundSchema),
]);

export function proxyConfigToJSON(proxyConfig: ProxyConfig): string {
  return JSON.stringify(ProxyConfig$outboundSchema.parse(proxyConfig));
}

/** @internal */
export type UpdateAccountRequest$Outbound = {
  accountId: string;
  label?: string | null | undefined;
  isDefault?: boolean | undefined;
  isActive?: boolean | undefined;
  proxyConfig?:
    | ProxyConfigResidential$Outbound
    | ProxyConfigIsp$Outbound
    | null
    | undefined;
};

/** @internal */
export const UpdateAccountRequest$outboundSchema: z.ZodMiniType<
  UpdateAccountRequest$Outbound,
  UpdateAccountRequest
> = z.object({
  accountId: z.string(),
  label: z.optional(z.nullable(z.string())),
  isDefault: z.optional(z.boolean()),
  isActive: z.optional(z.boolean()),
  proxyConfig: z.optional(
    z.nullable(z.union([
      z.lazy(() => ProxyConfigResidential$outboundSchema),
      z.lazy(() =>
        ProxyConfigIsp$outboundSchema
      ),
    ])),
  ),
});

export function updateAccountRequestToJSON(
  updateAccountRequest: UpdateAccountRequest,
): string {
  return JSON.stringify(
    UpdateAccountRequest$outboundSchema.parse(updateAccountRequest),
  );
}

/** @internal */
export const UpdateAccountProxyConfigResponse$inboundSchema: z.ZodMiniType<
  UpdateAccountProxyConfigResponse,
  unknown
> = z.object({
  city: types.nullable(types.string()),
  mode: types.nullable(types.string()),
  country: types.nullable(types.string()),
  rotationHours: types.nullable(types.number()),
});

export function updateAccountProxyConfigResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateAccountProxyConfigResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateAccountProxyConfigResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateAccountProxyConfigResponse' from JSON`,
  );
}

/** @internal */
export const UpdateAccountAccount$inboundSchema: z.ZodMiniType<
  UpdateAccountAccount,
  unknown
> = z.object({
  id: types.string(),
  label: types.nullable(types.string()),
  isDefault: types.boolean(),
  linkedinProfileId: types.nullable(types.string()),
  linkedinName: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePic: types.nullable(types.string()),
  isValid: types.boolean(),
  isActive: types.boolean(),
  proxyConfig: types.nullable(
    z.lazy(() => UpdateAccountProxyConfigResponse$inboundSchema),
  ),
});

export function updateAccountAccountFromJSON(
  jsonString: string,
): SafeParseResult<UpdateAccountAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateAccountAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateAccountAccount' from JSON`,
  );
}

/** @internal */
export const UpdateAccountCredits$inboundSchema: z.ZodMiniType<
  UpdateAccountCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function updateAccountCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UpdateAccountCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateAccountCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateAccountCredits' from JSON`,
  );
}

/** @internal */
export const UpdateAccountMeta$inboundSchema: z.ZodMiniType<
  UpdateAccountMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UpdateAccountCredits$inboundSchema),
});

export function updateAccountMetaFromJSON(
  jsonString: string,
): SafeParseResult<UpdateAccountMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateAccountMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateAccountMeta' from JSON`,
  );
}

/** @internal */
export const UpdateAccountResponse$inboundSchema: z.ZodMiniType<
  UpdateAccountResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    account: z.lazy(() => UpdateAccountAccount$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => UpdateAccountMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function updateAccountResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateAccountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateAccountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateAccountResponse' from JSON`,
  );
}

/** @internal */
export type GetFollowersRequest$Outbound = {
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const GetFollowersRequest$outboundSchema: z.ZodMiniType<
  GetFollowersRequest$Outbound,
  GetFollowersRequest
> = z.object({
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function getFollowersRequestToJSON(
  getFollowersRequest: GetFollowersRequest,
): string {
  return JSON.stringify(
    GetFollowersRequest$outboundSchema.parse(getFollowersRequest),
  );
}

/** @internal */
export const Follower$inboundSchema: z.ZodMiniType<Follower, unknown> = z
  .object({
    name: types.string(),
    headline: types.nullable(types.string()),
    profileUrl: types.nullable(types.string()),
    profilePicture: types.nullable(types.string()),
    followerCount: types.nullable(types.number()),
    following: types.boolean(),
    profileUrn: types.nullable(types.string()),
    publicIdentifier: types.nullable(types.string()),
  });

export function followerFromJSON(
  jsonString: string,
): SafeParseResult<Follower, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Follower$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Follower' from JSON`,
  );
}

/** @internal */
export const GetFollowersCredits$inboundSchema: z.ZodMiniType<
  GetFollowersCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getFollowersCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetFollowersCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFollowersCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFollowersCredits' from JSON`,
  );
}

/** @internal */
export const GetFollowersMeta$inboundSchema: z.ZodMiniType<
  GetFollowersMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetFollowersCredits$inboundSchema),
});

export function getFollowersMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetFollowersMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFollowersMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFollowersMeta' from JSON`,
  );
}

/** @internal */
export const GetFollowersResponse$inboundSchema: z.ZodMiniType<
  GetFollowersResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    followers: z.array(z.lazy(() => Follower$inboundSchema)),
    count: types.number(),
    totalFollowers: types.number(),
    start: types.number(),
    hasMore: types.boolean(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetFollowersMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getFollowersResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetFollowersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFollowersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFollowersResponse' from JSON`,
  );
}

/** @internal */
export const ConnectionRequestDaily$inboundSchema: z.ZodMiniType<
  ConnectionRequestDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function connectionRequestDailyFromJSON(
  jsonString: string,
): SafeParseResult<ConnectionRequestDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectionRequestDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectionRequestDaily' from JSON`,
  );
}

/** @internal */
export const ConnectionRequestWeekly$inboundSchema: z.ZodMiniType<
  ConnectionRequestWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function connectionRequestWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<ConnectionRequestWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectionRequestWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectionRequestWeekly' from JSON`,
  );
}

/** @internal */
export const ConnectionRequest$inboundSchema: z.ZodMiniType<
  ConnectionRequest,
  unknown
> = z.object({
  daily: types.nullable(z.lazy(() => ConnectionRequestDaily$inboundSchema)),
  weekly: types.nullable(z.lazy(() => ConnectionRequestWeekly$inboundSchema)),
  minIntervalSeconds: types.number(),
  nextResetDaily: types.nullable(types.string()),
  nextResetWeekly: types.nullable(types.string()),
});

export function connectionRequestFromJSON(
  jsonString: string,
): SafeParseResult<ConnectionRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectionRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectionRequest' from JSON`,
  );
}

/** @internal */
export const MessageDaily$inboundSchema: z.ZodMiniType<MessageDaily, unknown> =
  z.object({
    current: types.number(),
    limit: types.number(),
    remaining: types.number(),
  });

export function messageDailyFromJSON(
  jsonString: string,
): SafeParseResult<MessageDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MessageDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MessageDaily' from JSON`,
  );
}

/** @internal */
export const MessageWeekly$inboundSchema: z.ZodMiniType<
  MessageWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function messageWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<MessageWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MessageWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MessageWeekly' from JSON`,
  );
}

/** @internal */
export const GetLimitsMessage$inboundSchema: z.ZodMiniType<
  GetLimitsMessage,
  unknown
> = z.object({
  daily: types.nullable(z.lazy(() => MessageDaily$inboundSchema)),
  weekly: types.nullable(z.lazy(() => MessageWeekly$inboundSchema)),
  minIntervalSeconds: types.number(),
  nextResetDaily: types.nullable(types.string()),
  nextResetWeekly: types.nullable(types.string()),
});

export function getLimitsMessageFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsMessage' from JSON`,
  );
}

/** @internal */
export const ProfileVisitDaily$inboundSchema: z.ZodMiniType<
  ProfileVisitDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function profileVisitDailyFromJSON(
  jsonString: string,
): SafeParseResult<ProfileVisitDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProfileVisitDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProfileVisitDaily' from JSON`,
  );
}

/** @internal */
export const ProfileVisitWeekly$inboundSchema: z.ZodMiniType<
  ProfileVisitWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function profileVisitWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<ProfileVisitWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProfileVisitWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProfileVisitWeekly' from JSON`,
  );
}

/** @internal */
export const ProfileVisit$inboundSchema: z.ZodMiniType<ProfileVisit, unknown> =
  z.object({
    daily: types.nullable(z.lazy(() => ProfileVisitDaily$inboundSchema)),
    weekly: types.nullable(z.lazy(() => ProfileVisitWeekly$inboundSchema)),
    minIntervalSeconds: types.number(),
    nextResetDaily: types.nullable(types.string()),
    nextResetWeekly: types.nullable(types.string()),
  });

export function profileVisitFromJSON(
  jsonString: string,
): SafeParseResult<ProfileVisit, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProfileVisit$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProfileVisit' from JSON`,
  );
}

/** @internal */
export const ScrapingDaily$inboundSchema: z.ZodMiniType<
  ScrapingDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function scrapingDailyFromJSON(
  jsonString: string,
): SafeParseResult<ScrapingDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScrapingDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScrapingDaily' from JSON`,
  );
}

/** @internal */
export const ScrapingWeekly$inboundSchema: z.ZodMiniType<
  ScrapingWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function scrapingWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<ScrapingWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScrapingWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScrapingWeekly' from JSON`,
  );
}

/** @internal */
export const Scraping$inboundSchema: z.ZodMiniType<Scraping, unknown> = z
  .object({
    daily: types.nullable(z.lazy(() => ScrapingDaily$inboundSchema)),
    weekly: types.nullable(z.lazy(() => ScrapingWeekly$inboundSchema)),
    minIntervalSeconds: types.number(),
    nextResetDaily: types.nullable(types.string()),
    nextResetWeekly: types.nullable(types.string()),
  });

export function scrapingFromJSON(
  jsonString: string,
): SafeParseResult<Scraping, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Scraping$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Scraping' from JSON`,
  );
}

/** @internal */
export const PostDaily$inboundSchema: z.ZodMiniType<PostDaily, unknown> = z
  .object({
    current: types.number(),
    limit: types.number(),
    remaining: types.number(),
  });

export function postDailyFromJSON(
  jsonString: string,
): SafeParseResult<PostDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostDaily' from JSON`,
  );
}

/** @internal */
export const PostWeekly$inboundSchema: z.ZodMiniType<PostWeekly, unknown> = z
  .object({
    current: types.number(),
    limit: types.number(),
    remaining: types.number(),
  });

export function postWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<PostWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PostWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PostWeekly' from JSON`,
  );
}

/** @internal */
export const GetLimitsPost$inboundSchema: z.ZodMiniType<
  GetLimitsPost,
  unknown
> = z.object({
  daily: types.nullable(z.lazy(() => PostDaily$inboundSchema)),
  weekly: types.nullable(z.lazy(() => PostWeekly$inboundSchema)),
  minIntervalSeconds: types.number(),
  nextResetDaily: types.nullable(types.string()),
  nextResetWeekly: types.nullable(types.string()),
});

export function getLimitsPostFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsPost' from JSON`,
  );
}

/** @internal */
export const AcceptInvitationDaily2$inboundSchema: z.ZodMiniType<
  AcceptInvitationDaily2,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function acceptInvitationDaily2FromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitationDaily2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitationDaily2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitationDaily2' from JSON`,
  );
}

/** @internal */
export const AcceptInvitationWeekly2$inboundSchema: z.ZodMiniType<
  AcceptInvitationWeekly2,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function acceptInvitationWeekly2FromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitationWeekly2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitationWeekly2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitationWeekly2' from JSON`,
  );
}

/** @internal */
export const AcceptInvitation$inboundSchema: z.ZodMiniType<
  AcceptInvitation,
  unknown
> = z.object({
  daily: types.nullable(z.lazy(() => AcceptInvitationDaily2$inboundSchema)),
  weekly: types.nullable(z.lazy(() => AcceptInvitationWeekly2$inboundSchema)),
  minIntervalSeconds: types.number(),
  nextResetDaily: types.nullable(types.string()),
  nextResetWeekly: types.nullable(types.string()),
});

export function acceptInvitationFromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitation' from JSON`,
  );
}

/** @internal */
export const CommentPostDaily$inboundSchema: z.ZodMiniType<
  CommentPostDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function commentPostDailyFromJSON(
  jsonString: string,
): SafeParseResult<CommentPostDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentPostDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentPostDaily' from JSON`,
  );
}

/** @internal */
export const CommentPostWeekly$inboundSchema: z.ZodMiniType<
  CommentPostWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function commentPostWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<CommentPostWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentPostWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentPostWeekly' from JSON`,
  );
}

/** @internal */
export const CommentPost$inboundSchema: z.ZodMiniType<CommentPost, unknown> = z
  .object({
    daily: types.nullable(z.lazy(() => CommentPostDaily$inboundSchema)),
    weekly: types.nullable(z.lazy(() => CommentPostWeekly$inboundSchema)),
    minIntervalSeconds: types.number(),
    nextResetDaily: types.nullable(types.string()),
    nextResetWeekly: types.nullable(types.string()),
  });

export function commentPostFromJSON(
  jsonString: string,
): SafeParseResult<CommentPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentPost' from JSON`,
  );
}

/** @internal */
export const ReplyCommentDaily$inboundSchema: z.ZodMiniType<
  ReplyCommentDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function replyCommentDailyFromJSON(
  jsonString: string,
): SafeParseResult<ReplyCommentDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyCommentDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyCommentDaily' from JSON`,
  );
}

/** @internal */
export const ReplyCommentWeekly$inboundSchema: z.ZodMiniType<
  ReplyCommentWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function replyCommentWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<ReplyCommentWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyCommentWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyCommentWeekly' from JSON`,
  );
}

/** @internal */
export const ReplyComment$inboundSchema: z.ZodMiniType<ReplyComment, unknown> =
  z.object({
    daily: types.nullable(z.lazy(() => ReplyCommentDaily$inboundSchema)),
    weekly: types.nullable(z.lazy(() => ReplyCommentWeekly$inboundSchema)),
    minIntervalSeconds: types.number(),
    nextResetDaily: types.nullable(types.string()),
    nextResetWeekly: types.nullable(types.string()),
  });

export function replyCommentFromJSON(
  jsonString: string,
): SafeParseResult<ReplyComment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyComment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyComment' from JSON`,
  );
}

/** @internal */
export const LimitsDaily$inboundSchema: z.ZodMiniType<LimitsDaily, unknown> = z
  .object({
    current: types.number(),
    limit: types.number(),
    remaining: types.number(),
  });

export function limitsDailyFromJSON(
  jsonString: string,
): SafeParseResult<LimitsDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LimitsDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LimitsDaily' from JSON`,
  );
}

/** @internal */
export const LimitsWeekly$inboundSchema: z.ZodMiniType<LimitsWeekly, unknown> =
  z.object({
    current: types.number(),
    limit: types.number(),
    remaining: types.number(),
  });

export function limitsWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<LimitsWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LimitsWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LimitsWeekly' from JSON`,
  );
}

/** @internal */
export const GetLimitsProfile$inboundSchema: z.ZodMiniType<
  GetLimitsProfile,
  unknown
> = z.object({
  daily: types.nullable(z.lazy(() => LimitsDaily$inboundSchema)),
  weekly: types.nullable(z.lazy(() => LimitsWeekly$inboundSchema)),
  minIntervalSeconds: types.number(),
  nextResetDaily: types.nullable(types.string()),
  nextResetWeekly: types.nullable(types.string()),
});

export function getLimitsProfileFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsProfile, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsProfile$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsProfile' from JSON`,
  );
}

/** @internal */
export const EngagementDaily$inboundSchema: z.ZodMiniType<
  EngagementDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function engagementDailyFromJSON(
  jsonString: string,
): SafeParseResult<EngagementDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EngagementDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EngagementDaily' from JSON`,
  );
}

/** @internal */
export const EngagementWeekly$inboundSchema: z.ZodMiniType<
  EngagementWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
  remaining: types.number(),
});

export function engagementWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<EngagementWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EngagementWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EngagementWeekly' from JSON`,
  );
}

/** @internal */
export const Engagement$inboundSchema: z.ZodMiniType<Engagement, unknown> = z
  .object({
    daily: types.nullable(z.lazy(() => EngagementDaily$inboundSchema)),
    weekly: types.nullable(z.lazy(() => EngagementWeekly$inboundSchema)),
    minIntervalSeconds: types.number(),
    nextResetDaily: types.nullable(types.string()),
    nextResetWeekly: types.nullable(types.string()),
  });

export function engagementFromJSON(
  jsonString: string,
): SafeParseResult<Engagement, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Engagement$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Engagement' from JSON`,
  );
}

/** @internal */
export const Limits$inboundSchema: z.ZodMiniType<Limits, unknown> = z.pipe(
  z.object({
    connection_request: z.lazy(() => ConnectionRequest$inboundSchema),
    message: z.lazy(() => GetLimitsMessage$inboundSchema),
    profile_visit: z.lazy(() => ProfileVisit$inboundSchema),
    scraping: z.lazy(() => Scraping$inboundSchema),
    post: types.optional(z.lazy(() => GetLimitsPost$inboundSchema)),
    accept_invitation: types.optional(z.lazy(() =>
      AcceptInvitation$inboundSchema
    )),
    comment_post: types.optional(z.lazy(() => CommentPost$inboundSchema)),
    reply_comment: types.optional(z.lazy(() => ReplyComment$inboundSchema)),
    profile: types.optional(z.lazy(() => GetLimitsProfile$inboundSchema)),
    engagement: types.optional(z.lazy(() => Engagement$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "connection_request": "connectionRequest",
      "profile_visit": "profileVisit",
      "accept_invitation": "acceptInvitation",
      "comment_post": "commentPost",
      "reply_comment": "replyComment",
    });
  }),
);

export function limitsFromJSON(
  jsonString: string,
): SafeParseResult<Limits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Limits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Limits' from JSON`,
  );
}

/** @internal */
export const GetLimitsCredits$inboundSchema: z.ZodMiniType<
  GetLimitsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getLimitsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsCredits' from JSON`,
  );
}

/** @internal */
export const GetLimitsMeta$inboundSchema: z.ZodMiniType<
  GetLimitsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetLimitsCredits$inboundSchema),
});

export function getLimitsMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsMeta' from JSON`,
  );
}

/** @internal */
export const GetLimitsResponse$inboundSchema: z.ZodMiniType<
  GetLimitsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    multiplier: types.number(),
    limits: z.lazy(() => Limits$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetLimitsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getLimitsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetLimitsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetLimitsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetLimitsResponse' from JSON`,
  );
}

/** @internal */
export const State$inboundSchema: z.ZodMiniType<State, unknown> = openEnums
  .inboundSchema(State);

/** @internal */
export const UpcomingStatus$inboundSchema: z.ZodMiniType<
  UpcomingStatus,
  unknown
> = openEnums.inboundSchema(UpcomingStatus);

/** @internal */
export const UpcomingWhy$inboundSchema: z.ZodMiniType<UpcomingWhy, unknown> = z
  .object({
    status: UpcomingStatus$inboundSchema,
    detail: types.string(),
    clearsItself: types.nullable(types.boolean()),
    fixUrl: types.nullable(types.string()),
  });

export function upcomingWhyFromJSON(
  jsonString: string,
): SafeParseResult<UpcomingWhy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpcomingWhy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpcomingWhy' from JSON`,
  );
}

/** @internal */
export const Upcoming$inboundSchema: z.ZodMiniType<Upcoming, unknown> = z
  .object({
    contactId: types.string(),
    name: types.nullable(types.string()),
    avatarUrl: types.nullable(types.string()),
    headline: types.nullable(types.string()),
    linkedinUrl: types.nullable(types.string()),
    lastActivityAt: types.nullable(types.string()),
    thinProfile: types.boolean(),
    why: z.lazy(() => UpcomingWhy$inboundSchema),
    position: types.number(),
    etaDays: types.nullable(types.number()),
  });

export function upcomingFromJSON(
  jsonString: string,
): SafeParseResult<Upcoming, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Upcoming$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Upcoming' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusKind$inboundSchema: z.ZodMiniType<
  GetConnectionStatusKind,
  unknown
> = openEnums.inboundSchema(GetConnectionStatusKind);

/** @internal */
export const GetConnectionStatusBlocked$inboundSchema: z.ZodMiniType<
  GetConnectionStatusBlocked,
  unknown
> = z.object({
  kind: GetConnectionStatusKind$inboundSchema,
  message: types.string(),
  resumesAt: types.nullable(types.string()),
  clearsItself: types.boolean(),
  fixUrl: types.nullable(types.string()),
});

export function getConnectionStatusBlockedFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusBlocked, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusBlocked$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusBlocked' from JSON`,
  );
}

/** @internal */
export const CapProbe$inboundSchema: z.ZodMiniType<CapProbe, unknown> = z
  .object({
    nextProbeAt: types.nullable(types.string()),
    rung: types.number(),
    finalRung: types.number(),
  });

export function capProbeFromJSON(
  jsonString: string,
): SafeParseResult<CapProbe, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CapProbe$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CapProbe' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusSkipped$inboundSchema: z.ZodMiniType<
  GetConnectionStatusSkipped,
  unknown
> = z.object({
  reason: types.string(),
  count: types.number(),
});

export function getConnectionStatusSkippedFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusSkipped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusSkipped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusSkipped' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusAccount$inboundSchema: z.ZodMiniType<
  GetConnectionStatusAccount,
  unknown
> = z.object({
  linkedinCredentialsId: types.string(),
  accountLabel: types.string(),
  state: State$inboundSchema,
  upcoming: z.array(z.lazy(() => Upcoming$inboundSchema)),
  nextSendAt: types.nullable(types.string()),
  blocked: types.nullable(
    z.lazy(() => GetConnectionStatusBlocked$inboundSchema),
  ),
  capProbe: types.nullable(z.lazy(() => CapProbe$inboundSchema)),
  waitingTotal: types.number(),
  oldestWaitingHours: types.nullable(types.number()),
  awaitingResponse: types.number(),
  sentLast24h: types.number(),
  acceptedLast24h: types.number(),
  sendsPerDay: types.number(),
  expectedNext24h: types.nullable(types.number()),
  skipped: z.array(z.lazy(() => GetConnectionStatusSkipped$inboundSchema)),
});

export function getConnectionStatusAccountFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusAccount' from JSON`,
  );
}

/** @internal */
export const NextStatus$inboundSchema: z.ZodMiniType<NextStatus, unknown> =
  openEnums.inboundSchema(NextStatus);

/** @internal */
export const NextWhy$inboundSchema: z.ZodMiniType<NextWhy, unknown> = z.object({
  status: NextStatus$inboundSchema,
  detail: types.string(),
  clearsItself: types.nullable(types.boolean()),
  fixUrl: types.nullable(types.string()),
});

export function nextWhyFromJSON(
  jsonString: string,
): SafeParseResult<NextWhy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NextWhy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NextWhy' from JSON`,
  );
}

/** @internal */
export const Next$inboundSchema: z.ZodMiniType<Next, unknown> = z.object({
  contactId: types.string(),
  name: types.nullable(types.string()),
  avatarUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  linkedinUrl: types.nullable(types.string()),
  lastActivityAt: types.nullable(types.string()),
  thinProfile: types.boolean(),
  why: z.lazy(() => NextWhy$inboundSchema),
  position: types.number(),
  etaDays: types.nullable(types.number()),
  accountLabel: types.string(),
});

export function nextFromJSON(
  jsonString: string,
): SafeParseResult<Next, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Next$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Next' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusTotals$inboundSchema: z.ZodMiniType<
  GetConnectionStatusTotals,
  unknown
> = z.object({
  waitingTotal: types.number(),
  awaitingResponse: types.number(),
  sentLast24h: types.number(),
  acceptedLast24h: types.number(),
  expectedNext24h: types.nullable(types.number()),
  expectedIsPartial: types.boolean(),
  oldestWaitingHours: types.nullable(types.number()),
  accountsSending: types.number(),
  accountsBlocked: types.number(),
  accountsUnreadable: types.number(),
  next: types.nullable(z.lazy(() => Next$inboundSchema)),
});

export function getConnectionStatusTotalsFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusTotals' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusCredits$inboundSchema: z.ZodMiniType<
  GetConnectionStatusCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getConnectionStatusCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusCredits' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusMeta$inboundSchema: z.ZodMiniType<
  GetConnectionStatusMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetConnectionStatusCredits$inboundSchema),
});

export function getConnectionStatusMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusMeta' from JSON`,
  );
}

/** @internal */
export const GetConnectionStatusResponse$inboundSchema: z.ZodMiniType<
  GetConnectionStatusResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    accounts: z.array(z.lazy(() => GetConnectionStatusAccount$inboundSchema)),
    totals: types.nullable(
      z.lazy(() => GetConnectionStatusTotals$inboundSchema),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetConnectionStatusMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getConnectionStatusResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionStatusResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionStatusResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionStatusResponse' from JSON`,
  );
}

/** @internal */
export const GetCreditsCredits$inboundSchema: z.ZodMiniType<
  GetCreditsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getCreditsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetCreditsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetCreditsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetCreditsCredits' from JSON`,
  );
}

/** @internal */
export const GetCreditsMetaCredits$inboundSchema: z.ZodMiniType<
  GetCreditsMetaCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getCreditsMetaCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetCreditsMetaCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetCreditsMetaCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetCreditsMetaCredits' from JSON`,
  );
}

/** @internal */
export const GetCreditsMeta$inboundSchema: z.ZodMiniType<
  GetCreditsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetCreditsMetaCredits$inboundSchema),
});

export function getCreditsMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetCreditsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetCreditsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetCreditsMeta' from JSON`,
  );
}

/** @internal */
export const GetCreditsResponse$inboundSchema: z.ZodMiniType<
  GetCreditsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    credits: z.lazy(() => GetCreditsCredits$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetCreditsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getCreditsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetCreditsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetCreditsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetCreditsResponse' from JSON`,
  );
}

/** @internal */
export type SwitchAccountRequest$Outbound = {
  credentialsId: string;
};

/** @internal */
export const SwitchAccountRequest$outboundSchema: z.ZodMiniType<
  SwitchAccountRequest$Outbound,
  SwitchAccountRequest
> = z.object({
  credentialsId: z.string(),
});

export function switchAccountRequestToJSON(
  switchAccountRequest: SwitchAccountRequest,
): string {
  return JSON.stringify(
    SwitchAccountRequest$outboundSchema.parse(switchAccountRequest),
  );
}

/** @internal */
export const SwitchAccountAccount$inboundSchema: z.ZodMiniType<
  SwitchAccountAccount,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
  avatar: types.nullable(types.string()),
  plan: types.string(),
});

export function switchAccountAccountFromJSON(
  jsonString: string,
): SafeParseResult<SwitchAccountAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SwitchAccountAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SwitchAccountAccount' from JSON`,
  );
}

/** @internal */
export const SwitchAccountCredits$inboundSchema: z.ZodMiniType<
  SwitchAccountCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function switchAccountCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SwitchAccountCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SwitchAccountCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SwitchAccountCredits' from JSON`,
  );
}

/** @internal */
export const SwitchAccountMeta$inboundSchema: z.ZodMiniType<
  SwitchAccountMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SwitchAccountCredits$inboundSchema),
});

export function switchAccountMetaFromJSON(
  jsonString: string,
): SafeParseResult<SwitchAccountMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SwitchAccountMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SwitchAccountMeta' from JSON`,
  );
}

/** @internal */
export const SwitchAccountResponse$inboundSchema: z.ZodMiniType<
  SwitchAccountResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    account: z.lazy(() => SwitchAccountAccount$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SwitchAccountMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function switchAccountResponseFromJSON(
  jsonString: string,
): SafeParseResult<SwitchAccountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SwitchAccountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SwitchAccountResponse' from JSON`,
  );
}

/** @internal */
export const SortType$outboundSchema: z.ZodMiniEnum<typeof SortType> = z.enum(
  SortType,
);

/** @internal */
export type GetConnectionsRequest$Outbound = {
  start: number;
  count: number;
  sortType?: string | undefined;
  keywords?: string | undefined;
};

/** @internal */
export const GetConnectionsRequest$outboundSchema: z.ZodMiniType<
  GetConnectionsRequest$Outbound,
  GetConnectionsRequest
> = z.object({
  start: z._default(z.int(), 0),
  count: z._default(z.int(), 40),
  sortType: z.optional(SortType$outboundSchema),
  keywords: z.optional(z.string()),
});

export function getConnectionsRequestToJSON(
  getConnectionsRequest: GetConnectionsRequest,
): string {
  return JSON.stringify(
    GetConnectionsRequest$outboundSchema.parse(getConnectionsRequest),
  );
}

/** @internal */
export const ConnectedAt$inboundSchema: z.ZodMiniType<ConnectedAt, unknown> =
  smartUnion([types.number(), types.string()]);

export function connectedAtFromJSON(
  jsonString: string,
): SafeParseResult<ConnectedAt, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectedAt$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectedAt' from JSON`,
  );
}

/** @internal */
export const Connection$inboundSchema: z.ZodMiniType<Connection, unknown> = z
  .object({
    name: types.string(),
    headline: types.nullable(types.string()),
    profileUrl: types.nullable(types.string()),
    profileUrn: types.nullable(types.string()),
    publicIdentifier: types.nullable(types.string()),
    profilePicture: types.nullable(types.string()),
    connectedAt: types.nullable(smartUnion([types.number(), types.string()])),
  });

export function connectionFromJSON(
  jsonString: string,
): SafeParseResult<Connection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Connection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Connection' from JSON`,
  );
}

/** @internal */
export const GetConnectionsCredits$inboundSchema: z.ZodMiniType<
  GetConnectionsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getConnectionsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionsCredits' from JSON`,
  );
}

/** @internal */
export const GetConnectionsMeta$inboundSchema: z.ZodMiniType<
  GetConnectionsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetConnectionsCredits$inboundSchema),
});

export function getConnectionsMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionsMeta' from JSON`,
  );
}

/** @internal */
export const GetConnectionsResponse$inboundSchema: z.ZodMiniType<
  GetConnectionsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    connections: z.array(z.lazy(() => Connection$inboundSchema)),
    count: types.number(),
    start: types.number(),
    total: types.number(),
    hasMore: types.boolean(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetConnectionsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getConnectionsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetConnectionsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConnectionsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConnectionsResponse' from JSON`,
  );
}

/** @internal */
export const QueryParamTabType$outboundSchema: z.ZodMiniEnum<
  typeof QueryParamTabType
> = z.enum(QueryParamTabType);

/** @internal */
export type GetOwnActivityRequest$Outbound = {
  tabType: string;
  count: number;
  start: number;
  paginationToken?: string | undefined;
};

/** @internal */
export const GetOwnActivityRequest$outboundSchema: z.ZodMiniType<
  GetOwnActivityRequest$Outbound,
  GetOwnActivityRequest
> = z.object({
  tabType: z._default(QueryParamTabType$outboundSchema, "COMMENTS"),
  count: z._default(z.int(), 20),
  start: z._default(z.int(), 0),
  paginationToken: z.optional(z.string()),
});

export function getOwnActivityRequestToJSON(
  getOwnActivityRequest: GetOwnActivityRequest,
): string {
  return JSON.stringify(
    GetOwnActivityRequest$outboundSchema.parse(getOwnActivityRequest),
  );
}

/** @internal */
export const TabTypeResponse$inboundSchema: z.ZodMiniType<
  TabTypeResponse,
  unknown
> = openEnums.inboundSchema(TabTypeResponse);

/** @internal */
export const GetOwnActivityCredits$inboundSchema: z.ZodMiniType<
  GetOwnActivityCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getOwnActivityCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnActivityCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnActivityCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnActivityCredits' from JSON`,
  );
}

/** @internal */
export const GetOwnActivityMeta$inboundSchema: z.ZodMiniType<
  GetOwnActivityMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetOwnActivityCredits$inboundSchema),
});

export function getOwnActivityMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnActivityMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnActivityMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnActivityMeta' from JSON`,
  );
}

/** @internal */
export const GetOwnActivityResponse$inboundSchema: z.ZodMiniType<
  GetOwnActivityResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    actor: types.string(),
    tabType: TabTypeResponse$inboundSchema,
    items: z.array(z.any()),
    count: types.number(),
    total: types.number(),
    start: types.number(),
    paginationToken: types.nullable(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetOwnActivityMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getOwnActivityResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnActivityResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnActivityResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnActivityResponse' from JSON`,
  );
}

/** @internal */
export type GetOwnPostsRequest$Outbound = {
  count?: number | undefined;
  start?: number | undefined;
  paginationToken?: string | undefined;
};

/** @internal */
export const GetOwnPostsRequest$outboundSchema: z.ZodMiniType<
  GetOwnPostsRequest$Outbound,
  GetOwnPostsRequest
> = z.object({
  count: z.optional(z.int()),
  start: z.optional(z.int()),
  paginationToken: z.optional(z.string()),
});

export function getOwnPostsRequestToJSON(
  getOwnPostsRequest: GetOwnPostsRequest,
): string {
  return JSON.stringify(
    GetOwnPostsRequest$outboundSchema.parse(getOwnPostsRequest),
  );
}

/** @internal */
export const GetOwnPostsType$inboundSchema: z.ZodMiniType<
  GetOwnPostsType,
  unknown
> = openEnums.inboundSchema(GetOwnPostsType);
