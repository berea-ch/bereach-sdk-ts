/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Reactions, comments, or both (the default); pick the one the request names.
 */
export const Surface = {
  Likes: "likes",
  Comments: "comments",
  Both: "both",
} as const;
/**
 * Reactions, comments, or both (the default); pick the one the request names.
 */
export type Surface = ClosedEnum<typeof Surface>;

export type CollectEngagersRequest = {
  /**
   * LinkedIn feed/update or /posts/ URLs only. Not Pulse, Sales Navigator, company pages, or lnkd.in short links.
   */
  postUrls: Array<string>;
  /**
   * Next page of the same posts, set only when they asked for more people. Each page spends the connected account's action budget, so on a post with 500 reactions and comments or more, wait to be asked again.
   */
  more?: boolean | undefined;
  /**
   * Target size they named, at most 500, and 100 or more means 100. Only when a size was named; one call, since the run pages itself.
   */
  untilCount?: number | undefined;
  /**
   * Reactions, comments, or both (the default); pick the one the request names.
   */
  surface?: Surface | undefined;
  /**
   * Optional list to pin saved contacts onto. Ignored on the public preview path.
   */
  campaignSlug?: string | undefined;
};

export type CollectEngagersServiceUnavailableDetails = string | Array<string>;

export const CollectEngagersServiceUnavailableQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersServiceUnavailableQuotaKind = OpenEnum<
  typeof CollectEngagersServiceUnavailableQuotaKind
>;

export type CollectEngagersServiceUnavailableError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersServiceUnavailableQuotaKind | undefined;
};

export type CollectEngagersBadGatewayDetails = string | Array<string>;

export const CollectEngagersBadGatewayQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersBadGatewayQuotaKind = OpenEnum<
  typeof CollectEngagersBadGatewayQuotaKind
>;

export type CollectEngagersBadGatewayError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersBadGatewayQuotaKind | undefined;
};

export type CollectEngagersInternalServerErrorDetails = string | Array<string>;

export const CollectEngagersInternalServerErrorQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersInternalServerErrorQuotaKind = OpenEnum<
  typeof CollectEngagersInternalServerErrorQuotaKind
>;

export type CollectEngagersInternalServerErrorError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersInternalServerErrorQuotaKind | undefined;
};

export const CollectEngagersCode = {
  RateLimitExceeded: "rate_limit_exceeded",
} as const;
export type CollectEngagersCode = ClosedEnum<typeof CollectEngagersCode>;

/**
 * Current and max daily usage (null if no daily cap).
 */
export type CollectEngagersDaily = {
  current: number;
  limit: number;
};

/**
 * Current and max weekly usage (null if no weekly cap).
 */
export type CollectEngagersWeekly = {
  current: number;
  limit: number;
};

/**
 * Structured classification so clients can route to the right banner without keyword matching.
 */
export const CollectEngagersTooManyRequestsQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
/**
 * Structured classification so clients can route to the right banner without keyword matching.
 */
export type CollectEngagersTooManyRequestsQuotaKind = OpenEnum<
  typeof CollectEngagersTooManyRequestsQuotaKind
>;

export type CollectEngagersTooManyRequestsError = {
  code: CollectEngagersCode;
  message: string;
  docs?: string | undefined;
  /**
   * Seconds to wait before retrying.
   */
  retryAfter: number;
  /**
   * Current and max daily usage (null if no daily cap).
   */
  daily: CollectEngagersDaily | null;
  /**
   * Current and max weekly usage (null if no weekly cap).
   */
  weekly: CollectEngagersWeekly | null;
  /**
   * Structured classification so clients can route to the right banner without keyword matching.
   */
  quotaKind?: CollectEngagersTooManyRequestsQuotaKind | undefined;
};

export type CollectEngagersUnprocessableEntityDetails = string | Array<string>;

export const CollectEngagersUnprocessableEntityQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersUnprocessableEntityQuotaKind = OpenEnum<
  typeof CollectEngagersUnprocessableEntityQuotaKind
>;

export type CollectEngagersUnprocessableEntityError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersUnprocessableEntityQuotaKind | undefined;
};

export type CollectEngagersGoneDetails = string | Array<string>;

export const CollectEngagersGoneQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersGoneQuotaKind = OpenEnum<
  typeof CollectEngagersGoneQuotaKind
>;

export type CollectEngagersGoneError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersGoneQuotaKind | undefined;
};

export type CollectEngagersConflictDetails = string | Array<string>;

export const CollectEngagersConflictQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersConflictQuotaKind = OpenEnum<
  typeof CollectEngagersConflictQuotaKind
>;

export type CollectEngagersConflictError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersConflictQuotaKind | undefined;
};

export type CollectEngagersNotFoundDetails = string | Array<string>;

export const CollectEngagersNotFoundQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersNotFoundQuotaKind = OpenEnum<
  typeof CollectEngagersNotFoundQuotaKind
>;

export type CollectEngagersNotFoundError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersNotFoundQuotaKind | undefined;
};

export type CollectEngagersForbiddenDetails = string | Array<string>;

export const CollectEngagersForbiddenQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersForbiddenQuotaKind = OpenEnum<
  typeof CollectEngagersForbiddenQuotaKind
>;

export type CollectEngagersForbiddenError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersForbiddenQuotaKind | undefined;
};

export type CollectEngagersUnauthorizedDetails = string | Array<string>;

export const CollectEngagersUnauthorizedQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersUnauthorizedQuotaKind = OpenEnum<
  typeof CollectEngagersUnauthorizedQuotaKind
>;

export type CollectEngagersUnauthorizedError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersUnauthorizedQuotaKind | undefined;
};

export type CollectEngagersBadRequestDetails = string | Array<string>;

export const CollectEngagersBadRequestQuotaKind = {
  Credits: "credits",
  DailyCap: "daily-cap",
  LinkedinRate: "linkedin-rate",
  AiModelRate: "ai-model-rate",
  Pacing: "pacing",
} as const;
export type CollectEngagersBadRequestQuotaKind = OpenEnum<
  typeof CollectEngagersBadRequestQuotaKind
>;

export type CollectEngagersBadRequestError = {
  code: string;
  message: string;
  docs?: string | undefined;
  details?: string | Array<string> | undefined;
  upgradeUrl?: string | undefined;
  quotaKind?: CollectEngagersBadRequestQuotaKind | undefined;
};

/**
 * Present when no account is connected. Top visible commenters only; reactor identities are not public.
 */
export const Coverage = {
  Preview: "preview",
} as const;
/**
 * Present when no account is connected. Top visible commenters only; reactor identities are not public.
 */
export type Coverage = ClosedEnum<typeof Coverage>;

export const CollectEngagersType = {
  Like: "like",
  Comment: "comment",
} as const;
export type CollectEngagersType = OpenEnum<typeof CollectEngagersType>;

export type Person = {
  name: string | null;
  headline?: string | null | undefined;
  profileUrl: string | null;
  publicIdentifier?: string | null | undefined;
  profileUrn?: string | null | undefined;
  imageUrl?: string | null | undefined;
  commentText?: string | null | undefined;
  type?: CollectEngagersType | undefined;
};

/**
 * The post's own engagement badges, summed across the posts read. A sum, so it can never answer how many came from one post: `perPost` answers that. Absent while `count` is above 0 means the badge could not be read, never that nobody engaged. Both zero or absent WITH count 0 is the only shape that means no visible engagement on this page.
 */
export type CollectEngagersTotals = {
  likes?: number | null | undefined;
  comments?: number | null | undefined;
};

export type PerPost = {
  likes: number;
  comments: number;
};

/**
 * Chat-only connect card marker on the public preview path. autoRetry false means connecting does not start a collect run.
 */
export type DisplayLinkedInConnect = {
  reason: string;
  autoRetry?: boolean | undefined;
};

export type CollectEngagersCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CollectEngagersMeta = {
  credits: CollectEngagersCredits;
};

/**
 * Collect-run id, or a public commenter preview when no account is connected
 */
export type CollectEngagersResponse = {
  success: true;
  /**
   * Present when a connected account started a collect run. People are saved as contacts as the run proceeds, and they appear in the list rather than in the reply. A run id means the connected account was used: never explain this result as needing a connection.
   */
  runId?: string | undefined;
  /**
   * Where the run is. While it is running the list is the count, so do not state one. Once it is completed the saved number is final.
   */
  status?: string | undefined;
  /**
   * Present when no account is connected. Top visible commenters only; reactor identities are not public.
   */
  coverage?: Coverage | undefined;
  needsLinkedin?: boolean | undefined;
  people?: Array<Person> | undefined;
  postText?: string | null | undefined;
  /**
   * The post's own engagement badges, summed across the posts read. A sum, so it can never answer how many came from one post: `perPost` answers that. Absent while `count` is above 0 means the badge could not be read, never that nobody engaged. Both zero or absent WITH count 0 is the only shape that means no visible engagement on this page.
   */
  totals?: CollectEngagersTotals | undefined;
  /**
   * People saved by this call. The number to narrate. Zero is only an empty post when `unreadable` is 0; above 0 those people were not shown rather than absent.
   */
  count?: number | undefined;
  /**
   * People skipped because they need extra lookup we don't do yet. Not saved. Omit or 0 when none.
   */
  hiddenSkipped?: number | undefined;
  /**
   * Posts whose asked-for engagement could not be read. DISTINCT posts, not a sum of the two counters below: a post that lost both its lists is one post. Above 0 means the answer is incomplete, and those posts must never be reported as having no engagement.
   */
  unreadable?: number | undefined;
  /**
   * Posts where the reaction list could not be read. On a run that read both surfaces those posts contributed commenters only; on a run scoped with `surface` they contributed nobody. Say so when above 0; it does not mean nobody reacted.
   */
  likesUnavailable?: number | undefined;
  /**
   * Posts where the comment list could not be read. The comment half of the field above, and the same rule: it does not mean nobody commented.
   */
  commentsUnavailable?: number | undefined;
  /**
   * People SAVED per post, keyed by post id, split into likes and comments. Someone who engaged with several of the posts is counted once, under the first post they were saved from, so these are saved counts and not that post's engagement count: say saved, never say the post had only this many engagers. They add up to count. totals is a sum across posts, not a per-post figure.
   */
  perPost?: { [k: string]: PerPost } | undefined;
  hasMore?: boolean | undefined;
  /**
   * True when another page of people exists. Do not fetch it unless the user asked or tapped Load more.
   */
  moreAvailable?: boolean | undefined;
  /**
   * When more pages exist: this page only — do not say fully collected, all pages, or no more remain.
   */
  remainder?: string | undefined;
  /**
   * Preview only: posts whose page showed more commenters than the public view exposes. Above 0 means the people listed are the top slice, never everyone who engaged; the full thread needs a connected account.
   */
  commentersTruncated?: number | undefined;
  /**
   * A caveat about this result in words ready to relay, when one applies.
   */
  note?: string | undefined;
  /**
   * Chat-only connect card marker on the public preview path. autoRetry false means connecting does not start a collect run.
   */
  displayLinkedInConnect?: DisplayLinkedInConnect | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CollectEngagersMeta | undefined;
};

export type CollectPostsRequest = {
  /**
   * LinkedIn profile or company page URL, vanity name, or URN. Company URLs use the organization feed; personal URLs use the profile activity feed.
   */
  profileUrl: string;
  /**
   * Next page of the same profile. Set only when the user asked for more posts or tapped Load more. Each page uses the connected account's action budget.
   */
  more?: boolean | undefined;
};

export type CollectPostsTotals = {
  posts?: number | null | undefined;
};

/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export const CollectPostsType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export type CollectPostsType = OpenEnum<typeof CollectPostsType>;

/**
 * Type of media attached to the post.
 */
export const CollectPostsMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type CollectPostsMediaType = OpenEnum<typeof CollectPostsMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type CollectPostsMedia = {
  /**
   * Type of media attached to the post.
   */
  type: CollectPostsMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type CollectPostsReactionTypeCount = {
  reactionType: string;
  count: number;
};

export type CollectPostsPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
   */
  type: CollectPostsType;
  /**
   * True when the post is a repost/reshare of another post. Absent for original posts.
   */
  isRepost?: boolean | undefined;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: CollectPostsMedia | undefined;
  /**
   * Display name of the post's author.
   */
  authorName?: string | undefined;
  /**
   * Author's profile URL.
   */
  authorProfileUrl?: string | undefined;
  /**
   * Author's vanity slug, when they have one.
   */
  authorPublicIdentifier?: string | null | undefined;
  /**
   * Author's profile URN.
   */
  authorProfileUrn?: string | null | undefined;
  /**
   * Author's headline as shown on the post.
   */
  authorHeadline?: string | undefined;
  /**
   * Author's avatar URL.
   */
  authorImageUrl?: string | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<CollectPostsReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
};

export type CollectPostsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CollectPostsMeta = {
  credits: CollectPostsCredits;
};

/**
 * Collect-run id for profile posts
 */
export type CollectPostsResponse = {
  success: true;
  /**
   * Collect-run id. Posts arrive on the run record; this response may include the first pages already collected.
   */
  runId: string;
  status: string;
  totals?: CollectPostsTotals | undefined;
  posts?: Array<CollectPostsPost> | undefined;
  count?: number | undefined;
  /**
   * Pages of this profile's posts that LinkedIn would not serve. Above 0 means an empty or short list is unread rather than a profile that does not post, and must not be reported as one.
   */
  unreadable?: number | undefined;
  /**
   * The same count, named for the surface it belongs to.
   */
  postsUnavailable?: number | undefined;
  /**
   * True when another page of posts exists. Do not fetch it unless the user asked or tapped Load more.
   */
  moreAvailable?: boolean | undefined;
  hasMore?: boolean | undefined;
  /**
   * When more pages exist: this page only — do not say fully collected, all pages, or no more remain.
   */
  remainder?: string | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CollectPostsMeta | undefined;
};

export type VisitProfileRequest = {
  /**
   * LinkedIn profile identifier from a list row: full URL, vanity slug, or URN. Never a display name. A unique name in the saved pool is resolved server-side; prefer the URL from the row.
   */
  profile: string;
};

/**
 * Connection request status: 'pending' if request was sent successfully, 'failed' if request failed today, 'none' if not tracked
 */
export const PendingConnection = {
  Pending: "pending",
  Failed: "failed",
  None: "none",
} as const;
/**
 * Connection request status: 'pending' if request was sent successfully, 'failed' if request failed today, 'none' if not tracked
 */
export type PendingConnection = OpenEnum<typeof PendingConnection>;

export type VisitProfilePositionStartDate = {
  month: number | null;
  year: number;
};

export type VisitProfilePositionEndDate = {
  month: number | null;
  year: number;
};

export type VisitProfilePosition = {
  companyName: string | null;
  title: string | null;
  /**
   * Position description / responsibilities text (always fetched on visit_profile; may be null if LinkedIn has none)
   */
  description?: string | null | undefined;
  companyUrl: string | null;
  companyLogo: string | null;
  /**
   * Short description of the company (always fetched on visit_profile; may be null)
   */
  companyDescription?: string | null | undefined;
  startDate: VisitProfilePositionStartDate | null;
  endDate: VisitProfilePositionEndDate | null;
  isCurrent: boolean;
};

export type VisitProfileEducationStartDate = {
  month: number | null;
  year: number;
};

export type VisitProfileEducationEndDate = {
  month: number | null;
  year: number;
};

export type VisitProfileEducation = {
  schoolName: string | null;
  degreeName: string | null;
  fieldOfStudy: string | null;
  schoolUrl: string | null;
  schoolLogo: string | null;
  startDate: VisitProfileEducationStartDate | null;
  endDate: VisitProfileEducationEndDate | null;
};

/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export const VisitProfileLastPostType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export type VisitProfileLastPostType = OpenEnum<
  typeof VisitProfileLastPostType
>;

/**
 * Type of media attached to the post.
 */
export const VisitProfileMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type VisitProfileMediaType = OpenEnum<typeof VisitProfileMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type VisitProfileMedia = {
  /**
   * Type of media attached to the post.
   */
  type: VisitProfileMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type VisitProfileReactionTypeCount = {
  reactionType: string;
  count: number;
};

export type VisitProfileLastPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
   */
  type: VisitProfileLastPostType;
  /**
   * True when the post is a repost/reshare of another post. Absent for original posts.
   */
  isRepost?: boolean | undefined;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: VisitProfileMedia | undefined;
  /**
   * Display name of the post's author.
   */
  authorName?: string | undefined;
  /**
   * Author's profile URL.
   */
  authorProfileUrl?: string | undefined;
  /**
   * Author's vanity slug, when they have one.
   */
  authorPublicIdentifier?: string | null | undefined;
  /**
   * Author's profile URN.
   */
  authorProfileUrn?: string | null | undefined;
  /**
   * Author's headline as shown on the post.
   */
  authorHeadline?: string | undefined;
  /**
   * Author's avatar URL.
   */
  authorImageUrl?: string | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<VisitProfileReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
};

/**
 * Activity type: 'comment' for posts the user commented on
 */
export const LastCommentType = {
  Comment: "comment",
  Reaction: "reaction",
} as const;
/**
 * Activity type: 'comment' for posts the user commented on
 */
export type LastCommentType = OpenEnum<typeof LastCommentType>;

export type LastComment = {
  /**
   * Activity type: 'comment' for posts the user commented on
   */
  type: LastCommentType;
  /**
   * URN of the feed update (post) the user engaged with
   */
  activityUrn: string;
  /**
   * The user's comment text (null when not available from the API — this is a LinkedIn limitation)
   */
  text: string | null;
  /**
   * Reaction type (e.g. 'LIKE', 'CELEBRATE'). Only set for reaction-type activities
   */
  reactionType: string | null;
  /**
   * URN of the post the user commented on (e.g. 'urn:li:activity:1234567890')
   */
  targetPostUrn: string | null;
  /**
   * First ~200 chars of the post the user commented on. Use this to understand what topics they engage with
   */
  targetPostText: string | null;
  /**
   * Name of the post author (the person whose post was commented on)
   */
  targetPostAuthor: string | null;
  /**
   * Unix timestamp in milliseconds when the activity occurred
   */
  timestamp: number | null;
  /**
   * Name of the post author (same as targetPostAuthor when available)
   */
  actorName: string | null;
  /**
   * Profile URN of the post author
   */
  actorUrn: string | null;
};

/**
 * Activity type: 'comment' for posts the user commented on
 */
export const LastReactionType = {
  Comment: "comment",
  Reaction: "reaction",
} as const;
/**
 * Activity type: 'comment' for posts the user commented on
 */
export type LastReactionType = OpenEnum<typeof LastReactionType>;

export type LastReaction = {
  /**
   * Activity type: 'comment' for posts the user commented on
   */
  type: LastReactionType;
  /**
   * URN of the feed update (post) the user engaged with
   */
  activityUrn: string;
  /**
   * The user's comment text (null when not available from the API — this is a LinkedIn limitation)
   */
  text: string | null;
  /**
   * Reaction type (e.g. 'LIKE', 'CELEBRATE'). Only set for reaction-type activities
   */
  reactionType: string | null;
  /**
   * URN of the post the user commented on (e.g. 'urn:li:activity:1234567890')
   */
  targetPostUrn: string | null;
  /**
   * First ~200 chars of the post the user commented on. Use this to understand what topics they engage with
   */
  targetPostText: string | null;
  /**
   * Name of the post author (the person whose post was commented on)
   */
  targetPostAuthor: string | null;
  /**
   * Unix timestamp in milliseconds when the activity occurred
   */
  timestamp: number | null;
  /**
   * Name of the post author (same as targetPostAuthor when available)
   */
  actorName: string | null;
  /**
   * Profile URN of the post author
   */
  actorUrn: string | null;
};

export type VisitProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type VisitProfileMeta = {
  credits: VisitProfileCredits;
};

/**
 * Profile contact data
 */
export type VisitProfileResponse = {
  success: true;
  firstName: string;
  lastName: string;
  headline: string | null;
  /**
   * The profile's 'About' section text. Always fetched; null only when the profile has no About filled in.
   */
  summary: string | null;
  publicIdentifier: string;
  /**
   * LinkedIn profile URL. Null when publicIdentifier is unavailable (restricted profiles).
   */
  profileUrl: string | null;
  /**
   * LinkedIn profile URN (e.g. 'urn:li:fsd_profile:ACoAAA...'). Use this for matching against inbox participants.
   */
  profileUrn: string | null;
  imageUrl: string | null;
  email: string | null;
  /**
   * Phone number from LinkedIn profile (rarely available)
   */
  phone: string | null;
  location: string | null;
  /**
   * Current company name (from most recent position)
   */
  company: string | null;
  /**
   * Current job title (from most recent position)
   */
  position: string | null;
  /**
   * Connection degree: 0 (self), 1 (direct connection), 2+ (2nd degree, 3rd degree, etc.), or null if unknown
   */
  memberDistance: number | null;
  /**
   * Total number of connections. May be null for non-connected profiles or restricted visibility.
   */
  connectionsCount: number | null;
  /**
   * Total number of followers. Null for restricted profiles or when unavailable.
   */
  followersCount: number | null;
  /**
   * Whether the profile has a LinkedIn verification badge.
   */
  isVerified: boolean;
  /**
   * Connection request status: 'pending' if request was sent successfully, 'failed' if request failed today, 'none' if not tracked
   */
  pendingConnection: PendingConnection;
  /**
   * Work experience positions
   */
  positions?: Array<VisitProfilePosition> | undefined;
  /**
   * Education entries
   */
  educations?: Array<VisitProfileEducation> | undefined;
  /**
   * Last 5 posts from the profile (always fetched; array may be empty if none found).
   */
  lastPosts?: Array<VisitProfileLastPost> | undefined;
  /**
   * Last 5 posts the profile recently commented on (always fetched; array may be empty). Contains targetPostText and targetPostAuthor for engagement context.
   */
  lastComments?: Array<LastComment> | undefined;
  /**
   * Last 5 posts the profile recently reacted to (always fetched; array may be empty). Each entry carries reactionType, targetPostText, and targetPostAuthor for engagement context.
   */
  lastReactions?: Array<LastReaction> | undefined;
  /**
   * Always true — the About section is always fetched. Kept for backward compatibility.
   */
  aboutFetched?: boolean | undefined;
  /**
   * true if this result was served from a local cache rather than a fresh LinkedIn API call.
   */
  cached: boolean;
  /**
   * When the cached copy was taken. Present when the result came from cache.
   */
  cachedAt?: string | null | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: VisitProfileMeta | undefined;
};

/**
 * Sort order: RELEVANCE (default, algorithmic) or REV_CHRON (most recent first)
 */
export const SortOrder = {
  Relevance: "RELEVANCE",
  RevChron: "REV_CHRON",
} as const;
/**
 * Sort order: RELEVANCE (default, algorithmic) or REV_CHRON (most recent first)
 */
export type SortOrder = ClosedEnum<typeof SortOrder>;

export type GetFeedRequest = {
  /**
   * Number of feed items to return (1-50, default 20)
   */
  count?: number | undefined;
  /**
   * Sort order: RELEVANCE (default, algorithmic) or REV_CHRON (most recent first)
   */
  sortOrder?: SortOrder | undefined;
};

/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export const GetFeedType = {
  Activity: "activity",
  UgcPost: "ugcPost",
  Share: "share",
} as const;
/**
 * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
 */
export type GetFeedType = OpenEnum<typeof GetFeedType>;

/**
 * Type of media attached to the post.
 */
export const GetFeedMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type GetFeedMediaType = OpenEnum<typeof GetFeedMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type GetFeedMedia = {
  /**
   * Type of media attached to the post.
   */
  type: GetFeedMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type GetFeedReactionTypeCount = {
  reactionType: string;
  count: number;
};

export type GetFeedPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
   */
  type: GetFeedType;
  /**
   * True when the post is a repost/reshare of another post. Absent for original posts.
   */
  isRepost?: boolean | undefined;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: GetFeedMedia | undefined;
  /**
   * Display name of the post's author.
   */
  authorName?: string | undefined;
  /**
   * Author's profile URL.
   */
  authorProfileUrl?: string | undefined;
  /**
   * Author's vanity slug, when they have one.
   */
  authorPublicIdentifier?: string | null | undefined;
  /**
   * Author's profile URN.
   */
  authorProfileUrn?: string | null | undefined;
  /**
   * Author's headline as shown on the post.
   */
  authorHeadline?: string | undefined;
  /**
   * Author's avatar URL.
   */
  authorImageUrl?: string | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<GetFeedReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
};

export type GetFeedCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetFeedMeta = {
  credits: GetFeedCredits;
};

/**
 * Feed posts
 */
export type GetFeedResponse = {
  success: true;
  posts: Array<GetFeedPost>;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetFeedMeta | undefined;
};

export type SearchEmployeesRequest = {
  /**
   * The company, as its LinkedIn page URL, its vanity slug, or its display name. A URL or slug is exact; a display name is matched to a company page, and a name that matches none is refused rather than searched without the filter.
   */
  company: string;
  /**
   * Optional keywords to narrow people at that company (title, name, skills).
   */
  keywords?: string | undefined;
  /**
   * Pagination offset into the accumulated employee list (default 0).
   */
  start?: number | undefined;
  /**
   * How many people to return in total (default 10, max 50).
   */
  count?: number | undefined;
};

export const SearchEmployeesType = {
  People: "PEOPLE",
} as const;
export type SearchEmployeesType = ClosedEnum<typeof SearchEmployeesType>;

/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export const SearchEmployeesNetworkDistance = {
  Distance1: "DISTANCE_1",
  Distance2: "DISTANCE_2",
  Distance3: "DISTANCE_3",
  OutOfNetwork: "OUT_OF_NETWORK",
} as const;
/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export type SearchEmployeesNetworkDistance = OpenEnum<
  typeof SearchEmployeesNetworkDistance
>;

export type SearchEmployeesCurrentPosition = {
  company: string | null;
  role: string | null;
};

/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export const SearchEmployeesLane = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export type SearchEmployeesLane = ClosedEnum<typeof SearchEmployeesLane>;

export type SearchEmployeesItem = {
  type: SearchEmployeesType;
  name: string;
  profileUrl: string | null;
  headline: string | null;
  location: string | null;
  profilePicture: string | null;
  /**
   * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
   */
  networkDistance: SearchEmployeesNetworkDistance | null;
  /**
   * Current work positions for this profile.
   */
  currentPositions: Array<SearchEmployeesCurrentPosition>;
  /**
   * LinkedIn profile URN (e.g. urn:li:fsd_profile:ACoAAA...) when available
   */
  profileUrn: string | null;
  /**
   * Vanity slug from profile URL (e.g. john-doe) when not URN-based
   */
  publicIdentifier: string | null;
  /**
   * True when the search ranker matched the literal name (vs headline/skills). When picking between two same-named people, the one with nameMatch=true is the one the user typed.
   */
  nameMatch?: boolean | null | undefined;
  /**
   * Profile badge surfaced by LinkedIn (Top Voice / Premium / Verified / Influencer). Indicates investment in the platform and is a useful qualifier weight.
   */
  badgeText?: string | null | undefined;
  /**
   * Profile ring status visualised on LinkedIn (OPEN_TO_WORK, HIRING, OPEN_FOR_BUSINESS, etc.). Open intent signal — directly relevant for recruiters, vendors, and sales.
   */
  ringStatus?: string | null | undefined;
  /**
   * Additional profile snippet returned by the search ranker, distinct from headline. Sometimes contains mutual-connection context or topic affinity (e.g. 'Writes about B2B sales').
   */
  summary?: string | null | undefined;
  /**
   * LinkedIn-curated context strings — typical entries: 'X mutual connections', 'Follows {company}', 'Worked at {company}'. Use these directly in personalised outreach openers.
   */
  actorInsights?: Array<string> | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
   */
  lane?: SearchEmployeesLane | undefined;
};

export type SearchEmployeesPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchEmployeesWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchEmployeesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchEmployeesMeta = {
  credits: SearchEmployeesCredits;
};

/**
 * People at the company
 */
export type SearchEmployeesResponse = {
  success: true;
  /**
   * Resolved company key used for the search (slug or name as passed after parse).
   */
  company: string;
  items: Array<SearchEmployeesItem>;
  paging: SearchEmployeesPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchEmployeesWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchEmployeesMeta | undefined;
};

export type SearchPostsRequest = {
  /**
   * Search keywords (required). Searches across post text content. Supports LinkedIn Boolean syntax: use double quotes for exact phrases (e.g. "AI automation"), AND to require both terms, OR for alternatives (e.g. "growth hacking" OR "growth marketing"), NOT to exclude (e.g. outreach NOT webinar), and parentheses to group (e.g. (AI OR ML) AND "case study"). Operators must be UPPERCASE. Without quotes, multi-word queries are treated as AND by default which may return broad results.
   */
  keywords: string;
  /**
   * A LinkedIn search URL. The category and every filter the vertical can actually apply are read from it; a facet it carries that this search cannot apply is reported back as a URL_FACET warning rather than applied, so a result set carrying those is WIDER than the URL asked for. Explicit params override anything read from the URL.
   */
  url?: string | undefined;
  /**
   * Sort order. 'relevance' (default) or 'date' (most recent first). Natural forms ('newest','recent') are normalized server-side.
   */
  sortBy?: string | undefined;
  /**
   * Filter posts by recency window: 'past-24h' < 'past-week' < 'past-month'. Natural forms ('last week','this month','24 hours') are normalized server-side. On a high-volume query the newest posts are shared across windows, so the first page of results can look identical while the narrower window genuinely holds fewer; judge it by how deep the results go rather than by the top of the list.
   */
  datePosted?: string | undefined;
  /**
   * Filter by media type: 'images' (photos), 'videos', 'documents' (carousel/PDF). Natural forms ('video','photo','pdf') are normalized server-side.
   */
  contentType?: string | undefined;
  /**
   * Filter by the post author's industry. Pass human labels (e.g. ['Software Development']) — server resolves to LinkedIn industry IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  authorIndustry?: Array<string> | undefined;
  /**
   * Filter by the post author's company. Pass company names (e.g. ['Google','Stripe']) — server resolves to LinkedIn company IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  authorCompany?: Array<string> | undefined;
  /**
   * Filter by the post author's job title, as free text. The strongest filter on post search by a wide margin: two different titles return sets of authors that do not overlap at all, so it is also the best way to reach more people by asking twice. Needs no id, unlike the industry and company filters.
   */
  authorJobTitle?: Array<string> | undefined;
  /**
   * Restrict to posts from your own network: 'first' for first-degree connections, 'following' for people you follow. Returns far fewer results than an open search by nature, since it is bounded by who you know rather than by everything LinkedIn has indexed.
   */
  postedBy?: Array<string> | undefined;
  /**
   * Pagination offset (default 0). Use multiples of `count` to page through results. Example: start=0 for page 1, start=10 for page 2 (with count=10).
   */
  start?: number | undefined;
  /**
   * How many posts to return (default 20, max 50). One SDUI pager call can return up to fifty (measured).
   */
  count?: number | undefined;
};

export const SearchPostsCategory = {
  Posts: "posts",
} as const;
export type SearchPostsCategory = ClosedEnum<typeof SearchPostsCategory>;

/**
 * Type of media attached to the post.
 */
export const SearchPostsMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type SearchPostsMediaType = OpenEnum<typeof SearchPostsMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type SearchPostsMedia = {
  /**
   * Type of media attached to the post.
   */
  type: SearchPostsMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type SearchPostsReactionTypeCount = {
  reactionType: string;
  count: number;
};

export const SearchPostsType = {
  Post: "POST",
} as const;
export type SearchPostsType = ClosedEnum<typeof SearchPostsType>;

export type SearchPostsAuthor = {
  name: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  isCompany: boolean;
  /**
   * Vanity slug when not URN-based
   */
  publicIdentifier: string | null;
  /**
   * LinkedIn profile URN when available
   */
  profileUrn: string | null;
};

export type SearchPostsItem = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: SearchPostsMedia | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<SearchPostsReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
  type: SearchPostsType;
  author: SearchPostsAuthor;
  isRepost: boolean;
};

export type SearchPostsPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchPostsWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchPostsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchPostsMeta = {
  credits: SearchPostsCredits;
};

/**
 * List of LinkedIn posts matching the search criteria
 */
export type SearchPostsResponse = {
  success: true;
  category: SearchPostsCategory;
  items: Array<SearchPostsItem>;
  paging: SearchPostsPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchPostsWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchPostsMeta | undefined;
};

export const SearchPeopleConnectionDegree = {
  F: "F",
  S: "S",
  O: "O",
} as const;
export type SearchPeopleConnectionDegree = ClosedEnum<
  typeof SearchPeopleConnectionDegree
>;

export type SearchPeopleRequest = {
  /**
   * Search keywords. Matches against name, headline, company, skills, and bio. Optional — you can search by filters alone (e.g. all 2nd-degree connections in Paris). Supports LinkedIn Boolean syntax: use double quotes for exact phrases (e.g. "product manager"), AND to require both terms, OR for alternatives (e.g. CEO OR founder OR CTO), NOT to exclude (e.g. manager NOT assistant), and parentheses to group (e.g. (CEO OR CTO) AND SaaS). Operators must be UPPERCASE.
   */
  keywords?: string | undefined;
  /**
   * A LinkedIn search URL. The category and every filter the vertical can actually apply are read from it; a facet it carries that this search cannot apply is reported back as a URL_FACET warning rather than applied, so a result set carrying those is WIDER than the URL asked for. Explicit params override anything read from the URL.
   */
  url?: string | undefined;
  /**
   * Spend up to this many LinkedIn searches on one ask, cutting it along independent angles so the result reaches people a single search cannot. One search returns about ten people; the angles do not overlap much, so the total grows close to linearly with what you spend. Leave it out to run exactly one search.
   */
  maxSearches?: number | undefined;
  /**
   * Filter by connection degree. 'F' = 1st degree (your direct connections), 'S' = 2nd degree (connections of connections), 'O' = 3rd+ degree and out of network. Accepts multiple values, e.g. ['F','S'] for 1st and 2nd degree.
   */
  connectionDegree?: Array<SearchPeopleConnectionDegree> | undefined;
  /**
   * Filter by first name. Exact match (case-insensitive).
   */
  firstName?: string | undefined;
  /**
   * Filter by last name. Exact match (case-insensitive).
   */
  lastName?: string | undefined;
  /**
   * Filter by current job title. Matches against the person's current position title. Supports LinkedIn Boolean OR: 'CEO OR CTO OR Founder'. Pipe separators are auto-converted: 'CEO|CTO|Founder' becomes 'CEO OR CTO OR Founder'. Use double quotes for exact multi-word titles: '"Head of Marketing" OR "VP Sales"'.
   */
  title?: string | undefined;
  /**
   * Find connections of a specific person. Pass the person's LinkedIn profile URN (e.g. 'urn:li:fsd_profile:ACoAABZ0Qo4B...'). Returns people who are connected to this person.
   */
  connectionOf?: string | undefined;
  /**
   * Filter by LinkedIn profile language. Array of ISO 639-1 codes (e.g. ['en','fr']); the server resolves any code.
   */
  profileLanguage?: Array<string> | undefined;
  /**
   * Filter by school/university. Pass human names (e.g. ['Harvard','Stanford']) — server resolves to LinkedIn IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  school?: Array<string> | undefined;
  /**
   * Filter by geographic location. Pass human names (e.g. ['Paris','France']) — server resolves to LinkedIn geo IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  location?: Array<string> | undefined;
  /**
   * Filter by industry. Pass human names (e.g. ['Software Development','Marketing']) — server resolves to LinkedIn industry IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  industry?: Array<string> | undefined;
  /**
   * Filter by current employer. Pass company names (e.g. ['Stripe','Datadog']) — server resolves to LinkedIn company IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  currentCompany?: Array<string> | undefined;
  /**
   * Filter by past employer. Pass company names — server resolves to LinkedIn company IDs via typeahead. Numeric IDs pass through if you already have them. Finds people who previously worked at these companies.
   */
  pastCompany?: Array<string> | undefined;
  /**
   * Find people who follow a specific creator/thought-leader. Array of LinkedIn profile URNs (e.g. 'urn:li:fsd_profile:ACoAAB...'). A warm-discovery vector: the audience of an influential voice in your space. Get URNs from a prior search_people / visit_profile result.
   */
  followersOf?: Array<string> | undefined;
  /**
   * When true, only return people who marked themselves open to volunteering.
   */
  openToVolunteering?: boolean | undefined;
  /**
   * Filter by LinkedIn Service-Marketplace category — for finding freelancers/consultants. Pass category NAMES (e.g. ['Digital Marketing','Web Development','Management Consulting']) — resolved to IDs server-side.
   */
  serviceCategories?: Array<string> | undefined;
  /**
   * Only people whose identity LinkedIn has verified. Reaches a different set rather than a smaller one: measured, it kept about half the unfiltered results and replaced the rest.
   */
  verifiedOnly?: boolean | undefined;
  /**
   * School as free text, matched on the name rather than an id. Use this when you know what the school is called but not its LinkedIn id; use `school` when you have the id.
   */
  schoolFreetext?: string | undefined;
  /**
   * Location of the company HEADQUARTERS (geo labels or ids), which is a different question from where the person is. A rep in Paris at a Berlin-headquartered firm matches one and not the other. Labels resolve via typeahead.
   */
  companyHQ?: Array<string> | undefined;
  /**
   * Company size on the PEOPLE search (employer headcount). Pass a human employee range and it is converted for you: A=1, B=2-10, C=11-50, D=51-200, E=201-500, F=501-1000, G=1001-5000, H=5001-10000, I=10001+.
   */
  companySize?: Array<string> | undefined;
  /**
   * Pagination offset into the accumulated result list (default 0). LinkedIn serves about ten people per HTML page; use multiples of that page size to skip whole pages.
   */
  start?: number | undefined;
  /**
   * How many people to return in total (default 10, max 50). LinkedIn's HTML search page is fixed at about ten results; the server pages with ?page=N until this total is reached.
   */
  count?: number | undefined;
};

export const SearchPeopleCategory = {
  People: "people",
} as const;
export type SearchPeopleCategory = ClosedEnum<typeof SearchPeopleCategory>;

export const SearchPeopleType = {
  People: "PEOPLE",
} as const;
export type SearchPeopleType = ClosedEnum<typeof SearchPeopleType>;

/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export const SearchPeopleNetworkDistance = {
  Distance1: "DISTANCE_1",
  Distance2: "DISTANCE_2",
  Distance3: "DISTANCE_3",
  OutOfNetwork: "OUT_OF_NETWORK",
} as const;
/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export type SearchPeopleNetworkDistance = OpenEnum<
  typeof SearchPeopleNetworkDistance
>;

export type SearchPeopleCurrentPosition = {
  company: string | null;
  role: string | null;
};

/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export const SearchPeopleLane = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export type SearchPeopleLane = ClosedEnum<typeof SearchPeopleLane>;

export type SearchPeopleItem = {
  type: SearchPeopleType;
  name: string;
  profileUrl: string | null;
  headline: string | null;
  location: string | null;
  profilePicture: string | null;
  /**
   * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
   */
  networkDistance: SearchPeopleNetworkDistance | null;
  /**
   * Current work positions for this profile.
   */
  currentPositions: Array<SearchPeopleCurrentPosition>;
  /**
   * LinkedIn profile URN (e.g. urn:li:fsd_profile:ACoAAA...) when available
   */
  profileUrn: string | null;
  /**
   * Vanity slug from profile URL (e.g. john-doe) when not URN-based
   */
  publicIdentifier: string | null;
  /**
   * True when the search ranker matched the literal name (vs headline/skills). When picking between two same-named people, the one with nameMatch=true is the one the user typed.
   */
  nameMatch?: boolean | null | undefined;
  /**
   * Profile badge surfaced by LinkedIn (Top Voice / Premium / Verified / Influencer). Indicates investment in the platform and is a useful qualifier weight.
   */
  badgeText?: string | null | undefined;
  /**
   * Profile ring status visualised on LinkedIn (OPEN_TO_WORK, HIRING, OPEN_FOR_BUSINESS, etc.). Open intent signal — directly relevant for recruiters, vendors, and sales.
   */
  ringStatus?: string | null | undefined;
  /**
   * Additional profile snippet returned by the search ranker, distinct from headline. Sometimes contains mutual-connection context or topic affinity (e.g. 'Writes about B2B sales').
   */
  summary?: string | null | undefined;
  /**
   * LinkedIn-curated context strings — typical entries: 'X mutual connections', 'Follows {company}', 'Worked at {company}'. Use these directly in personalised outreach openers.
   */
  actorInsights?: Array<string> | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
   */
  lane?: SearchPeopleLane | undefined;
};

export type SearchPeoplePaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchPeopleWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchPeopleCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchPeopleMeta = {
  credits: SearchPeopleCredits;
};

/**
 * List of LinkedIn people matching the search criteria
 */
export type SearchPeopleResponse = {
  success: true;
  category: SearchPeopleCategory;
  items: Array<SearchPeopleItem>;
  paging: SearchPeoplePaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchPeopleWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchPeopleMeta | undefined;
};

export type SearchCompaniesRequest = {
  /**
   * Search keywords. Matches against company name, description, and specialties. Optional — you can search by filters alone. Supports LinkedIn Boolean syntax: use double quotes for exact phrases (e.g. "artificial intelligence"), AND to require both terms, OR for alternatives (e.g. SaaS OR "cloud computing"), NOT to exclude (e.g. consulting NOT staffing), and parentheses to group (e.g. (AI OR ML) AND healthcare). Operators must be UPPERCASE.
   */
  keywords?: string | undefined;
  /**
   * A LinkedIn search URL. The category and every filter the vertical can actually apply are read from it; a facet it carries that this search cannot apply is reported back as a URL_FACET warning rather than applied, so a result set carrying those is WIDER than the URL asked for. Explicit params override anything read from the URL.
   */
  url?: string | undefined;
  /**
   * Filter by company HQ location. Pass human labels (e.g. ['Paris','France']) — server resolves to LinkedIn geo IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  location?: Array<string> | undefined;
  /**
   * Filter by company industry. Pass human labels (e.g. ['Software Development']) — server resolves to LinkedIn industry IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  industry?: Array<string> | undefined;
  /**
   * Filter by employee count, several bands allowed and combined as OR. Pass a human employee range and it is converted for you, which is the safer form: the underlying codes are A=1, B=2-10, C=11-50, D=51-200, E=201-500, F=501-1000, G=1001-5000, H=5001-10000, I=10001+, and passing the wrong letter searches the wrong band silently rather than failing.
   */
  companySize?: Array<string> | undefined;
  /**
   * Pagination offset into the accumulated result list (default 0). LinkedIn serves about ten companies per HTML page; use multiples of that page size to skip whole pages.
   */
  start?: number | undefined;
  /**
   * How many companies to return in total (default 10, max 50). LinkedIn's HTML search page is fixed at about ten results; the server pages with ?page=N until this total is reached.
   */
  count?: number | undefined;
};

export const SearchCompaniesCategory = {
  Companies: "companies",
} as const;
export type SearchCompaniesCategory = ClosedEnum<
  typeof SearchCompaniesCategory
>;

export const SearchCompaniesType = {
  Company: "COMPANY",
} as const;
export type SearchCompaniesType = ClosedEnum<typeof SearchCompaniesType>;

/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export const SearchCompaniesLane = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export type SearchCompaniesLane = ClosedEnum<typeof SearchCompaniesLane>;

export type SearchCompaniesItem = {
  type: SearchCompaniesType;
  name: string;
  profileUrl: string | null;
  summary: string | null;
  industry: string | null;
  location: string | null;
  followersCount: number | null;
  /**
   * Company logo URL when LinkedIn surfaces it on the result entity. Display this in the company card.
   */
  logoUrl?: string | null | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data only.
   */
  lane?: SearchCompaniesLane | undefined;
};

export type SearchCompaniesPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchCompaniesWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchCompaniesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchCompaniesMeta = {
  credits: SearchCompaniesCredits;
};

/**
 * List of LinkedIn companies matching the search criteria
 */
export type SearchCompaniesResponse = {
  success: true;
  category: SearchCompaniesCategory;
  items: Array<SearchCompaniesItem>;
  paging: SearchCompaniesPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchCompaniesWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchCompaniesMeta | undefined;
};

export type SearchJobsRequest = {
  /**
   * Search keywords. Matches against job title, company name, and description. Optional — you can search by filters alone. Supports LinkedIn Boolean syntax: use double quotes for exact phrases (e.g. "frontend engineer"), AND to require both terms, OR for alternatives (e.g. React OR Vue OR Angular), NOT to exclude (e.g. engineer NOT intern), and parentheses to group (e.g. (React OR Vue) AND "senior engineer"). Operators must be UPPERCASE.
   */
  keywords?: string | undefined;
  /**
   * A LinkedIn search URL. The category and every filter the vertical can actually apply are read from it; a facet it carries that this search cannot apply is reported back as a URL_FACET warning rather than applied, so a result set carrying those is WIDER than the URL asked for. Explicit params override anything read from the URL.
   */
  url?: string | undefined;
  /**
   * Filter by job location. Pass human labels (e.g. ['Paris','France']) — server resolves to LinkedIn geo IDs via typeahead. Numeric IDs pass through if you already have them.
   */
  location?: Array<string> | undefined;
  /**
   * Filter by posting date: 'past-24h'/'past-week'/'past-month' or natural ('last week','this month'). Normalized server-side.
   */
  datePosted?: string | undefined;
  /**
   * Sort order: 'relevance' (default) or 'date'/'newest' (most recent first). Normalized server-side.
   */
  sortBy?: string | undefined;
  /**
   * Filter by employment type (OR logic). Pass a natural label (full-time, part-time, contract, temporary, internship, volunteer, other) OR a code F/P/C/T/I/V/O — normalized server-side.
   */
  jobType?: Array<string> | undefined;
  /**
   * Filter by seniority (OR logic). Pass a natural label (internship, entry, associate, mid-senior, director, executive) OR a code 1-6 — normalized server-side.
   */
  experienceLevel?: Array<string> | undefined;
  /**
   * Filter by workplace type (OR logic). Pass 'onsite'/'remote'/'hybrid' or a code 1/2/3 — labels are normalized server-side.
   */
  workplaceType?: Array<string> | undefined;
  /**
   * Filter by hiring company. Pass company NAMES (e.g. ['Stripe','Datadog']) — they are resolved to LinkedIn company IDs server-side. Numeric IDs also accepted.
   */
  company?: Array<string> | undefined;
  /**
   * Filter by company industry. Pass industry NAMES (e.g. ['Software Development','Financial Services']) — resolved to LinkedIn industry IDs server-side. Numeric IDs also accepted.
   */
  industry?: Array<string> | undefined;
  /**
   * Filter by job function. Pass function names: Information Technology, Engineering, Consulting, Sales, Marketing, Finance, Business Development, Management, Project Management, Quality Assurance, Analyst, Research, Manufacturing, Other.
   */
  jobFunction?: Array<string> | undefined;
  /**
   * When true, only return jobs with LinkedIn Easy Apply (apply without leaving LinkedIn).
   */
  easyApply?: boolean | undefined;
  /**
   * When true, only return jobs with fewer than 10 applicants — less competition.
   */
  under10Applicants?: boolean | undefined;
  /**
   * When true, only return jobs at companies where the credential holder has a connection.
   */
  inYourNetwork?: boolean | undefined;
  /**
   * When true, only return jobs from verified job posters.
   */
  hasVerifications?: boolean | undefined;
  /**
   * When true, only return jobs from Fair Chance employers.
   */
  fairChanceEmployer?: boolean | undefined;
  /**
   * Filter by advertised benefits. Names: Medical insurance, Vision insurance, Dental insurance, 401(k), Pension plan, Paid maternity leave, Paid paternity leave, Commuter benefits, Student loan assistance, Tuition assistance, Disability insurance.
   */
  benefits?: Array<string> | undefined;
  /**
   * Filter by employer commitments. Names: Diversity equity and inclusion, Environmental sustainability, Work-life balance, Social impact, Career growth and learning.
   */
  commitments?: Array<string> | undefined;
  /**
   * Pagination offset (default 0). Use multiples of `count` to page through results.
   */
  start?: number | undefined;
  /**
   * Results per request (default 50, max 100). LinkedIn's JobCards endpoint honors up to 100 in one call (measured).
   */
  count?: number | undefined;
};

export const SearchJobsCategory = {
  Jobs: "jobs",
} as const;
export type SearchJobsCategory = ClosedEnum<typeof SearchJobsCategory>;

export const SearchJobsType = {
  Job: "JOB",
} as const;
export type SearchJobsType = ClosedEnum<typeof SearchJobsType>;

export type SearchJobsItem = {
  type: SearchJobsType;
  title: string;
  company: string | null;
  companyUrl: string | null;
  companyLogo: string | null;
  location: string | null;
  workplaceType: string | null;
  postedAt: string | null;
  jobUrl: string;
  /**
   * LinkedIn job listing id — pass to /visit/linkedin/job to fetch the full posting (applicant count, full description, employment status, listed/expire timestamps, inferred benefits, job functions, industries, apply method).
   */
  listingId: string;
};

export type SearchJobsPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchJobsWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchJobsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchJobsMeta = {
  credits: SearchJobsCredits;
};

/**
 * List of LinkedIn job listings matching the search criteria
 */
export type SearchJobsResponse = {
  success: true;
  category: SearchJobsCategory;
  items: Array<SearchJobsItem>;
  paging: SearchJobsPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchJobsWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchJobsMeta | undefined;
};

export type SearchByUrlRequest = {
  /**
   * A LinkedIn search URL. The endpoint automatically extracts the category, keywords, and all filters from the URL. Example: 'https://www.linkedin.com/search/results/people/?keywords=engineer&network=%5B%22S%22%5D'
   */
  url: string;
  /**
   * Override pagination offset. If not provided, uses the page from the URL (or defaults to 0).
   */
  start?: number | undefined;
  /**
   * Override results per page (default 10, max 50).
   */
  count?: number | undefined;
};

export const SearchByUrlCategory = {
  Posts: "posts",
  People: "people",
  Companies: "companies",
  Jobs: "jobs",
} as const;
export type SearchByUrlCategory = OpenEnum<typeof SearchByUrlCategory>;

export const SearchByUrlTypeJob = {
  Job: "JOB",
} as const;
export type SearchByUrlTypeJob = ClosedEnum<typeof SearchByUrlTypeJob>;

export type ItemsJob = {
  type: SearchByUrlTypeJob;
  title: string;
  company: string | null;
  companyUrl: string | null;
  companyLogo: string | null;
  location: string | null;
  workplaceType: string | null;
  postedAt: string | null;
  jobUrl: string;
  /**
   * LinkedIn job listing id — pass to /visit/linkedin/job to fetch the full posting (applicant count, full description, employment status, listed/expire timestamps, inferred benefits, job functions, industries, apply method).
   */
  listingId: string;
};

export const SearchByUrlTypeCompany = {
  Company: "COMPANY",
} as const;
export type SearchByUrlTypeCompany = ClosedEnum<typeof SearchByUrlTypeCompany>;

/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export const SearchByUrlLane2 = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export type SearchByUrlLane2 = ClosedEnum<typeof SearchByUrlLane2>;

export type ItemsCompany = {
  type: SearchByUrlTypeCompany;
  name: string;
  profileUrl: string | null;
  summary: string | null;
  industry: string | null;
  location: string | null;
  followersCount: number | null;
  /**
   * Company logo URL when LinkedIn surfaces it on the result entity. Display this in the company card.
   */
  logoUrl?: string | null | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data only.
   */
  lane?: SearchByUrlLane2 | undefined;
};

export const SearchByUrlTypePeople = {
  People: "PEOPLE",
} as const;
export type SearchByUrlTypePeople = ClosedEnum<typeof SearchByUrlTypePeople>;

/** @internal */
export const Surface$outboundSchema: z.ZodMiniEnum<typeof Surface> = z.enum(
  Surface,
);

/** @internal */
export type CollectEngagersRequest$Outbound = {
  postUrls: Array<string>;
  more?: boolean | undefined;
  untilCount?: number | undefined;
  surface?: string | undefined;
  campaignSlug?: string | undefined;
};

/** @internal */
export const CollectEngagersRequest$outboundSchema: z.ZodMiniType<
  CollectEngagersRequest$Outbound,
  CollectEngagersRequest
> = z.object({
  postUrls: z.array(z.string()),
  more: z.optional(z.boolean()),
  untilCount: z.optional(z.int()),
  surface: z.optional(Surface$outboundSchema),
  campaignSlug: z.optional(z.string()),
});

export function collectEngagersRequestToJSON(
  collectEngagersRequest: CollectEngagersRequest,
): string {
  return JSON.stringify(
    CollectEngagersRequest$outboundSchema.parse(collectEngagersRequest),
  );
}

/** @internal */
export const CollectEngagersServiceUnavailableDetails$inboundSchema:
  z.ZodMiniType<CollectEngagersServiceUnavailableDetails, unknown> = smartUnion(
    [types.string(), z.array(types.string())],
  );

export function collectEngagersServiceUnavailableDetailsFromJSON(
  jsonString: string,
): SafeParseResult<
  CollectEngagersServiceUnavailableDetails,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersServiceUnavailableDetails$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'CollectEngagersServiceUnavailableDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersServiceUnavailableQuotaKind$inboundSchema:
  z.ZodMiniType<CollectEngagersServiceUnavailableQuotaKind, unknown> = openEnums
    .inboundSchema(CollectEngagersServiceUnavailableQuotaKind);

/** @internal */
export const CollectEngagersServiceUnavailableError$inboundSchema:
  z.ZodMiniType<CollectEngagersServiceUnavailableError, unknown> = z.object({
    code: types.string(),
    message: types.string(),
    docs: types.optional(types.string()),
    details: types.optional(
      smartUnion([types.string(), z.array(types.string())]),
    ),
    upgradeUrl: types.optional(types.string()),
    quotaKind: types.optional(
      CollectEngagersServiceUnavailableQuotaKind$inboundSchema,
    ),
  });

export function collectEngagersServiceUnavailableErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersServiceUnavailableError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersServiceUnavailableError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersServiceUnavailableError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersBadGatewayDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersBadGatewayDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersBadGatewayDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersBadGatewayDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersBadGatewayDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersBadGatewayDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersBadGatewayQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersBadGatewayQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersBadGatewayQuotaKind);

/** @internal */
export const CollectEngagersBadGatewayError$inboundSchema: z.ZodMiniType<
  CollectEngagersBadGatewayError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersBadGatewayQuotaKind$inboundSchema),
});

export function collectEngagersBadGatewayErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersBadGatewayError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersBadGatewayError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersBadGatewayError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersInternalServerErrorDetails$inboundSchema:
  z.ZodMiniType<CollectEngagersInternalServerErrorDetails, unknown> =
    smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersInternalServerErrorDetailsFromJSON(
  jsonString: string,
): SafeParseResult<
  CollectEngagersInternalServerErrorDetails,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersInternalServerErrorDetails$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'CollectEngagersInternalServerErrorDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersInternalServerErrorQuotaKind$inboundSchema:
  z.ZodMiniType<CollectEngagersInternalServerErrorQuotaKind, unknown> =
    openEnums.inboundSchema(CollectEngagersInternalServerErrorQuotaKind);

/** @internal */
export const CollectEngagersInternalServerErrorError$inboundSchema:
  z.ZodMiniType<CollectEngagersInternalServerErrorError, unknown> = z.object({
    code: types.string(),
    message: types.string(),
    docs: types.optional(types.string()),
    details: types.optional(
      smartUnion([types.string(), z.array(types.string())]),
    ),
    upgradeUrl: types.optional(types.string()),
    quotaKind: types.optional(
      CollectEngagersInternalServerErrorQuotaKind$inboundSchema,
    ),
  });

export function collectEngagersInternalServerErrorErrorFromJSON(
  jsonString: string,
): SafeParseResult<
  CollectEngagersInternalServerErrorError,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersInternalServerErrorError$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'CollectEngagersInternalServerErrorError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersCode$inboundSchema: z.ZodMiniEnum<
  typeof CollectEngagersCode
> = z.enum(CollectEngagersCode);

/** @internal */
export const CollectEngagersDaily$inboundSchema: z.ZodMiniType<
  CollectEngagersDaily,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
});

export function collectEngagersDailyFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersDaily, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersDaily$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersDaily' from JSON`,
  );
}

/** @internal */
export const CollectEngagersWeekly$inboundSchema: z.ZodMiniType<
  CollectEngagersWeekly,
  unknown
> = z.object({
  current: types.number(),
  limit: types.number(),
});

export function collectEngagersWeeklyFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersWeekly, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersWeekly$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersWeekly' from JSON`,
  );
}

/** @internal */
export const CollectEngagersTooManyRequestsQuotaKind$inboundSchema:
  z.ZodMiniType<CollectEngagersTooManyRequestsQuotaKind, unknown> = openEnums
    .inboundSchema(CollectEngagersTooManyRequestsQuotaKind);

/** @internal */
export const CollectEngagersTooManyRequestsError$inboundSchema: z.ZodMiniType<
  CollectEngagersTooManyRequestsError,
  unknown
> = z.object({
  code: CollectEngagersCode$inboundSchema,
  message: types.string(),
  docs: types.optional(types.string()),
  retryAfter: types.number(),
  daily: types.nullable(z.lazy(() => CollectEngagersDaily$inboundSchema)),
  weekly: types.nullable(z.lazy(() => CollectEngagersWeekly$inboundSchema)),
  quotaKind: types.optional(
    CollectEngagersTooManyRequestsQuotaKind$inboundSchema,
  ),
});

export function collectEngagersTooManyRequestsErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersTooManyRequestsError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersTooManyRequestsError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersTooManyRequestsError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersUnprocessableEntityDetails$inboundSchema:
  z.ZodMiniType<CollectEngagersUnprocessableEntityDetails, unknown> =
    smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersUnprocessableEntityDetailsFromJSON(
  jsonString: string,
): SafeParseResult<
  CollectEngagersUnprocessableEntityDetails,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersUnprocessableEntityDetails$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'CollectEngagersUnprocessableEntityDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersUnprocessableEntityQuotaKind$inboundSchema:
  z.ZodMiniType<CollectEngagersUnprocessableEntityQuotaKind, unknown> =
    openEnums.inboundSchema(CollectEngagersUnprocessableEntityQuotaKind);

/** @internal */
export const CollectEngagersUnprocessableEntityError$inboundSchema:
  z.ZodMiniType<CollectEngagersUnprocessableEntityError, unknown> = z.object({
    code: types.string(),
    message: types.string(),
    docs: types.optional(types.string()),
    details: types.optional(
      smartUnion([types.string(), z.array(types.string())]),
    ),
    upgradeUrl: types.optional(types.string()),
    quotaKind: types.optional(
      CollectEngagersUnprocessableEntityQuotaKind$inboundSchema,
    ),
  });

export function collectEngagersUnprocessableEntityErrorFromJSON(
  jsonString: string,
): SafeParseResult<
  CollectEngagersUnprocessableEntityError,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersUnprocessableEntityError$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'CollectEngagersUnprocessableEntityError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersGoneDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersGoneDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersGoneDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersGoneDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersGoneDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersGoneDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersGoneQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersGoneQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersGoneQuotaKind);

/** @internal */
export const CollectEngagersGoneError$inboundSchema: z.ZodMiniType<
  CollectEngagersGoneError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersGoneQuotaKind$inboundSchema),
});

export function collectEngagersGoneErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersGoneError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersGoneError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersGoneError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersConflictDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersConflictDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersConflictDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersConflictDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersConflictDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersConflictDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersConflictQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersConflictQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersConflictQuotaKind);

/** @internal */
export const CollectEngagersConflictError$inboundSchema: z.ZodMiniType<
  CollectEngagersConflictError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersConflictQuotaKind$inboundSchema),
});

export function collectEngagersConflictErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersConflictError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersConflictError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersConflictError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersNotFoundDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersNotFoundDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersNotFoundDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersNotFoundDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersNotFoundDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersNotFoundDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersNotFoundQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersNotFoundQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersNotFoundQuotaKind);

/** @internal */
export const CollectEngagersNotFoundError$inboundSchema: z.ZodMiniType<
  CollectEngagersNotFoundError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersNotFoundQuotaKind$inboundSchema),
});

export function collectEngagersNotFoundErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersNotFoundError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersNotFoundError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersNotFoundError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersForbiddenDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersForbiddenDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersForbiddenDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersForbiddenDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersForbiddenDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersForbiddenDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersForbiddenQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersForbiddenQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersForbiddenQuotaKind);

/** @internal */
export const CollectEngagersForbiddenError$inboundSchema: z.ZodMiniType<
  CollectEngagersForbiddenError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersForbiddenQuotaKind$inboundSchema),
});

export function collectEngagersForbiddenErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersForbiddenError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersForbiddenError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersForbiddenError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersUnauthorizedDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersUnauthorizedDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersUnauthorizedDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersUnauthorizedDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CollectEngagersUnauthorizedDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersUnauthorizedDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersUnauthorizedQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersUnauthorizedQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersUnauthorizedQuotaKind);

/** @internal */
export const CollectEngagersUnauthorizedError$inboundSchema: z.ZodMiniType<
  CollectEngagersUnauthorizedError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersUnauthorizedQuotaKind$inboundSchema),
});

export function collectEngagersUnauthorizedErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersUnauthorizedError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersUnauthorizedError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersUnauthorizedError' from JSON`,
  );
}

/** @internal */
export const CollectEngagersBadRequestDetails$inboundSchema: z.ZodMiniType<
  CollectEngagersBadRequestDetails,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function collectEngagersBadRequestDetailsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersBadRequestDetails, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersBadRequestDetails$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersBadRequestDetails' from JSON`,
  );
}

/** @internal */
export const CollectEngagersBadRequestQuotaKind$inboundSchema: z.ZodMiniType<
  CollectEngagersBadRequestQuotaKind,
  unknown
> = openEnums.inboundSchema(CollectEngagersBadRequestQuotaKind);

/** @internal */
export const CollectEngagersBadRequestError$inboundSchema: z.ZodMiniType<
  CollectEngagersBadRequestError,
  unknown
> = z.object({
  code: types.string(),
  message: types.string(),
  docs: types.optional(types.string()),
  details: types.optional(
    smartUnion([types.string(), z.array(types.string())]),
  ),
  upgradeUrl: types.optional(types.string()),
  quotaKind: types.optional(CollectEngagersBadRequestQuotaKind$inboundSchema),
});

export function collectEngagersBadRequestErrorFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersBadRequestError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersBadRequestError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersBadRequestError' from JSON`,
  );
}

/** @internal */
export const Coverage$inboundSchema: z.ZodMiniEnum<typeof Coverage> = z.enum(
  Coverage,
);

/** @internal */
export const CollectEngagersType$inboundSchema: z.ZodMiniType<
  CollectEngagersType,
  unknown
> = openEnums.inboundSchema(CollectEngagersType);

/** @internal */
export const Person$inboundSchema: z.ZodMiniType<Person, unknown> = z.object({
  name: types.nullable(types.string()),
  headline: z.optional(z.nullable(types.string())),
  profileUrl: types.nullable(types.string()),
  publicIdentifier: z.optional(z.nullable(types.string())),
  profileUrn: z.optional(z.nullable(types.string())),
  imageUrl: z.optional(z.nullable(types.string())),
  commentText: z.optional(z.nullable(types.string())),
  type: types.optional(CollectEngagersType$inboundSchema),
});

export function personFromJSON(
  jsonString: string,
): SafeParseResult<Person, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Person$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Person' from JSON`,
  );
}

/** @internal */
export const CollectEngagersTotals$inboundSchema: z.ZodMiniType<
  CollectEngagersTotals,
  unknown
> = z.object({
  likes: z.optional(z.nullable(types.number())),
  comments: z.optional(z.nullable(types.number())),
});

export function collectEngagersTotalsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersTotals' from JSON`,
  );
}

/** @internal */
export const PerPost$inboundSchema: z.ZodMiniType<PerPost, unknown> = z.object({
  likes: types.number(),
  comments: types.number(),
});

export function perPostFromJSON(
  jsonString: string,
): SafeParseResult<PerPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PerPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PerPost' from JSON`,
  );
}

/** @internal */
export const DisplayLinkedInConnect$inboundSchema: z.ZodMiniType<
  DisplayLinkedInConnect,
  unknown
> = z.object({
  reason: types.string(),
  autoRetry: types.optional(types.boolean()),
});

export function displayLinkedInConnectFromJSON(
  jsonString: string,
): SafeParseResult<DisplayLinkedInConnect, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DisplayLinkedInConnect$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DisplayLinkedInConnect' from JSON`,
  );
}

/** @internal */
export const CollectEngagersCredits$inboundSchema: z.ZodMiniType<
  CollectEngagersCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function collectEngagersCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersCredits' from JSON`,
  );
}

/** @internal */
export const CollectEngagersMeta$inboundSchema: z.ZodMiniType<
  CollectEngagersMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CollectEngagersCredits$inboundSchema),
});

export function collectEngagersMetaFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersMeta' from JSON`,
  );
}

/** @internal */
export const CollectEngagersResponse$inboundSchema: z.ZodMiniType<
  CollectEngagersResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    runId: types.optional(types.string()),
    status: types.optional(types.string()),
    coverage: types.optional(Coverage$inboundSchema),
    needs_linkedin: types.optional(types.boolean()),
    people: types.optional(z.array(z.lazy(() => Person$inboundSchema))),
    postText: z.optional(z.nullable(types.string())),
    totals: types.optional(z.lazy(() => CollectEngagersTotals$inboundSchema)),
    count: types.optional(types.number()),
    hiddenSkipped: types.optional(types.number()),
    unreadable: types.optional(types.number()),
    likesUnavailable: types.optional(types.number()),
    commentsUnavailable: types.optional(types.number()),
    perPost: types.optional(
      z.record(z.string(), z.lazy(() => PerPost$inboundSchema)),
    ),
    hasMore: types.optional(types.boolean()),
    moreAvailable: types.optional(types.boolean()),
    remainder: types.optional(types.string()),
    commentersTruncated: types.optional(types.number()),
    note: types.optional(types.string()),
    _displayLinkedInConnect: types.optional(
      z.lazy(() => DisplayLinkedInConnect$inboundSchema),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => CollectEngagersMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "needs_linkedin": "needsLinkedin",
      "_displayLinkedInConnect": "displayLinkedInConnect",
      "_meta": "meta",
    });
  }),
);

export function collectEngagersResponseFromJSON(
  jsonString: string,
): SafeParseResult<CollectEngagersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectEngagersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectEngagersResponse' from JSON`,
  );
}

/** @internal */
export type CollectPostsRequest$Outbound = {
  profileUrl: string;
  more?: boolean | undefined;
};

/** @internal */
export const CollectPostsRequest$outboundSchema: z.ZodMiniType<
  CollectPostsRequest$Outbound,
  CollectPostsRequest
> = z.object({
  profileUrl: z.string(),
  more: z.optional(z.boolean()),
});

export function collectPostsRequestToJSON(
  collectPostsRequest: CollectPostsRequest,
): string {
  return JSON.stringify(
    CollectPostsRequest$outboundSchema.parse(collectPostsRequest),
  );
}

/** @internal */
export const CollectPostsTotals$inboundSchema: z.ZodMiniType<
  CollectPostsTotals,
  unknown
> = z.object({
  posts: z.optional(z.nullable(types.number())),
});

export function collectPostsTotalsFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsTotals' from JSON`,
  );
}

/** @internal */
export const CollectPostsType$inboundSchema: z.ZodMiniType<
  CollectPostsType,
  unknown
> = openEnums.inboundSchema(CollectPostsType);

/** @internal */
export const CollectPostsMediaType$inboundSchema: z.ZodMiniType<
  CollectPostsMediaType,
  unknown
> = openEnums.inboundSchema(CollectPostsMediaType);

/** @internal */
export const CollectPostsMedia$inboundSchema: z.ZodMiniType<
  CollectPostsMedia,
  unknown
> = z.object({
  type: CollectPostsMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function collectPostsMediaFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsMedia' from JSON`,
  );
}

/** @internal */
export const CollectPostsReactionTypeCount$inboundSchema: z.ZodMiniType<
  CollectPostsReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function collectPostsReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const CollectPostsPost$inboundSchema: z.ZodMiniType<
  CollectPostsPost,
  unknown
> = z.object({
  postUrl: types.string(),
  text: types.string(),
  date: types.number(),
  likesCount: types.number(),
  commentsCount: types.number(),
  sharesCount: types.number(),
  postUrn: types.string(),
  postId: types.string(),
  type: CollectPostsType$inboundSchema,
  isRepost: types.optional(types.boolean()),
  media: types.optional(z.lazy(() => CollectPostsMedia$inboundSchema)),
  authorName: types.optional(types.string()),
  authorProfileUrl: types.optional(types.string()),
  authorPublicIdentifier: z.optional(z.nullable(types.string())),
  authorProfileUrn: z.optional(z.nullable(types.string())),
  authorHeadline: types.optional(types.string()),
  authorImageUrl: types.optional(types.string()),
  repostedFromName: types.optional(types.string()),
  reactionTypeCounts: types.optional(
    z.array(z.lazy(() => CollectPostsReactionTypeCount$inboundSchema)),
  ),
  numImpressions: z.optional(z.nullable(types.number())),
  highlightedReactorName: z.optional(z.nullable(types.string())),
  viewerLiked: z.optional(z.nullable(types.boolean())),
});

export function collectPostsPostFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsPost' from JSON`,
  );
}

/** @internal */
export const CollectPostsCredits$inboundSchema: z.ZodMiniType<
  CollectPostsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function collectPostsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsCredits' from JSON`,
  );
}

/** @internal */
export const CollectPostsMeta$inboundSchema: z.ZodMiniType<
  CollectPostsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CollectPostsCredits$inboundSchema),
});

export function collectPostsMetaFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsMeta' from JSON`,
  );
}

/** @internal */
export const CollectPostsResponse$inboundSchema: z.ZodMiniType<
  CollectPostsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    runId: types.string(),
    status: types.string(),
    totals: types.optional(z.lazy(() => CollectPostsTotals$inboundSchema)),
    posts: types.optional(
      z.array(z.lazy(() => CollectPostsPost$inboundSchema)),
    ),
    count: types.optional(types.number()),
    unreadable: types.optional(types.number()),
    postsUnavailable: types.optional(types.number()),
    moreAvailable: types.optional(types.boolean()),
    hasMore: types.optional(types.boolean()),
    remainder: types.optional(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => CollectPostsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function collectPostsResponseFromJSON(
  jsonString: string,
): SafeParseResult<CollectPostsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollectPostsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollectPostsResponse' from JSON`,
  );
}

/** @internal */
export type VisitProfileRequest$Outbound = {
  profile: string;
};

/** @internal */
export const VisitProfileRequest$outboundSchema: z.ZodMiniType<
  VisitProfileRequest$Outbound,
  VisitProfileRequest
> = z.object({
  profile: z.string(),
});

export function visitProfileRequestToJSON(
  visitProfileRequest: VisitProfileRequest,
): string {
  return JSON.stringify(
    VisitProfileRequest$outboundSchema.parse(visitProfileRequest),
  );
}

/** @internal */
export const PendingConnection$inboundSchema: z.ZodMiniType<
  PendingConnection,
  unknown
> = openEnums.inboundSchema(PendingConnection);

/** @internal */
export const VisitProfilePositionStartDate$inboundSchema: z.ZodMiniType<
  VisitProfilePositionStartDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function visitProfilePositionStartDateFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfilePositionStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfilePositionStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfilePositionStartDate' from JSON`,
  );
}

/** @internal */
export const VisitProfilePositionEndDate$inboundSchema: z.ZodMiniType<
  VisitProfilePositionEndDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function visitProfilePositionEndDateFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfilePositionEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfilePositionEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfilePositionEndDate' from JSON`,
  );
}

/** @internal */
export const VisitProfilePosition$inboundSchema: z.ZodMiniType<
  VisitProfilePosition,
  unknown
> = z.object({
  companyName: types.nullable(types.string()),
  title: types.nullable(types.string()),
  description: z.optional(z.nullable(types.string())),
  companyUrl: types.nullable(types.string()),
  companyLogo: types.nullable(types.string()),
  companyDescription: z.optional(z.nullable(types.string())),
  startDate: types.nullable(
    z.lazy(() => VisitProfilePositionStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => VisitProfilePositionEndDate$inboundSchema),
  ),
  isCurrent: types.boolean(),
});

export function visitProfilePositionFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfilePosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfilePosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfilePosition' from JSON`,
  );
}

/** @internal */
export const VisitProfileEducationStartDate$inboundSchema: z.ZodMiniType<
  VisitProfileEducationStartDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function visitProfileEducationStartDateFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileEducationStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileEducationStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileEducationStartDate' from JSON`,
  );
}

/** @internal */
export const VisitProfileEducationEndDate$inboundSchema: z.ZodMiniType<
  VisitProfileEducationEndDate,
  unknown
> = z.object({
  month: types.nullable(types.number()),
  year: types.number(),
});

export function visitProfileEducationEndDateFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileEducationEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileEducationEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileEducationEndDate' from JSON`,
  );
}

/** @internal */
export const VisitProfileEducation$inboundSchema: z.ZodMiniType<
  VisitProfileEducation,
  unknown
> = z.object({
  schoolName: types.nullable(types.string()),
  degreeName: types.nullable(types.string()),
  fieldOfStudy: types.nullable(types.string()),
  schoolUrl: types.nullable(types.string()),
  schoolLogo: types.nullable(types.string()),
  startDate: types.nullable(
    z.lazy(() => VisitProfileEducationStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => VisitProfileEducationEndDate$inboundSchema),
  ),
});

export function visitProfileEducationFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileEducation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileEducation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileEducation' from JSON`,
  );
}

/** @internal */
export const VisitProfileLastPostType$inboundSchema: z.ZodMiniType<
  VisitProfileLastPostType,
  unknown
> = openEnums.inboundSchema(VisitProfileLastPostType);

/** @internal */
export const VisitProfileMediaType$inboundSchema: z.ZodMiniType<
  VisitProfileMediaType,
  unknown
> = openEnums.inboundSchema(VisitProfileMediaType);

/** @internal */
export const VisitProfileMedia$inboundSchema: z.ZodMiniType<
  VisitProfileMedia,
  unknown
> = z.object({
  type: VisitProfileMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function visitProfileMediaFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileMedia' from JSON`,
  );
}

/** @internal */
export const VisitProfileReactionTypeCount$inboundSchema: z.ZodMiniType<
  VisitProfileReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function visitProfileReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const VisitProfileLastPost$inboundSchema: z.ZodMiniType<
  VisitProfileLastPost,
  unknown
> = z.object({
  postUrl: types.string(),
  text: types.string(),
  date: types.number(),
  likesCount: types.number(),
  commentsCount: types.number(),
  sharesCount: types.number(),
  postUrn: types.string(),
  postId: types.string(),
  type: VisitProfileLastPostType$inboundSchema,
  isRepost: types.optional(types.boolean()),
  media: types.optional(z.lazy(() => VisitProfileMedia$inboundSchema)),
  authorName: types.optional(types.string()),
  authorProfileUrl: types.optional(types.string()),
  authorPublicIdentifier: z.optional(z.nullable(types.string())),
  authorProfileUrn: z.optional(z.nullable(types.string())),
  authorHeadline: types.optional(types.string()),
  authorImageUrl: types.optional(types.string()),
  repostedFromName: types.optional(types.string()),
  reactionTypeCounts: types.optional(
    z.array(z.lazy(() => VisitProfileReactionTypeCount$inboundSchema)),
  ),
  numImpressions: z.optional(z.nullable(types.number())),
  highlightedReactorName: z.optional(z.nullable(types.string())),
  viewerLiked: z.optional(z.nullable(types.boolean())),
});

export function visitProfileLastPostFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileLastPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileLastPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileLastPost' from JSON`,
  );
}

/** @internal */
export const LastCommentType$inboundSchema: z.ZodMiniType<
  LastCommentType,
  unknown
> = openEnums.inboundSchema(LastCommentType);

/** @internal */
export const LastComment$inboundSchema: z.ZodMiniType<LastComment, unknown> = z
  .object({
    type: LastCommentType$inboundSchema,
    activityUrn: types.string(),
    text: types.nullable(types.string()),
    reactionType: types.nullable(types.string()),
    targetPostUrn: types.nullable(types.string()),
    targetPostText: types.nullable(types.string()),
    targetPostAuthor: types.nullable(types.string()),
    timestamp: types.nullable(types.number()),
    actorName: types.nullable(types.string()),
    actorUrn: types.nullable(types.string()),
  });

export function lastCommentFromJSON(
  jsonString: string,
): SafeParseResult<LastComment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LastComment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LastComment' from JSON`,
  );
}

/** @internal */
export const LastReactionType$inboundSchema: z.ZodMiniType<
  LastReactionType,
  unknown
> = openEnums.inboundSchema(LastReactionType);

/** @internal */
export const LastReaction$inboundSchema: z.ZodMiniType<LastReaction, unknown> =
  z.object({
    type: LastReactionType$inboundSchema,
    activityUrn: types.string(),
    text: types.nullable(types.string()),
    reactionType: types.nullable(types.string()),
    targetPostUrn: types.nullable(types.string()),
    targetPostText: types.nullable(types.string()),
    targetPostAuthor: types.nullable(types.string()),
    timestamp: types.nullable(types.number()),
    actorName: types.nullable(types.string()),
    actorUrn: types.nullable(types.string()),
  });

export function lastReactionFromJSON(
  jsonString: string,
): SafeParseResult<LastReaction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LastReaction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LastReaction' from JSON`,
  );
}

/** @internal */
export const VisitProfileCredits$inboundSchema: z.ZodMiniType<
  VisitProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function visitProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileCredits' from JSON`,
  );
}

/** @internal */
export const VisitProfileMeta$inboundSchema: z.ZodMiniType<
  VisitProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => VisitProfileCredits$inboundSchema),
});

export function visitProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileMeta' from JSON`,
  );
}

/** @internal */
export const VisitProfileResponse$inboundSchema: z.ZodMiniType<
  VisitProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    firstName: types.string(),
    lastName: types.string(),
    headline: types.nullable(types.string()),
    summary: types.nullable(types.string()),
    publicIdentifier: types.string(),
    profileUrl: types.nullable(types.string()),
    profileUrn: types.nullable(types.string()),
    imageUrl: types.nullable(types.string()),
    email: types.nullable(types.string()),
    phone: types.nullable(types.string()),
    location: types.nullable(types.string()),
    company: types.nullable(types.string()),
    position: types.nullable(types.string()),
    memberDistance: types.nullable(types.number()),
    connectionsCount: types.nullable(types.number()),
    followersCount: types.nullable(types.number()),
    isVerified: types.boolean(),
    pendingConnection: PendingConnection$inboundSchema,
    positions: types.optional(
      z.array(z.lazy(() => VisitProfilePosition$inboundSchema)),
    ),
    educations: types.optional(
      z.array(z.lazy(() => VisitProfileEducation$inboundSchema)),
    ),
    lastPosts: types.optional(
      z.array(z.lazy(() => VisitProfileLastPost$inboundSchema)),
    ),
    lastComments: types.optional(
      z.array(z.lazy(() => LastComment$inboundSchema)),
    ),
    lastReactions: types.optional(
      z.array(z.lazy(() => LastReaction$inboundSchema)),
    ),
    aboutFetched: types.optional(types.boolean()),
    cached: types.boolean(),
    cachedAt: z.optional(z.nullable(types.string())),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => VisitProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function visitProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<VisitProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VisitProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VisitProfileResponse' from JSON`,
  );
}

/** @internal */
export const SortOrder$outboundSchema: z.ZodMiniEnum<typeof SortOrder> = z.enum(
  SortOrder,
);

/** @internal */
export type GetFeedRequest$Outbound = {
  count: number;
  sortOrder: string;
};

/** @internal */
export const GetFeedRequest$outboundSchema: z.ZodMiniType<
  GetFeedRequest$Outbound,
  GetFeedRequest
> = z.object({
  count: z._default(z.int(), 20),
  sortOrder: z._default(SortOrder$outboundSchema, "RELEVANCE"),
});

export function getFeedRequestToJSON(getFeedRequest: GetFeedRequest): string {
  return JSON.stringify(GetFeedRequest$outboundSchema.parse(getFeedRequest));
}

/** @internal */
export const GetFeedType$inboundSchema: z.ZodMiniType<GetFeedType, unknown> =
  openEnums.inboundSchema(GetFeedType);

/** @internal */
export const GetFeedMediaType$inboundSchema: z.ZodMiniType<
  GetFeedMediaType,
  unknown
> = openEnums.inboundSchema(GetFeedMediaType);

/** @internal */
export const GetFeedMedia$inboundSchema: z.ZodMiniType<GetFeedMedia, unknown> =
  z.object({
    type: GetFeedMediaType$inboundSchema,
    urls: z.array(types.string()),
    title: types.optional(types.string()),
    thumbnailUrl: types.optional(types.string()),
  });

export function getFeedMediaFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedMedia' from JSON`,
  );
}

/** @internal */
export const GetFeedReactionTypeCount$inboundSchema: z.ZodMiniType<
  GetFeedReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function getFeedReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const GetFeedPost$inboundSchema: z.ZodMiniType<GetFeedPost, unknown> = z
  .object({
    postUrl: types.string(),
    text: types.string(),
    date: types.number(),
    likesCount: types.number(),
    commentsCount: types.number(),
    sharesCount: types.number(),
    postUrn: types.string(),
    postId: types.string(),
    type: GetFeedType$inboundSchema,
    isRepost: types.optional(types.boolean()),
    media: types.optional(z.lazy(() => GetFeedMedia$inboundSchema)),
    authorName: types.optional(types.string()),
    authorProfileUrl: types.optional(types.string()),
    authorPublicIdentifier: z.optional(z.nullable(types.string())),
    authorProfileUrn: z.optional(z.nullable(types.string())),
    authorHeadline: types.optional(types.string()),
    authorImageUrl: types.optional(types.string()),
    repostedFromName: types.optional(types.string()),
    reactionTypeCounts: types.optional(
      z.array(z.lazy(() => GetFeedReactionTypeCount$inboundSchema)),
    ),
    numImpressions: z.optional(z.nullable(types.number())),
    highlightedReactorName: z.optional(z.nullable(types.string())),
    viewerLiked: z.optional(z.nullable(types.boolean())),
  });

export function getFeedPostFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedPost' from JSON`,
  );
}

/** @internal */
export const GetFeedCredits$inboundSchema: z.ZodMiniType<
  GetFeedCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getFeedCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedCredits' from JSON`,
  );
}

/** @internal */
export const GetFeedMeta$inboundSchema: z.ZodMiniType<GetFeedMeta, unknown> = z
  .object({
    credits: z.lazy(() => GetFeedCredits$inboundSchema),
  });

export function getFeedMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedMeta' from JSON`,
  );
}

/** @internal */
export const GetFeedResponse$inboundSchema: z.ZodMiniType<
  GetFeedResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    posts: z.array(z.lazy(() => GetFeedPost$inboundSchema)),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetFeedMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getFeedResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetFeedResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFeedResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFeedResponse' from JSON`,
  );
}

/** @internal */
export type SearchEmployeesRequest$Outbound = {
  company: string;
  keywords?: string | undefined;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchEmployeesRequest$outboundSchema: z.ZodMiniType<
  SearchEmployeesRequest$Outbound,
  SearchEmployeesRequest
> = z.object({
  company: z.string(),
  keywords: z.optional(z.string()),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchEmployeesRequestToJSON(
  searchEmployeesRequest: SearchEmployeesRequest,
): string {
  return JSON.stringify(
    SearchEmployeesRequest$outboundSchema.parse(searchEmployeesRequest),
  );
}

/** @internal */
export const SearchEmployeesType$inboundSchema: z.ZodMiniEnum<
  typeof SearchEmployeesType
> = z.enum(SearchEmployeesType);

/** @internal */
export const SearchEmployeesNetworkDistance$inboundSchema: z.ZodMiniType<
  SearchEmployeesNetworkDistance,
  unknown
> = openEnums.inboundSchema(SearchEmployeesNetworkDistance);

/** @internal */
export const SearchEmployeesCurrentPosition$inboundSchema: z.ZodMiniType<
  SearchEmployeesCurrentPosition,
  unknown
> = z.object({
  company: types.nullable(types.string()),
  role: types.nullable(types.string()),
});

export function searchEmployeesCurrentPositionFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesCurrentPosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesCurrentPosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesCurrentPosition' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesLane$inboundSchema: z.ZodMiniEnum<
  typeof SearchEmployeesLane
> = z.enum(SearchEmployeesLane);

/** @internal */
export const SearchEmployeesItem$inboundSchema: z.ZodMiniType<
  SearchEmployeesItem,
  unknown
> = z.object({
  type: SearchEmployeesType$inboundSchema,
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  location: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  networkDistance: types.nullable(SearchEmployeesNetworkDistance$inboundSchema),
  currentPositions: z.array(
    z.lazy(() => SearchEmployeesCurrentPosition$inboundSchema),
  ),
  profileUrn: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  nameMatch: z.optional(z.nullable(types.boolean())),
  badgeText: z.optional(z.nullable(types.string())),
  ringStatus: z.optional(z.nullable(types.string())),
  summary: z.optional(z.nullable(types.string())),
  actorInsights: types.optional(z.array(types.string())),
  lane: types.optional(SearchEmployeesLane$inboundSchema),
});

export function searchEmployeesItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesItem' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesPaging$inboundSchema: z.ZodMiniType<
  SearchEmployeesPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchEmployeesPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesPaging' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesWarning$inboundSchema: z.ZodMiniType<
  SearchEmployeesWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchEmployeesWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesWarning' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesCredits$inboundSchema: z.ZodMiniType<
  SearchEmployeesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchEmployeesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesCredits' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesMeta$inboundSchema: z.ZodMiniType<
  SearchEmployeesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchEmployeesCredits$inboundSchema),
});

export function searchEmployeesMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesMeta' from JSON`,
  );
}

/** @internal */
export const SearchEmployeesResponse$inboundSchema: z.ZodMiniType<
  SearchEmployeesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    company: types.string(),
    items: z.array(z.lazy(() => SearchEmployeesItem$inboundSchema)),
    paging: z.lazy(() => SearchEmployeesPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchEmployeesWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchEmployeesMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchEmployeesResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchEmployeesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchEmployeesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchEmployeesResponse' from JSON`,
  );
}

/** @internal */
export type SearchPostsRequest$Outbound = {
  keywords: string;
  url?: string | undefined;
  sortBy?: string | undefined;
  datePosted?: string | undefined;
  contentType?: string | undefined;
  authorIndustry?: Array<string> | undefined;
  authorCompany?: Array<string> | undefined;
  authorJobTitle?: Array<string> | undefined;
  postedBy?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchPostsRequest$outboundSchema: z.ZodMiniType<
  SearchPostsRequest$Outbound,
  SearchPostsRequest
> = z.object({
  keywords: z.string(),
  url: z.optional(z.string()),
  sortBy: z.optional(z.string()),
  datePosted: z.optional(z.string()),
  contentType: z.optional(z.string()),
  authorIndustry: z.optional(z.array(z.string())),
  authorCompany: z.optional(z.array(z.string())),
  authorJobTitle: z.optional(z.array(z.string())),
  postedBy: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchPostsRequestToJSON(
  searchPostsRequest: SearchPostsRequest,
): string {
  return JSON.stringify(
    SearchPostsRequest$outboundSchema.parse(searchPostsRequest),
  );
}

/** @internal */
export const SearchPostsCategory$inboundSchema: z.ZodMiniEnum<
  typeof SearchPostsCategory
> = z.enum(SearchPostsCategory);

/** @internal */
export const SearchPostsMediaType$inboundSchema: z.ZodMiniType<
  SearchPostsMediaType,
  unknown
> = openEnums.inboundSchema(SearchPostsMediaType);

/** @internal */
export const SearchPostsMedia$inboundSchema: z.ZodMiniType<
  SearchPostsMedia,
  unknown
> = z.object({
  type: SearchPostsMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function searchPostsMediaFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsMedia' from JSON`,
  );
}

/** @internal */
export const SearchPostsReactionTypeCount$inboundSchema: z.ZodMiniType<
  SearchPostsReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function searchPostsReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const SearchPostsType$inboundSchema: z.ZodMiniEnum<
  typeof SearchPostsType
> = z.enum(SearchPostsType);

/** @internal */
export const SearchPostsAuthor$inboundSchema: z.ZodMiniType<
  SearchPostsAuthor,
  unknown
> = z.object({
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  isCompany: types.boolean(),
  publicIdentifier: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
});

export function searchPostsAuthorFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsAuthor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsAuthor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsAuthor' from JSON`,
  );
}

/** @internal */
export const SearchPostsItem$inboundSchema: z.ZodMiniType<
  SearchPostsItem,
  unknown
> = z.object({
  postUrl: types.string(),
  text: types.string(),
  date: types.number(),
  likesCount: types.number(),
  commentsCount: types.number(),
  sharesCount: types.number(),
  postUrn: types.string(),
  postId: types.string(),
  media: types.optional(z.lazy(() => SearchPostsMedia$inboundSchema)),
  repostedFromName: types.optional(types.string()),
  reactionTypeCounts: types.optional(
    z.array(z.lazy(() => SearchPostsReactionTypeCount$inboundSchema)),
  ),
  numImpressions: z.optional(z.nullable(types.number())),
  highlightedReactorName: z.optional(z.nullable(types.string())),
  viewerLiked: z.optional(z.nullable(types.boolean())),
  type: SearchPostsType$inboundSchema,
  author: z.lazy(() => SearchPostsAuthor$inboundSchema),
  isRepost: types.boolean(),
});

export function searchPostsItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsItem' from JSON`,
  );
}

/** @internal */
export const SearchPostsPaging$inboundSchema: z.ZodMiniType<
  SearchPostsPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchPostsPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsPaging' from JSON`,
  );
}

/** @internal */
export const SearchPostsWarning$inboundSchema: z.ZodMiniType<
  SearchPostsWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchPostsWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsWarning' from JSON`,
  );
}

/** @internal */
export const SearchPostsCredits$inboundSchema: z.ZodMiniType<
  SearchPostsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchPostsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsCredits' from JSON`,
  );
}

/** @internal */
export const SearchPostsMeta$inboundSchema: z.ZodMiniType<
  SearchPostsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchPostsCredits$inboundSchema),
});

export function searchPostsMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsMeta' from JSON`,
  );
}

/** @internal */
export const SearchPostsResponse$inboundSchema: z.ZodMiniType<
  SearchPostsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchPostsCategory$inboundSchema,
    items: z.array(z.lazy(() => SearchPostsItem$inboundSchema)),
    paging: z.lazy(() => SearchPostsPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchPostsWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchPostsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchPostsResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchPostsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPostsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPostsResponse' from JSON`,
  );
}

/** @internal */
export const SearchPeopleConnectionDegree$outboundSchema: z.ZodMiniEnum<
  typeof SearchPeopleConnectionDegree
> = z.enum(SearchPeopleConnectionDegree);

/** @internal */
export type SearchPeopleRequest$Outbound = {
  keywords?: string | undefined;
  url?: string | undefined;
  maxSearches?: number | undefined;
  connectionDegree?: Array<string> | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
  title?: string | undefined;
  connectionOf?: string | undefined;
  profileLanguage?: Array<string> | undefined;
  school?: Array<string> | undefined;
  location?: Array<string> | undefined;
  industry?: Array<string> | undefined;
  currentCompany?: Array<string> | undefined;
  pastCompany?: Array<string> | undefined;
  followersOf?: Array<string> | undefined;
  openToVolunteering?: boolean | undefined;
  serviceCategories?: Array<string> | undefined;
  verifiedOnly?: boolean | undefined;
  schoolFreetext?: string | undefined;
  companyHQ?: Array<string> | undefined;
  companySize?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchPeopleRequest$outboundSchema: z.ZodMiniType<
  SearchPeopleRequest$Outbound,
  SearchPeopleRequest
> = z.object({
  keywords: z.optional(z.string()),
  url: z.optional(z.string()),
  maxSearches: z.optional(z.int()),
  connectionDegree: z.optional(
    z.array(SearchPeopleConnectionDegree$outboundSchema),
  ),
  firstName: z.optional(z.string()),
  lastName: z.optional(z.string()),
  title: z.optional(z.string()),
  connectionOf: z.optional(z.string()),
  profileLanguage: z.optional(z.array(z.string())),
  school: z.optional(z.array(z.string())),
  location: z.optional(z.array(z.string())),
  industry: z.optional(z.array(z.string())),
  currentCompany: z.optional(z.array(z.string())),
  pastCompany: z.optional(z.array(z.string())),
  followersOf: z.optional(z.array(z.string())),
  openToVolunteering: z.optional(z.boolean()),
  serviceCategories: z.optional(z.array(z.string())),
  verifiedOnly: z.optional(z.boolean()),
  schoolFreetext: z.optional(z.string()),
  companyHQ: z.optional(z.array(z.string())),
  companySize: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchPeopleRequestToJSON(
  searchPeopleRequest: SearchPeopleRequest,
): string {
  return JSON.stringify(
    SearchPeopleRequest$outboundSchema.parse(searchPeopleRequest),
  );
}

/** @internal */
export const SearchPeopleCategory$inboundSchema: z.ZodMiniEnum<
  typeof SearchPeopleCategory
> = z.enum(SearchPeopleCategory);

/** @internal */
export const SearchPeopleType$inboundSchema: z.ZodMiniEnum<
  typeof SearchPeopleType
> = z.enum(SearchPeopleType);

/** @internal */
export const SearchPeopleNetworkDistance$inboundSchema: z.ZodMiniType<
  SearchPeopleNetworkDistance,
  unknown
> = openEnums.inboundSchema(SearchPeopleNetworkDistance);

/** @internal */
export const SearchPeopleCurrentPosition$inboundSchema: z.ZodMiniType<
  SearchPeopleCurrentPosition,
  unknown
> = z.object({
  company: types.nullable(types.string()),
  role: types.nullable(types.string()),
});

export function searchPeopleCurrentPositionFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleCurrentPosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleCurrentPosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleCurrentPosition' from JSON`,
  );
}

/** @internal */
export const SearchPeopleLane$inboundSchema: z.ZodMiniEnum<
  typeof SearchPeopleLane
> = z.enum(SearchPeopleLane);

/** @internal */
export const SearchPeopleItem$inboundSchema: z.ZodMiniType<
  SearchPeopleItem,
  unknown
> = z.object({
  type: SearchPeopleType$inboundSchema,
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  location: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  networkDistance: types.nullable(SearchPeopleNetworkDistance$inboundSchema),
  currentPositions: z.array(
    z.lazy(() => SearchPeopleCurrentPosition$inboundSchema),
  ),
  profileUrn: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  nameMatch: z.optional(z.nullable(types.boolean())),
  badgeText: z.optional(z.nullable(types.string())),
  ringStatus: z.optional(z.nullable(types.string())),
  summary: z.optional(z.nullable(types.string())),
  actorInsights: types.optional(z.array(types.string())),
  lane: types.optional(SearchPeopleLane$inboundSchema),
});

export function searchPeopleItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleItem' from JSON`,
  );
}

/** @internal */
export const SearchPeoplePaging$inboundSchema: z.ZodMiniType<
  SearchPeoplePaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchPeoplePagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeoplePaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeoplePaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeoplePaging' from JSON`,
  );
}

/** @internal */
export const SearchPeopleWarning$inboundSchema: z.ZodMiniType<
  SearchPeopleWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchPeopleWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleWarning' from JSON`,
  );
}

/** @internal */
export const SearchPeopleCredits$inboundSchema: z.ZodMiniType<
  SearchPeopleCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchPeopleCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleCredits' from JSON`,
  );
}

/** @internal */
export const SearchPeopleMeta$inboundSchema: z.ZodMiniType<
  SearchPeopleMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchPeopleCredits$inboundSchema),
});

export function searchPeopleMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleMeta' from JSON`,
  );
}

/** @internal */
export const SearchPeopleResponse$inboundSchema: z.ZodMiniType<
  SearchPeopleResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchPeopleCategory$inboundSchema,
    items: z.array(z.lazy(() => SearchPeopleItem$inboundSchema)),
    paging: z.lazy(() => SearchPeoplePaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchPeopleWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchPeopleMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchPeopleResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchPeopleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchPeopleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchPeopleResponse' from JSON`,
  );
}

/** @internal */
export type SearchCompaniesRequest$Outbound = {
  keywords?: string | undefined;
  url?: string | undefined;
  location?: Array<string> | undefined;
  industry?: Array<string> | undefined;
  companySize?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchCompaniesRequest$outboundSchema: z.ZodMiniType<
  SearchCompaniesRequest$Outbound,
  SearchCompaniesRequest
> = z.object({
  keywords: z.optional(z.string()),
  url: z.optional(z.string()),
  location: z.optional(z.array(z.string())),
  industry: z.optional(z.array(z.string())),
  companySize: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchCompaniesRequestToJSON(
  searchCompaniesRequest: SearchCompaniesRequest,
): string {
  return JSON.stringify(
    SearchCompaniesRequest$outboundSchema.parse(searchCompaniesRequest),
  );
}

/** @internal */
export const SearchCompaniesCategory$inboundSchema: z.ZodMiniEnum<
  typeof SearchCompaniesCategory
> = z.enum(SearchCompaniesCategory);

/** @internal */
export const SearchCompaniesType$inboundSchema: z.ZodMiniEnum<
  typeof SearchCompaniesType
> = z.enum(SearchCompaniesType);

/** @internal */
export const SearchCompaniesLane$inboundSchema: z.ZodMiniEnum<
  typeof SearchCompaniesLane
> = z.enum(SearchCompaniesLane);

/** @internal */
export const SearchCompaniesItem$inboundSchema: z.ZodMiniType<
  SearchCompaniesItem,
  unknown
> = z.object({
  type: SearchCompaniesType$inboundSchema,
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  summary: types.nullable(types.string()),
  industry: types.nullable(types.string()),
  location: types.nullable(types.string()),
  followersCount: types.nullable(types.number()),
  logoUrl: z.optional(z.nullable(types.string())),
  lane: types.optional(SearchCompaniesLane$inboundSchema),
});

export function searchCompaniesItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesItem' from JSON`,
  );
}

/** @internal */
export const SearchCompaniesPaging$inboundSchema: z.ZodMiniType<
  SearchCompaniesPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchCompaniesPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesPaging' from JSON`,
  );
}

/** @internal */
export const SearchCompaniesWarning$inboundSchema: z.ZodMiniType<
  SearchCompaniesWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchCompaniesWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesWarning' from JSON`,
  );
}

/** @internal */
export const SearchCompaniesCredits$inboundSchema: z.ZodMiniType<
  SearchCompaniesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchCompaniesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesCredits' from JSON`,
  );
}

/** @internal */
export const SearchCompaniesMeta$inboundSchema: z.ZodMiniType<
  SearchCompaniesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchCompaniesCredits$inboundSchema),
});

export function searchCompaniesMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesMeta' from JSON`,
  );
}

/** @internal */
export const SearchCompaniesResponse$inboundSchema: z.ZodMiniType<
  SearchCompaniesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchCompaniesCategory$inboundSchema,
    items: z.array(z.lazy(() => SearchCompaniesItem$inboundSchema)),
    paging: z.lazy(() => SearchCompaniesPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchCompaniesWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchCompaniesMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchCompaniesResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchCompaniesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchCompaniesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchCompaniesResponse' from JSON`,
  );
}

/** @internal */
export type SearchJobsRequest$Outbound = {
  keywords?: string | undefined;
  url?: string | undefined;
  location?: Array<string> | undefined;
  datePosted?: string | undefined;
  sortBy?: string | undefined;
  jobType?: Array<string> | undefined;
  experienceLevel?: Array<string> | undefined;
  workplaceType?: Array<string> | undefined;
  company?: Array<string> | undefined;
  industry?: Array<string> | undefined;
  jobFunction?: Array<string> | undefined;
  easyApply?: boolean | undefined;
  under10Applicants?: boolean | undefined;
  inYourNetwork?: boolean | undefined;
  hasVerifications?: boolean | undefined;
  fairChanceEmployer?: boolean | undefined;
  benefits?: Array<string> | undefined;
  commitments?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchJobsRequest$outboundSchema: z.ZodMiniType<
  SearchJobsRequest$Outbound,
  SearchJobsRequest
> = z.object({
  keywords: z.optional(z.string()),
  url: z.optional(z.string()),
  location: z.optional(z.array(z.string())),
  datePosted: z.optional(z.string()),
  sortBy: z.optional(z.string()),
  jobType: z.optional(z.array(z.string())),
  experienceLevel: z.optional(z.array(z.string())),
  workplaceType: z.optional(z.array(z.string())),
  company: z.optional(z.array(z.string())),
  industry: z.optional(z.array(z.string())),
  jobFunction: z.optional(z.array(z.string())),
  easyApply: z.optional(z.boolean()),
  under10Applicants: z.optional(z.boolean()),
  inYourNetwork: z.optional(z.boolean()),
  hasVerifications: z.optional(z.boolean()),
  fairChanceEmployer: z.optional(z.boolean()),
  benefits: z.optional(z.array(z.string())),
  commitments: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchJobsRequestToJSON(
  searchJobsRequest: SearchJobsRequest,
): string {
  return JSON.stringify(
    SearchJobsRequest$outboundSchema.parse(searchJobsRequest),
  );
}

/** @internal */
export const SearchJobsCategory$inboundSchema: z.ZodMiniEnum<
  typeof SearchJobsCategory
> = z.enum(SearchJobsCategory);

/** @internal */
export const SearchJobsType$inboundSchema: z.ZodMiniEnum<
  typeof SearchJobsType
> = z.enum(SearchJobsType);

/** @internal */
export const SearchJobsItem$inboundSchema: z.ZodMiniType<
  SearchJobsItem,
  unknown
> = z.object({
  type: SearchJobsType$inboundSchema,
  title: types.string(),
  company: types.nullable(types.string()),
  companyUrl: types.nullable(types.string()),
  companyLogo: types.nullable(types.string()),
  location: types.nullable(types.string()),
  workplaceType: types.nullable(types.string()),
  postedAt: types.nullable(types.string()),
  jobUrl: types.string(),
  listingId: types.string(),
});

export function searchJobsItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsItem' from JSON`,
  );
}

/** @internal */
export const SearchJobsPaging$inboundSchema: z.ZodMiniType<
  SearchJobsPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchJobsPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsPaging' from JSON`,
  );
}

/** @internal */
export const SearchJobsWarning$inboundSchema: z.ZodMiniType<
  SearchJobsWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchJobsWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsWarning' from JSON`,
  );
}

/** @internal */
export const SearchJobsCredits$inboundSchema: z.ZodMiniType<
  SearchJobsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchJobsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsCredits' from JSON`,
  );
}

/** @internal */
export const SearchJobsMeta$inboundSchema: z.ZodMiniType<
  SearchJobsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchJobsCredits$inboundSchema),
});

export function searchJobsMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsMeta' from JSON`,
  );
}

/** @internal */
export const SearchJobsResponse$inboundSchema: z.ZodMiniType<
  SearchJobsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchJobsCategory$inboundSchema,
    items: z.array(z.lazy(() => SearchJobsItem$inboundSchema)),
    paging: z.lazy(() => SearchJobsPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchJobsWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchJobsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchJobsResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchJobsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchJobsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchJobsResponse' from JSON`,
  );
}

/** @internal */
export type SearchByUrlRequest$Outbound = {
  url: string;
  start?: number | undefined;
  count?: number | undefined;
};

/** @internal */
export const SearchByUrlRequest$outboundSchema: z.ZodMiniType<
  SearchByUrlRequest$Outbound,
  SearchByUrlRequest
> = z.object({
  url: z.string(),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
});

export function searchByUrlRequestToJSON(
  searchByUrlRequest: SearchByUrlRequest,
): string {
  return JSON.stringify(
    SearchByUrlRequest$outboundSchema.parse(searchByUrlRequest),
  );
}

/** @internal */
export const SearchByUrlCategory$inboundSchema: z.ZodMiniType<
  SearchByUrlCategory,
  unknown
> = openEnums.inboundSchema(SearchByUrlCategory);

/** @internal */
export const SearchByUrlTypeJob$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlTypeJob
> = z.enum(SearchByUrlTypeJob);

/** @internal */
export const ItemsJob$inboundSchema: z.ZodMiniType<ItemsJob, unknown> = z
  .object({
    type: SearchByUrlTypeJob$inboundSchema,
    title: types.string(),
    company: types.nullable(types.string()),
    companyUrl: types.nullable(types.string()),
    companyLogo: types.nullable(types.string()),
    location: types.nullable(types.string()),
    workplaceType: types.nullable(types.string()),
    postedAt: types.nullable(types.string()),
    jobUrl: types.string(),
    listingId: types.string(),
  });

export function itemsJobFromJSON(
  jsonString: string,
): SafeParseResult<ItemsJob, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ItemsJob$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ItemsJob' from JSON`,
  );
}

/** @internal */
export const SearchByUrlTypeCompany$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlTypeCompany
> = z.enum(SearchByUrlTypeCompany);

/** @internal */
export const SearchByUrlLane2$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlLane2
> = z.enum(SearchByUrlLane2);

/** @internal */
export const ItemsCompany$inboundSchema: z.ZodMiniType<ItemsCompany, unknown> =
  z.object({
    type: SearchByUrlTypeCompany$inboundSchema,
    name: types.string(),
    profileUrl: types.nullable(types.string()),
    summary: types.nullable(types.string()),
    industry: types.nullable(types.string()),
    location: types.nullable(types.string()),
    followersCount: types.nullable(types.number()),
    logoUrl: z.optional(z.nullable(types.string())),
    lane: types.optional(SearchByUrlLane2$inboundSchema),
  });

export function itemsCompanyFromJSON(
  jsonString: string,
): SafeParseResult<ItemsCompany, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ItemsCompany$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ItemsCompany' from JSON`,
  );
}

/** @internal */
export const SearchByUrlTypePeople$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlTypePeople
> = z.enum(SearchByUrlTypePeople);
