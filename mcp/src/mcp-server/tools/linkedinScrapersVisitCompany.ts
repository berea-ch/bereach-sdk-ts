/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinScrapersVisitCompany } from "../../funcs/linkedinScrapersVisitCompany.js";
import { VisitLinkedInCompanyRequest$zodSchema } from "../../models/visitlinkedincompanyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VisitLinkedInCompanyRequest$zodSchema,
};

export const tool$linkedinScrapersVisitCompany: ToolDefinition<typeof args> = {
  name: "linkedin-scrapers-visit-company",
  description: `Visit LinkedIn company page and extract profile data

Fetches a LinkedIn company profile by URL or universal name. Returns structured company data including description, industry, employee count, headquarters, logo, affiliated/similar companies, and more. Optionally enriches with workplace policy data (costs 1 extra credit per optional API call). Base cost: 1 credit.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinScrapersVisitCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
