/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsSyncActions } from "../../funcs/campaignsSyncActions.js";
import { SyncCampaignActionsRequest$zodSchema } from "../../models/synccampaignactionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SyncCampaignActionsRequest$zodSchema,
};

export const tool$campaignsSyncActions: ToolDefinition<typeof args> = {
  name: "campaigns-sync-actions",
  description: `Mark actions as completed without performing them

Manually mark actions as completed for profiles within a campaign. Use when actions were performed outside the API (e.g. manual DMs) and need to be synced. 0 credits, no rate limit.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await campaignsSyncActions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
