/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedInActionsListInvitations } from "../../funcs/linkedInActionsListInvitations.js";
import { ListLinkedInInvitationsRequest$zodSchema } from "../../models/listlinkedininvitationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListLinkedInInvitationsRequest$zodSchema.optional(),
};

export const tool$linkedInActionsListInvitations: ToolDefinition<typeof args> =
  {
    name: "linked-in-actions-list-invitations",
    description: `List received LinkedIn connection invitations

Returns pending connection invitations received by the authenticated LinkedIn account. Each invitation includes the invitationId and sharedSecret needed to accept it via the accept endpoint. Costs 1 credit per request.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await linkedInActionsListInvitations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
