/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { profileGetPosts } from "../../funcs/profileGetPosts.js";
import { GetMyLinkedInPostsRequest$zodSchema } from "../../models/getmylinkedinpostsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMyLinkedInPostsRequest$zodSchema.optional(),
};

export const tool$profileGetPosts: ToolDefinition<typeof args> = {
  name: "profile-get-posts",
  description: `Get authenticated user's LinkedIn posts

Returns paginated posts from the authenticated user's own LinkedIn profile. No credits consumed. Requires valid LinkedIn credentials and a stored profileUrn (call /me/linkedin/refresh first if needed).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await profileGetPosts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
