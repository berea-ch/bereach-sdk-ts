/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedInActionsConnectProfile } from "../../funcs/linkedInActionsConnectProfile.js";
import { ConnectLinkedInProfileRequest$zodSchema } from "../../models/connectlinkedinprofileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectLinkedInProfileRequest$zodSchema,
};

export const tool$linkedInActionsConnectProfile: ToolDefinition<typeof args> = {
  name: "linked-in-actions-connect-profile",
  description: `Send LinkedIn connection request

Authenticates the requester, validates LinkedIn credentials, and sends a connection request to the specified LinkedIn profile. Rate limited to 80 requests per day per user.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedInActionsConnectProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
