/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinScrapersCollectComments } from "../../funcs/linkedinScrapersCollectComments.js";
import { CollectLinkedInCommentsRequest$zodSchema } from "../../models/collectlinkedincommentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CollectLinkedInCommentsRequest$zodSchema,
};

export const tool$linkedinScrapersCollectComments: ToolDefinition<typeof args> =
  {
    name: "linkedin-scrapers-collect-comments",
    description: `Scrape LinkedIn post comments

Returns paginated top-level comments for a LinkedIn post (newest first). Use count=0 for a free total-only check (0 credits, no rate-limit slot consumed). Response includes previousTotal (server-cached) to detect new comments without client tracking.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await linkedinScrapersCollectComments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
