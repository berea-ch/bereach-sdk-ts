/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsGetStats } from "../../funcs/campaignsGetStats.js";
import { GetCampaignStatsRequest$zodSchema } from "../../models/getcampaignstatsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCampaignStatsRequest$zodSchema,
};

export const tool$campaignsGetStats: ToolDefinition<typeof args> = {
  name: "campaigns-get-stats",
  description: `Get aggregate campaign statistics

Returns per-action counts, unique profile count, and total credits used for a campaign. 0 credits, no rate limit.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await campaignsGetStats(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
