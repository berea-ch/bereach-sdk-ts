# ScheduledMessageCreateHold

Why the run held this message out of the send, and the line it objected to. Present while it is held.

## Example Usage

```typescript
import { ScheduledMessageCreateHold } from "bereach/models/operations";

let value: ScheduledMessageCreateHold = {
  reason: "<value>",
};
```

## Fields

| Field              | Type               | Required           | Description        |
| ------------------ | ------------------ | ------------------ | ------------------ |
| `reason`           | *string*           | :heavy_check_mark: | N/A                |
| `quote`            | *string*           | :heavy_minus_sign: | N/A                |