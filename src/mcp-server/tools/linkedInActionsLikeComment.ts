/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedInActionsLikeComment } from "../../funcs/linkedInActionsLikeComment.js";
import { LikeLinkedInCommentRequest$zodSchema } from "../../models/likelinkedincommentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LikeLinkedInCommentRequest$zodSchema,
};

export const tool$linkedInActionsLikeComment: ToolDefinition<typeof args> = {
  name: "linked-in-actions-like-comment",
  description: `Like a LinkedIn comment

Authenticates the requester, validates LinkedIn credentials, and likes the specified LinkedIn comment using the GraphQL Reactions API.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedInActionsLikeComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
