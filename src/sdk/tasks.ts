/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksAgentSnapshot } from "../funcs/tasks-agent-snapshot.js";
import { tasksEventsFeed } from "../funcs/tasks-events-feed.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Tasks extends ClientSDK {
  /**
   * Poll for events
   *
   * @remarks
   * Returns recent user events (task completions, replies, connections, campaign milestones). Use the cursor for pagination. Events expire after 48 hours..
   */
  async eventsFeed(
    request?: operations.EventsFeedRequest | undefined,
    options?: RequestOptions,
  ): Promise<operations.EventsFeedResponse> {
    return unwrapAsync(tasksEventsFeed(
      this,
      request,
      options,
    ));
  }

  /**
   * Agent session snapshot
   *
   * @remarks
   * Full session snapshot, initialization only: credits, pipeline, every saved context, pending drafts, accounts and events in one call. Not a targeted lookup: read specific context entries with context_list by type= and scope=.
   */
  async agentSnapshot(
    options?: RequestOptions,
  ): Promise<operations.ContextGetResponse> {
    return unwrapAsync(tasksAgentSnapshot(
      this,
      options,
    ));
  }
}
