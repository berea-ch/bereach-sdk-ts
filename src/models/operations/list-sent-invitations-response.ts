/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as discriminatedUnionTypes from "../../types/discriminated-union.js";
import { discriminatedUnion } from "../../types/discriminated-union.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  ItemsCompany,
  ItemsCompany$inboundSchema,
  ItemsJob,
  ItemsJob$inboundSchema,
  SearchByUrlCategory,
  SearchByUrlCategory$inboundSchema,
  SearchByUrlTypePeople,
  SearchByUrlTypePeople$inboundSchema,
} from "./search-by-url-type-people.js";

/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export const SearchByUrlNetworkDistance = {
  Distance1: "DISTANCE_1",
  Distance2: "DISTANCE_2",
  Distance3: "DISTANCE_3",
  OutOfNetwork: "OUT_OF_NETWORK",
} as const;
/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export type SearchByUrlNetworkDistance = OpenEnum<
  typeof SearchByUrlNetworkDistance
>;

export type SearchByUrlCurrentPosition = {
  company: string | null;
  role: string | null;
};

/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export const SearchByUrlLane1 = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
 */
export type SearchByUrlLane1 = ClosedEnum<typeof SearchByUrlLane1>;

export type ItemsPeople = {
  type: SearchByUrlTypePeople;
  name: string;
  profileUrl: string | null;
  headline: string | null;
  location: string | null;
  profilePicture: string | null;
  /**
   * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
   */
  networkDistance: SearchByUrlNetworkDistance | null;
  /**
   * Current work positions for this profile.
   */
  currentPositions: Array<SearchByUrlCurrentPosition>;
  /**
   * LinkedIn profile URN (e.g. urn:li:fsd_profile:ACoAAA...) when available
   */
  profileUrn: string | null;
  /**
   * Vanity slug from profile URL (e.g. john-doe) when not URN-based
   */
  publicIdentifier: string | null;
  /**
   * True when the search ranker matched the literal name (vs headline/skills). When picking between two same-named people, the one with nameMatch=true is the one the user typed.
   */
  nameMatch?: boolean | null | undefined;
  /**
   * Profile badge surfaced by LinkedIn (Top Voice / Premium / Verified / Influencer). Indicates investment in the platform and is a useful qualifier weight.
   */
  badgeText?: string | null | undefined;
  /**
   * Profile ring status visualised on LinkedIn (OPEN_TO_WORK, HIRING, OPEN_FOR_BUSINESS, etc.). Open intent signal — directly relevant for recruiters, vendors, and sales.
   */
  ringStatus?: string | null | undefined;
  /**
   * Additional profile snippet returned by the search ranker, distinct from headline. Sometimes contains mutual-connection context or topic affinity (e.g. 'Writes about B2B sales').
   */
  summary?: string | null | undefined;
  /**
   * LinkedIn-curated context strings — typical entries: 'X mutual connections', 'Follows {company}', 'Worked at {company}'. Use these directly in personalised outreach openers.
   */
  actorInsights?: Array<string> | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data: fields a session would fill may be empty, and the name may be derived from the profile URL.
   */
  lane?: SearchByUrlLane1 | undefined;
};

/**
 * Type of media attached to the post.
 */
export const SearchByUrlMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type SearchByUrlMediaType = OpenEnum<typeof SearchByUrlMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type SearchByUrlMedia = {
  /**
   * Type of media attached to the post.
   */
  type: SearchByUrlMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type SearchByUrlReactionTypeCount = {
  reactionType: string;
  count: number;
};

export const SearchByUrlTypePost = {
  Post: "POST",
} as const;
export type SearchByUrlTypePost = ClosedEnum<typeof SearchByUrlTypePost>;

export type SearchByUrlAuthor = {
  name: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  isCompany: boolean;
  /**
   * Vanity slug when not URN-based
   */
  publicIdentifier: string | null;
  /**
   * LinkedIn profile URN when available
   */
  profileUrn: string | null;
};

export type ItemsPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: SearchByUrlMedia | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<SearchByUrlReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
  type: SearchByUrlTypePost;
  author: SearchByUrlAuthor;
  isRepost: boolean;
};

export type Items =
  | Array<ItemsPost>
  | Array<ItemsPeople>
  | Array<ItemsCompany>
  | Array<ItemsJob>;

export type SearchByUrlPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchByUrlWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchByUrlCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchByUrlMeta = {
  credits: SearchByUrlCredits;
};

/**
 * Search results parsed from the LinkedIn URL
 */
export type SearchByUrlResponse = {
  success: true;
  category: SearchByUrlCategory;
  items:
    | Array<ItemsPost>
    | Array<ItemsPeople>
    | Array<ItemsCompany>
    | Array<ItemsJob>;
  paging: SearchByUrlPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchByUrlWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchByUrlMeta | undefined;
};

/**
 * How to supply a value: 'enum' = pick a label; 'entity' = pass a name (company/school/etc); 'toggle' = boolean; 'range' = {min,max}; 'text' = free string.
 */
export const ValueKind = {
  Enum: "enum",
  Entity: "entity",
  Toggle: "toggle",
  Range: "range",
  Text: "text",
} as const;
/**
 * How to supply a value: 'enum' = pick a label; 'entity' = pass a name (company/school/etc); 'toggle' = boolean; 'range' = {min,max}; 'text' = free string.
 */
export type ValueKind = OpenEnum<typeof ValueKind>;

export type Filter = {
  /**
   * User-facing filter name, e.g. 'Seniority level'.
   */
  name: string;
  /**
   * The BeReach schema field name to set on a search, e.g. 'seniority'.
   */
  field: string;
  /**
   * UI group: Company / Role / Personal / Buyer intent / Best path in / Workflow.
   */
  group: string;
  /**
   * How to supply a value: 'enum' = pick a label; 'entity' = pass a name (company/school/etc); 'toggle' = boolean; 'range' = {min,max}; 'text' = free string.
   */
  valueKind: ValueKind;
  /**
   * Whether the filter accepts an exclude list.
   */
  supportsExclude: boolean;
  /**
   * Whether THIS credential's Sales Nav seat tier exposes the filter.
   */
  available: boolean;
};

export type ListSalesNavFiltersCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListSalesNavFiltersMeta = {
  credits: ListSalesNavFiltersCredits;
};

/**
 * Available Sales Nav filters for this seat.
 */
export type ListSalesNavFiltersResponse = {
  success: true;
  filters: Array<Filter>;
  count: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListSalesNavFiltersMeta | undefined;
};

export type Persona = {
  id: string;
  name: string;
  description: string | null;
  /**
   * Human-readable summary of the persona's filter criteria (e.g. 'VP+ in Sales, US/UK'). Use this to match the user's intent to a saved persona.
   */
  criteriaSummary: string | null;
};

export type ListSalesNavPersonasCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListSalesNavPersonasMeta = {
  credits: ListSalesNavPersonasCredits;
};

/**
 * User's saved personas.
 */
export type ListSalesNavPersonasResponse = {
  success: true;
  personas: Array<Persona>;
  count: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListSalesNavPersonasMeta | undefined;
};

export const ListSalesNavSavedSearchesCategory = {
  People: "people",
  Companies: "companies",
} as const;
export type ListSalesNavSavedSearchesCategory = OpenEnum<
  typeof ListSalesNavSavedSearchesCategory
>;

export type SavedSearch = {
  id: string;
  name: string;
  category: ListSalesNavSavedSearchesCategory | null;
  /**
   * Full Sales Navigator search URL — pass straight into search_sales_nav_people/companies via the `url` field.
   */
  savedSearchUrl: string;
  /**
   * New matches since the user last viewed this saved search, if LinkedIn reports it.
   */
  newResultsCount: number | null;
  lastUsedAt: string | null;
};

export type ListSalesNavSavedSearchesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListSalesNavSavedSearchesMeta = {
  credits: ListSalesNavSavedSearchesCredits;
};

/**
 * User's saved searches.
 */
export type ListSalesNavSavedSearchesResponse = {
  success: true;
  savedSearches: Array<SavedSearch>;
  count: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListSalesNavSavedSearchesMeta | undefined;
};

export type SearchParametersRequest = {
  /**
   * Which kind of value to resolve. Entity kinds resolve live; the closed-value kinds resolve from a dictionary and need no connected account.
   */
  type: string;
  /**
   * The text to resolve. Empty on a closed-value type lists every option.
   */
  keywords?: string | undefined;
  limit?: number | undefined;
};

export type SearchParametersItem = {
  /**
   * The id LinkedIn's own filters take.
   */
  id: string;
  /**
   * Human-readable label for the id.
   */
  title: string;
  /**
   * Echo of the requested type.
   */
  type: string;
  /**
   * Present on the closed-enum types.
   */
  displayValue?: string | undefined;
  /**
   * Where the id came from: LinkedIn's typeahead, the curated alias map, or a Sales Navigator enum snapshot.
   */
  source?: string | undefined;
  /**
   * Present when the curated alias map answered.
   */
  aliasKind?: string | undefined;
};

export type SearchParametersCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchParametersMeta = {
  credits: SearchParametersCredits;
};

/**
 * Matching ids, or a hint explaining why there were none
 */
export type SearchParametersResponse = {
  success: true;
  items: Array<SearchParametersItem>;
  count: number;
  /**
   * Present ONLY when nothing matched, and it says why rather than leaving an empty array to be read as an empty world.
   */
  hint?: string | undefined;
  /**
   * True when the curated alias map answered rather than LinkedIn.
   */
  aliasResolved: boolean;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchParametersMeta | undefined;
};

export type ResolveProfilesRequest = {
  /**
   * Up to 50 LinkedIn identifiers to resolve. Accepts any mix of: full profile URLs (https://www.linkedin.com/in/username), vanity names (john-doe), profile URNs (urn:li:fsd_profile:ACoAAA...), bare ACoA ids, and Sales Navigator deep links. Duplicates are auto-removed.
   */
  inputs: Array<string>;
};

export type Result = {
  /**
   * Echo of what was submitted.
   */
  input: string;
  /**
   * Canonical /in/<slug> URL when one was found.
   */
  profileUrl: string | null;
  profileUrn: string | null;
  /**
   * The vanity slug, never the encrypted id.
   */
  publicIdentifier: string | null;
  /**
   * resolved = a canonical vanity slug; acoa_only = an encrypted id and nothing better; failed = nothing.
   */
  status: string;
  /**
   * A code, present only on failure.
   */
  error: string | null;
  /**
   * True when served from cache, which costs nothing.
   */
  cached: boolean;
};

export type ResolveProfilesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ResolveProfilesMeta = {
  credits: ResolveProfilesCredits;
};

/**
 * One result per distinct input, in the order submitted
 */
export type ResolveProfilesResponse = {
  results: Array<Result>;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ResolveProfilesMeta | undefined;
};

/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export const SearchSalesNavCategoryRequest = {
  People: "people",
  Companies: "companies",
} as const;
/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export type SearchSalesNavCategoryRequest = ClosedEnum<
  typeof SearchSalesNavCategoryRequest
>;

/**
 * Current-employer filter (people only). Accepts company names ('Stripe', 'Datadog') OR LinkedIn numeric ids. Server resolves names via typeahead.
 */
export type SearchSalesNavCompany = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
 */
export type SearchSalesNavIndustry = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

export type SearchSalesNavTenure = {
  /**
   * Minimum years
   */
  min?: number | undefined;
  /**
   * Maximum years
   */
  max?: number | undefined;
};

/**
 * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
 */
export type SearchSalesNavPastCompany = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
 */
export type SearchSalesNavCompanyHeadquarters = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

export type SearchSalesNavRequest = {
  /**
   * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
   */
  category?: SearchSalesNavCategoryRequest | undefined;
  /**
   * Sales Navigator search URL — paste directly from your browser. Category and filters are extracted automatically. Explicit params override URL-derived values. Example: https://www.linkedin.com/sales/search/people?query=...
   */
  url?: string | undefined;
  /**
   * Search keywords. Supports LinkedIn Boolean syntax: use double quotes for exact phrases, AND/OR/NOT operators (UPPERCASE), and parentheses for grouping.
   */
  keywords?: string | undefined;
  /**
   * Job title keywords (people only). Supports Boolean OR: 'CEO OR CTO OR Founder'.
   */
  title?: string | undefined;
  /**
   * Current-employer filter (people only). Accepts company names ('Stripe', 'Datadog') OR LinkedIn numeric ids. Server resolves names via typeahead.
   */
  company?: SearchSalesNavCompany | undefined;
  /**
   * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
   */
  industry?: SearchSalesNavIndustry | undefined;
  /**
   * Geography filter (people & companies). Accepts location names ('France', 'San Francisco Bay Area', 'EMEA') OR LinkedIn geo ids. Server resolves via typeahead.
   */
  location?: Array<string> | undefined;
  /**
   * Geography filter to EXCLUDE. Same accepted shapes as `location`. Useful for 'all of Europe except Switzerland' patterns.
   */
  locationExclude?: Array<string> | undefined;
  /**
   * Seniority levels (people only). Accepts labels ('CXO', 'VP', 'Director', 'Owner / Partner', 'Senior', 'Entry Level', etc.) OR LinkedIn numeric ids. Server resolves labels to LinkedIn's 3-digit ids (CXO=310, etc.) — do NOT guess the ids.
   */
  seniority?: Array<string> | undefined;
  /**
   * Job function (people only). Accepts function names ('Sales', 'Engineering', 'Marketing', 'Finance', etc. — 26 canonical) OR LinkedIn numeric ids.
   */
  function?: Array<string> | undefined;
  /**
   * Years at current company (people only). Array of ranges, e.g. [{min:3}, {max:10}].
   */
  tenure?: Array<SearchSalesNavTenure> | undefined;
  /**
   * Profile language (people only). Accepts ISO 639-1 codes (e.g. 'en') or full language names; the server resolves either.
   */
  profileLanguage?: Array<string> | undefined;
  /**
   * Network distance: 'F' (1st-degree), 'S' (2nd-degree), 'O' (3rd-degree and beyond), or 'GROUP'. The labels '1st degree', '2nd degree', '3rd degree' are also accepted.
   */
  connectionDegree?: Array<string> | undefined;
  /**
   * School filter (people only). Accepts school names ('Stanford', 'HEC Paris') OR LinkedIn numeric ids. Server resolves via typeahead.
   */
  school?: Array<string> | undefined;
  /**
   * Overall years of experience (people only). Accepts labels ('Less than 1 year', '1 to 2 years', '3 to 5 years', '6 to 10 years', 'More than 10 years') OR LinkedIn numeric ids 1-5.
   */
  yearsOfExperience?: Array<string> | undefined;
  /**
   * Buyer Persona IDs the user has configured on sales.linkedin.com home (combos of function+seniority+title+geo). Match the IDs from the URL filter ((type:PERSONA,values:List((id:<id>,…)))).
   */
  persona?: Array<string> | undefined;
  /**
   * Filter to leads who changed jobs in the last 90 days (LinkedIn buying-intent flag). True = include only job-changers; omit/false = no constraint.
   */
  recentlyChangedJobs?: boolean | undefined;
  /**
   * Filter to leads who recently posted on LinkedIn (engagement signal — active posters reply more often). True = include only recent posters; omit/false = no constraint.
   */
  postedOnLinkedin?: boolean | undefined;
  /**
   * Filter to leads with shared experience with the searcher (same school / past employer / group). True = include only shared-experience matches; omit/false = no constraint.
   */
  sharedExperiences?: boolean | undefined;
  /**
   * Filter to leads who recently viewed YOUR profile (LinkedIn flag VYP). Highest-intent engagement signal — they already showed interest. Requires Sales Nav seat that captures profile views.
   */
  viewedYourProfile?: boolean | undefined;
  /**
   * Filter to leads who follow YOUR company page (LinkedIn flag CF). Warm-touch signal — they opted into your brand.
   */
  followsYourCompany?: boolean | undefined;
  /**
   * Filter to leads connected to a specific person. Values are LinkedIn ENCRYPTED member URN ids (e.g. 'ACwAAAim9ZcB…'), not numeric ids. Useful for 'find people connected to my champion at X' warm-intro patterns.
   */
  connectionOf?: Array<string> | undefined;
  /**
   * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
   */
  pastCompany?: SearchSalesNavPastCompany | undefined;
  /**
   * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
   */
  companyHeadquarters?: SearchSalesNavCompanyHeadquarters | undefined;
  /**
   * Years in current ROLE/POSITION (people only). Same accepted labels as `yearsOfExperience` ('Less than 1 year', '1 to 2 years', etc.). Distinct from `tenure` which counts years at current COMPANY.
   */
  yearsInCurrentPosition?: Array<string> | undefined;
  /**
   * Past job title keywords (people only). Free-text, supports Boolean OR. Finds leads who previously held a matching title.
   */
  pastTitle?: string | undefined;
  /**
   * First name (people only). Exact name filter.
   */
  firstName?: string | undefined;
  /**
   * Last name (people only). Exact name filter.
   */
  lastName?: string | undefined;
  /**
   * LinkedIn Groups membership (people only). Accepts group names ('Digital Marketing') OR numeric group ids. Server resolves names via typeahead.
   */
  groups?: Array<string> | undefined;
  /**
   * Filter to leads who are past colleagues of the searcher (worked at the same company, overlapping dates). True = include only past colleagues; omit/false = no constraint. Warm-intro signal.
   */
  pastColleague?: boolean | undefined;
  /**
   * Filter by your past interaction with the lead (people only). Accepts labels ('Messaged') OR LinkedIn codes.
   */
  leadInteractions?: Array<string> | undefined;
  /**
   * Filter to your saved leads / accounts (people only). Accepts 'All my saved leads' / 'All my saved accounts' OR codes SL/SA.
   */
  savedLeadsAndAccounts?: Array<string> | undefined;
  /**
   * Employee count (companies only). Accepts labels ('1-10', '11-50', '51-200', '201-500', '501-1,000', '1,001-5,000', '5,001-10,000', '10,001+', 'Self-employed') OR LinkedIn letter codes A-I.
   */
  companyHeadcount?: Array<string> | undefined;
  /**
   * Company type (companies only). Accepts labels ('Public Company', 'Privately Held', 'Non Profit', 'Educational Institution', 'Government Agency', 'Self-Employed', 'Self-Owned', 'Partnership') OR LinkedIn letter codes.
   */
  companyType?: Array<string> | undefined;
  /**
   * Company follower-count buckets (companies only). Accepts labels ('1-50', '51-100', '101-1,000', '1,001-5,000', '5,001+') OR ids 1-5.
   */
  numOfFollowers?: Array<string> | undefined;
  /**
   * Fortune-list membership (companies only). Accepts 'Fortune 50' / 'Fortune 100' / 'Fortune 250' / 'Fortune 500' OR codes FORTUNE_50/100/250/500.
   */
  fortune?: Array<string> | undefined;
  /**
   * Account buying-intent signals (companies only). Accepts labels ('Senior leadership changes in last 3 months', 'Funding events in past 12 months') OR codes SLC/RFE.
   */
  accountActivities?: Array<string> | undefined;
  /**
   * Account hiring signals (companies only). Accepts the LinkedIn hiring-signal labels OR codes.
   */
  jobOpportunities?: Array<string> | undefined;
  /**
   * Pagination offset (default 0).
   */
  start?: number | undefined;
  /**
   * Results per page (default 25, max 25).
   */
  count?: number | undefined;
  /**
   * Campaign ID. Auto-adds discovered contacts to this campaign in background. Mandatory whenever a campaign is active.
   */
  campaignSlug?: string | undefined;
};

export const SearchSalesNavTypeCompany = {
  Company: "COMPANY",
} as const;
export type SearchSalesNavTypeCompany = ClosedEnum<
  typeof SearchSalesNavTypeCompany
>;

/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export const SearchSalesNavLane = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export type SearchSalesNavLane = ClosedEnum<typeof SearchSalesNavLane>;

export type SearchSalesNavItemCompany = {
  type: SearchSalesNavTypeCompany;
  name: string;
  profileUrl: string | null;
  summary: string | null;
  industry: string | null;
  location: string | null;
  /**
   * Company logo URL when LinkedIn surfaces it on the result entity. Display this in the company card.
   */
  logoUrl?: string | null | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data only.
   */
  lane?: SearchSalesNavLane | undefined;
  /**
   * Sales Navigator company ID
   */
  id: string;
  /**
   * Employee count or range
   */
  headcount: string | null;
};

export type SearchSalesNavPaging2 = {
  start: number;
  count: number;
  total: number;
};

export type SearchSalesNavWarning2 = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchSalesNavCredits2 = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchSalesNavMeta2 = {
  credits: SearchSalesNavCredits2;
};

export type Companies = {
  success: true;
  category: "companies";
  items: Array<SearchSalesNavItemCompany>;
  paging: SearchSalesNavPaging2;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchSalesNavWarning2> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchSalesNavMeta2 | undefined;
};

export const SearchSalesNavTypePeople = {
  People: "PEOPLE",
} as const;
export type SearchSalesNavTypePeople = ClosedEnum<
  typeof SearchSalesNavTypePeople
>;

/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export const SearchSalesNavNetworkDistance = {
  Distance1: "DISTANCE_1",
  Distance2: "DISTANCE_2",
  Distance3: "DISTANCE_3",
  OutOfNetwork: "OUT_OF_NETWORK",
} as const;
/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export type SearchSalesNavNetworkDistance = OpenEnum<
  typeof SearchSalesNavNetworkDistance
>;

export type SearchSalesNavTenureAtCompany = {
  years?: number | undefined;
  months?: number | undefined;
};

export type SearchSalesNavTenureAtRole = {
  years?: number | undefined;
  months?: number | undefined;
};

export type SearchSalesNavCurrentPosition = {
  company: string | null;
  companyId: string | null;
  role: string | null;
  description: string | null;
  location: string | null;
  tenureAtCompany: SearchSalesNavTenureAtCompany | null;
  tenureAtRole: SearchSalesNavTenureAtRole | null;
};

export type SearchSalesNavItemPeople = {
  type: SearchSalesNavTypePeople;
  /**
   * Sales Navigator lead ID
   */
  id: string;
  name: string;
  firstName: string | null;
  lastName: string | null;
  /**
   * LinkedIn member URN (e.g. urn:li:member:12345)
   */
  memberUrn: string | null;
  /**
   * Canonical profile URN (urn:li:fsd_profile:ACo...) derived from any ACoA id found in the payload. Null only when LinkedIn returns a bare numeric memberUrn with no ACoA trace.
   */
  profileUrn: string | null;
  /**
   * LinkedIn vanity URL slug
   */
  publicIdentifier: string | null;
  /**
   * Profile URL. 3-tier fallback: (1) real /in/<slug>, (2) synthesized /in/<encrypted-id> when only a URN is known — LinkedIn accepts both ACoA… and ACwA… encrypted ids as the slug (case-sensitive), (3) /sales/lead/<id> as last resort when neither is available. Tier 3 URLs are not persistable in Bereach — call POST /resolve/linkedin/profiles to upgrade them.
   */
  profileUrl: string | null;
  /**
   * Sales Navigator lead URL
   */
  salesNavUrl: string | null;
  profilePicture: string | null;
  headline: string | null;
  location: string | null;
  /**
   * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
   */
  networkDistance: SearchSalesNavNetworkDistance | null;
  /**
   * True if the person has a LinkedIn Premium account.
   */
  premium: boolean | null;
  /**
   * True if the person has Open Profile — can be messaged without a connection.
   */
  openProfile: boolean | null;
  /**
   * True if you have an outstanding connection invitation to this person.
   */
  pendingInvitation: boolean | null;
  /**
   * Current work positions for this profile.
   */
  currentPositions: Array<SearchSalesNavCurrentPosition>;
};

export type SearchSalesNavPaging1 = {
  start: number;
  count: number;
  total: number;
};

export type SearchSalesNavWarning1 = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchSalesNavCredits1 = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchSalesNavMeta1 = {
  credits: SearchSalesNavCredits1;
};

export type People = {
  success: true;
  category: "people";
  items: Array<SearchSalesNavItemPeople>;
  paging: SearchSalesNavPaging1;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchSalesNavWarning1> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchSalesNavMeta1 | undefined;
};

/**
 * Search results from Sales Navigator
 */
export type SearchSalesNavResponse =
  | People
  | Companies
  | discriminatedUnionTypes.Unknown<"category">;

/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export const SearchSalesNavPeopleCategoryRequest = {
  People: "people",
  Companies: "companies",
} as const;
/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export type SearchSalesNavPeopleCategoryRequest = ClosedEnum<
  typeof SearchSalesNavPeopleCategoryRequest
>;

/**
 * Current-employer filter (people only). Accepts company names ('Stripe', 'Datadog') OR LinkedIn numeric ids. Server resolves names via typeahead.
 */
export type SearchSalesNavPeopleCompany = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
 */
export type SearchSalesNavPeopleIndustry = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

export type SearchSalesNavPeopleTenure = {
  /**
   * Minimum years
   */
  min?: number | undefined;
  /**
   * Maximum years
   */
  max?: number | undefined;
};

/**
 * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
 */
export type SearchSalesNavPeoplePastCompany = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
 */
export type SearchSalesNavPeopleCompanyHeadquarters = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

export type SearchSalesNavPeopleRequest = {
  /**
   * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
   */
  category?: SearchSalesNavPeopleCategoryRequest | undefined;
  /**
   * Sales Navigator search URL — paste directly from your browser. Category and filters are extracted automatically. Explicit params override URL-derived values. Example: https://www.linkedin.com/sales/search/people?query=...
   */
  url?: string | undefined;
  /**
   * Search keywords. Supports LinkedIn Boolean syntax: use double quotes for exact phrases, AND/OR/NOT operators (UPPERCASE), and parentheses for grouping.
   */
  keywords?: string | undefined;
  /**
   * Job title keywords (people only). Supports Boolean OR: 'CEO OR CTO OR Founder'.
   */
  title?: string | undefined;
  /**
   * Current-employer filter (people only). Accepts company names ('Stripe', 'Datadog') OR LinkedIn numeric ids. Server resolves names via typeahead.
   */
  company?: SearchSalesNavPeopleCompany | undefined;
  /**
   * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
   */
  industry?: SearchSalesNavPeopleIndustry | undefined;
  /**
   * Geography filter (people & companies). Accepts location names ('France', 'San Francisco Bay Area', 'EMEA') OR LinkedIn geo ids. Server resolves via typeahead.
   */
  location?: Array<string> | undefined;
  /**
   * Geography filter to EXCLUDE. Same accepted shapes as `location`. Useful for 'all of Europe except Switzerland' patterns.
   */
  locationExclude?: Array<string> | undefined;
  /**
   * Seniority levels (people only). Accepts labels ('CXO', 'VP', 'Director', 'Owner / Partner', 'Senior', 'Entry Level', etc.) OR LinkedIn numeric ids. Server resolves labels to LinkedIn's 3-digit ids (CXO=310, etc.) — do NOT guess the ids.
   */
  seniority?: Array<string> | undefined;
  /**
   * Job function (people only). Accepts function names ('Sales', 'Engineering', 'Marketing', 'Finance', etc. — 26 canonical) OR LinkedIn numeric ids.
   */
  function?: Array<string> | undefined;
  /**
   * Years at current company (people only). Array of ranges, e.g. [{min:3}, {max:10}].
   */
  tenure?: Array<SearchSalesNavPeopleTenure> | undefined;
  /**
   * Profile language (people only). Accepts ISO 639-1 codes (e.g. 'en') or full language names; the server resolves either.
   */
  profileLanguage?: Array<string> | undefined;
  /**
   * Network distance: 'F' (1st-degree), 'S' (2nd-degree), 'O' (3rd-degree and beyond), or 'GROUP'. The labels '1st degree', '2nd degree', '3rd degree' are also accepted.
   */
  connectionDegree?: Array<string> | undefined;
  /**
   * School filter (people only). Accepts school names ('Stanford', 'HEC Paris') OR LinkedIn numeric ids. Server resolves via typeahead.
   */
  school?: Array<string> | undefined;
  /**
   * Overall years of experience (people only). Accepts labels ('Less than 1 year', '1 to 2 years', '3 to 5 years', '6 to 10 years', 'More than 10 years') OR LinkedIn numeric ids 1-5.
   */
  yearsOfExperience?: Array<string> | undefined;
  /**
   * Buyer Persona IDs the user has configured on sales.linkedin.com home (combos of function+seniority+title+geo). Match the IDs from the URL filter ((type:PERSONA,values:List((id:<id>,…)))).
   */
  persona?: Array<string> | undefined;
  /**
   * Filter to leads who changed jobs in the last 90 days (LinkedIn buying-intent flag). True = include only job-changers; omit/false = no constraint.
   */
  recentlyChangedJobs?: boolean | undefined;
  /**
   * Filter to leads who recently posted on LinkedIn (engagement signal — active posters reply more often). True = include only recent posters; omit/false = no constraint.
   */
  postedOnLinkedin?: boolean | undefined;
  /**
   * Filter to leads with shared experience with the searcher (same school / past employer / group). True = include only shared-experience matches; omit/false = no constraint.
   */
  sharedExperiences?: boolean | undefined;
  /**
   * Filter to leads who recently viewed YOUR profile (LinkedIn flag VYP). Highest-intent engagement signal — they already showed interest. Requires Sales Nav seat that captures profile views.
   */
  viewedYourProfile?: boolean | undefined;
  /**
   * Filter to leads who follow YOUR company page (LinkedIn flag CF). Warm-touch signal — they opted into your brand.
   */
  followsYourCompany?: boolean | undefined;
  /**
   * Filter to leads connected to a specific person. Values are LinkedIn ENCRYPTED member URN ids (e.g. 'ACwAAAim9ZcB…'), not numeric ids. Useful for 'find people connected to my champion at X' warm-intro patterns.
   */
  connectionOf?: Array<string> | undefined;
  /**
   * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
   */
  pastCompany?: SearchSalesNavPeoplePastCompany | undefined;
  /**
   * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
   */
  companyHeadquarters?: SearchSalesNavPeopleCompanyHeadquarters | undefined;
  /**
   * Years in current ROLE/POSITION (people only). Same accepted labels as `yearsOfExperience` ('Less than 1 year', '1 to 2 years', etc.). Distinct from `tenure` which counts years at current COMPANY.
   */
  yearsInCurrentPosition?: Array<string> | undefined;
  /**
   * Past job title keywords (people only). Free-text, supports Boolean OR. Finds leads who previously held a matching title.
   */
  pastTitle?: string | undefined;
  /**
   * First name (people only). Exact name filter.
   */
  firstName?: string | undefined;
  /**
   * Last name (people only). Exact name filter.
   */
  lastName?: string | undefined;
  /**
   * LinkedIn Groups membership (people only). Accepts group names ('Digital Marketing') OR numeric group ids. Server resolves names via typeahead.
   */
  groups?: Array<string> | undefined;
  /**
   * Filter to leads who are past colleagues of the searcher (worked at the same company, overlapping dates). True = include only past colleagues; omit/false = no constraint. Warm-intro signal.
   */
  pastColleague?: boolean | undefined;
  /**
   * Filter by your past interaction with the lead (people only). Accepts labels ('Messaged') OR LinkedIn codes.
   */
  leadInteractions?: Array<string> | undefined;
  /**
   * Filter to your saved leads / accounts (people only). Accepts 'All my saved leads' / 'All my saved accounts' OR codes SL/SA.
   */
  savedLeadsAndAccounts?: Array<string> | undefined;
  /**
   * Company follower-count buckets (companies only). Accepts labels ('1-50', '51-100', '101-1,000', '1,001-5,000', '5,001+') OR ids 1-5.
   */
  numOfFollowers?: Array<string> | undefined;
  /**
   * Fortune-list membership (companies only). Accepts 'Fortune 50' / 'Fortune 100' / 'Fortune 250' / 'Fortune 500' OR codes FORTUNE_50/100/250/500.
   */
  fortune?: Array<string> | undefined;
  /**
   * Account buying-intent signals (companies only). Accepts labels ('Senior leadership changes in last 3 months', 'Funding events in past 12 months') OR codes SLC/RFE.
   */
  accountActivities?: Array<string> | undefined;
  /**
   * Account hiring signals (companies only). Accepts the LinkedIn hiring-signal labels OR codes.
   */
  jobOpportunities?: Array<string> | undefined;
  /**
   * Pagination offset (default 0).
   */
  start?: number | undefined;
  /**
   * Results per page (default 25, max 25).
   */
  count?: number | undefined;
  /**
   * Campaign ID. Auto-adds discovered contacts to this campaign in background. Mandatory whenever a campaign is active.
   */
  campaignSlug?: string | undefined;
};

export const SearchSalesNavPeopleCategoryPeople = {
  People: "people",
} as const;
export type SearchSalesNavPeopleCategoryPeople = ClosedEnum<
  typeof SearchSalesNavPeopleCategoryPeople
>;

export const SearchSalesNavPeopleType = {
  People: "PEOPLE",
} as const;
export type SearchSalesNavPeopleType = ClosedEnum<
  typeof SearchSalesNavPeopleType
>;

/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export const SearchSalesNavPeopleNetworkDistance = {
  Distance1: "DISTANCE_1",
  Distance2: "DISTANCE_2",
  Distance3: "DISTANCE_3",
  OutOfNetwork: "OUT_OF_NETWORK",
} as const;
/**
 * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
 */
export type SearchSalesNavPeopleNetworkDistance = OpenEnum<
  typeof SearchSalesNavPeopleNetworkDistance
>;

export type SearchSalesNavPeopleTenureAtCompany = {
  years?: number | undefined;
  months?: number | undefined;
};

export type SearchSalesNavPeopleTenureAtRole = {
  years?: number | undefined;
  months?: number | undefined;
};

export type SearchSalesNavPeopleCurrentPosition = {
  company: string | null;
  companyId: string | null;
  role: string | null;
  description: string | null;
  location: string | null;
  tenureAtCompany: SearchSalesNavPeopleTenureAtCompany | null;
  tenureAtRole: SearchSalesNavPeopleTenureAtRole | null;
};

export type SearchSalesNavPeopleItem = {
  type: SearchSalesNavPeopleType;
  /**
   * Sales Navigator lead ID
   */
  id: string;
  name: string;
  firstName: string | null;
  lastName: string | null;
  /**
   * LinkedIn member URN (e.g. urn:li:member:12345)
   */
  memberUrn: string | null;
  /**
   * Canonical profile URN (urn:li:fsd_profile:ACo...) derived from any ACoA id found in the payload. Null only when LinkedIn returns a bare numeric memberUrn with no ACoA trace.
   */
  profileUrn: string | null;
  /**
   * LinkedIn vanity URL slug
   */
  publicIdentifier: string | null;
  /**
   * Profile URL. 3-tier fallback: (1) real /in/<slug>, (2) synthesized /in/<encrypted-id> when only a URN is known — LinkedIn accepts both ACoA… and ACwA… encrypted ids as the slug (case-sensitive), (3) /sales/lead/<id> as last resort when neither is available. Tier 3 URLs are not persistable in Bereach — call POST /resolve/linkedin/profiles to upgrade them.
   */
  profileUrl: string | null;
  /**
   * Sales Navigator lead URL
   */
  salesNavUrl: string | null;
  profilePicture: string | null;
  headline: string | null;
  location: string | null;
  /**
   * LinkedIn network distance: DISTANCE_1=connected, DISTANCE_2=2nd degree, DISTANCE_3=3rd degree, OUT_OF_NETWORK=not connected.
   */
  networkDistance: SearchSalesNavPeopleNetworkDistance | null;
  /**
   * True if the person has a LinkedIn Premium account.
   */
  premium: boolean | null;
  /**
   * True if the person has Open Profile — can be messaged without a connection.
   */
  openProfile: boolean | null;
  /**
   * True if you have an outstanding connection invitation to this person.
   */
  pendingInvitation: boolean | null;
  /**
   * Current work positions for this profile.
   */
  currentPositions: Array<SearchSalesNavPeopleCurrentPosition>;
};

export type SearchSalesNavPeoplePaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchSalesNavPeopleWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchSalesNavPeopleCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchSalesNavPeopleMeta = {
  credits: SearchSalesNavPeopleCredits;
};

/**
 * Sales Navigator people search results
 */
export type SearchSalesNavPeopleResponse = {
  success: true;
  category: SearchSalesNavPeopleCategoryPeople;
  items: Array<SearchSalesNavPeopleItem>;
  paging: SearchSalesNavPeoplePaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchSalesNavPeopleWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchSalesNavPeopleMeta | undefined;
};

/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export const SearchSalesNavCompaniesCategoryRequest = {
  People: "people",
  Companies: "companies",
} as const;
/**
 * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
 */
export type SearchSalesNavCompaniesCategoryRequest = ClosedEnum<
  typeof SearchSalesNavCompaniesCategoryRequest
>;

/**
 * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
 */
export type SearchSalesNavCompaniesIndustry = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
 */
export type SearchSalesNavCompaniesPastCompany = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

/**
 * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
 */
export type SearchSalesNavCompaniesCompanyHeadquarters = {
  /**
   * Labels OR numeric IDs to include. Labels are resolved server-side (no need to call /parameters first).
   */
  include?: Array<string> | undefined;
  /**
   * Labels OR numeric IDs to exclude. Labels are resolved server-side (no need to call /parameters first).
   */
  exclude?: Array<string> | undefined;
};

export type SearchSalesNavCompaniesRequest = {
  /**
   * Type of Sales Navigator search: 'people' (leads) or 'companies' (accounts). Required unless url is provided.
   */
  category?: SearchSalesNavCompaniesCategoryRequest | undefined;
  /**
   * Sales Navigator search URL — paste directly from your browser. Category and filters are extracted automatically. Explicit params override URL-derived values. Example: https://www.linkedin.com/sales/search/people?query=...
   */
  url?: string | undefined;
  /**
   * Search keywords. Supports LinkedIn Boolean syntax: use double quotes for exact phrases, AND/OR/NOT operators (UPPERCASE), and parentheses for grouping.
   */
  keywords?: string | undefined;
  /**
   * Industry filter. Accepts industry names ('Software Development', 'Hospitals and Health Care') OR LinkedIn numeric ids. Server resolves.
   */
  industry?: SearchSalesNavCompaniesIndustry | undefined;
  /**
   * Geography filter (people & companies). Accepts location names ('France', 'San Francisco Bay Area', 'EMEA') OR LinkedIn geo ids. Server resolves via typeahead.
   */
  location?: Array<string> | undefined;
  /**
   * Geography filter to EXCLUDE. Same accepted shapes as `location`. Useful for 'all of Europe except Switzerland' patterns.
   */
  locationExclude?: Array<string> | undefined;
  /**
   * Buyer Persona IDs the user has configured on sales.linkedin.com home (combos of function+seniority+title+geo). Match the IDs from the URL filter ((type:PERSONA,values:List((id:<id>,…)))).
   */
  persona?: Array<string> | undefined;
  /**
   * Filter to leads who changed jobs in the last 90 days (LinkedIn buying-intent flag). True = include only job-changers; omit/false = no constraint.
   */
  recentlyChangedJobs?: boolean | undefined;
  /**
   * Filter to leads who recently posted on LinkedIn (engagement signal — active posters reply more often). True = include only recent posters; omit/false = no constraint.
   */
  postedOnLinkedin?: boolean | undefined;
  /**
   * Filter to leads with shared experience with the searcher (same school / past employer / group). True = include only shared-experience matches; omit/false = no constraint.
   */
  sharedExperiences?: boolean | undefined;
  /**
   * Filter to leads who recently viewed YOUR profile (LinkedIn flag VYP). Highest-intent engagement signal — they already showed interest. Requires Sales Nav seat that captures profile views.
   */
  viewedYourProfile?: boolean | undefined;
  /**
   * Filter to leads who follow YOUR company page (LinkedIn flag CF). Warm-touch signal — they opted into your brand.
   */
  followsYourCompany?: boolean | undefined;
  /**
   * Filter to leads connected to a specific person. Values are LinkedIn ENCRYPTED member URN ids (e.g. 'ACwAAAim9ZcB…'), not numeric ids. Useful for 'find people connected to my champion at X' warm-intro patterns.
   */
  connectionOf?: Array<string> | undefined;
  /**
   * Past-employer filter (people only). Accepts company names ('Stripe') OR LinkedIn numeric ids. The server wraps numeric ids as 'urn:li:organization:<id>' (the wire shape Sales Nav requires) — callers can pass the bare id or the URN.
   */
  pastCompany?: SearchSalesNavCompaniesPastCompany | undefined;
  /**
   * Where the lead's CURRENT employer is HQ'd (people only). Accepts location names ('France', 'United States') OR LinkedIn geo ids. Distinct from `location` which is the lead's own location — useful for 'sell to UK-headquartered companies regardless of where the buyer lives'.
   */
  companyHeadquarters?: SearchSalesNavCompaniesCompanyHeadquarters | undefined;
  /**
   * Years in current ROLE/POSITION (people only). Same accepted labels as `yearsOfExperience` ('Less than 1 year', '1 to 2 years', etc.). Distinct from `tenure` which counts years at current COMPANY.
   */
  yearsInCurrentPosition?: Array<string> | undefined;
  /**
   * Past job title keywords (people only). Free-text, supports Boolean OR. Finds leads who previously held a matching title.
   */
  pastTitle?: string | undefined;
  /**
   * First name (people only). Exact name filter.
   */
  firstName?: string | undefined;
  /**
   * Last name (people only). Exact name filter.
   */
  lastName?: string | undefined;
  /**
   * LinkedIn Groups membership (people only). Accepts group names ('Digital Marketing') OR numeric group ids. Server resolves names via typeahead.
   */
  groups?: Array<string> | undefined;
  /**
   * Filter to leads who are past colleagues of the searcher (worked at the same company, overlapping dates). True = include only past colleagues; omit/false = no constraint. Warm-intro signal.
   */
  pastColleague?: boolean | undefined;
  /**
   * Filter by your past interaction with the lead (people only). Accepts labels ('Messaged') OR LinkedIn codes.
   */
  leadInteractions?: Array<string> | undefined;
  /**
   * Filter to your saved leads / accounts (people only). Accepts 'All my saved leads' / 'All my saved accounts' OR codes SL/SA.
   */
  savedLeadsAndAccounts?: Array<string> | undefined;
  /**
   * Employee count (companies only). Accepts labels ('1-10', '11-50', '51-200', '201-500', '501-1,000', '1,001-5,000', '5,001-10,000', '10,001+', 'Self-employed') OR LinkedIn letter codes A-I.
   */
  companyHeadcount?: Array<string> | undefined;
  /**
   * Company type (companies only). Accepts labels ('Public Company', 'Privately Held', 'Non Profit', 'Educational Institution', 'Government Agency', 'Self-Employed', 'Self-Owned', 'Partnership') OR LinkedIn letter codes.
   */
  companyType?: Array<string> | undefined;
  /**
   * Company follower-count buckets (companies only). Accepts labels ('1-50', '51-100', '101-1,000', '1,001-5,000', '5,001+') OR ids 1-5.
   */
  numOfFollowers?: Array<string> | undefined;
  /**
   * Fortune-list membership (companies only). Accepts 'Fortune 50' / 'Fortune 100' / 'Fortune 250' / 'Fortune 500' OR codes FORTUNE_50/100/250/500.
   */
  fortune?: Array<string> | undefined;
  /**
   * Account buying-intent signals (companies only). Accepts labels ('Senior leadership changes in last 3 months', 'Funding events in past 12 months') OR codes SLC/RFE.
   */
  accountActivities?: Array<string> | undefined;
  /**
   * Account hiring signals (companies only). Accepts the LinkedIn hiring-signal labels OR codes.
   */
  jobOpportunities?: Array<string> | undefined;
  /**
   * Pagination offset (default 0).
   */
  start?: number | undefined;
  /**
   * Results per page (default 25, max 25).
   */
  count?: number | undefined;
  /**
   * Campaign ID. Auto-adds discovered contacts to this campaign in background. Mandatory whenever a campaign is active.
   */
  campaignSlug?: string | undefined;
};

export const SearchSalesNavCompaniesCategoryCompanies = {
  Companies: "companies",
} as const;
export type SearchSalesNavCompaniesCategoryCompanies = ClosedEnum<
  typeof SearchSalesNavCompaniesCategoryCompanies
>;

export const SearchSalesNavCompaniesType = {
  Company: "COMPANY",
} as const;
export type SearchSalesNavCompaniesType = ClosedEnum<
  typeof SearchSalesNavCompaniesType
>;

/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export const SearchSalesNavCompaniesLane = {
  Public: "public",
} as const;
/**
 * Present when this row came from the public lane covering for the connected account. Public data only.
 */
export type SearchSalesNavCompaniesLane = ClosedEnum<
  typeof SearchSalesNavCompaniesLane
>;

export type SearchSalesNavCompaniesItem = {
  type: SearchSalesNavCompaniesType;
  name: string;
  profileUrl: string | null;
  summary: string | null;
  industry: string | null;
  location: string | null;
  /**
   * Company logo URL when LinkedIn surfaces it on the result entity. Display this in the company card.
   */
  logoUrl?: string | null | undefined;
  /**
   * Present when this row came from the public lane covering for the connected account. Public data only.
   */
  lane?: SearchSalesNavCompaniesLane | undefined;
  /**
   * Sales Navigator company ID
   */
  id: string;
  /**
   * Employee count or range
   */
  headcount: string | null;
};

export type SearchSalesNavCompaniesPaging = {
  start: number;
  count: number;
  total: number;
};

export type SearchSalesNavCompaniesWarning = {
  /**
   * Which part of the request this is about, or `search` when it is about the whole result set.
   */
  field: string;
  /**
   * The value involved: what was dropped, or a code naming the condition.
   */
  value: string;
  /**
   * What kind of caveat this is. SEARCH_QUOTA: LinkedIn capped this account for the month, so the set is TRUNCATED and not the whole market. PARSE_EMPTY: the page answered and no rows could be read, and LinkedIn did not say the market is empty, so treat it as unread rather than as nobody matching. URL_FACET: a pasted search URL carried a filter this search cannot apply, so it ran wider than the one on screen. ENUM: a value was not recognised and that filter was dropped. LANE_FALLBACK: some or all rows came from the public lane, which fills fewer fields. AND_BLOB / OR_OVERFLOW: the keyword shape over-constrained and was retried or flagged. SEARCH_PLAN: the ask was spread across a search budget rather than paged.
   */
  type: string;
  /**
   * One sentence a caller can show a person.
   */
  hint?: string | undefined;
};

export type SearchSalesNavCompaniesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SearchSalesNavCompaniesMeta = {
  credits: SearchSalesNavCompaniesCredits;
};

/**
 * Sales Navigator company search results
 */
export type SearchSalesNavCompaniesResponse = {
  success: true;
  category: SearchSalesNavCompaniesCategoryCompanies;
  items: Array<SearchSalesNavCompaniesItem>;
  paging: SearchSalesNavCompaniesPaging;
  hasMore: boolean;
  /**
   * Caveats about this result set. Absent or empty means the search ran exactly as asked.
   */
  warnings?: Array<SearchSalesNavCompaniesWarning> | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SearchSalesNavCompaniesMeta | undefined;
};

/**
 * One LinkedIn profile, or many. Accepts full URLs (e.g. https://www.linkedin.com/in/username), vanity names (e.g. john-doe), or profile URNs (e.g. urn:li:fsd_profile:ACoAAA...).
 */
export type ConnectProfileProfile = string | Array<string>;

export type KnownPerson = {
  profile: string;
  name?: string | undefined;
  headline?: string | undefined;
  company?: string | undefined;
  location?: string | undefined;
  avatarUrl?: string | undefined;
};

/**
 * Where these people join the queue. "front" (default) puts them ahead of everyone already waiting, since naming somebody means this one next. "end" puts them behind everyone named, taking the ordinary place among the rest (most recently active on LinkedIn first, then arrival order); re-calling with "end" for people already in line gives up their front spot. Within one call, the most recently active go first either way.
 */
export const PlacementRequest = {
  Front: "front",
  End: "end",
} as const;
/**
 * Where these people join the queue. "front" (default) puts them ahead of everyone already waiting, since naming somebody means this one next. "end" puts them behind everyone named, taking the ordinary place among the rest (most recently active on LinkedIn first, then arrival order); re-calling with "end" for people already in line gives up their front spot. Within one call, the most recently active go first either way.
 */
export type PlacementRequest = ClosedEnum<typeof PlacementRequest>;

export type ConnectProfileRequest = {
  /**
   * One LinkedIn profile, or many. Accepts full URLs (e.g. https://www.linkedin.com/in/username), vanity names (e.g. john-doe), or profile URNs (e.g. urn:li:fsd_profile:ACoAAA...).
   */
  profile: string | Array<string>;
  /**
   * Names already known for the profiles in this call, matched by URL; omit when you only have the URL.
   */
  knownPeople?: Array<KnownPerson> | undefined;
  /**
   * Optional note on the invitation, the same one for everyone in the call; left out it goes bare.
   */
  message?: string | undefined;
  /**
   * Attempt one real send inside this call and report what happened, instead of only reporting where they stand.
   */
  now?: boolean | undefined;
  /**
   * Which list to add them to; omit for this account's default one.
   */
  list?: string | undefined;
  /**
   * Previews the call (list size, per-profile eligibility) with a hard guarantee that nothing is added, invited or sent.
   */
  dryRun?: boolean | undefined;
  /**
   * Where these people join the queue. "front" (default) puts them ahead of everyone already waiting, since naming somebody means this one next. "end" puts them behind everyone named, taking the ordinary place among the rest (most recently active on LinkedIn first, then arrival order); re-calling with "end" for people already in line gives up their front spot. Within one call, the most recently active go first either way.
   */
  placement?: PlacementRequest | undefined;
};

export type Queued = {
  profile: string;
  /**
   * Place in line for this account, 1 being next. Null when they are in line but further back than this call looked: still queued, just not numbered.
   */
  position: number | null;
  /**
   * Rough wait in days, from how fast this account has actually been sending. Null when there is no history to estimate from, and null whenever the account is blocked, because the wait then depends on it recovering rather than on how fast it sends.
   */
  etaDays: number | null;
};

export type ConnectProfileSkipped = {
  profile: string;
  reason: string;
};

/**
 * Set when the account itself cannot send right now. Nothing goes out until this clears, and resumesAt is null when clearing it needs a person.
 */
export type ConnectProfileBlocked = {
  /**
   * Which kind of block, as a stable code: challenge, session_dead, no_credit, invite_cap, rate_limited. Read this rather than the message when deciding how to present it. The ordinary spacing between two invitations is never reported here.
   */
  kind: string;
  /**
   * True when the block expires on its own; false when a person has to do something (open LinkedIn, reconnect the account).
   */
  clearsItself: boolean;
  message: string;
  resumesAt: string | null;
  /**
   * Where to send the account owner to fix it, when a person has to act. Null for the kinds that clear on their own.
   */
  fixUrl: string | null;
};

/**
 * Where the people named joined the line, echoed from the request: front (the default) went ahead of everyone already waiting, end went behind the people asked for by name before.
 */
export const PlacementResponse = {
  Front: "front",
  End: "end",
} as const;
/**
 * Where the people named joined the line, echoed from the request: front (the default) went ahead of everyone already waiting, end went behind the people asked for by name before.
 */
export type PlacementResponse = OpenEnum<typeof PlacementResponse>;

/**
 * Only present on a dryRun response: which list this call would actually target, and its real current state.
 */
export type List = {
  name: string;
  /**
   * True if this exact list does not exist yet and a real call would create it.
   */
  isNew: boolean;
  /**
   * How many people are already in this list, before this call.
   */
  totalMembers: number;
};

export type ConnectProfileCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ConnectProfileMeta = {
  credits: ConnectProfileCredits;
};

/**
 * Connection request sent successfully
 */
export type ConnectProfileResponse = {
  success: true;
  message: string;
  /**
   * How many invitations actually reached LinkedIn during this call.
   */
  sent: number;
  /**
   * The people named in this call that are now in line, in the order they will go out. Not the whole line: that is waitingTotal on the status call.
   */
  queued: Array<Queued>;
  /**
   * Anyone not added, each with the reason in plain words.
   */
  skipped: Array<ConnectProfileSkipped>;
  /**
   * Set when the account itself cannot send right now. Nothing goes out until this clears, and resumesAt is null when clearing it needs a person.
   */
  blocked: ConnectProfileBlocked | null;
  /**
   * How many OTHER people were already waiting on this account before this call, the people named here not counted. With the default placement, everyone just queued went ahead of all of them: say so when that number is not small, and offer once to put them behind instead.
   */
  waitingBefore?: number | undefined;
  /**
   * Where the people named joined the line, echoed from the request: front (the default) went ahead of everyone already waiting, end went behind the people asked for by name before.
   */
  placement?: PlacementResponse | undefined;
  /**
   * True when nothing was sent and nobody joined the line because every profile named already has a reason of its own in skipped: already invited or connected, marked do not contact, given up on, or on a list that is archived. Read skipped for the reason.
   */
  duplicate?: boolean | undefined;
  /**
   * True when this was a preview: nothing was added, attempted, or sent. Everything else in this response describes what a real call would do right now.
   */
  dryRun?: boolean | undefined;
  /**
   * Only present on a dryRun response: which list this call would actually target, and its real current state.
   */
  list?: List | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ConnectProfileMeta | undefined;
};

export type ListInvitationsRequest = {
  /**
   * Pagination offset (0-based)
   */
  start?: number | undefined;
  /**
   * Number of invitations to return (max 100)
   */
  count?: number | undefined;
};

/**
 * Profile info of the person who sent the invitation
 */
export type FromMember = {
  name: string | null;
  headline: string | null;
  profileUrl: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  profileUrn: string | null;
};

export type ListInvitationsInvitation = {
  /**
   * Invitation ID needed for accepting
   */
  invitationId: string;
  /**
   * Shared secret needed for accepting
   */
  sharedSecret: string;
  /**
   * Full invitation URN
   */
  entityUrn: string;
  /**
   * ISO 8601 timestamp when invitation was sent
   */
  sentAt: string | null;
  /**
   * Custom message included with the invitation
   */
  message: string | null;
  /**
   * Sender's profile ID (pass to accept endpoint)
   */
  senderProfileId: string | null;
  /**
   * Sender's first name (pass to accept endpoint)
   */
  senderFirstName: string | null;
  /**
   * Sender's last name (pass to accept endpoint)
   */
  senderLastName: string | null;
  /**
   * Profile info of the person who sent the invitation
   */
  fromMember: FromMember | null;
  /**
   * Epoch ms, beside the ISO `sentAt`. Sortable without re-parsing.
   */
  sentTime?: number | null | undefined;
  /**
   * Connection invitation or another kind. A caller piping every row into accept should filter on this first.
   */
  invitationType?: string | null | undefined;
  /**
   * Links this invitation to its inbox thread.
   */
  mailboxItemId?: string | null | undefined;
  /**
   * Whether the sender attached a note, which `message` alone cannot distinguish from an empty one.
   */
  customMessage?: boolean | undefined;
  /**
   * New since this account last looked. The only field that lets a poller triage instead of re-processing everything.
   */
  unseen?: boolean | undefined;
  /**
   * Shared-employer signal, when LinkedIn attaches one.
   */
  mutualCurrentCompany?: any | null | undefined;
  /**
   * Degrees of separation from the sender.
   */
  connectionDistance?: number | null | undefined;
  /**
   * Whether the sender already follows this account.
   */
  inviterFollowingInvitee?: boolean | undefined;
  /**
   * Present when the invitation came out of an event.
   */
  fromEvent?: any | undefined;
  /**
   * Present when LinkedIn names who the invitation is on behalf of.
   */
  inviterActors?: Array<any> | undefined;
};

export type ListInvitationsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListInvitationsMeta = {
  credits: ListInvitationsCredits;
};

/**
 * List of pending invitations
 */
export type ListInvitationsResponse = {
  success: true;
  invitations: Array<ListInvitationsInvitation>;
  /**
   * Total number of pending invitations
   */
  total: number;
  start: number;
  count: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListInvitationsMeta | undefined;
};

export type AcceptInvitationRequest = {
  /**
   * Invitation ID (from the list invitations endpoint)
   */
  invitationId: string;
  /**
   * Shared secret / validation token (from the list invitations endpoint)
   */
  sharedSecret: string;
  /**
   * Sender's profile ID (from the list invitations endpoint, e.g. 'ACoAAB6pI5sB...'). Recommended for reliability.
   */
  senderProfileId?: string | undefined;
  /**
   * Sender's first name (from the list invitations endpoint). Recommended for reliability.
   */
  firstName?: string | undefined;
  /**
   * Sender's last name (from the list invitations endpoint). Recommended for reliability.
   */
  lastName?: string | undefined;
};

export type AcceptInvitationCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type AcceptInvitationMeta = {
  credits: AcceptInvitationCredits;
};

/**
 * Invitation accepted successfully
 */
export type AcceptInvitationResponse = {
  success: true;
  message: string;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: AcceptInvitationMeta | undefined;
};

export type SendMessageRequest = {
  /**
   * Accepts full LinkedIn profile URLs (e.g. https://www.linkedin.com/in/username), vanity names (e.g. john-doe), or profile URNs (e.g. urn:li:member:123456).
   */
  profile?: string | undefined;
  /**
   * LinkedIn conversation URN. When provided, bypasses profile URL lookup and replies to an existing conversation.
   */
  conversationUrn?: string | undefined;
  /**
   * Message content to send
   */
  message: string;
  /**
   * Campaign identifier for deduplication. Dedup by profile automatically.
   */
  campaignSlug?: string | undefined;
  /**
   * Deprecated. Use campaignSlug only.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  actionSlug?: string | undefined;
};

export type SendMessageCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SendMessageMeta = {
  credits: SendMessageCredits;
};

/**
 * Message sent successfully
 */
export type SendMessageResponse = {
  success: true;
  messageId: string;
  /**
   * True if this action was already executed for the given campaignSlug + target.
   */
  duplicate?: boolean | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SendMessageMeta | undefined;
};

export type ReplyToCommentRequest = {
  /**
   * LinkedIn comment URN (e.g., 'urn:li:comment:(activity:7399026485694537728,7399230513867026433)')
   */
  commentUrn: string;
  /**
   * Reply message text
   */
  message: string;
  /**
   * Campaign identifier for deduplication. Dedup by commentUrn automatically.
   */
  campaignSlug?: string | undefined;
  /**
   * Deprecated. Use campaignSlug only.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  actionSlug?: string | undefined;
};

export type ReplyToCommentCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ReplyToCommentMeta = {
  credits: ReplyToCommentCredits;
};

/**
 * Reply sent successfully
 */
export type ReplyToCommentResponse = {
  success: true;
  /**
   * URN of the created reply comment
   */
  replyUrn?: string | undefined;
  /**
   * True if this action was already executed for the given campaignSlug + target.
   */
  duplicate?: boolean | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ReplyToCommentMeta | undefined;
};

/**
 * Reaction type (default: LIKE)
 */
export const LikeCommentReactionType = {
  Like: "LIKE",
  Love: "LOVE",
  Celebrate: "CELEBRATE",
  Support: "SUPPORT",
  Funny: "FUNNY",
  Insightful: "INSIGHTFUL",
} as const;
/**
 * Reaction type (default: LIKE)
 */
export type LikeCommentReactionType = ClosedEnum<
  typeof LikeCommentReactionType
>;

export type LikeCommentRequest = {
  /**
   * LinkedIn comment URN (e.g., 'urn:li:comment:(activity:7399026485694537728,7399230513867026433)')
   */
  commentUrn: string;
  /**
   * Reaction type (default: LIKE)
   */
  reactionType?: LikeCommentReactionType | undefined;
  /**
   * Campaign identifier for deduplication. Dedup by commentUrn automatically.
   */
  campaignSlug?: string | undefined;
  /**
   * Deprecated. Use campaignSlug only.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  actionSlug?: string | undefined;
};

export type LikeCommentCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type LikeCommentMeta = {
  credits: LikeCommentCredits;
};

/**
 * Comment liked successfully
 */
export type LikeCommentResponse = {
  success: true;
  /**
   * Resource key of the created reaction
   */
  resourceKey?: string | undefined;
  /**
   * True if this action was already executed for the given campaignSlug + target.
   */
  duplicate?: boolean | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: LikeCommentMeta | undefined;
};

export type CommentOnPostRequest = {
  /**
   * LinkedIn post URL (e.g., 'https://www.linkedin.com/feed/update/urn:li:activity:7399026485694537728/')
   */
  postUrl: string;
  /**
   * Comment text
   */
  message: string;
  /**
   * If provided, comment will be posted as this company page (organization ID). User must be admin of the page.
   */
  companyId?: string | undefined;
  /**
   * Campaign identifier for deduplication. Dedup by postUrl automatically.
   */
  campaignSlug?: string | undefined;
  /**
   * Deprecated. Use campaignSlug only.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  actionSlug?: string | undefined;
};

export type CommentOnPostCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CommentOnPostMeta = {
  credits: CommentOnPostCredits;
};

/**
 * Comment posted successfully
 */
export type CommentOnPostResponse = {
  success: true;
  /**
   * URN of the created comment (e.g., 'urn:li:comment:(activity:PARENT_ID,COMMENT_ID)')
   */
  commentUrn?: string | undefined;
  /**
   * FSD-format comment URN for use with the edit comment endpoint.
   */
  fsdCommentUrn?: string | undefined;
  /**
   * True if this action was already executed for the given campaignSlug + target.
   */
  duplicate?: boolean | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CommentOnPostMeta | undefined;
};

/**
 * Reaction type (default: LIKE)
 */
export const LikePostReactionType = {
  Like: "LIKE",
  Love: "LOVE",
  Celebrate: "CELEBRATE",
  Support: "SUPPORT",
  Funny: "FUNNY",
  Insightful: "INSIGHTFUL",
} as const;
/**
 * Reaction type (default: LIKE)
 */
export type LikePostReactionType = ClosedEnum<typeof LikePostReactionType>;

export type LikePostRequest = {
  /**
   * LinkedIn post URL (e.g., 'https://www.linkedin.com/feed/update/urn:li:activity:7399026485694537728/')
   */
  postUrl: string;
  /**
   * Reaction type (default: LIKE)
   */
  reactionType?: LikePostReactionType | undefined;
  /**
   * Act as a company page. Pass the numeric company ID from listCompanyPages. Requires admin access.
   */
  companyId?: string | undefined;
  /**
   * Campaign identifier for deduplication. Dedup by postUrl automatically.
   */
  campaignSlug?: string | undefined;
  /**
   * Deprecated. Use campaignSlug only.
   *
   * @deprecated field: This will be removed in a future release, please migrate away from it as soon as possible.
   */
  actionSlug?: string | undefined;
};

export type LikePostCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type LikePostMeta = {
  credits: LikePostCredits;
};

/**
 * Post liked successfully
 */
export type LikePostResponse = {
  success: true;
  /**
   * Resource key of the created reaction
   */
  resourceKey?: string | undefined;
  /**
   * True if this action was already executed for the given campaignSlug + target.
   */
  duplicate?: boolean | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: LikePostMeta | undefined;
};

export type ListSentInvitationsRequest = {
  /**
   * Pagination offset
   */
  start?: number | undefined;
  /**
   * Number of invitations to return
   */
  count?: number | undefined;
};

export type ToMember = {
  name: string | null;
  headline: string | null;
  profileUrl: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  profileUrn: string | null;
  memberId: string | null;
};

export type ListSentInvitationsInvitation = {
  invitationId: string;
  invitationUrn: string;
  entityUrn: string;
  sentAt: string | null;
  sentTime: number | null;
  message: string | null;
  targetProfileId: string | null;
  targetFirstName: string | null;
  targetLastName: string | null;
  toMember: ToMember | null;
  invitationType: string | null;
  mailboxItemId: string | null;
  customMessage: boolean;
  unseen: boolean;
  mutualCurrentCompany?: any | null | undefined;
  connectionDistance?: any | null | undefined;
  inviterFollowingInvitee: boolean;
};

export type ListSentInvitationsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListSentInvitationsMeta = {
  credits: ListSentInvitationsCredits;
};

/**
 * Sent invitations
 */
export type ListSentInvitationsResponse = {
  success: true;
  invitations: Array<ListSentInvitationsInvitation>;
  total: number;
  start: number;
  count: number;
  /**
   * Present ONLY when the list could not be confirmed complete. While it is present, absence from `invitations` proves nothing.
   */
  warning?: string | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListSentInvitationsMeta | undefined;
};

/** @internal */
export const SearchByUrlNetworkDistance$inboundSchema: z.ZodMiniType<
  SearchByUrlNetworkDistance,
  unknown
> = openEnums.inboundSchema(SearchByUrlNetworkDistance);

/** @internal */
export const SearchByUrlCurrentPosition$inboundSchema: z.ZodMiniType<
  SearchByUrlCurrentPosition,
  unknown
> = z.object({
  company: types.nullable(types.string()),
  role: types.nullable(types.string()),
});

export function searchByUrlCurrentPositionFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlCurrentPosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlCurrentPosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlCurrentPosition' from JSON`,
  );
}

/** @internal */
export const SearchByUrlLane1$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlLane1
> = z.enum(SearchByUrlLane1);

/** @internal */
export const ItemsPeople$inboundSchema: z.ZodMiniType<ItemsPeople, unknown> = z
  .object({
    type: SearchByUrlTypePeople$inboundSchema,
    name: types.string(),
    profileUrl: types.nullable(types.string()),
    headline: types.nullable(types.string()),
    location: types.nullable(types.string()),
    profilePicture: types.nullable(types.string()),
    networkDistance: types.nullable(SearchByUrlNetworkDistance$inboundSchema),
    currentPositions: z.array(
      z.lazy(() => SearchByUrlCurrentPosition$inboundSchema),
    ),
    profileUrn: types.nullable(types.string()),
    publicIdentifier: types.nullable(types.string()),
    nameMatch: z.optional(z.nullable(types.boolean())),
    badgeText: z.optional(z.nullable(types.string())),
    ringStatus: z.optional(z.nullable(types.string())),
    summary: z.optional(z.nullable(types.string())),
    actorInsights: types.optional(z.array(types.string())),
    lane: types.optional(SearchByUrlLane1$inboundSchema),
  });

export function itemsPeopleFromJSON(
  jsonString: string,
): SafeParseResult<ItemsPeople, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ItemsPeople$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ItemsPeople' from JSON`,
  );
}

/** @internal */
export const SearchByUrlMediaType$inboundSchema: z.ZodMiniType<
  SearchByUrlMediaType,
  unknown
> = openEnums.inboundSchema(SearchByUrlMediaType);

/** @internal */
export const SearchByUrlMedia$inboundSchema: z.ZodMiniType<
  SearchByUrlMedia,
  unknown
> = z.object({
  type: SearchByUrlMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function searchByUrlMediaFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlMedia' from JSON`,
  );
}

/** @internal */
export const SearchByUrlReactionTypeCount$inboundSchema: z.ZodMiniType<
  SearchByUrlReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function searchByUrlReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const SearchByUrlTypePost$inboundSchema: z.ZodMiniEnum<
  typeof SearchByUrlTypePost
> = z.enum(SearchByUrlTypePost);

/** @internal */
export const SearchByUrlAuthor$inboundSchema: z.ZodMiniType<
  SearchByUrlAuthor,
  unknown
> = z.object({
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  isCompany: types.boolean(),
  publicIdentifier: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
});

export function searchByUrlAuthorFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlAuthor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlAuthor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlAuthor' from JSON`,
  );
}

/** @internal */
export const ItemsPost$inboundSchema: z.ZodMiniType<ItemsPost, unknown> = z
  .object({
    postUrl: types.string(),
    text: types.string(),
    date: types.number(),
    likesCount: types.number(),
    commentsCount: types.number(),
    sharesCount: types.number(),
    postUrn: types.string(),
    postId: types.string(),
    media: types.optional(z.lazy(() => SearchByUrlMedia$inboundSchema)),
    repostedFromName: types.optional(types.string()),
    reactionTypeCounts: types.optional(
      z.array(z.lazy(() => SearchByUrlReactionTypeCount$inboundSchema)),
    ),
    numImpressions: z.optional(z.nullable(types.number())),
    highlightedReactorName: z.optional(z.nullable(types.string())),
    viewerLiked: z.optional(z.nullable(types.boolean())),
    type: SearchByUrlTypePost$inboundSchema,
    author: z.lazy(() => SearchByUrlAuthor$inboundSchema),
    isRepost: types.boolean(),
  });

export function itemsPostFromJSON(
  jsonString: string,
): SafeParseResult<ItemsPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ItemsPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ItemsPost' from JSON`,
  );
}

/** @internal */
export const Items$inboundSchema: z.ZodMiniType<Items, unknown> = smartUnion([
  z.array(z.lazy(() => ItemsPost$inboundSchema)),
  z.array(z.lazy(() => ItemsPeople$inboundSchema)),
  z.array(ItemsCompany$inboundSchema),
  z.array(ItemsJob$inboundSchema),
]);

export function itemsFromJSON(
  jsonString: string,
): SafeParseResult<Items, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Items$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Items' from JSON`,
  );
}

/** @internal */
export const SearchByUrlPaging$inboundSchema: z.ZodMiniType<
  SearchByUrlPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchByUrlPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlPaging' from JSON`,
  );
}

/** @internal */
export const SearchByUrlWarning$inboundSchema: z.ZodMiniType<
  SearchByUrlWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchByUrlWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlWarning' from JSON`,
  );
}

/** @internal */
export const SearchByUrlCredits$inboundSchema: z.ZodMiniType<
  SearchByUrlCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchByUrlCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlCredits' from JSON`,
  );
}

/** @internal */
export const SearchByUrlMeta$inboundSchema: z.ZodMiniType<
  SearchByUrlMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchByUrlCredits$inboundSchema),
});

export function searchByUrlMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlMeta' from JSON`,
  );
}

/** @internal */
export const SearchByUrlResponse$inboundSchema: z.ZodMiniType<
  SearchByUrlResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchByUrlCategory$inboundSchema,
    items: smartUnion([
      z.array(z.lazy(() => ItemsPost$inboundSchema)),
      z.array(z.lazy(() => ItemsPeople$inboundSchema)),
      z.array(ItemsCompany$inboundSchema),
      z.array(ItemsJob$inboundSchema),
    ]),
    paging: z.lazy(() => SearchByUrlPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchByUrlWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchByUrlMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchByUrlResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchByUrlResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchByUrlResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchByUrlResponse' from JSON`,
  );
}

/** @internal */
export const ValueKind$inboundSchema: z.ZodMiniType<ValueKind, unknown> =
  openEnums.inboundSchema(ValueKind);

/** @internal */
export const Filter$inboundSchema: z.ZodMiniType<Filter, unknown> = z.object({
  name: types.string(),
  field: types.string(),
  group: types.string(),
  valueKind: ValueKind$inboundSchema,
  supportsExclude: types.boolean(),
  available: types.boolean(),
});

export function filterFromJSON(
  jsonString: string,
): SafeParseResult<Filter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Filter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Filter' from JSON`,
  );
}

/** @internal */
export const ListSalesNavFiltersCredits$inboundSchema: z.ZodMiniType<
  ListSalesNavFiltersCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listSalesNavFiltersCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavFiltersCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavFiltersCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavFiltersCredits' from JSON`,
  );
}

/** @internal */
export const ListSalesNavFiltersMeta$inboundSchema: z.ZodMiniType<
  ListSalesNavFiltersMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListSalesNavFiltersCredits$inboundSchema),
});

export function listSalesNavFiltersMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavFiltersMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavFiltersMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavFiltersMeta' from JSON`,
  );
}

/** @internal */
export const ListSalesNavFiltersResponse$inboundSchema: z.ZodMiniType<
  ListSalesNavFiltersResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    filters: z.array(z.lazy(() => Filter$inboundSchema)),
    count: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListSalesNavFiltersMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listSalesNavFiltersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavFiltersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavFiltersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavFiltersResponse' from JSON`,
  );
}

/** @internal */
export const Persona$inboundSchema: z.ZodMiniType<Persona, unknown> = z.object({
  id: types.string(),
  name: types.string(),
  description: types.nullable(types.string()),
  criteriaSummary: types.nullable(types.string()),
});

export function personaFromJSON(
  jsonString: string,
): SafeParseResult<Persona, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Persona$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Persona' from JSON`,
  );
}

/** @internal */
export const ListSalesNavPersonasCredits$inboundSchema: z.ZodMiniType<
  ListSalesNavPersonasCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listSalesNavPersonasCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavPersonasCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavPersonasCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavPersonasCredits' from JSON`,
  );
}

/** @internal */
export const ListSalesNavPersonasMeta$inboundSchema: z.ZodMiniType<
  ListSalesNavPersonasMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListSalesNavPersonasCredits$inboundSchema),
});

export function listSalesNavPersonasMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavPersonasMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavPersonasMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavPersonasMeta' from JSON`,
  );
}

/** @internal */
export const ListSalesNavPersonasResponse$inboundSchema: z.ZodMiniType<
  ListSalesNavPersonasResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    personas: z.array(z.lazy(() => Persona$inboundSchema)),
    count: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListSalesNavPersonasMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listSalesNavPersonasResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavPersonasResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavPersonasResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavPersonasResponse' from JSON`,
  );
}

/** @internal */
export const ListSalesNavSavedSearchesCategory$inboundSchema: z.ZodMiniType<
  ListSalesNavSavedSearchesCategory,
  unknown
> = openEnums.inboundSchema(ListSalesNavSavedSearchesCategory);

/** @internal */
export const SavedSearch$inboundSchema: z.ZodMiniType<SavedSearch, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    category: types.nullable(ListSalesNavSavedSearchesCategory$inboundSchema),
    savedSearchUrl: types.string(),
    newResultsCount: types.nullable(types.number()),
    lastUsedAt: types.nullable(types.string()),
  });

export function savedSearchFromJSON(
  jsonString: string,
): SafeParseResult<SavedSearch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SavedSearch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SavedSearch' from JSON`,
  );
}

/** @internal */
export const ListSalesNavSavedSearchesCredits$inboundSchema: z.ZodMiniType<
  ListSalesNavSavedSearchesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listSalesNavSavedSearchesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavSavedSearchesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavSavedSearchesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavSavedSearchesCredits' from JSON`,
  );
}

/** @internal */
export const ListSalesNavSavedSearchesMeta$inboundSchema: z.ZodMiniType<
  ListSalesNavSavedSearchesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListSalesNavSavedSearchesCredits$inboundSchema),
});

export function listSalesNavSavedSearchesMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavSavedSearchesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavSavedSearchesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavSavedSearchesMeta' from JSON`,
  );
}

/** @internal */
export const ListSalesNavSavedSearchesResponse$inboundSchema: z.ZodMiniType<
  ListSalesNavSavedSearchesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    savedSearches: z.array(z.lazy(() => SavedSearch$inboundSchema)),
    count: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => ListSalesNavSavedSearchesMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listSalesNavSavedSearchesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSalesNavSavedSearchesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSalesNavSavedSearchesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSalesNavSavedSearchesResponse' from JSON`,
  );
}

/** @internal */
export type SearchParametersRequest$Outbound = {
  type: string;
  keywords?: string | undefined;
  limit: number;
};

/** @internal */
export const SearchParametersRequest$outboundSchema: z.ZodMiniType<
  SearchParametersRequest$Outbound,
  SearchParametersRequest
> = z.object({
  type: z.string(),
  keywords: z.optional(z.string()),
  limit: z._default(z.int(), 10),
});

export function searchParametersRequestToJSON(
  searchParametersRequest: SearchParametersRequest,
): string {
  return JSON.stringify(
    SearchParametersRequest$outboundSchema.parse(searchParametersRequest),
  );
}

/** @internal */
export const SearchParametersItem$inboundSchema: z.ZodMiniType<
  SearchParametersItem,
  unknown
> = z.object({
  id: types.string(),
  title: types.string(),
  type: types.string(),
  displayValue: types.optional(types.string()),
  source: types.optional(types.string()),
  aliasKind: types.optional(types.string()),
});

export function searchParametersItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchParametersItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchParametersItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchParametersItem' from JSON`,
  );
}

/** @internal */
export const SearchParametersCredits$inboundSchema: z.ZodMiniType<
  SearchParametersCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchParametersCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchParametersCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchParametersCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchParametersCredits' from JSON`,
  );
}

/** @internal */
export const SearchParametersMeta$inboundSchema: z.ZodMiniType<
  SearchParametersMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchParametersCredits$inboundSchema),
});

export function searchParametersMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchParametersMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchParametersMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchParametersMeta' from JSON`,
  );
}

/** @internal */
export const SearchParametersResponse$inboundSchema: z.ZodMiniType<
  SearchParametersResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    items: z.array(z.lazy(() => SearchParametersItem$inboundSchema)),
    count: types.number(),
    hint: types.optional(types.string()),
    aliasResolved: types.boolean(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchParametersMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchParametersResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchParametersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchParametersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchParametersResponse' from JSON`,
  );
}

/** @internal */
export type ResolveProfilesRequest$Outbound = {
  inputs: Array<string>;
};

/** @internal */
export const ResolveProfilesRequest$outboundSchema: z.ZodMiniType<
  ResolveProfilesRequest$Outbound,
  ResolveProfilesRequest
> = z.object({
  inputs: z.array(z.string()),
});

export function resolveProfilesRequestToJSON(
  resolveProfilesRequest: ResolveProfilesRequest,
): string {
  return JSON.stringify(
    ResolveProfilesRequest$outboundSchema.parse(resolveProfilesRequest),
  );
}

/** @internal */
export const Result$inboundSchema: z.ZodMiniType<Result, unknown> = z.object({
  input: types.string(),
  profileUrl: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  status: types.string(),
  error: types.nullable(types.string()),
  cached: types.boolean(),
});

export function resultFromJSON(
  jsonString: string,
): SafeParseResult<Result, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Result$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Result' from JSON`,
  );
}

/** @internal */
export const ResolveProfilesCredits$inboundSchema: z.ZodMiniType<
  ResolveProfilesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function resolveProfilesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ResolveProfilesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResolveProfilesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResolveProfilesCredits' from JSON`,
  );
}

/** @internal */
export const ResolveProfilesMeta$inboundSchema: z.ZodMiniType<
  ResolveProfilesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ResolveProfilesCredits$inboundSchema),
});

export function resolveProfilesMetaFromJSON(
  jsonString: string,
): SafeParseResult<ResolveProfilesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResolveProfilesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResolveProfilesMeta' from JSON`,
  );
}

/** @internal */
export const ResolveProfilesResponse$inboundSchema: z.ZodMiniType<
  ResolveProfilesResponse,
  unknown
> = z.pipe(
  z.object({
    results: z.array(z.lazy(() => Result$inboundSchema)),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ResolveProfilesMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function resolveProfilesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ResolveProfilesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResolveProfilesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResolveProfilesResponse' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCategoryRequest$outboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavCategoryRequest
> = z.enum(SearchSalesNavCategoryRequest);

/** @internal */
export type SearchSalesNavCompany$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavCompany$outboundSchema: z.ZodMiniType<
  SearchSalesNavCompany$Outbound,
  SearchSalesNavCompany
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavCompanyToJSON(
  searchSalesNavCompany: SearchSalesNavCompany,
): string {
  return JSON.stringify(
    SearchSalesNavCompany$outboundSchema.parse(searchSalesNavCompany),
  );
}

/** @internal */
export type SearchSalesNavIndustry$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavIndustry$outboundSchema: z.ZodMiniType<
  SearchSalesNavIndustry$Outbound,
  SearchSalesNavIndustry
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavIndustryToJSON(
  searchSalesNavIndustry: SearchSalesNavIndustry,
): string {
  return JSON.stringify(
    SearchSalesNavIndustry$outboundSchema.parse(searchSalesNavIndustry),
  );
}

/** @internal */
export type SearchSalesNavTenure$Outbound = {
  min?: number | undefined;
  max?: number | undefined;
};

/** @internal */
export const SearchSalesNavTenure$outboundSchema: z.ZodMiniType<
  SearchSalesNavTenure$Outbound,
  SearchSalesNavTenure
> = z.object({
  min: z.optional(z.int()),
  max: z.optional(z.int()),
});

export function searchSalesNavTenureToJSON(
  searchSalesNavTenure: SearchSalesNavTenure,
): string {
  return JSON.stringify(
    SearchSalesNavTenure$outboundSchema.parse(searchSalesNavTenure),
  );
}

/** @internal */
export type SearchSalesNavPastCompany$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavPastCompany$outboundSchema: z.ZodMiniType<
  SearchSalesNavPastCompany$Outbound,
  SearchSalesNavPastCompany
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavPastCompanyToJSON(
  searchSalesNavPastCompany: SearchSalesNavPastCompany,
): string {
  return JSON.stringify(
    SearchSalesNavPastCompany$outboundSchema.parse(searchSalesNavPastCompany),
  );
}

/** @internal */
export type SearchSalesNavCompanyHeadquarters$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavCompanyHeadquarters$outboundSchema: z.ZodMiniType<
  SearchSalesNavCompanyHeadquarters$Outbound,
  SearchSalesNavCompanyHeadquarters
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavCompanyHeadquartersToJSON(
  searchSalesNavCompanyHeadquarters: SearchSalesNavCompanyHeadquarters,
): string {
  return JSON.stringify(
    SearchSalesNavCompanyHeadquarters$outboundSchema.parse(
      searchSalesNavCompanyHeadquarters,
    ),
  );
}

/** @internal */
export type SearchSalesNavRequest$Outbound = {
  category?: string | undefined;
  url?: string | undefined;
  keywords?: string | undefined;
  title?: string | undefined;
  company?: SearchSalesNavCompany$Outbound | undefined;
  industry?: SearchSalesNavIndustry$Outbound | undefined;
  location?: Array<string> | undefined;
  locationExclude?: Array<string> | undefined;
  seniority?: Array<string> | undefined;
  function?: Array<string> | undefined;
  tenure?: Array<SearchSalesNavTenure$Outbound> | undefined;
  profileLanguage?: Array<string> | undefined;
  connectionDegree?: Array<string> | undefined;
  school?: Array<string> | undefined;
  yearsOfExperience?: Array<string> | undefined;
  persona?: Array<string> | undefined;
  recentlyChangedJobs?: boolean | undefined;
  postedOnLinkedin?: boolean | undefined;
  sharedExperiences?: boolean | undefined;
  viewedYourProfile?: boolean | undefined;
  followsYourCompany?: boolean | undefined;
  connectionOf?: Array<string> | undefined;
  pastCompany?: SearchSalesNavPastCompany$Outbound | undefined;
  companyHeadquarters?: SearchSalesNavCompanyHeadquarters$Outbound | undefined;
  yearsInCurrentPosition?: Array<string> | undefined;
  pastTitle?: string | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
  groups?: Array<string> | undefined;
  pastColleague?: boolean | undefined;
  leadInteractions?: Array<string> | undefined;
  savedLeadsAndAccounts?: Array<string> | undefined;
  companyHeadcount?: Array<string> | undefined;
  companyType?: Array<string> | undefined;
  numOfFollowers?: Array<string> | undefined;
  fortune?: Array<string> | undefined;
  accountActivities?: Array<string> | undefined;
  jobOpportunities?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
  campaignSlug?: string | undefined;
};

/** @internal */
export const SearchSalesNavRequest$outboundSchema: z.ZodMiniType<
  SearchSalesNavRequest$Outbound,
  SearchSalesNavRequest
> = z.object({
  category: z.optional(SearchSalesNavCategoryRequest$outboundSchema),
  url: z.optional(z.string()),
  keywords: z.optional(z.string()),
  title: z.optional(z.string()),
  company: z.optional(z.lazy(() => SearchSalesNavCompany$outboundSchema)),
  industry: z.optional(z.lazy(() => SearchSalesNavIndustry$outboundSchema)),
  location: z.optional(z.array(z.string())),
  locationExclude: z.optional(z.array(z.string())),
  seniority: z.optional(z.array(z.string())),
  function: z.optional(z.array(z.string())),
  tenure: z.optional(
    z.array(z.lazy(() => SearchSalesNavTenure$outboundSchema)),
  ),
  profileLanguage: z.optional(z.array(z.string())),
  connectionDegree: z.optional(z.array(z.string())),
  school: z.optional(z.array(z.string())),
  yearsOfExperience: z.optional(z.array(z.string())),
  persona: z.optional(z.array(z.string())),
  recentlyChangedJobs: z.optional(z.boolean()),
  postedOnLinkedin: z.optional(z.boolean()),
  sharedExperiences: z.optional(z.boolean()),
  viewedYourProfile: z.optional(z.boolean()),
  followsYourCompany: z.optional(z.boolean()),
  connectionOf: z.optional(z.array(z.string())),
  pastCompany: z.optional(
    z.lazy(() => SearchSalesNavPastCompany$outboundSchema),
  ),
  companyHeadquarters: z.optional(
    z.lazy(() => SearchSalesNavCompanyHeadquarters$outboundSchema),
  ),
  yearsInCurrentPosition: z.optional(z.array(z.string())),
  pastTitle: z.optional(z.string()),
  firstName: z.optional(z.string()),
  lastName: z.optional(z.string()),
  groups: z.optional(z.array(z.string())),
  pastColleague: z.optional(z.boolean()),
  leadInteractions: z.optional(z.array(z.string())),
  savedLeadsAndAccounts: z.optional(z.array(z.string())),
  companyHeadcount: z.optional(z.array(z.string())),
  companyType: z.optional(z.array(z.string())),
  numOfFollowers: z.optional(z.array(z.string())),
  fortune: z.optional(z.array(z.string())),
  accountActivities: z.optional(z.array(z.string())),
  jobOpportunities: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
  campaignSlug: z.optional(z.string()),
});

export function searchSalesNavRequestToJSON(
  searchSalesNavRequest: SearchSalesNavRequest,
): string {
  return JSON.stringify(
    SearchSalesNavRequest$outboundSchema.parse(searchSalesNavRequest),
  );
}

/** @internal */
export const SearchSalesNavTypeCompany$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavTypeCompany
> = z.enum(SearchSalesNavTypeCompany);

/** @internal */
export const SearchSalesNavLane$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavLane
> = z.enum(SearchSalesNavLane);

/** @internal */
export const SearchSalesNavItemCompany$inboundSchema: z.ZodMiniType<
  SearchSalesNavItemCompany,
  unknown
> = z.object({
  type: SearchSalesNavTypeCompany$inboundSchema,
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  summary: types.nullable(types.string()),
  industry: types.nullable(types.string()),
  location: types.nullable(types.string()),
  logoUrl: z.optional(z.nullable(types.string())),
  lane: types.optional(SearchSalesNavLane$inboundSchema),
  id: types.string(),
  headcount: types.nullable(types.string()),
});

export function searchSalesNavItemCompanyFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavItemCompany, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavItemCompany$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavItemCompany' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPaging2$inboundSchema: z.ZodMiniType<
  SearchSalesNavPaging2,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchSalesNavPaging2FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPaging2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPaging2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPaging2' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavWarning2$inboundSchema: z.ZodMiniType<
  SearchSalesNavWarning2,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchSalesNavWarning2FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavWarning2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavWarning2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavWarning2' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCredits2$inboundSchema: z.ZodMiniType<
  SearchSalesNavCredits2,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchSalesNavCredits2FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCredits2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCredits2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCredits2' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavMeta2$inboundSchema: z.ZodMiniType<
  SearchSalesNavMeta2,
  unknown
> = z.object({
  credits: z.lazy(() => SearchSalesNavCredits2$inboundSchema),
});

export function searchSalesNavMeta2FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavMeta2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavMeta2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavMeta2' from JSON`,
  );
}

/** @internal */
export const Companies$inboundSchema: z.ZodMiniType<Companies, unknown> = z
  .pipe(
    z.object({
      success: types.literal(true),
      category: types.literal("companies"),
      items: z.array(z.lazy(() => SearchSalesNavItemCompany$inboundSchema)),
      paging: z.lazy(() => SearchSalesNavPaging2$inboundSchema),
      hasMore: types.boolean(),
      warnings: types.optional(
        z.array(z.lazy(() => SearchSalesNavWarning2$inboundSchema)),
      ),
      creditsUsed: types.number(),
      retryAfter: types.number(),
      _meta: types.optional(z.lazy(() => SearchSalesNavMeta2$inboundSchema)),
    }),
    z.transform((v) => {
      return remap$(v, {
        "_meta": "meta",
      });
    }),
  );

export function companiesFromJSON(
  jsonString: string,
): SafeParseResult<Companies, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Companies$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Companies' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavTypePeople$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavTypePeople
> = z.enum(SearchSalesNavTypePeople);

/** @internal */
export const SearchSalesNavNetworkDistance$inboundSchema: z.ZodMiniType<
  SearchSalesNavNetworkDistance,
  unknown
> = openEnums.inboundSchema(SearchSalesNavNetworkDistance);

/** @internal */
export const SearchSalesNavTenureAtCompany$inboundSchema: z.ZodMiniType<
  SearchSalesNavTenureAtCompany,
  unknown
> = z.object({
  years: types.optional(types.number()),
  months: types.optional(types.number()),
});

export function searchSalesNavTenureAtCompanyFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavTenureAtCompany, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavTenureAtCompany$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavTenureAtCompany' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavTenureAtRole$inboundSchema: z.ZodMiniType<
  SearchSalesNavTenureAtRole,
  unknown
> = z.object({
  years: types.optional(types.number()),
  months: types.optional(types.number()),
});

export function searchSalesNavTenureAtRoleFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavTenureAtRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavTenureAtRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavTenureAtRole' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCurrentPosition$inboundSchema: z.ZodMiniType<
  SearchSalesNavCurrentPosition,
  unknown
> = z.object({
  company: types.nullable(types.string()),
  companyId: types.nullable(types.string()),
  role: types.nullable(types.string()),
  description: types.nullable(types.string()),
  location: types.nullable(types.string()),
  tenureAtCompany: types.nullable(
    z.lazy(() => SearchSalesNavTenureAtCompany$inboundSchema),
  ),
  tenureAtRole: types.nullable(
    z.lazy(() => SearchSalesNavTenureAtRole$inboundSchema),
  ),
});

export function searchSalesNavCurrentPositionFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCurrentPosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCurrentPosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCurrentPosition' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavItemPeople$inboundSchema: z.ZodMiniType<
  SearchSalesNavItemPeople,
  unknown
> = z.object({
  type: SearchSalesNavTypePeople$inboundSchema,
  id: types.string(),
  name: types.string(),
  firstName: types.nullable(types.string()),
  lastName: types.nullable(types.string()),
  memberUrn: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  profileUrl: types.nullable(types.string()),
  salesNavUrl: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  location: types.nullable(types.string()),
  networkDistance: types.nullable(SearchSalesNavNetworkDistance$inboundSchema),
  premium: types.nullable(types.boolean()),
  openProfile: types.nullable(types.boolean()),
  pendingInvitation: types.nullable(types.boolean()),
  currentPositions: z.array(
    z.lazy(() => SearchSalesNavCurrentPosition$inboundSchema),
  ),
});

export function searchSalesNavItemPeopleFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavItemPeople, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavItemPeople$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavItemPeople' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPaging1$inboundSchema: z.ZodMiniType<
  SearchSalesNavPaging1,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchSalesNavPaging1FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPaging1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPaging1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPaging1' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavWarning1$inboundSchema: z.ZodMiniType<
  SearchSalesNavWarning1,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchSalesNavWarning1FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavWarning1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavWarning1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavWarning1' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCredits1$inboundSchema: z.ZodMiniType<
  SearchSalesNavCredits1,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchSalesNavCredits1FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCredits1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCredits1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCredits1' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavMeta1$inboundSchema: z.ZodMiniType<
  SearchSalesNavMeta1,
  unknown
> = z.object({
  credits: z.lazy(() => SearchSalesNavCredits1$inboundSchema),
});

export function searchSalesNavMeta1FromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavMeta1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavMeta1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavMeta1' from JSON`,
  );
}

/** @internal */
export const People$inboundSchema: z.ZodMiniType<People, unknown> = z.pipe(
  z.object({
    success: types.literal(true),
    category: types.literal("people"),
    items: z.array(z.lazy(() => SearchSalesNavItemPeople$inboundSchema)),
    paging: z.lazy(() => SearchSalesNavPaging1$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(z.array(z.lazy(() =>
      SearchSalesNavWarning1$inboundSchema
    ))),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchSalesNavMeta1$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function peopleFromJSON(
  jsonString: string,
): SafeParseResult<People, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => People$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'People' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavResponse$inboundSchema: z.ZodMiniType<
  SearchSalesNavResponse,
  unknown
> = discriminatedUnion("category", {
  people: z.lazy(() => People$inboundSchema),
  companies: z.lazy(() => Companies$inboundSchema),
});

export function searchSalesNavResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavResponse' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleCategoryRequest$outboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavPeopleCategoryRequest
> = z.enum(SearchSalesNavPeopleCategoryRequest);

/** @internal */
export type SearchSalesNavPeopleCompany$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavPeopleCompany$outboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleCompany$Outbound,
  SearchSalesNavPeopleCompany
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavPeopleCompanyToJSON(
  searchSalesNavPeopleCompany: SearchSalesNavPeopleCompany,
): string {
  return JSON.stringify(
    SearchSalesNavPeopleCompany$outboundSchema.parse(
      searchSalesNavPeopleCompany,
    ),
  );
}

/** @internal */
export type SearchSalesNavPeopleIndustry$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavPeopleIndustry$outboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleIndustry$Outbound,
  SearchSalesNavPeopleIndustry
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavPeopleIndustryToJSON(
  searchSalesNavPeopleIndustry: SearchSalesNavPeopleIndustry,
): string {
  return JSON.stringify(
    SearchSalesNavPeopleIndustry$outboundSchema.parse(
      searchSalesNavPeopleIndustry,
    ),
  );
}

/** @internal */
export type SearchSalesNavPeopleTenure$Outbound = {
  min?: number | undefined;
  max?: number | undefined;
};

/** @internal */
export const SearchSalesNavPeopleTenure$outboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleTenure$Outbound,
  SearchSalesNavPeopleTenure
> = z.object({
  min: z.optional(z.int()),
  max: z.optional(z.int()),
});

export function searchSalesNavPeopleTenureToJSON(
  searchSalesNavPeopleTenure: SearchSalesNavPeopleTenure,
): string {
  return JSON.stringify(
    SearchSalesNavPeopleTenure$outboundSchema.parse(searchSalesNavPeopleTenure),
  );
}

/** @internal */
export type SearchSalesNavPeoplePastCompany$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavPeoplePastCompany$outboundSchema: z.ZodMiniType<
  SearchSalesNavPeoplePastCompany$Outbound,
  SearchSalesNavPeoplePastCompany
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavPeoplePastCompanyToJSON(
  searchSalesNavPeoplePastCompany: SearchSalesNavPeoplePastCompany,
): string {
  return JSON.stringify(
    SearchSalesNavPeoplePastCompany$outboundSchema.parse(
      searchSalesNavPeoplePastCompany,
    ),
  );
}

/** @internal */
export type SearchSalesNavPeopleCompanyHeadquarters$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavPeopleCompanyHeadquarters$outboundSchema:
  z.ZodMiniType<
    SearchSalesNavPeopleCompanyHeadquarters$Outbound,
    SearchSalesNavPeopleCompanyHeadquarters
  > = z.object({
    include: z.optional(z.array(z.string())),
    exclude: z.optional(z.array(z.string())),
  });

export function searchSalesNavPeopleCompanyHeadquartersToJSON(
  searchSalesNavPeopleCompanyHeadquarters:
    SearchSalesNavPeopleCompanyHeadquarters,
): string {
  return JSON.stringify(
    SearchSalesNavPeopleCompanyHeadquarters$outboundSchema.parse(
      searchSalesNavPeopleCompanyHeadquarters,
    ),
  );
}

/** @internal */
export type SearchSalesNavPeopleRequest$Outbound = {
  category?: string | undefined;
  url?: string | undefined;
  keywords?: string | undefined;
  title?: string | undefined;
  company?: SearchSalesNavPeopleCompany$Outbound | undefined;
  industry?: SearchSalesNavPeopleIndustry$Outbound | undefined;
  location?: Array<string> | undefined;
  locationExclude?: Array<string> | undefined;
  seniority?: Array<string> | undefined;
  function?: Array<string> | undefined;
  tenure?: Array<SearchSalesNavPeopleTenure$Outbound> | undefined;
  profileLanguage?: Array<string> | undefined;
  connectionDegree?: Array<string> | undefined;
  school?: Array<string> | undefined;
  yearsOfExperience?: Array<string> | undefined;
  persona?: Array<string> | undefined;
  recentlyChangedJobs?: boolean | undefined;
  postedOnLinkedin?: boolean | undefined;
  sharedExperiences?: boolean | undefined;
  viewedYourProfile?: boolean | undefined;
  followsYourCompany?: boolean | undefined;
  connectionOf?: Array<string> | undefined;
  pastCompany?: SearchSalesNavPeoplePastCompany$Outbound | undefined;
  companyHeadquarters?:
    | SearchSalesNavPeopleCompanyHeadquarters$Outbound
    | undefined;
  yearsInCurrentPosition?: Array<string> | undefined;
  pastTitle?: string | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
  groups?: Array<string> | undefined;
  pastColleague?: boolean | undefined;
  leadInteractions?: Array<string> | undefined;
  savedLeadsAndAccounts?: Array<string> | undefined;
  numOfFollowers?: Array<string> | undefined;
  fortune?: Array<string> | undefined;
  accountActivities?: Array<string> | undefined;
  jobOpportunities?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
  campaignSlug?: string | undefined;
};

/** @internal */
export const SearchSalesNavPeopleRequest$outboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleRequest$Outbound,
  SearchSalesNavPeopleRequest
> = z.object({
  category: z.optional(SearchSalesNavPeopleCategoryRequest$outboundSchema),
  url: z.optional(z.string()),
  keywords: z.optional(z.string()),
  title: z.optional(z.string()),
  company: z.optional(z.lazy(() => SearchSalesNavPeopleCompany$outboundSchema)),
  industry: z.optional(
    z.lazy(() => SearchSalesNavPeopleIndustry$outboundSchema),
  ),
  location: z.optional(z.array(z.string())),
  locationExclude: z.optional(z.array(z.string())),
  seniority: z.optional(z.array(z.string())),
  function: z.optional(z.array(z.string())),
  tenure: z.optional(
    z.array(z.lazy(() => SearchSalesNavPeopleTenure$outboundSchema)),
  ),
  profileLanguage: z.optional(z.array(z.string())),
  connectionDegree: z.optional(z.array(z.string())),
  school: z.optional(z.array(z.string())),
  yearsOfExperience: z.optional(z.array(z.string())),
  persona: z.optional(z.array(z.string())),
  recentlyChangedJobs: z.optional(z.boolean()),
  postedOnLinkedin: z.optional(z.boolean()),
  sharedExperiences: z.optional(z.boolean()),
  viewedYourProfile: z.optional(z.boolean()),
  followsYourCompany: z.optional(z.boolean()),
  connectionOf: z.optional(z.array(z.string())),
  pastCompany: z.optional(
    z.lazy(() => SearchSalesNavPeoplePastCompany$outboundSchema),
  ),
  companyHeadquarters: z.optional(
    z.lazy(() => SearchSalesNavPeopleCompanyHeadquarters$outboundSchema),
  ),
  yearsInCurrentPosition: z.optional(z.array(z.string())),
  pastTitle: z.optional(z.string()),
  firstName: z.optional(z.string()),
  lastName: z.optional(z.string()),
  groups: z.optional(z.array(z.string())),
  pastColleague: z.optional(z.boolean()),
  leadInteractions: z.optional(z.array(z.string())),
  savedLeadsAndAccounts: z.optional(z.array(z.string())),
  numOfFollowers: z.optional(z.array(z.string())),
  fortune: z.optional(z.array(z.string())),
  accountActivities: z.optional(z.array(z.string())),
  jobOpportunities: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
  campaignSlug: z.optional(z.string()),
});

export function searchSalesNavPeopleRequestToJSON(
  searchSalesNavPeopleRequest: SearchSalesNavPeopleRequest,
): string {
  return JSON.stringify(
    SearchSalesNavPeopleRequest$outboundSchema.parse(
      searchSalesNavPeopleRequest,
    ),
  );
}

/** @internal */
export const SearchSalesNavPeopleCategoryPeople$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavPeopleCategoryPeople
> = z.enum(SearchSalesNavPeopleCategoryPeople);

/** @internal */
export const SearchSalesNavPeopleType$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavPeopleType
> = z.enum(SearchSalesNavPeopleType);

/** @internal */
export const SearchSalesNavPeopleNetworkDistance$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleNetworkDistance,
  unknown
> = openEnums.inboundSchema(SearchSalesNavPeopleNetworkDistance);

/** @internal */
export const SearchSalesNavPeopleTenureAtCompany$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleTenureAtCompany,
  unknown
> = z.object({
  years: types.optional(types.number()),
  months: types.optional(types.number()),
});

export function searchSalesNavPeopleTenureAtCompanyFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleTenureAtCompany, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SearchSalesNavPeopleTenureAtCompany$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleTenureAtCompany' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleTenureAtRole$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleTenureAtRole,
  unknown
> = z.object({
  years: types.optional(types.number()),
  months: types.optional(types.number()),
});

export function searchSalesNavPeopleTenureAtRoleFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleTenureAtRole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleTenureAtRole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleTenureAtRole' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleCurrentPosition$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleCurrentPosition,
  unknown
> = z.object({
  company: types.nullable(types.string()),
  companyId: types.nullable(types.string()),
  role: types.nullable(types.string()),
  description: types.nullable(types.string()),
  location: types.nullable(types.string()),
  tenureAtCompany: types.nullable(
    z.lazy(() => SearchSalesNavPeopleTenureAtCompany$inboundSchema),
  ),
  tenureAtRole: types.nullable(
    z.lazy(() => SearchSalesNavPeopleTenureAtRole$inboundSchema),
  ),
});

export function searchSalesNavPeopleCurrentPositionFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleCurrentPosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      SearchSalesNavPeopleCurrentPosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleCurrentPosition' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleItem$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleItem,
  unknown
> = z.object({
  type: SearchSalesNavPeopleType$inboundSchema,
  id: types.string(),
  name: types.string(),
  firstName: types.nullable(types.string()),
  lastName: types.nullable(types.string()),
  memberUrn: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  profileUrl: types.nullable(types.string()),
  salesNavUrl: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  location: types.nullable(types.string()),
  networkDistance: types.nullable(
    SearchSalesNavPeopleNetworkDistance$inboundSchema,
  ),
  premium: types.nullable(types.boolean()),
  openProfile: types.nullable(types.boolean()),
  pendingInvitation: types.nullable(types.boolean()),
  currentPositions: z.array(
    z.lazy(() => SearchSalesNavPeopleCurrentPosition$inboundSchema),
  ),
});

export function searchSalesNavPeopleItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleItem' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeoplePaging$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeoplePaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchSalesNavPeoplePagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeoplePaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeoplePaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeoplePaging' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleWarning$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchSalesNavPeopleWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleWarning' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleCredits$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchSalesNavPeopleCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleCredits' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleMeta$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchSalesNavPeopleCredits$inboundSchema),
});

export function searchSalesNavPeopleMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleMeta' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavPeopleResponse$inboundSchema: z.ZodMiniType<
  SearchSalesNavPeopleResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchSalesNavPeopleCategoryPeople$inboundSchema,
    items: z.array(z.lazy(() => SearchSalesNavPeopleItem$inboundSchema)),
    paging: z.lazy(() => SearchSalesNavPeoplePaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchSalesNavPeopleWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SearchSalesNavPeopleMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchSalesNavPeopleResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavPeopleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavPeopleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavPeopleResponse' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesCategoryRequest$outboundSchema:
  z.ZodMiniEnum<typeof SearchSalesNavCompaniesCategoryRequest> = z.enum(
    SearchSalesNavCompaniesCategoryRequest,
  );

/** @internal */
export type SearchSalesNavCompaniesIndustry$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavCompaniesIndustry$outboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesIndustry$Outbound,
  SearchSalesNavCompaniesIndustry
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavCompaniesIndustryToJSON(
  searchSalesNavCompaniesIndustry: SearchSalesNavCompaniesIndustry,
): string {
  return JSON.stringify(
    SearchSalesNavCompaniesIndustry$outboundSchema.parse(
      searchSalesNavCompaniesIndustry,
    ),
  );
}

/** @internal */
export type SearchSalesNavCompaniesPastCompany$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavCompaniesPastCompany$outboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesPastCompany$Outbound,
  SearchSalesNavCompaniesPastCompany
> = z.object({
  include: z.optional(z.array(z.string())),
  exclude: z.optional(z.array(z.string())),
});

export function searchSalesNavCompaniesPastCompanyToJSON(
  searchSalesNavCompaniesPastCompany: SearchSalesNavCompaniesPastCompany,
): string {
  return JSON.stringify(
    SearchSalesNavCompaniesPastCompany$outboundSchema.parse(
      searchSalesNavCompaniesPastCompany,
    ),
  );
}

/** @internal */
export type SearchSalesNavCompaniesCompanyHeadquarters$Outbound = {
  include?: Array<string> | undefined;
  exclude?: Array<string> | undefined;
};

/** @internal */
export const SearchSalesNavCompaniesCompanyHeadquarters$outboundSchema:
  z.ZodMiniType<
    SearchSalesNavCompaniesCompanyHeadquarters$Outbound,
    SearchSalesNavCompaniesCompanyHeadquarters
  > = z.object({
    include: z.optional(z.array(z.string())),
    exclude: z.optional(z.array(z.string())),
  });

export function searchSalesNavCompaniesCompanyHeadquartersToJSON(
  searchSalesNavCompaniesCompanyHeadquarters:
    SearchSalesNavCompaniesCompanyHeadquarters,
): string {
  return JSON.stringify(
    SearchSalesNavCompaniesCompanyHeadquarters$outboundSchema.parse(
      searchSalesNavCompaniesCompanyHeadquarters,
    ),
  );
}

/** @internal */
export type SearchSalesNavCompaniesRequest$Outbound = {
  category?: string | undefined;
  url?: string | undefined;
  keywords?: string | undefined;
  industry?: SearchSalesNavCompaniesIndustry$Outbound | undefined;
  location?: Array<string> | undefined;
  locationExclude?: Array<string> | undefined;
  persona?: Array<string> | undefined;
  recentlyChangedJobs?: boolean | undefined;
  postedOnLinkedin?: boolean | undefined;
  sharedExperiences?: boolean | undefined;
  viewedYourProfile?: boolean | undefined;
  followsYourCompany?: boolean | undefined;
  connectionOf?: Array<string> | undefined;
  pastCompany?: SearchSalesNavCompaniesPastCompany$Outbound | undefined;
  companyHeadquarters?:
    | SearchSalesNavCompaniesCompanyHeadquarters$Outbound
    | undefined;
  yearsInCurrentPosition?: Array<string> | undefined;
  pastTitle?: string | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
  groups?: Array<string> | undefined;
  pastColleague?: boolean | undefined;
  leadInteractions?: Array<string> | undefined;
  savedLeadsAndAccounts?: Array<string> | undefined;
  companyHeadcount?: Array<string> | undefined;
  companyType?: Array<string> | undefined;
  numOfFollowers?: Array<string> | undefined;
  fortune?: Array<string> | undefined;
  accountActivities?: Array<string> | undefined;
  jobOpportunities?: Array<string> | undefined;
  start?: number | undefined;
  count?: number | undefined;
  campaignSlug?: string | undefined;
};

/** @internal */
export const SearchSalesNavCompaniesRequest$outboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesRequest$Outbound,
  SearchSalesNavCompaniesRequest
> = z.object({
  category: z.optional(SearchSalesNavCompaniesCategoryRequest$outboundSchema),
  url: z.optional(z.string()),
  keywords: z.optional(z.string()),
  industry: z.optional(
    z.lazy(() => SearchSalesNavCompaniesIndustry$outboundSchema),
  ),
  location: z.optional(z.array(z.string())),
  locationExclude: z.optional(z.array(z.string())),
  persona: z.optional(z.array(z.string())),
  recentlyChangedJobs: z.optional(z.boolean()),
  postedOnLinkedin: z.optional(z.boolean()),
  sharedExperiences: z.optional(z.boolean()),
  viewedYourProfile: z.optional(z.boolean()),
  followsYourCompany: z.optional(z.boolean()),
  connectionOf: z.optional(z.array(z.string())),
  pastCompany: z.optional(
    z.lazy(() => SearchSalesNavCompaniesPastCompany$outboundSchema),
  ),
  companyHeadquarters: z.optional(
    z.lazy(() => SearchSalesNavCompaniesCompanyHeadquarters$outboundSchema),
  ),
  yearsInCurrentPosition: z.optional(z.array(z.string())),
  pastTitle: z.optional(z.string()),
  firstName: z.optional(z.string()),
  lastName: z.optional(z.string()),
  groups: z.optional(z.array(z.string())),
  pastColleague: z.optional(z.boolean()),
  leadInteractions: z.optional(z.array(z.string())),
  savedLeadsAndAccounts: z.optional(z.array(z.string())),
  companyHeadcount: z.optional(z.array(z.string())),
  companyType: z.optional(z.array(z.string())),
  numOfFollowers: z.optional(z.array(z.string())),
  fortune: z.optional(z.array(z.string())),
  accountActivities: z.optional(z.array(z.string())),
  jobOpportunities: z.optional(z.array(z.string())),
  start: z.optional(z.int()),
  count: z.optional(z.int()),
  campaignSlug: z.optional(z.string()),
});

export function searchSalesNavCompaniesRequestToJSON(
  searchSalesNavCompaniesRequest: SearchSalesNavCompaniesRequest,
): string {
  return JSON.stringify(
    SearchSalesNavCompaniesRequest$outboundSchema.parse(
      searchSalesNavCompaniesRequest,
    ),
  );
}

/** @internal */
export const SearchSalesNavCompaniesCategoryCompanies$inboundSchema:
  z.ZodMiniEnum<typeof SearchSalesNavCompaniesCategoryCompanies> = z.enum(
    SearchSalesNavCompaniesCategoryCompanies,
  );

/** @internal */
export const SearchSalesNavCompaniesType$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavCompaniesType
> = z.enum(SearchSalesNavCompaniesType);

/** @internal */
export const SearchSalesNavCompaniesLane$inboundSchema: z.ZodMiniEnum<
  typeof SearchSalesNavCompaniesLane
> = z.enum(SearchSalesNavCompaniesLane);

/** @internal */
export const SearchSalesNavCompaniesItem$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesItem,
  unknown
> = z.object({
  type: SearchSalesNavCompaniesType$inboundSchema,
  name: types.string(),
  profileUrl: types.nullable(types.string()),
  summary: types.nullable(types.string()),
  industry: types.nullable(types.string()),
  location: types.nullable(types.string()),
  logoUrl: z.optional(z.nullable(types.string())),
  lane: types.optional(SearchSalesNavCompaniesLane$inboundSchema),
  id: types.string(),
  headcount: types.nullable(types.string()),
});

export function searchSalesNavCompaniesItemFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesItem' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesPaging$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesPaging,
  unknown
> = z.object({
  start: types.number(),
  count: types.number(),
  total: types.number(),
});

export function searchSalesNavCompaniesPagingFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesPaging, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesPaging$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesPaging' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesWarning$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesWarning,
  unknown
> = z.object({
  field: types.string(),
  value: types.string(),
  type: types.string(),
  hint: types.optional(types.string()),
});

export function searchSalesNavCompaniesWarningFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesWarning, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesWarning$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesWarning' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesCredits$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function searchSalesNavCompaniesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesCredits' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesMeta$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SearchSalesNavCompaniesCredits$inboundSchema),
});

export function searchSalesNavCompaniesMetaFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesMeta' from JSON`,
  );
}

/** @internal */
export const SearchSalesNavCompaniesResponse$inboundSchema: z.ZodMiniType<
  SearchSalesNavCompaniesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    category: SearchSalesNavCompaniesCategoryCompanies$inboundSchema,
    items: z.array(z.lazy(() => SearchSalesNavCompaniesItem$inboundSchema)),
    paging: z.lazy(() => SearchSalesNavCompaniesPaging$inboundSchema),
    hasMore: types.boolean(),
    warnings: types.optional(
      z.array(z.lazy(() => SearchSalesNavCompaniesWarning$inboundSchema)),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => SearchSalesNavCompaniesMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function searchSalesNavCompaniesResponseFromJSON(
  jsonString: string,
): SafeParseResult<SearchSalesNavCompaniesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchSalesNavCompaniesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchSalesNavCompaniesResponse' from JSON`,
  );
}

/** @internal */
export type ConnectProfileProfile$Outbound = string | Array<string>;

/** @internal */
export const ConnectProfileProfile$outboundSchema: z.ZodMiniType<
  ConnectProfileProfile$Outbound,
  ConnectProfileProfile
> = smartUnion([z.string(), z.array(z.string())]);

export function connectProfileProfileToJSON(
  connectProfileProfile: ConnectProfileProfile,
): string {
  return JSON.stringify(
    ConnectProfileProfile$outboundSchema.parse(connectProfileProfile),
  );
}

/** @internal */
export type KnownPerson$Outbound = {
  profile: string;
  name?: string | undefined;
  headline?: string | undefined;
  company?: string | undefined;
  location?: string | undefined;
  avatarUrl?: string | undefined;
};

/** @internal */
export const KnownPerson$outboundSchema: z.ZodMiniType<
  KnownPerson$Outbound,
  KnownPerson
> = z.object({
  profile: z.string(),
  name: z.optional(z.string()),
  headline: z.optional(z.string()),
  company: z.optional(z.string()),
  location: z.optional(z.string()),
  avatarUrl: z.optional(z.string()),
});

export function knownPersonToJSON(knownPerson: KnownPerson): string {
  return JSON.stringify(KnownPerson$outboundSchema.parse(knownPerson));
}

/** @internal */
export const PlacementRequest$outboundSchema: z.ZodMiniEnum<
  typeof PlacementRequest
> = z.enum(PlacementRequest);

/** @internal */
export type ConnectProfileRequest$Outbound = {
  profile: string | Array<string>;
  knownPeople?: Array<KnownPerson$Outbound> | undefined;
  message?: string | undefined;
  now?: boolean | undefined;
  list?: string | undefined;
  dryRun?: boolean | undefined;
  placement?: string | undefined;
};

/** @internal */
export const ConnectProfileRequest$outboundSchema: z.ZodMiniType<
  ConnectProfileRequest$Outbound,
  ConnectProfileRequest
> = z.object({
  profile: smartUnion([z.string(), z.array(z.string())]),
  knownPeople: z.optional(z.array(z.lazy(() => KnownPerson$outboundSchema))),
  message: z.optional(z.string()),
  now: z.optional(z.boolean()),
  list: z.optional(z.string()),
  dryRun: z.optional(z.boolean()),
  placement: z.optional(PlacementRequest$outboundSchema),
});

export function connectProfileRequestToJSON(
  connectProfileRequest: ConnectProfileRequest,
): string {
  return JSON.stringify(
    ConnectProfileRequest$outboundSchema.parse(connectProfileRequest),
  );
}

/** @internal */
export const Queued$inboundSchema: z.ZodMiniType<Queued, unknown> = z.object({
  profile: types.string(),
  position: types.nullable(types.number()),
  etaDays: types.nullable(types.number()),
});

export function queuedFromJSON(
  jsonString: string,
): SafeParseResult<Queued, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Queued$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Queued' from JSON`,
  );
}

/** @internal */
export const ConnectProfileSkipped$inboundSchema: z.ZodMiniType<
  ConnectProfileSkipped,
  unknown
> = z.object({
  profile: types.string(),
  reason: types.string(),
});

export function connectProfileSkippedFromJSON(
  jsonString: string,
): SafeParseResult<ConnectProfileSkipped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectProfileSkipped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectProfileSkipped' from JSON`,
  );
}

/** @internal */
export const ConnectProfileBlocked$inboundSchema: z.ZodMiniType<
  ConnectProfileBlocked,
  unknown
> = z.object({
  kind: types.string(),
  clearsItself: types.boolean(),
  message: types.string(),
  resumesAt: types.nullable(types.string()),
  fixUrl: types.nullable(types.string()),
});

export function connectProfileBlockedFromJSON(
  jsonString: string,
): SafeParseResult<ConnectProfileBlocked, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectProfileBlocked$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectProfileBlocked' from JSON`,
  );
}

/** @internal */
export const PlacementResponse$inboundSchema: z.ZodMiniType<
  PlacementResponse,
  unknown
> = openEnums.inboundSchema(PlacementResponse);

/** @internal */
export const List$inboundSchema: z.ZodMiniType<List, unknown> = z.object({
  name: types.string(),
  isNew: types.boolean(),
  totalMembers: types.number(),
});

export function listFromJSON(
  jsonString: string,
): SafeParseResult<List, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => List$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'List' from JSON`,
  );
}

/** @internal */
export const ConnectProfileCredits$inboundSchema: z.ZodMiniType<
  ConnectProfileCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function connectProfileCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ConnectProfileCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectProfileCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectProfileCredits' from JSON`,
  );
}

/** @internal */
export const ConnectProfileMeta$inboundSchema: z.ZodMiniType<
  ConnectProfileMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ConnectProfileCredits$inboundSchema),
});

export function connectProfileMetaFromJSON(
  jsonString: string,
): SafeParseResult<ConnectProfileMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectProfileMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectProfileMeta' from JSON`,
  );
}

/** @internal */
export const ConnectProfileResponse$inboundSchema: z.ZodMiniType<
  ConnectProfileResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    sent: types.number(),
    queued: z.array(z.lazy(() => Queued$inboundSchema)),
    skipped: z.array(z.lazy(() => ConnectProfileSkipped$inboundSchema)),
    blocked: types.nullable(z.lazy(() => ConnectProfileBlocked$inboundSchema)),
    waitingBefore: types.optional(types.number()),
    placement: types.optional(PlacementResponse$inboundSchema),
    duplicate: types.optional(types.boolean()),
    dryRun: types.optional(types.boolean()),
    list: types.optional(z.lazy(() => List$inboundSchema)),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ConnectProfileMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function connectProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<ConnectProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectProfileResponse' from JSON`,
  );
}

/** @internal */
export type ListInvitationsRequest$Outbound = {
  start: number;
  count: number;
};

/** @internal */
export const ListInvitationsRequest$outboundSchema: z.ZodMiniType<
  ListInvitationsRequest$Outbound,
  ListInvitationsRequest
> = z.object({
  start: z._default(z.int(), 0),
  count: z._default(z.int(), 10),
});

export function listInvitationsRequestToJSON(
  listInvitationsRequest: ListInvitationsRequest,
): string {
  return JSON.stringify(
    ListInvitationsRequest$outboundSchema.parse(listInvitationsRequest),
  );
}

/** @internal */
export const FromMember$inboundSchema: z.ZodMiniType<FromMember, unknown> = z
  .object({
    name: types.nullable(types.string()),
    headline: types.nullable(types.string()),
    profileUrl: types.nullable(types.string()),
    profilePicture: types.nullable(types.string()),
    publicIdentifier: types.nullable(types.string()),
    profileUrn: types.nullable(types.string()),
  });

export function fromMemberFromJSON(
  jsonString: string,
): SafeParseResult<FromMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FromMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FromMember' from JSON`,
  );
}

/** @internal */
export const ListInvitationsInvitation$inboundSchema: z.ZodMiniType<
  ListInvitationsInvitation,
  unknown
> = z.object({
  invitationId: types.string(),
  sharedSecret: types.string(),
  entityUrn: types.string(),
  sentAt: types.nullable(types.string()),
  message: types.nullable(types.string()),
  senderProfileId: types.nullable(types.string()),
  senderFirstName: types.nullable(types.string()),
  senderLastName: types.nullable(types.string()),
  fromMember: types.nullable(z.lazy(() => FromMember$inboundSchema)),
  sentTime: z.optional(z.nullable(types.number())),
  invitationType: z.optional(z.nullable(types.string())),
  mailboxItemId: z.optional(z.nullable(types.string())),
  customMessage: types.optional(types.boolean()),
  unseen: types.optional(types.boolean()),
  mutualCurrentCompany: z.optional(z.nullable(z.any())),
  connectionDistance: z.optional(z.nullable(types.number())),
  inviterFollowingInvitee: types.optional(types.boolean()),
  fromEvent: types.optional(z.any()),
  inviterActors: types.optional(z.array(z.any())),
});

export function listInvitationsInvitationFromJSON(
  jsonString: string,
): SafeParseResult<ListInvitationsInvitation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListInvitationsInvitation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListInvitationsInvitation' from JSON`,
  );
}

/** @internal */
export const ListInvitationsCredits$inboundSchema: z.ZodMiniType<
  ListInvitationsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listInvitationsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListInvitationsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListInvitationsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListInvitationsCredits' from JSON`,
  );
}

/** @internal */
export const ListInvitationsMeta$inboundSchema: z.ZodMiniType<
  ListInvitationsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListInvitationsCredits$inboundSchema),
});

export function listInvitationsMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListInvitationsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListInvitationsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListInvitationsMeta' from JSON`,
  );
}

/** @internal */
export const ListInvitationsResponse$inboundSchema: z.ZodMiniType<
  ListInvitationsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    invitations: z.array(z.lazy(() => ListInvitationsInvitation$inboundSchema)),
    total: types.number(),
    start: types.number(),
    count: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListInvitationsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listInvitationsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListInvitationsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListInvitationsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListInvitationsResponse' from JSON`,
  );
}

/** @internal */
export type AcceptInvitationRequest$Outbound = {
  invitationId: string;
  sharedSecret: string;
  senderProfileId?: string | undefined;
  firstName?: string | undefined;
  lastName?: string | undefined;
};

/** @internal */
export const AcceptInvitationRequest$outboundSchema: z.ZodMiniType<
  AcceptInvitationRequest$Outbound,
  AcceptInvitationRequest
> = z.object({
  invitationId: z.string(),
  sharedSecret: z.string(),
  senderProfileId: z.optional(z.string()),
  firstName: z.optional(z.string()),
  lastName: z.optional(z.string()),
});

export function acceptInvitationRequestToJSON(
  acceptInvitationRequest: AcceptInvitationRequest,
): string {
  return JSON.stringify(
    AcceptInvitationRequest$outboundSchema.parse(acceptInvitationRequest),
  );
}

/** @internal */
export const AcceptInvitationCredits$inboundSchema: z.ZodMiniType<
  AcceptInvitationCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function acceptInvitationCreditsFromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitationCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitationCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitationCredits' from JSON`,
  );
}

/** @internal */
export const AcceptInvitationMeta$inboundSchema: z.ZodMiniType<
  AcceptInvitationMeta,
  unknown
> = z.object({
  credits: z.lazy(() => AcceptInvitationCredits$inboundSchema),
});

export function acceptInvitationMetaFromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitationMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitationMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitationMeta' from JSON`,
  );
}

/** @internal */
export const AcceptInvitationResponse$inboundSchema: z.ZodMiniType<
  AcceptInvitationResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => AcceptInvitationMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function acceptInvitationResponseFromJSON(
  jsonString: string,
): SafeParseResult<AcceptInvitationResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AcceptInvitationResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AcceptInvitationResponse' from JSON`,
  );
}

/** @internal */
export type SendMessageRequest$Outbound = {
  profile?: string | undefined;
  conversationUrn?: string | undefined;
  message: string;
  campaignSlug?: string | undefined;
  actionSlug?: string | undefined;
};

/** @internal */
export const SendMessageRequest$outboundSchema: z.ZodMiniType<
  SendMessageRequest$Outbound,
  SendMessageRequest
> = z.object({
  profile: z.optional(z.string()),
  conversationUrn: z.optional(z.string()),
  message: z.string(),
  campaignSlug: z.optional(z.string()),
  actionSlug: z.optional(z.string()),
});

export function sendMessageRequestToJSON(
  sendMessageRequest: SendMessageRequest,
): string {
  return JSON.stringify(
    SendMessageRequest$outboundSchema.parse(sendMessageRequest),
  );
}

/** @internal */
export const SendMessageCredits$inboundSchema: z.ZodMiniType<
  SendMessageCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function sendMessageCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SendMessageCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SendMessageCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SendMessageCredits' from JSON`,
  );
}

/** @internal */
export const SendMessageMeta$inboundSchema: z.ZodMiniType<
  SendMessageMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SendMessageCredits$inboundSchema),
});

export function sendMessageMetaFromJSON(
  jsonString: string,
): SafeParseResult<SendMessageMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SendMessageMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SendMessageMeta' from JSON`,
  );
}

/** @internal */
export const SendMessageResponse$inboundSchema: z.ZodMiniType<
  SendMessageResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    messageId: types.string(),
    duplicate: types.optional(types.boolean()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => SendMessageMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function sendMessageResponseFromJSON(
  jsonString: string,
): SafeParseResult<SendMessageResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SendMessageResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SendMessageResponse' from JSON`,
  );
}

/** @internal */
export type ReplyToCommentRequest$Outbound = {
  commentUrn: string;
  message: string;
  campaignSlug?: string | undefined;
  actionSlug?: string | undefined;
};

/** @internal */
export const ReplyToCommentRequest$outboundSchema: z.ZodMiniType<
  ReplyToCommentRequest$Outbound,
  ReplyToCommentRequest
> = z.object({
  commentUrn: z.string(),
  message: z.string(),
  campaignSlug: z.optional(z.string()),
  actionSlug: z.optional(z.string()),
});

export function replyToCommentRequestToJSON(
  replyToCommentRequest: ReplyToCommentRequest,
): string {
  return JSON.stringify(
    ReplyToCommentRequest$outboundSchema.parse(replyToCommentRequest),
  );
}

/** @internal */
export const ReplyToCommentCredits$inboundSchema: z.ZodMiniType<
  ReplyToCommentCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function replyToCommentCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ReplyToCommentCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyToCommentCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyToCommentCredits' from JSON`,
  );
}

/** @internal */
export const ReplyToCommentMeta$inboundSchema: z.ZodMiniType<
  ReplyToCommentMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ReplyToCommentCredits$inboundSchema),
});

export function replyToCommentMetaFromJSON(
  jsonString: string,
): SafeParseResult<ReplyToCommentMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyToCommentMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyToCommentMeta' from JSON`,
  );
}

/** @internal */
export const ReplyToCommentResponse$inboundSchema: z.ZodMiniType<
  ReplyToCommentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    replyUrn: types.optional(types.string()),
    duplicate: types.optional(types.boolean()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ReplyToCommentMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function replyToCommentResponseFromJSON(
  jsonString: string,
): SafeParseResult<ReplyToCommentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReplyToCommentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReplyToCommentResponse' from JSON`,
  );
}

/** @internal */
export const LikeCommentReactionType$outboundSchema: z.ZodMiniEnum<
  typeof LikeCommentReactionType
> = z.enum(LikeCommentReactionType);

/** @internal */
export type LikeCommentRequest$Outbound = {
  commentUrn: string;
  reactionType: string;
  campaignSlug?: string | undefined;
  actionSlug?: string | undefined;
};

/** @internal */
export const LikeCommentRequest$outboundSchema: z.ZodMiniType<
  LikeCommentRequest$Outbound,
  LikeCommentRequest
> = z.object({
  commentUrn: z.string(),
  reactionType: z._default(LikeCommentReactionType$outboundSchema, "LIKE"),
  campaignSlug: z.optional(z.string()),
  actionSlug: z.optional(z.string()),
});

export function likeCommentRequestToJSON(
  likeCommentRequest: LikeCommentRequest,
): string {
  return JSON.stringify(
    LikeCommentRequest$outboundSchema.parse(likeCommentRequest),
  );
}

/** @internal */
export const LikeCommentCredits$inboundSchema: z.ZodMiniType<
  LikeCommentCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function likeCommentCreditsFromJSON(
  jsonString: string,
): SafeParseResult<LikeCommentCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikeCommentCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikeCommentCredits' from JSON`,
  );
}

/** @internal */
export const LikeCommentMeta$inboundSchema: z.ZodMiniType<
  LikeCommentMeta,
  unknown
> = z.object({
  credits: z.lazy(() => LikeCommentCredits$inboundSchema),
});

export function likeCommentMetaFromJSON(
  jsonString: string,
): SafeParseResult<LikeCommentMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikeCommentMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikeCommentMeta' from JSON`,
  );
}

/** @internal */
export const LikeCommentResponse$inboundSchema: z.ZodMiniType<
  LikeCommentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    resourceKey: types.optional(types.string()),
    duplicate: types.optional(types.boolean()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => LikeCommentMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function likeCommentResponseFromJSON(
  jsonString: string,
): SafeParseResult<LikeCommentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikeCommentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikeCommentResponse' from JSON`,
  );
}

/** @internal */
export type CommentOnPostRequest$Outbound = {
  postUrl: string;
  message: string;
  companyId?: string | undefined;
  campaignSlug?: string | undefined;
  actionSlug?: string | undefined;
};

/** @internal */
export const CommentOnPostRequest$outboundSchema: z.ZodMiniType<
  CommentOnPostRequest$Outbound,
  CommentOnPostRequest
> = z.object({
  postUrl: z.string(),
  message: z.string(),
  companyId: z.optional(z.string()),
  campaignSlug: z.optional(z.string()),
  actionSlug: z.optional(z.string()),
});

export function commentOnPostRequestToJSON(
  commentOnPostRequest: CommentOnPostRequest,
): string {
  return JSON.stringify(
    CommentOnPostRequest$outboundSchema.parse(commentOnPostRequest),
  );
}

/** @internal */
export const CommentOnPostCredits$inboundSchema: z.ZodMiniType<
  CommentOnPostCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function commentOnPostCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CommentOnPostCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentOnPostCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentOnPostCredits' from JSON`,
  );
}

/** @internal */
export const CommentOnPostMeta$inboundSchema: z.ZodMiniType<
  CommentOnPostMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CommentOnPostCredits$inboundSchema),
});

export function commentOnPostMetaFromJSON(
  jsonString: string,
): SafeParseResult<CommentOnPostMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentOnPostMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentOnPostMeta' from JSON`,
  );
}

/** @internal */
export const CommentOnPostResponse$inboundSchema: z.ZodMiniType<
  CommentOnPostResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    commentUrn: types.optional(types.string()),
    fsdCommentUrn: types.optional(types.string()),
    duplicate: types.optional(types.boolean()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => CommentOnPostMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function commentOnPostResponseFromJSON(
  jsonString: string,
): SafeParseResult<CommentOnPostResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentOnPostResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentOnPostResponse' from JSON`,
  );
}

/** @internal */
export const LikePostReactionType$outboundSchema: z.ZodMiniEnum<
  typeof LikePostReactionType
> = z.enum(LikePostReactionType);

/** @internal */
export type LikePostRequest$Outbound = {
  postUrl: string;
  reactionType: string;
  companyId?: string | undefined;
  campaignSlug?: string | undefined;
  actionSlug?: string | undefined;
};

/** @internal */
export const LikePostRequest$outboundSchema: z.ZodMiniType<
  LikePostRequest$Outbound,
  LikePostRequest
> = z.object({
  postUrl: z.string(),
  reactionType: z._default(LikePostReactionType$outboundSchema, "LIKE"),
  companyId: z.optional(z.string()),
  campaignSlug: z.optional(z.string()),
  actionSlug: z.optional(z.string()),
});

export function likePostRequestToJSON(
  likePostRequest: LikePostRequest,
): string {
  return JSON.stringify(LikePostRequest$outboundSchema.parse(likePostRequest));
}

/** @internal */
export const LikePostCredits$inboundSchema: z.ZodMiniType<
  LikePostCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function likePostCreditsFromJSON(
  jsonString: string,
): SafeParseResult<LikePostCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikePostCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikePostCredits' from JSON`,
  );
}

/** @internal */
export const LikePostMeta$inboundSchema: z.ZodMiniType<LikePostMeta, unknown> =
  z.object({
    credits: z.lazy(() => LikePostCredits$inboundSchema),
  });

export function likePostMetaFromJSON(
  jsonString: string,
): SafeParseResult<LikePostMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikePostMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikePostMeta' from JSON`,
  );
}

/** @internal */
export const LikePostResponse$inboundSchema: z.ZodMiniType<
  LikePostResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    resourceKey: types.optional(types.string()),
    duplicate: types.optional(types.boolean()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => LikePostMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function likePostResponseFromJSON(
  jsonString: string,
): SafeParseResult<LikePostResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LikePostResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LikePostResponse' from JSON`,
  );
}

/** @internal */
export type ListSentInvitationsRequest$Outbound = {
  start: number;
  count: number;
};

/** @internal */
export const ListSentInvitationsRequest$outboundSchema: z.ZodMiniType<
  ListSentInvitationsRequest$Outbound,
  ListSentInvitationsRequest
> = z.object({
  start: z._default(z.int(), 0),
  count: z._default(z.int(), 10),
});

export function listSentInvitationsRequestToJSON(
  listSentInvitationsRequest: ListSentInvitationsRequest,
): string {
  return JSON.stringify(
    ListSentInvitationsRequest$outboundSchema.parse(listSentInvitationsRequest),
  );
}

/** @internal */
export const ToMember$inboundSchema: z.ZodMiniType<ToMember, unknown> = z
  .object({
    name: types.nullable(types.string()),
    headline: types.nullable(types.string()),
    profileUrl: types.nullable(types.string()),
    profilePicture: types.nullable(types.string()),
    publicIdentifier: types.nullable(types.string()),
    profileUrn: types.nullable(types.string()),
    memberId: types.nullable(types.string()),
  });

export function toMemberFromJSON(
  jsonString: string,
): SafeParseResult<ToMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToMember' from JSON`,
  );
}

/** @internal */
export const ListSentInvitationsInvitation$inboundSchema: z.ZodMiniType<
  ListSentInvitationsInvitation,
  unknown
> = z.object({
  invitationId: types.string(),
  invitationUrn: types.string(),
  entityUrn: types.string(),
  sentAt: types.nullable(types.string()),
  sentTime: types.nullable(types.number()),
  message: types.nullable(types.string()),
  targetProfileId: types.nullable(types.string()),
  targetFirstName: types.nullable(types.string()),
  targetLastName: types.nullable(types.string()),
  toMember: types.nullable(z.lazy(() => ToMember$inboundSchema)),
  invitationType: types.nullable(types.string()),
  mailboxItemId: types.nullable(types.string()),
  customMessage: types.boolean(),
  unseen: types.boolean(),
  mutualCurrentCompany: z.optional(z.nullable(z.any())),
  connectionDistance: z.optional(z.nullable(z.any())),
  inviterFollowingInvitee: types.boolean(),
});

export function listSentInvitationsInvitationFromJSON(
  jsonString: string,
): SafeParseResult<ListSentInvitationsInvitation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSentInvitationsInvitation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSentInvitationsInvitation' from JSON`,
  );
}

/** @internal */
export const ListSentInvitationsCredits$inboundSchema: z.ZodMiniType<
  ListSentInvitationsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listSentInvitationsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListSentInvitationsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSentInvitationsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSentInvitationsCredits' from JSON`,
  );
}

/** @internal */
export const ListSentInvitationsMeta$inboundSchema: z.ZodMiniType<
  ListSentInvitationsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListSentInvitationsCredits$inboundSchema),
});

export function listSentInvitationsMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListSentInvitationsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSentInvitationsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSentInvitationsMeta' from JSON`,
  );
}

/** @internal */
export const ListSentInvitationsResponse$inboundSchema: z.ZodMiniType<
  ListSentInvitationsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    invitations: z.array(
      z.lazy(() => ListSentInvitationsInvitation$inboundSchema),
    ),
    total: types.number(),
    start: types.number(),
    count: types.number(),
    warning: types.optional(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListSentInvitationsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listSentInvitationsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListSentInvitationsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListSentInvitationsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListSentInvitationsResponse' from JSON`,
  );
}
