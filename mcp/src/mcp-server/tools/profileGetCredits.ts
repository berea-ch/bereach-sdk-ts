/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { profileGetCredits } from "../../funcs/profileGetCredits.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$profileGetCredits: ToolDefinition = {
  name: "profile-get-credits",
  description: `Get current BeReach credit balance

Returns the current credit balance for the workspace. Includes credits used, total limit, remaining credits, and usage percentage. No credits consumed.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await profileGetCredits(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
