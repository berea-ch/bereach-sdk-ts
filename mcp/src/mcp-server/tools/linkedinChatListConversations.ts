/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinChatListConversations } from "../../funcs/linkedinChatListConversations.js";
import { ListInboxConversationsRequest$zodSchema } from "../../models/listinboxconversationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListInboxConversationsRequest$zodSchema,
};

export const tool$linkedinChatListConversations: ToolDefinition<typeof args> = {
  name: "linkedin-chat-list-conversations",
  description: `List LinkedIn inbox conversations

List inbox conversations for the authenticated user. Returns conversations with participants, last message, and read status. Paginate via nextCursor.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinChatListConversations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
