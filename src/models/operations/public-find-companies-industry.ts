/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  GetOwnPostsType,
  GetOwnPostsType$inboundSchema,
} from "./get-own-posts-type.js";

/**
 * Type of media attached to the post.
 */
export const GetOwnPostsMediaType = {
  Image: "image",
  Video: "video",
  Document: "document",
  Article: "article",
} as const;
/**
 * Type of media attached to the post.
 */
export type GetOwnPostsMediaType = OpenEnum<typeof GetOwnPostsMediaType>;

/**
 * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
 */
export type GetOwnPostsMedia = {
  /**
   * Type of media attached to the post.
   */
  type: GetOwnPostsMediaType;
  /**
   * Media URLs (image URLs for carousels, video streaming URL, article link, etc.).
   */
  urls: Array<string>;
  /**
   * Title of the article or document, when available.
   */
  title?: string | undefined;
  /**
   * Thumbnail URL for videos, articles, or document covers.
   */
  thumbnailUrl?: string | undefined;
};

export type GetOwnPostsReactionTypeCount = {
  reactionType: string;
  count: number;
};

export type GetOwnPostsPost = {
  /**
   * Public URL of the post.
   */
  postUrl: string;
  /**
   * Post text content.
   */
  text: string;
  /**
   * Post creation timestamp (Unix milliseconds).
   */
  date: number;
  /**
   * Total reactions on this post.
   */
  likesCount: number;
  /**
   * Total top-level comments on this post.
   */
  commentsCount: number;
  /**
   * Total shares/reposts of this post.
   */
  sharesCount: number;
  /**
   * LinkedIn internal URN for this post. Use with engagement tools (like, comment, collect_engagers).
   */
  postUrn: string;
  /**
   * Numeric post identifier parsed from the URN.
   */
  postId: string;
  /**
   * LinkedIn post type: 'ugcPost' = standard post, 'share' = native share/repost, 'activity' = legacy format.
   */
  type: GetOwnPostsType;
  /**
   * True when the post is a repost/reshare of another post. Absent for original posts.
   */
  isRepost?: boolean | undefined;
  /**
   * Media attached to the post (image, video, document, or article). Absent when the post is text-only.
   */
  media?: GetOwnPostsMedia | undefined;
  /**
   * Display name of the post's author.
   */
  authorName?: string | undefined;
  /**
   * Author's profile URL.
   */
  authorProfileUrl?: string | undefined;
  /**
   * Author's vanity slug, when they have one.
   */
  authorPublicIdentifier?: string | null | undefined;
  /**
   * Author's profile URN.
   */
  authorProfileUrn?: string | null | undefined;
  /**
   * Author's headline as shown on the post.
   */
  authorHeadline?: string | undefined;
  /**
   * Author's avatar URL.
   */
  authorImageUrl?: string | undefined;
  /**
   * On a repost, the ORIGINAL author's name when it differs from the resharer.
   */
  repostedFromName?: string | undefined;
  /**
   * Per-reaction-type breakdown, when LinkedIn exposes it.
   */
  reactionTypeCounts?: Array<GetOwnPostsReactionTypeCount> | undefined;
  /**
   * Total impressions, exposed only on Creator-mode posts.
   */
  numImpressions?: number | null | undefined;
  /**
   * Social-proof attribution, when LinkedIn surfaces one in the feed.
   */
  highlightedReactorName?: string | null | undefined;
  /**
   * Whether the authenticated account has reacted to this post.
   */
  viewerLiked?: boolean | null | undefined;
};

export type GetOwnPostsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetOwnPostsMeta = {
  credits: GetOwnPostsCredits;
};

/**
 * The user's own posts
 */
export type GetOwnPostsResponse = {
  success: true;
  posts: Array<GetOwnPostsPost>;
  /**
   * Posts in THIS page.
   */
  count: number;
  /**
   * Posts LinkedIn reports in total.
   */
  total: number;
  /**
   * Offset this page started at.
   */
  start: number;
  hasMore: boolean;
  /**
   * Cursor for the next page, or null on the last one.
   */
  paginationToken: string | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetOwnPostsMeta | undefined;
};

export type User = {
  id: string;
  name: string | null;
  email: string | null;
  image: string | null;
  phone: string | null;
  createdAt: string;
};

export type GetSettingsLocation = {
  country: string | null;
  city: string | null;
};

export type Proxy = {
  enabled: boolean;
  /**
   * Which pool the account egresses through.
   */
  mode?: string | undefined;
  /**
   * Exit country, ISO alpha-2, uppercase on this surface.
   */
  country?: string | undefined;
  /**
   * How long an exit is held before rotating.
   */
  rotationHours?: number | undefined;
};

export type Subscription = {
  plan: string;
  tier: string;
  proSeatsIncluded: number;
  proSeatsUsed: number;
  status: string | null;
  currentPeriodEnd: string | null;
  hasStripe: boolean;
  trialUsed?: boolean | undefined;
  trialEndsAt?: string | null | undefined;
  /**
   * Whether this plan generation includes AI. No longer affects the credit pool size (every tier gets one pool regardless); this is an entitlement/generation marker only.
   */
  aiIncluded?: boolean | undefined;
  isUnlimited?: boolean | undefined;
  /**
   * Whether the caller owns the workspace (the billing payer), as opposed to being a member of it. Gates billing UI only.
   */
  isOwner?: boolean | undefined;
  /**
   * Whether the caller can see and act as every LinkedIn account in the workspace (AccountPicker + acting-as) — true for the owner, or any member with workspaceRole="admin" (the default).
   */
  canManageAllAccounts?: boolean | undefined;
  /**
   * Scales the daily and weekly action caps.
   */
  limitMultiplier?: number | undefined;
  referralBoostActive?: boolean | undefined;
  referralBoostExpiresAt?: string | null | undefined;
};

export type LinkedinAccountProxy = {
  enabled: boolean;
  /**
   * Which pool the account egresses through.
   */
  mode?: string | undefined;
  /**
   * Exit country, ISO alpha-2, uppercase on this surface.
   */
  country?: string | undefined;
  /**
   * How long an exit is held before rotating.
   */
  rotationHours?: number | undefined;
};

export type ApiKey = {
  partialKey: string;
  createdAt: string;
};

export type LinkedinAccount = {
  id: string;
  label: string | null;
  isDefault: boolean;
  linkedinName: string | null;
  linkedinEmail: string | null;
  profilePic: string | null;
  profileUrl: string | null;
  headline: string | null;
  isValid: boolean;
  isActive: boolean;
  validationError: string | null;
  accountPlan: string | null;
  isUnlimited: boolean;
  /**
   * True when this account belongs to the caller rather than to another member of the workspace.
   */
  ownedByCaller?: boolean | undefined;
  proxy: LinkedinAccountProxy;
  apiKey: ApiKey | null;
  createdAt: string;
};

export type PendingInvite = {
  id: string;
  email: string | null;
  name: string | null;
  code: string;
  maxUses: number;
  useCount: number;
  expiresAt: string | null;
  createdAt: string;
};

export const Role = {
  Admin: "admin",
  Member: "member",
} as const;
export type Role = OpenEnum<typeof Role>;

export type WorkspaceMember = {
  id: string;
  name: string | null;
  email: string | null;
  isOwner: boolean;
  role: Role;
};

/**
 * Account settings
 */
export type GetSettingsResponse = {
  success: true;
  /**
   * Whether this workspace has BeReach-staff admin-panel access. The SAME value for every member of a workspace — not a per-member role.
   */
  isAdmin: boolean;
  user: User;
  location: GetSettingsLocation;
  proxy: Proxy;
  subscription: Subscription;
  linkedinAccounts: Array<LinkedinAccount>;
  pendingInvites: Array<PendingInvite>;
  workspaceMembers: Array<WorkspaceMember>;
};

export type PatchSettingsRequest = {
  /**
   * ISO country code
   */
  country?: string | undefined;
  city?: string | undefined;
  phone?: string | null | undefined;
};

/**
 * Settings updated
 */
export type PatchSettingsResponse = {
  success: true;
};

export type GetApiTokenRequest = {
  /**
   * LinkedIn credentials ID (defaults to current account)
   */
  accountId?: string | undefined;
};

/**
 * Token returned
 */
export type GetApiTokenResponse = {
  token: string | null;
  partialKey?: string | undefined;
  createdAt?: string | undefined;
  accountId?: string | undefined;
};

export type CreateApiTokenRequest = {
  /**
   * LinkedIn credentials ID (defaults to current account)
   */
  accountId?: string | undefined;
};

/**
 * Token created
 */
export type CreateApiTokenResponse = {
  success: true;
  /**
   * Full API token (brc_...) - only shown once
   */
  token: string;
  partialKey: string;
  accountId: string;
};

export type DeleteApiTokenRequest = {
  /**
   * LinkedIn credentials ID (defaults to current account)
   */
  accountId?: string | undefined;
};

/**
 * Token deleted
 */
export type DeleteApiTokenResponse = {
  success: true;
};

export type Page = {
  /**
   * LinkedIn numeric company ID.
   */
  id: string;
  /**
   * Company URL slug (e.g. 'openai').
   */
  universalName: string;
  name: string;
  url: string;
  logoUrl: string | null;
  followerCount: number | null;
  industry: string | null;
  employeeCount: number | null;
  /**
   * Admin role (e.g. 'ADMINISTRATOR', 'CONTENT_ADMIN'). Null if role is unknown.
   */
  adminRole: string | null;
};

export type ListCompanyPagesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ListCompanyPagesMeta = {
  credits: ListCompanyPagesCredits;
};

/**
 * List of managed company pages
 */
export type ListCompanyPagesResponse = {
  success: true;
  pages: Array<Page>;
  total: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ListCompanyPagesMeta | undefined;
};

export type CompanyPagePostsRequest = {
  /**
   * Company URL slug (e.g. 'openai'). Same value as returned by listCompanyPages.
   */
  universalName: string;
  /**
   * Number of posts to return (max 50).
   */
  count?: number | undefined;
  /**
   * Pagination offset.
   */
  start?: number | undefined;
};

export type CompanyPagePostsPost = {
  /**
   * LinkedIn activity URN for this post.
   */
  activityUrn: string;
  /**
   * Direct URL to the post on LinkedIn.
   */
  postUrl: string;
  /**
   * Post text content (null if media-only).
   */
  text: string | null;
  /**
   * Unix timestamp in milliseconds (null if unavailable).
   */
  publishedAt: number | null;
  /**
   * Number of likes/reactions.
   */
  numLikes: number | null;
  /**
   * Number of comments.
   */
  numComments: number | null;
};

export type CompanyPagePostsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CompanyPagePostsMeta = {
  credits: CompanyPagePostsCredits;
};

/**
 * Company page posts
 */
export type CompanyPagePostsResponse = {
  success: true;
  posts: Array<CompanyPagePostsPost>;
  /**
   * Total number of posts available.
   */
  total: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CompanyPagePostsMeta | undefined;
};

export type CompanyPagePermissionsRequest = {
  /**
   * Company URL slug (e.g. 'openai'). Same value as returned by listCompanyPages.
   */
  universalName: string;
};

export type Permissions = {
  /**
   * Admin role (e.g. 'SUPER_ADMINISTRATOR', 'ADMINISTRATOR', 'CONTENT_ADMIN').
   */
  role: string | null;
  /**
   * Can post as the company page.
   */
  canCreateOrganicShare: boolean;
  /**
   * Can comment as the company page.
   */
  canCreateComment: boolean;
  /**
   * Can like/react as the company page.
   */
  canCreateReaction: boolean;
  /**
   * Can delete company page posts.
   */
  canDeleteShare: boolean;
  /**
   * Can pin posts on the company page.
   */
  canPinShare: boolean;
  /**
   * Can send messages from the company page inbox.
   */
  canSendMessages: boolean;
  /**
   * Can read the company page inbox.
   */
  canReadMessages: boolean;
  /**
   * Can access the admin analytics dashboard.
   */
  canReadAdminDashboard: boolean;
  /**
   * Can view post/update analytics.
   */
  canReadOrganizationUpdateAnalytics: boolean;
  /**
   * Can view visitor analytics.
   */
  canReadOrganizationVisitorAnalytics: boolean;
  /**
   * Can view follower analytics.
   */
  canReadOrganizationFollowerAnalytics: boolean;
  /**
   * Can view search appearance analytics.
   */
  canReadOrganizationSearchAppearanceAnalytics: boolean;
  /**
   * Can edit the company page profile.
   */
  canUpdateOrganizationProfile: boolean;
  /**
   * Can add/remove page admins.
   */
  canEditAdministrators: boolean;
  /**
   * Can deactivate the company page.
   */
  canDeactivateOrganization: boolean;
  /**
   * Can create/edit company events.
   */
  canEditEvents: boolean;
  /**
   * Can view company events.
   */
  canReadEvents: boolean;
  /**
   * Can sponsor/boost posts.
   */
  canSponsorShare: boolean;
  /**
   * Can manage the career page section.
   */
  canManageCareerPages: boolean;
  /**
   * Can manage messaging settings.
   */
  canManageMessagingAccess: boolean;
  /**
   * Can notify employees about posts.
   */
  canNotifyEmployees: boolean;
  /**
   * Can export lead-gen form leads.
   */
  canExportLeads: boolean;
};

export type CompanyPagePermissionsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CompanyPagePermissionsMeta = {
  credits: CompanyPagePermissionsCredits;
};

/**
 * Company page admin permissions
 */
export type CompanyPagePermissionsResponse = {
  success: true;
  universalName: string;
  permissions: Permissions;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CompanyPagePermissionsMeta | undefined;
};

export type CompanyPageAnalyticsRequest = {
  /**
   * Company page ID (numeric, e.g. '111652438')
   */
  companyId: string;
};

export type FoundedOn = {
  year: number;
  month?: number | undefined;
  day?: number | undefined;
};

export type Headquarter = {
  city: string;
  country: string;
  line1?: string | undefined;
  postalCode?: string | undefined;
};

export type Analytics = {
  name: string;
  universalName: string;
  followerCount: number | null;
  employeeCount: number | null;
  visitorsInPastMonthCount: number | null;
  isActive: boolean;
  foundedOn: FoundedOn | null;
  headquarter: Headquarter | null;
  description: string | null;
  tagline: string | null;
  websiteUrl: string | null;
  organizationType: string | null;
  specialities: Array<string>;
  industryUrns: Array<string>;
};

export type CompanyPageAnalyticsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CompanyPageAnalyticsMeta = {
  credits: CompanyPageAnalyticsCredits;
};

/**
 * Company page overview analytics
 */
export type CompanyPageAnalyticsResponse = {
  success: true;
  analytics: Analytics;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CompanyPageAnalyticsMeta | undefined;
};

export type InboxListRequest = {
  /**
   * Pagination cursor from a previous response
   */
  nextCursor?: string | undefined;
  /**
   * Number of conversations to return (default 20, max 25)
   */
  count?: number | undefined;
};

export type InboxListParticipant = {
  profileUrn: string;
  firstName: string;
  lastName: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  isPremium?: boolean | undefined;
  /**
   * Identity-verified profile.
   */
  isVerified?: boolean | undefined;
  /**
   * Niche-authority badge, when LinkedIn attaches one.
   */
  memberBadgeType?: string | null | undefined;
  /**
   * Raw identity urn. CAN BE A COMPANY rather than a person, which is what a page-admin reply looks like from this side.
   */
  participantHostUrn?: string | null | undefined;
};

export type InboxListLastMessage = {
  text: string | null;
  deliveredAt: number;
  senderProfileUrn: string;
};

export type InboxListConversation = {
  conversationUrn: string;
  conversationUrl: string;
  lastActivityAt: number;
  createdAt: number;
  read: boolean;
  unreadCount: number;
  groupChat: boolean;
  participants: Array<InboxListParticipant>;
  lastMessage: InboxListLastMessage | null;
  /**
   * Epoch ms this account last read the thread.
   */
  lastReadAt?: number | null | undefined;
  /**
   * Whether the thread is active, muted or snoozed for this account.
   */
  notificationStatus?: string | null | undefined;
};

export type InboxListCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type InboxListMeta = {
  credits: InboxListCredits;
};

/**
 * Inbox conversations
 */
export type InboxListResponse = {
  success: true;
  conversations: Array<InboxListConversation>;
  /**
   * Cursor for fetching next page
   */
  nextCursor: string | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: InboxListMeta | undefined;
};

export type InboxSearchRequest = {
  /**
   * Search keywords
   */
  keywords: string;
  /**
   * Pagination cursor from a previous response
   */
  nextCursor?: string | undefined;
};

export type InboxSearchParticipant = {
  profileUrn: string;
  firstName: string;
  lastName: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  isPremium?: boolean | undefined;
  /**
   * Identity-verified profile.
   */
  isVerified?: boolean | undefined;
  /**
   * Niche-authority badge, when LinkedIn attaches one.
   */
  memberBadgeType?: string | null | undefined;
  /**
   * Raw identity urn. CAN BE A COMPANY rather than a person, which is what a page-admin reply looks like from this side.
   */
  participantHostUrn?: string | null | undefined;
};

export type InboxSearchLastMessage = {
  text: string | null;
  deliveredAt: number;
  senderProfileUrn: string;
};

export type InboxSearchConversation = {
  conversationUrn: string;
  conversationUrl: string;
  lastActivityAt: number;
  createdAt: number;
  read: boolean;
  unreadCount: number;
  groupChat: boolean;
  participants: Array<InboxSearchParticipant>;
  lastMessage: InboxSearchLastMessage | null;
  /**
   * Epoch ms this account last read the thread.
   */
  lastReadAt?: number | null | undefined;
  /**
   * Whether the thread is active, muted or snoozed for this account.
   */
  notificationStatus?: string | null | undefined;
};

export type InboxSearchCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type InboxSearchMeta = {
  credits: InboxSearchCredits;
};

/**
 * Matching conversations
 */
export type InboxSearchResponse = {
  success: true;
  conversations: Array<InboxSearchConversation>;
  /**
   * Cursor for fetching next page
   */
  nextCursor: string | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: InboxSearchMeta | undefined;
};

export type FindConversationRequest = {
  /**
   * Accepts full profile URLs, vanity names (e.g. marie-sandre), or profile URNs.
   */
  profile: string;
  /**
   * If true, also return the conversation's recent messages.
   */
  includeMessages?: boolean | undefined;
};

/**
 * Lightweight conversation reference (O(1) lookup returns URN only, not full details).
 */
export type FindConversationConversation = {
  conversationUrn: string;
};

export type FindConversationSender = {
  firstName: string;
  lastName: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  profileUrn: string | null;
};

export const FindConversationType = {
  Audio: "audio",
  SharedPost: "shared_post",
  RepliedMessage: "replied_message",
  Image: "image",
  File: "file",
  Unknown: "unknown",
} as const;
export type FindConversationType = OpenEnum<typeof FindConversationType>;

export type FindConversationAttachment = {
  type: FindConversationType;
  audioUrl?: string | undefined;
  audioDuration?: number | undefined;
  hostUrn?: string | undefined;
  repliedText?: string | undefined;
  repliedSenderName?: string | undefined;
  imageUrl?: string | undefined;
  fileName?: string | undefined;
  fileUrl?: string | undefined;
};

export type FindConversationMessage = {
  messageUrn: string;
  text: string | null;
  deliveredAt: number;
  senderProfileUrn: string;
  sender: FindConversationSender;
  attachments: Array<FindConversationAttachment>;
  /**
   * True if the authenticated user sent this message.
   */
  isOutbound: boolean;
};

export type FindConversationCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type FindConversationMeta = {
  credits: FindConversationCredits;
};

/**
 * Conversation found (or not)
 */
export type FindConversationResponse = {
  success: true;
  found: boolean;
  /**
   * Lightweight conversation reference (O(1) lookup returns URN only, not full details).
   */
  conversation: FindConversationConversation | null;
  messages: Array<FindConversationMessage> | null;
  /**
   * deliveredAt timestamp (ms) of the oldest message — pass as 'deliveredAt' to load older messages. Only present when includeMessages is true.
   */
  prevCursor?: number | null | undefined;
  /**
   * The authenticated user's profile URN. Useful for determining message direction.
   */
  userProfileUrn?: string | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: FindConversationMeta | undefined;
};

export type InboxMessagesRequest = {
  /**
   * Full conversation URN as returned by /chats/linkedin (e.g. 'urn:li:msg_conversation:(urn:li:fsd_profile:ACoAAXXX,2-YWUx...)')
   */
  conversationUrn: string;
  /**
   * Timestamp (ms) of the oldest message from previous page — pass this to load older messages
   */
  deliveredAt?: number | undefined;
};

export type InboxMessagesSender = {
  firstName: string;
  lastName: string;
  profileUrl: string | null;
  headline: string | null;
  profilePicture: string | null;
  publicIdentifier: string | null;
  profileUrn: string | null;
};

export const InboxMessagesType = {
  Audio: "audio",
  SharedPost: "shared_post",
  RepliedMessage: "replied_message",
  Image: "image",
  File: "file",
  Unknown: "unknown",
} as const;
export type InboxMessagesType = OpenEnum<typeof InboxMessagesType>;

export type InboxMessagesAttachment = {
  type: InboxMessagesType;
  audioUrl?: string | undefined;
  audioDuration?: number | undefined;
  hostUrn?: string | undefined;
  repliedText?: string | undefined;
  repliedSenderName?: string | undefined;
  imageUrl?: string | undefined;
  fileName?: string | undefined;
  fileUrl?: string | undefined;
};

export type InboxMessagesMessage = {
  messageUrn: string;
  text: string | null;
  deliveredAt: number;
  senderProfileUrn: string;
  sender: InboxMessagesSender;
  attachments: Array<InboxMessagesAttachment>;
  /**
   * True if the authenticated user sent this message.
   */
  isOutbound: boolean;
};

export type InboxMessagesCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type InboxMessagesMeta = {
  credits: InboxMessagesCredits;
};

/**
 * Conversation messages
 */
export type InboxMessagesResponse = {
  success: true;
  messages: Array<InboxMessagesMessage>;
  /**
   * deliveredAt timestamp (ms) of the oldest message — pass as 'deliveredAt' to load older messages. Null when no more messages.
   */
  prevCursor: number | null;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: InboxMessagesMeta | undefined;
};

export type MarkSeenRequest = {
  /**
   * Full conversation URN (e.g. 'urn:li:msg_conversation:(urn:li:fsd_profile:ACoAAXXX,2-YWUx...)')
   */
  conversationUrn: string;
};

export type MarkSeenCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type MarkSeenMeta = {
  credits: MarkSeenCredits;
};

/**
 * Conversation marked as read
 */
export type MarkSeenResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: MarkSeenMeta | undefined;
};

export type MarkAllReadRequest = {};

export type MarkAllReadCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type MarkAllReadMeta = {
  credits: MarkAllReadCredits;
};

/**
 * All conversations marked as read
 */
export type MarkAllReadResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: MarkAllReadMeta | undefined;
};

export type InboxUnreadCountCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type InboxUnreadCountMeta = {
  credits: InboxUnreadCountCredits;
};

/**
 * Unread count
 */
export type InboxUnreadCountResponse = {
  success: true;
  /**
   * Number of unread conversations/messages
   */
  unreadCount: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: InboxUnreadCountMeta | undefined;
};

export type GetConversationSummaryRequest = {
  /**
   * LinkedIn profile URL or vanity name of the contact
   */
  profile: string;
};

export type GetConversationSummaryCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetConversationSummaryMeta = {
  credits: GetConversationSummaryCredits;
};

/**
 * Summary retrieved
 */
export type GetConversationSummaryResponse = {
  success: true;
  /**
   * Whether a contact with a saved summary was found
   */
  found: boolean;
  /**
   * Internal contact ID — present only when found is true
   */
  contactId?: string | undefined;
  /**
   * The saved conversation summary text, null if none exists
   */
  summary: string | null;
  /**
   * ISO timestamp of when the summary was last saved — present only when found is true
   */
  summarizedAt?: string | null | undefined;
  /**
   * Message count at the time of summarization — present only when found is true
   */
  messageCount?: number | null | undefined;
  /**
   * Timestamp of last conversation data update — present only when found is true
   */
  conversationUpdatedAt?: string | null | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetConversationSummaryMeta | undefined;
};

export type SaveConversationSummaryRequest = {
  /**
   * LinkedIn profile URL or vanity name of the contact
   */
  profile: string;
  /**
   * Conversation summary text to save
   */
  summary: string;
};

export type SaveConversationSummaryCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type SaveConversationSummaryMeta = {
  credits: SaveConversationSummaryCredits;
};

/**
 * Summary saved
 */
export type SaveConversationSummaryResponse = {
  success: true;
  /**
   * Internal contact ID (created if not found)
   */
  contactId: string;
  /**
   * ISO timestamp of when the summary was saved
   */
  summarizedAt: string;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: SaveConversationSummaryMeta | undefined;
};

export type PublicProfileRequest = {
  /**
   * Full public profile URL. Provide this or slug.
   */
  profileUrl?: string | undefined;
  /**
   * Profile vanity slug. Provide this or profileUrl.
   */
  slug?: string | undefined;
  /**
   * ISO alpha-2 country code for the proxy exit.
   */
  country?: string | undefined;
  /**
   * When true, the fetched profile is saved to your contacts.
   */
  save?: boolean | undefined;
};

export const PublicProfileSource = {
  Public: "public",
} as const;
export type PublicProfileSource = ClosedEnum<typeof PublicProfileSource>;

/**
 * Public profile data
 */
export type PublicProfileResponse = {
  source: PublicProfileSource;
  /**
   * How complete the fetched public profile is.
   */
  completeness: string;
  /**
   * Whether the profile was saved to your contacts.
   */
  saved: boolean;
  /**
   * The public profile data.
   */
  profile: { [k: string]: any };
};

/**
 * Role or job title facet. One value or a list of alternatives.
 */
export type PublicFindPeopleTitle = string | Array<string>;

/**
 * City, region, or country facet. One value or a list of alternatives.
 */
export type PublicFindPeopleLocation = string | Array<string>;

/**
 * Company name facet. One value or a list of alternatives.
 */
export type PublicFindPeopleCompanyUnion = string | Array<string>;

/**
 * Industry facet. One value or a list of alternatives.
 */
export type PublicFindPeopleIndustry = string | Array<string>;

/**
 * Seniority facet. One value or a list of alternatives.
 */
export type PublicFindPeopleSeniority = string | Array<string>;

/**
 * Search facets. Provide at least one of title, location, company, industry, or keywords.
 */
export type PublicFindPeopleFilters = {
  /**
   * Role or job title facet. One value or a list of alternatives.
   */
  title?: string | Array<string> | undefined;
  /**
   * City, region, or country facet. One value or a list of alternatives.
   */
  location?: string | Array<string> | undefined;
  /**
   * Company name facet. One value or a list of alternatives.
   */
  company?: string | Array<string> | undefined;
  /**
   * Industry facet. One value or a list of alternatives.
   */
  industry?: string | Array<string> | undefined;
  /**
   * Seniority facet. One value or a list of alternatives.
   */
  seniority?: string | Array<string> | undefined;
  /**
   * Free-text keywords appended to the search query.
   */
  keywords?: string | undefined;
  /**
   * ISO alpha-2 country code used to scope results to one country.
   */
  countryCode?: string | undefined;
};

/**
 * keep (the default) when the people are the list being built, skip only when this search is certainly a step toward one person's posts or audience or a check of who somebody is, ask when unsure; narrow first rather than keep then remove; report what was actually written.
 */
export const Save = {
  Keep: "keep",
  Skip: "skip",
  Ask: "ask",
} as const;
/**
 * keep (the default) when the people are the list being built, skip only when this search is certainly a step toward one person's posts or audience or a check of who somebody is, ask when unsure; narrow first rather than keep then remove; report what was actually written.
 */
export type Save = ClosedEnum<typeof Save>;

/**
 * Turns on a two-step company-discovery search, find matching companies by industry or location, then search the role inside each, for when no company is named yet; once companies are named, use filters.company instead.
 */
export type Chain = {
  /**
   * Role searched inside each matched company.
   */
  role: string;
  /**
   * How many companies to search inside.
   */
  companiesLimit?: number | undefined;
};

/**
 * Company-headcount constraint. Not searchable here: echoed under notSearched, results run wider.
 */
export type CompanySize = string | Array<string>;

/**
 * Network-degree constraint. Not searchable here: echoed under notSearched, results run wider.
 */
export type PublicFindPeopleConnectionDegree = string | Array<string>;

/**
 * School as a structured filter (word-matching goes in filters.keywords instead). Not searchable here: echoed under notSearched.
 */
export type School = string | Array<string>;

/**
 * Past employer as an employment record. Not searchable here: echoed under notSearched.
 */
export type PastCompanies = string | Array<string>;

/**
 * Profile-language constraint. Not searchable here: echoed under notSearched.
 */
export type ProfileLanguage = string | Array<string>;

/**
 * Company-headquarters constraint. Not searchable here: echoed under notSearched.
 */
export type CompanyHQ = string | Array<string>;

/**
 * Follows-this-creator constraint. Not searchable here: echoed under notSearched.
 */
export type FollowersOf = string | Array<string>;

/**
 * Profile URLs or slugs to leave out, from rows already seen. Applied before any per-person fetch.
 */
export type ExcludeProfiles = string | Array<string>;

/**
 * Words or phrases that disqualify a row when they appear in its title or headline. Applied before any per-person fetch where possible.
 */
export type ExcludeWords = string | Array<string>;

export type PublicFindPeopleRequest = {
  /**
   * Search facets. Provide at least one of title, location, company, industry, or keywords.
   */
  filters: PublicFindPeopleFilters;
  /**
   * How many NEW profiles to fetch; already-saved people the search also finds are added on top at no cost, so the result can run over this number without the limit having been ignored.
   */
  limit?: number | undefined;
  /**
   * keep (the default) when the people are the list being built, skip only when this search is certainly a step toward one person's posts or audience or a check of who somebody is, ask when unsure; narrow first rather than keep then remove; report what was actually written.
   */
  save?: Save | undefined;
  /**
   * Turns on a two-step company-discovery search, find matching companies by industry or location, then search the role inside each, for when no company is named yet; once companies are named, use filters.company instead.
   */
  chain?: Chain | undefined;
  /**
   * Continue an earlier search on these same filters instead of returning its people again.
   */
  more?: boolean | undefined;
  /**
   * The person's own words, sent only when they asked for somebody in particular rather than a crowd: a named person, a role at a named organisation, everyone there, or people who used to work there. Always sent alongside the facets, never instead of them, so the search can answer the named question directly rather than slicing a market that has no such answer.
   */
  sentence?: string | undefined;
  /**
   * Company-headcount constraint. Not searchable here: echoed under notSearched, results run wider.
   */
  companySize?: string | Array<string> | undefined;
  /**
   * Network-degree constraint. Not searchable here: echoed under notSearched, results run wider.
   */
  connectionDegree?: string | Array<string> | undefined;
  /**
   * Verified-identity constraint. Not searchable here: echoed under notSearched.
   */
  verified?: boolean | undefined;
  /**
   * School as a structured filter (word-matching goes in filters.keywords instead). Not searchable here: echoed under notSearched.
   */
  school?: string | Array<string> | undefined;
  /**
   * Past employer as an employment record. Not searchable here: echoed under notSearched.
   */
  pastCompanies?: string | Array<string> | undefined;
  /**
   * Profile-language constraint. Not searchable here: echoed under notSearched.
   */
  profileLanguage?: string | Array<string> | undefined;
  /**
   * Company-headquarters constraint. Not searchable here: echoed under notSearched.
   */
  companyHQ?: string | Array<string> | undefined;
  /**
   * Follows-this-creator constraint. Not searchable here: echoed under notSearched.
   */
  followersOf?: string | Array<string> | undefined;
  /**
   * Profile URLs or slugs to leave out, from rows already seen. Applied before any per-person fetch.
   */
  excludeProfiles?: string | Array<string> | undefined;
  /**
   * Words or phrases that disqualify a row when they appear in its title or headline. Applied before any per-person fetch where possible.
   */
  excludeWords?: string | Array<string> | undefined;
  /**
   * Approval to run this search through the user's connected LinkedIn account when a facet needs it. Never pass true on your own: the result's needs quote is relayed to the user first, and their agreement in their next message is what this flag carries.
   */
  approveLinkedInSpend?: boolean | undefined;
};

export const PublicFindPeopleSource = {
  Public: "public",
} as const;
export type PublicFindPeopleSource = ClosedEnum<typeof PublicFindPeopleSource>;

/**
 * Set when the request could not be searched at all. 'nothing-to-search' means nothing in it named anybody. 'wants-companies' means it asked for organisations, which this operation does not find: use the company search instead. Neither is a retry: say what happened and change the request.
 */
export const Refused = {
  NothingToSearch: "nothing-to-search",
  WantsCompanies: "wants-companies",
} as const;
/**
 * Set when the request could not be searched at all. 'nothing-to-search' means nothing in it named anybody. 'wants-companies' means it asked for organisations, which this operation does not find: use the company search instead. Neither is a retry: say what happened and change the request.
 */
export type Refused = OpenEnum<typeof Refused>;

/**
 * Finer than moreAvailable: 'axes-exhausted' means every search angle for this audience has genuinely been tried, calling again with more will not find anybody new. 'batch-failed' means this attempt could not reach the search provider, worth retrying. 'more-available' is the ordinary case.
 */
export const Exhaustion = {
  MoreAvailable: "more-available",
  AxesExhausted: "axes-exhausted",
  BatchFailed: "batch-failed",
} as const;
/**
 * Finer than moreAvailable: 'axes-exhausted' means every search angle for this audience has genuinely been tried, calling again with more will not find anybody new. 'batch-failed' means this attempt could not reach the search provider, worth retrying. 'more-available' is the ordinary case.
 */
export type Exhaustion = OpenEnum<typeof Exhaustion>;

export type AxesTried = {
  /**
   * Which search angle this row covers, e.g. title, city, school, tool.
   */
  axis: string;
  /**
   * How many searches this angle could run in total.
   */
  queriesPlanned: number;
  /**
   * How many of those have actually run so far.
   */
  attempted: number;
  /**
   * New people this angle has contributed.
   */
  yielded: number;
  /**
   * Whether this angle has nothing left to try.
   */
  exhausted: boolean;
};

export type AxesAvailable = {
  /**
   * A search angle this audience supports, e.g. title, city, school, tool.
   */
  axis: string;
  /**
   * How many searches that angle could run in total.
   */
  queries: number;
};

/**
 * How likely this country/title combination is to return false-positive matches from elsewhere.
 */
export const Risk = {
  Low: "low",
  Medium: "medium",
  High: "high",
} as const;
/**
 * How likely this country/title combination is to return false-positive matches from elsewhere.
 */
export type Risk = OpenEnum<typeof Risk>;

/**
 * A non-blocking caveat about this market, e.g. a country whose profiles are hard to tell apart from a bigger anglophone one. Never a reason to withhold or shrink the result — surface it as a caution alongside the real count, never in place of one.
 */
export type CountryRiskNote = {
  /**
   * How likely this country/title combination is to return false-positive matches from elsewhere.
   */
  risk: Risk;
  /**
   * Why, in plain language.
   */
  reason: string;
};

/**
 * How far the running search has gotten, alongside stillRunning or partial.
 */
export type Progress = {
  peopleFound?: number | undefined;
  queriesRun?: number | undefined;
  [additionalProperties: string]: unknown;
};

export type PublicFindPeopleCompanyResponse = {
  name?: string | null | undefined;
  url?: string | null | undefined;
  [additionalProperties: string]: unknown;
};

export type NotSearched = {
  /**
   * The constraint that could not be applied.
   */
  field: string;
  /**
   * What was asked for.
   */
  value: string;
  /**
   * Why it could not be applied, in plain words.
   */
  why: string;
  /**
   * What that does to the results: wider than asked, never different.
   */
  effect: string;
};

/**
 * What the save choice actually did, so claims about saving cite this rather than the request that was sent.
 */
export type Saved = {
  /**
   * People written to the contact list by THIS call.
   */
  count: number;
  /**
   * The caller was not sure whether to keep these people, so nobody was written and the person is offered the choice on the result itself. Do not tell them anything was kept, and do not ask them again in words.
   */
  decide?: boolean | undefined;
};

export type Ambiguity = {
  /**
   * The organisation name that is ambiguous.
   */
  name: string;
  /**
   * What to ask before trusting a single row.
   */
  note: string;
};

export const PublicFindPeopleKind = {
  LinkedinSpend: "linkedin_spend",
} as const;
export type PublicFindPeopleKind = ClosedEnum<typeof PublicFindPeopleKind>;

export type Because = {
  field: string;
  value: string;
};

/**
 * What approving would spend, in LinkedIn searches.
 */
export type Estimate = {
  linkedinSearches: number;
};

/**
 * A quote, not an answer: this ask carries facets only the connected account can honour, and nothing was run or spent. Relay it plainly, ask the user, and repeat the call with approveLinkedInSpend true only after they agree. contacts is empty alongside this by construction.
 */
export type Needs = {
  kind: PublicFindPeopleKind;
  /**
   * The facets that need the connected account.
   */
  because: Array<Because>;
  /**
   * What approving would spend, in LinkedIn searches.
   */
  estimate: Estimate;
  /**
   * The no-spend path, ready to relay.
   */
  alternative: string;
};

/**
 * Premium cohorts run both lanes in parallel and merge: the public sweep plus one exact account search, every account row marked lane linkedin. Present when the account arm added people; both spends are receipted.
 */
export type Union = {
  /**
   * Rows the connected-account arm added beyond what the public sweep found.
   */
  added: number;
  /**
   * LinkedIn searches the arm spent.
   */
  linkedinSearches: number;
};

/**
 * Who approved the account-lane run: the user's explicit yes, or the account's LinkedIn Premium (no monthly search cap, so exact runs directly). Relay which.
 */
export const ApprovedBy = {
  User: "user",
  Premium: "premium",
} as const;
/**
 * Who approved the account-lane run: the user's explicit yes, or the account's LinkedIn Premium (no monthly search cap, so exact runs directly). Relay which.
 */
export type ApprovedBy = OpenEnum<typeof ApprovedBy>;

/**
 * Present when this row came from the user's connected LinkedIn account on an approved run: exact employment-record data rather than public page text.
 */
export const PublicFindPeopleLane = {
  Linkedin: "linkedin",
} as const;
/**
 * Present when this row came from the user's connected LinkedIn account on an approved run: exact employment-record data rather than public page text.
 */
export type PublicFindPeopleLane = ClosedEnum<typeof PublicFindPeopleLane>;

export type PublicFindPeoplePositionStartDate = {
  year: number;
};

export type PublicFindPeoplePositionEndDate = {
  year: number;
};

export type PublicFindPeoplePosition = {
  companyName: string;
  title: string | null;
  companyUrl: string | null;
  startDate: PublicFindPeoplePositionStartDate | null;
  endDate: PublicFindPeoplePositionEndDate | null;
  isCurrent: boolean;
};

export type PublicFindPeopleEducationStartDate = {
  year: number;
};

export type PublicFindPeopleEducationEndDate = {
  year: number;
};

export type PublicFindPeopleEducation = {
  schoolName: string;
  schoolUrl: string | null;
  startDate: PublicFindPeopleEducationStartDate | null;
  endDate: PublicFindPeopleEducationEndDate | null;
};

export type RecentPost = {
  url: string | null;
  text: string | null;
  likes: number | null;
  datePublished: string | null;
};

export type PublicFindPeopleContact = {
  name: string | null;
  headline: string | null;
  company: string | null;
  location: string | null;
  profileUrl: string | null;
  completeness: string;
  /**
   * Present when this row came from the user's connected LinkedIn account on an approved run: exact employment-record data rather than public page text.
   */
  lane?: PublicFindPeopleLane | undefined;
  /**
   * The profile's About text, when the public page carried one.
   */
  summary?: string | undefined;
  /**
   * Connection count, when the public page carried one.
   */
  connections?: number | undefined;
  /**
   * The profile's display photo (from the public page's og:image), when the public page carried one. A raw LinkedIn CDN URL — may expire over time, unlike a re-hosted avatar.
   */
  imageUrl?: string | undefined;
  /**
   * The profile's public slug, when known.
   */
  publicIdentifier?: string | undefined;
  /**
   * Connection degree between this person and the connected account (DISTANCE_1, DISTANCE_2, DISTANCE_3). Present only on account-lane rows: it is the one datum the public lane cannot know.
   */
  networkDistance?: string | undefined;
  /**
   * School name from the public page, when present.
   */
  education?: string | undefined;
  /**
   * Public follower count from the preview page, when present. Not a login-only exact count.
   */
  followers?: number | undefined;
  /**
   * Public profile badge from the preview page, when present.
   */
  badge?: string | undefined;
  /**
   * Languages listed on the public preview page, when present.
   */
  languages?: Array<string> | undefined;
  /**
   * Visible employers from the public preview page, when present.
   */
  positions?: Array<PublicFindPeoplePosition> | undefined;
  /**
   * Visible schools from the public preview page, when present.
   */
  educations?: Array<PublicFindPeopleEducation> | undefined;
  /**
   * A sample of the person's own public feed posts from the preview page, when present. Search returns at most three; the saved contact keeps the fuller public list.
   */
  recentPosts?: Array<RecentPost> | undefined;
};

/**
 * Found and enriched people
 */
export type PublicFindPeopleResponse = {
  source: PublicFindPeopleSource;
  /**
   * The search provider that answered.
   */
  provider: string;
  /**
   * The exact query sent, so a thin result set can be read rather than guessed at.
   */
  query: string;
  /**
   * How many of the people returned were already in your contacts. They ARE in `contacts` like everyone else, marked completeness "already-a-contact", and were not re-fetched because their profile was already read once. Absent when there were none. Not a reason to search again: the search already answered.
   */
  existingCount?: number | undefined;
  /**
   * How many results carry the words YOU typed, rather than the words the search expanded them into. Zero on a full result set that otherwise looks healthy is a strong caveat worth telling the reader about, most often meaning the job title or company named was not found. It is a caveat and not a verdict: a partial result, or a title nobody writes in a headline, can also read zero. Corroborate before saying the person or company does not exist.
   */
  askedFor?: number | undefined;
  /**
   * Why this result is thinner than its count suggests, in words ready to relay. Three causes: the ask itself matched nobody and these people are the closest the wider search reached (a continuation continues that wider search, not the original ask); a continuation reached the end of this search's or this company's list, so there is genuinely nobody further; or the connected account hit LinkedIn's monthly search cap, so the set is truncated rather than complete. Relay it rather than presenting the rows as the whole answer.
   */
  exhaustionNote?: string | undefined;
  /**
   * Present when the answer had to run on a different lane than the ask deserved, in product words: for example a company roster built from public mentions because no usable LinkedIn account was connected. Relay it verbatim; it already says what to do next.
   */
  laneNote?: string | undefined;
  /**
   * Set when the request could not be searched at all. 'nothing-to-search' means nothing in it named anybody. 'wants-companies' means it asked for organisations, which this operation does not find: use the company search instead. Neither is a retry: say what happened and change the request.
   */
  refused?: Refused | undefined;
  /**
   * People you named by name that the search could not find. When this is set, no result is that person: say so rather than presenting the people returned as them.
   */
  notFound?: Array<string> | undefined;
  /**
   * Total profile URLs found.
   */
  urlsFound: number;
  /**
   * Profiles fetched on this call. People already in your contacts are returned too, from the search result, without being fetched again.
   */
  newUrls: number;
  /**
   * Profiles fetched and enriched in this call.
   */
  hydrated: number;
  /**
   * How many searches it took to find these people.
   */
  searches?: number | undefined;
  /**
   * Searches that did not reach the provider. When this is high the result says nothing about the market.
   */
  searchesFailed?: number | undefined;
  /**
   * Whether asking again with more can still reach people you have not seen. Set on every lane, including a run through the connected account. False after a continuation means that lane is walked out, not that the search failed.
   */
  moreAvailable?: boolean | undefined;
  /**
   * Finer than moreAvailable: 'axes-exhausted' means every search angle for this audience has genuinely been tried, calling again with more will not find anybody new. 'batch-failed' means this attempt could not reach the search provider, worth retrying. 'more-available' is the ordinary case.
   */
  exhaustion?: Exhaustion | undefined;
  /**
   * What was actually tried, broken down by search angle, against what each angle could still do.
   */
  axesTried?: Array<AxesTried> | undefined;
  /**
   * Every search angle this audience supports and how many queries each could run — the full potential, whether or not it has been searched yet. Free to compute, no extra searches run to produce it.
   */
  axesAvailable?: Array<AxesAvailable> | undefined;
  /**
   * A non-blocking caveat about this market, e.g. a country whose profiles are hard to tell apart from a bigger anglophone one. Never a reason to withhold or shrink the result — surface it as a caution alongside the real count, never in place of one.
   */
  countryRiskNote?: CountryRiskNote | undefined;
  /**
   * Profiles found but deferred because the scraping pool is busy.
   */
  rateLimited?: number | undefined;
  /**
   * Explanation when part of the result set was deferred.
   */
  note?: string | undefined;
  /**
   * The search outlived this request's watch window and is still working server side. Everything in this response is real and already found. Ask the SAME filters again shortly to collect more: that joins the running search rather than starting or paying for a new one.
   */
  stillRunning?: boolean | undefined;
  /**
   * The search stopped before finishing, so this is what it found, not the whole answer. Asking again starts a fresh search. Never present a partial result as everybody there is.
   */
  partial?: boolean | undefined;
  /**
   * How far the running search has gotten, alongside stillRunning or partial.
   */
  progress?: Progress | undefined;
  /**
   * Profiles found but not read because the workspace credit pool emptied part way through. Real matches, not the full result set.
   */
  creditsExhausted?: number | undefined;
  /**
   * Credits this call spent from the workspace pool: search pages plus profile fetches that succeeded.
   */
  creditsUsed?: number | undefined;
  /**
   * People this continuation found again that an earlier call on the same filters already returned. They were filtered out without being re-fetched or re-charged.
   */
  duplicates?: number | undefined;
  /**
   * Every query this call actually executed, so a multi-part ask can be read rather than guessed at. `query` above stays the single representative one.
   */
  queries?: Array<string> | undefined;
  /**
   * How many of these people were linked into the target list or conversation. Zero alongside non-empty results means found but not saved to a list.
   */
  pooled?: number | undefined;
  /**
   * Whether a target list existed for this call at all; gives pooled its meaning.
   */
  poolTargeted?: boolean | undefined;
  /**
   * How many people the list holds in total after this call, counted server side over the same rows the list itself shows. This is the ONLY running total: cite it as it stands and never add it to another number.
   */
  listTotal?: number | undefined;
  /**
   * Chain mode only: the companies discovered in step one, whose people step two searched.
   */
  companies?: Array<PublicFindPeopleCompanyResponse> | undefined;
  /**
   * Constraints the request carried that this search could not express. When present, the results ran WIDER than asked: everyone requested is still eligible to appear, plus others. Relay this plainly; never present the list as filtered by these.
   */
  notSearched?: Array<NotSearched> | undefined;
  /**
   * What the save choice actually did, so claims about saving cite this rather than the request that was sent.
   */
  saved?: Saved | undefined;
  /**
   * Organisation names in the ask that several real organisations share. The results may mix them: ask which one was meant before presenting a single answer as the answer.
   */
  ambiguity?: Array<Ambiguity> | undefined;
  /**
   * A quote, not an answer: this ask carries facets only the connected account can honour, and nothing was run or spent. Relay it plainly, ask the user, and repeat the call with approveLinkedInSpend true only after they agree. contacts is empty alongside this by construction.
   */
  needs?: Needs | undefined;
  /**
   * On an approved account-lane run: the exact facets that were genuinely applied.
   */
  honoured?: Array<string> | undefined;
  /**
   * Premium cohorts run both lanes in parallel and merge: the public sweep plus one exact account search, every account row marked lane linkedin. Present when the account arm added people; both spends are receipted.
   */
  union?: Union | undefined;
  /**
   * Who approved the account-lane run: the user's explicit yes, or the account's LinkedIn Premium (no monthly search cap, so exact runs directly). Relay which.
   */
  approvedBy?: ApprovedBy | undefined;
  /**
   * On an approved account-lane run: LinkedIn searches this call spent from the account's monthly allowance.
   */
  linkedinSearches?: number | undefined;
  /**
   * Rows dropped by excludeProfiles/excludeWords before fetching, so a refinement is visible and never re-paid.
   */
  excludedCount?: number | undefined;
  contacts: Array<PublicFindPeopleContact>;
};

/**
 * Role or job title facet. One value or a list of alternatives.
 */
export type PublicFindCompaniesTitle = string | Array<string>;

/**
 * City, region, or country facet. One value or a list of alternatives.
 */
export type PublicFindCompaniesLocation = string | Array<string>;

/**
 * Company name facet. One value or a list of alternatives.
 */
export type PublicFindCompaniesCompanyUnion = string | Array<string>;

/**
 * Industry facet. One value or a list of alternatives.
 */
export type PublicFindCompaniesIndustry = string | Array<string>;

/** @internal */
export const GetOwnPostsMediaType$inboundSchema: z.ZodMiniType<
  GetOwnPostsMediaType,
  unknown
> = openEnums.inboundSchema(GetOwnPostsMediaType);

/** @internal */
export const GetOwnPostsMedia$inboundSchema: z.ZodMiniType<
  GetOwnPostsMedia,
  unknown
> = z.object({
  type: GetOwnPostsMediaType$inboundSchema,
  urls: z.array(types.string()),
  title: types.optional(types.string()),
  thumbnailUrl: types.optional(types.string()),
});

export function getOwnPostsMediaFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsMedia, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsMedia$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsMedia' from JSON`,
  );
}

/** @internal */
export const GetOwnPostsReactionTypeCount$inboundSchema: z.ZodMiniType<
  GetOwnPostsReactionTypeCount,
  unknown
> = z.object({
  reactionType: types.string(),
  count: types.number(),
});

export function getOwnPostsReactionTypeCountFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsReactionTypeCount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsReactionTypeCount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsReactionTypeCount' from JSON`,
  );
}

/** @internal */
export const GetOwnPostsPost$inboundSchema: z.ZodMiniType<
  GetOwnPostsPost,
  unknown
> = z.object({
  postUrl: types.string(),
  text: types.string(),
  date: types.number(),
  likesCount: types.number(),
  commentsCount: types.number(),
  sharesCount: types.number(),
  postUrn: types.string(),
  postId: types.string(),
  type: GetOwnPostsType$inboundSchema,
  isRepost: types.optional(types.boolean()),
  media: types.optional(z.lazy(() => GetOwnPostsMedia$inboundSchema)),
  authorName: types.optional(types.string()),
  authorProfileUrl: types.optional(types.string()),
  authorPublicIdentifier: z.optional(z.nullable(types.string())),
  authorProfileUrn: z.optional(z.nullable(types.string())),
  authorHeadline: types.optional(types.string()),
  authorImageUrl: types.optional(types.string()),
  repostedFromName: types.optional(types.string()),
  reactionTypeCounts: types.optional(
    z.array(z.lazy(() => GetOwnPostsReactionTypeCount$inboundSchema)),
  ),
  numImpressions: z.optional(z.nullable(types.number())),
  highlightedReactorName: z.optional(z.nullable(types.string())),
  viewerLiked: z.optional(z.nullable(types.boolean())),
});

export function getOwnPostsPostFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsPost' from JSON`,
  );
}

/** @internal */
export const GetOwnPostsCredits$inboundSchema: z.ZodMiniType<
  GetOwnPostsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getOwnPostsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsCredits' from JSON`,
  );
}

/** @internal */
export const GetOwnPostsMeta$inboundSchema: z.ZodMiniType<
  GetOwnPostsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetOwnPostsCredits$inboundSchema),
});

export function getOwnPostsMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsMeta' from JSON`,
  );
}

/** @internal */
export const GetOwnPostsResponse$inboundSchema: z.ZodMiniType<
  GetOwnPostsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    posts: z.array(z.lazy(() => GetOwnPostsPost$inboundSchema)),
    count: types.number(),
    total: types.number(),
    start: types.number(),
    hasMore: types.boolean(),
    paginationToken: types.nullable(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetOwnPostsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getOwnPostsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetOwnPostsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetOwnPostsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetOwnPostsResponse' from JSON`,
  );
}

/** @internal */
export const User$inboundSchema: z.ZodMiniType<User, unknown> = z.object({
  id: types.string(),
  name: types.nullable(types.string()),
  email: types.nullable(types.string()),
  image: types.nullable(types.string()),
  phone: types.nullable(types.string()),
  createdAt: types.string(),
});

export function userFromJSON(
  jsonString: string,
): SafeParseResult<User, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => User$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'User' from JSON`,
  );
}

/** @internal */
export const GetSettingsLocation$inboundSchema: z.ZodMiniType<
  GetSettingsLocation,
  unknown
> = z.object({
  country: types.nullable(types.string()),
  city: types.nullable(types.string()),
});

export function getSettingsLocationFromJSON(
  jsonString: string,
): SafeParseResult<GetSettingsLocation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetSettingsLocation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetSettingsLocation' from JSON`,
  );
}

/** @internal */
export const Proxy$inboundSchema: z.ZodMiniType<Proxy, unknown> = z.object({
  enabled: types.boolean(),
  mode: types.optional(types.string()),
  country: types.optional(types.string()),
  rotationHours: types.optional(types.number()),
});

export function proxyFromJSON(
  jsonString: string,
): SafeParseResult<Proxy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Proxy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Proxy' from JSON`,
  );
}

/** @internal */
export const Subscription$inboundSchema: z.ZodMiniType<Subscription, unknown> =
  z.object({
    plan: types.string(),
    tier: types.string(),
    proSeatsIncluded: types.number(),
    proSeatsUsed: types.number(),
    status: types.nullable(types.string()),
    currentPeriodEnd: types.nullable(types.string()),
    hasStripe: types.boolean(),
    trialUsed: types.optional(types.boolean()),
    trialEndsAt: z.optional(z.nullable(types.string())),
    aiIncluded: types.optional(types.boolean()),
    isUnlimited: types.optional(types.boolean()),
    isOwner: types.optional(types.boolean()),
    canManageAllAccounts: types.optional(types.boolean()),
    limitMultiplier: types.optional(types.number()),
    referralBoostActive: types.optional(types.boolean()),
    referralBoostExpiresAt: z.optional(z.nullable(types.string())),
  });

export function subscriptionFromJSON(
  jsonString: string,
): SafeParseResult<Subscription, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Subscription$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Subscription' from JSON`,
  );
}

/** @internal */
export const LinkedinAccountProxy$inboundSchema: z.ZodMiniType<
  LinkedinAccountProxy,
  unknown
> = z.object({
  enabled: types.boolean(),
  mode: types.optional(types.string()),
  country: types.optional(types.string()),
  rotationHours: types.optional(types.number()),
});

export function linkedinAccountProxyFromJSON(
  jsonString: string,
): SafeParseResult<LinkedinAccountProxy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LinkedinAccountProxy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LinkedinAccountProxy' from JSON`,
  );
}

/** @internal */
export const ApiKey$inboundSchema: z.ZodMiniType<ApiKey, unknown> = z.object({
  partialKey: types.string(),
  createdAt: types.string(),
});

export function apiKeyFromJSON(
  jsonString: string,
): SafeParseResult<ApiKey, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ApiKey$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ApiKey' from JSON`,
  );
}

/** @internal */
export const LinkedinAccount$inboundSchema: z.ZodMiniType<
  LinkedinAccount,
  unknown
> = z.object({
  id: types.string(),
  label: types.nullable(types.string()),
  isDefault: types.boolean(),
  linkedinName: types.nullable(types.string()),
  linkedinEmail: types.nullable(types.string()),
  profilePic: types.nullable(types.string()),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  isValid: types.boolean(),
  isActive: types.boolean(),
  validationError: types.nullable(types.string()),
  accountPlan: types.nullable(types.string()),
  isUnlimited: types.boolean(),
  ownedByCaller: types.optional(types.boolean()),
  proxy: z.lazy(() => LinkedinAccountProxy$inboundSchema),
  apiKey: types.nullable(z.lazy(() => ApiKey$inboundSchema)),
  createdAt: types.string(),
});

export function linkedinAccountFromJSON(
  jsonString: string,
): SafeParseResult<LinkedinAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LinkedinAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LinkedinAccount' from JSON`,
  );
}

/** @internal */
export const PendingInvite$inboundSchema: z.ZodMiniType<
  PendingInvite,
  unknown
> = z.object({
  id: types.string(),
  email: types.nullable(types.string()),
  name: types.nullable(types.string()),
  code: types.string(),
  maxUses: types.number(),
  useCount: types.number(),
  expiresAt: types.nullable(types.string()),
  createdAt: types.string(),
});

export function pendingInviteFromJSON(
  jsonString: string,
): SafeParseResult<PendingInvite, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PendingInvite$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PendingInvite' from JSON`,
  );
}

/** @internal */
export const Role$inboundSchema: z.ZodMiniType<Role, unknown> = openEnums
  .inboundSchema(Role);

/** @internal */
export const WorkspaceMember$inboundSchema: z.ZodMiniType<
  WorkspaceMember,
  unknown
> = z.object({
  id: types.string(),
  name: types.nullable(types.string()),
  email: types.nullable(types.string()),
  isOwner: types.boolean(),
  role: Role$inboundSchema,
});

export function workspaceMemberFromJSON(
  jsonString: string,
): SafeParseResult<WorkspaceMember, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkspaceMember$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkspaceMember' from JSON`,
  );
}

/** @internal */
export const GetSettingsResponse$inboundSchema: z.ZodMiniType<
  GetSettingsResponse,
  unknown
> = z.object({
  success: types.literal(true),
  isAdmin: types.boolean(),
  user: z.lazy(() => User$inboundSchema),
  location: z.lazy(() => GetSettingsLocation$inboundSchema),
  proxy: z.lazy(() => Proxy$inboundSchema),
  subscription: z.lazy(() => Subscription$inboundSchema),
  linkedinAccounts: z.array(z.lazy(() => LinkedinAccount$inboundSchema)),
  pendingInvites: z.array(z.lazy(() => PendingInvite$inboundSchema)),
  workspaceMembers: z.array(z.lazy(() => WorkspaceMember$inboundSchema)),
});

export function getSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetSettingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetSettingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetSettingsResponse' from JSON`,
  );
}

/** @internal */
export type PatchSettingsRequest$Outbound = {
  country?: string | undefined;
  city?: string | undefined;
  phone?: string | null | undefined;
};

/** @internal */
export const PatchSettingsRequest$outboundSchema: z.ZodMiniType<
  PatchSettingsRequest$Outbound,
  PatchSettingsRequest
> = z.object({
  country: z.optional(z.string()),
  city: z.optional(z.string()),
  phone: z.optional(z.nullable(z.string())),
});

export function patchSettingsRequestToJSON(
  patchSettingsRequest: PatchSettingsRequest,
): string {
  return JSON.stringify(
    PatchSettingsRequest$outboundSchema.parse(patchSettingsRequest),
  );
}

/** @internal */
export const PatchSettingsResponse$inboundSchema: z.ZodMiniType<
  PatchSettingsResponse,
  unknown
> = z.object({
  success: types.literal(true),
});

export function patchSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<PatchSettingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchSettingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchSettingsResponse' from JSON`,
  );
}

/** @internal */
export type GetApiTokenRequest$Outbound = {
  accountId?: string | undefined;
};

/** @internal */
export const GetApiTokenRequest$outboundSchema: z.ZodMiniType<
  GetApiTokenRequest$Outbound,
  GetApiTokenRequest
> = z.object({
  accountId: z.optional(z.string()),
});

export function getApiTokenRequestToJSON(
  getApiTokenRequest: GetApiTokenRequest,
): string {
  return JSON.stringify(
    GetApiTokenRequest$outboundSchema.parse(getApiTokenRequest),
  );
}

/** @internal */
export const GetApiTokenResponse$inboundSchema: z.ZodMiniType<
  GetApiTokenResponse,
  unknown
> = z.object({
  token: types.nullable(types.string()),
  partialKey: types.optional(types.string()),
  createdAt: types.optional(types.string()),
  accountId: types.optional(types.string()),
});

export function getApiTokenResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetApiTokenResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetApiTokenResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetApiTokenResponse' from JSON`,
  );
}

/** @internal */
export type CreateApiTokenRequest$Outbound = {
  accountId?: string | undefined;
};

/** @internal */
export const CreateApiTokenRequest$outboundSchema: z.ZodMiniType<
  CreateApiTokenRequest$Outbound,
  CreateApiTokenRequest
> = z.object({
  accountId: z.optional(z.string()),
});

export function createApiTokenRequestToJSON(
  createApiTokenRequest: CreateApiTokenRequest,
): string {
  return JSON.stringify(
    CreateApiTokenRequest$outboundSchema.parse(createApiTokenRequest),
  );
}

/** @internal */
export const CreateApiTokenResponse$inboundSchema: z.ZodMiniType<
  CreateApiTokenResponse,
  unknown
> = z.object({
  success: types.literal(true),
  token: types.string(),
  partialKey: types.string(),
  accountId: types.string(),
});

export function createApiTokenResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateApiTokenResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateApiTokenResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateApiTokenResponse' from JSON`,
  );
}

/** @internal */
export type DeleteApiTokenRequest$Outbound = {
  accountId?: string | undefined;
};

/** @internal */
export const DeleteApiTokenRequest$outboundSchema: z.ZodMiniType<
  DeleteApiTokenRequest$Outbound,
  DeleteApiTokenRequest
> = z.object({
  accountId: z.optional(z.string()),
});

export function deleteApiTokenRequestToJSON(
  deleteApiTokenRequest: DeleteApiTokenRequest,
): string {
  return JSON.stringify(
    DeleteApiTokenRequest$outboundSchema.parse(deleteApiTokenRequest),
  );
}

/** @internal */
export const DeleteApiTokenResponse$inboundSchema: z.ZodMiniType<
  DeleteApiTokenResponse,
  unknown
> = z.object({
  success: types.literal(true),
});

export function deleteApiTokenResponseFromJSON(
  jsonString: string,
): SafeParseResult<DeleteApiTokenResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteApiTokenResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteApiTokenResponse' from JSON`,
  );
}

/** @internal */
export const Page$inboundSchema: z.ZodMiniType<Page, unknown> = z.object({
  id: types.string(),
  universalName: types.string(),
  name: types.string(),
  url: types.string(),
  logoUrl: types.nullable(types.string()),
  followerCount: types.nullable(types.number()),
  industry: types.nullable(types.string()),
  employeeCount: types.nullable(types.number()),
  adminRole: types.nullable(types.string()),
});

export function pageFromJSON(
  jsonString: string,
): SafeParseResult<Page, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Page$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Page' from JSON`,
  );
}

/** @internal */
export const ListCompanyPagesCredits$inboundSchema: z.ZodMiniType<
  ListCompanyPagesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function listCompanyPagesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ListCompanyPagesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCompanyPagesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCompanyPagesCredits' from JSON`,
  );
}

/** @internal */
export const ListCompanyPagesMeta$inboundSchema: z.ZodMiniType<
  ListCompanyPagesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ListCompanyPagesCredits$inboundSchema),
});

export function listCompanyPagesMetaFromJSON(
  jsonString: string,
): SafeParseResult<ListCompanyPagesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCompanyPagesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCompanyPagesMeta' from JSON`,
  );
}

/** @internal */
export const ListCompanyPagesResponse$inboundSchema: z.ZodMiniType<
  ListCompanyPagesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    pages: z.array(z.lazy(() => Page$inboundSchema)),
    total: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ListCompanyPagesMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function listCompanyPagesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListCompanyPagesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListCompanyPagesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListCompanyPagesResponse' from JSON`,
  );
}

/** @internal */
export type CompanyPagePostsRequest$Outbound = {
  universalName: string;
  count: number;
  start: number;
};

/** @internal */
export const CompanyPagePostsRequest$outboundSchema: z.ZodMiniType<
  CompanyPagePostsRequest$Outbound,
  CompanyPagePostsRequest
> = z.object({
  universalName: z.string(),
  count: z._default(z.int(), 10),
  start: z._default(z.int(), 0),
});

export function companyPagePostsRequestToJSON(
  companyPagePostsRequest: CompanyPagePostsRequest,
): string {
  return JSON.stringify(
    CompanyPagePostsRequest$outboundSchema.parse(companyPagePostsRequest),
  );
}

/** @internal */
export const CompanyPagePostsPost$inboundSchema: z.ZodMiniType<
  CompanyPagePostsPost,
  unknown
> = z.object({
  activityUrn: types.string(),
  postUrl: types.string(),
  text: types.nullable(types.string()),
  publishedAt: types.nullable(types.number()),
  numLikes: types.nullable(types.number()),
  numComments: types.nullable(types.number()),
});

export function companyPagePostsPostFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePostsPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePostsPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePostsPost' from JSON`,
  );
}

/** @internal */
export const CompanyPagePostsCredits$inboundSchema: z.ZodMiniType<
  CompanyPagePostsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function companyPagePostsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePostsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePostsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePostsCredits' from JSON`,
  );
}

/** @internal */
export const CompanyPagePostsMeta$inboundSchema: z.ZodMiniType<
  CompanyPagePostsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CompanyPagePostsCredits$inboundSchema),
});

export function companyPagePostsMetaFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePostsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePostsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePostsMeta' from JSON`,
  );
}

/** @internal */
export const CompanyPagePostsResponse$inboundSchema: z.ZodMiniType<
  CompanyPagePostsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    posts: z.array(z.lazy(() => CompanyPagePostsPost$inboundSchema)),
    total: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => CompanyPagePostsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function companyPagePostsResponseFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePostsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePostsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePostsResponse' from JSON`,
  );
}

/** @internal */
export type CompanyPagePermissionsRequest$Outbound = {
  universalName: string;
};

/** @internal */
export const CompanyPagePermissionsRequest$outboundSchema: z.ZodMiniType<
  CompanyPagePermissionsRequest$Outbound,
  CompanyPagePermissionsRequest
> = z.object({
  universalName: z.string(),
});

export function companyPagePermissionsRequestToJSON(
  companyPagePermissionsRequest: CompanyPagePermissionsRequest,
): string {
  return JSON.stringify(
    CompanyPagePermissionsRequest$outboundSchema.parse(
      companyPagePermissionsRequest,
    ),
  );
}

/** @internal */
export const Permissions$inboundSchema: z.ZodMiniType<Permissions, unknown> = z
  .object({
    role: types.nullable(types.string()),
    canCreateOrganicShare: types.boolean(),
    canCreateComment: types.boolean(),
    canCreateReaction: types.boolean(),
    canDeleteShare: types.boolean(),
    canPinShare: types.boolean(),
    canSendMessages: types.boolean(),
    canReadMessages: types.boolean(),
    canReadAdminDashboard: types.boolean(),
    canReadOrganizationUpdateAnalytics: types.boolean(),
    canReadOrganizationVisitorAnalytics: types.boolean(),
    canReadOrganizationFollowerAnalytics: types.boolean(),
    canReadOrganizationSearchAppearanceAnalytics: types.boolean(),
    canUpdateOrganizationProfile: types.boolean(),
    canEditAdministrators: types.boolean(),
    canDeactivateOrganization: types.boolean(),
    canEditEvents: types.boolean(),
    canReadEvents: types.boolean(),
    canSponsorShare: types.boolean(),
    canManageCareerPages: types.boolean(),
    canManageMessagingAccess: types.boolean(),
    canNotifyEmployees: types.boolean(),
    canExportLeads: types.boolean(),
  });

export function permissionsFromJSON(
  jsonString: string,
): SafeParseResult<Permissions, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Permissions$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Permissions' from JSON`,
  );
}

/** @internal */
export const CompanyPagePermissionsCredits$inboundSchema: z.ZodMiniType<
  CompanyPagePermissionsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function companyPagePermissionsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePermissionsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePermissionsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePermissionsCredits' from JSON`,
  );
}

/** @internal */
export const CompanyPagePermissionsMeta$inboundSchema: z.ZodMiniType<
  CompanyPagePermissionsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CompanyPagePermissionsCredits$inboundSchema),
});

export function companyPagePermissionsMetaFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePermissionsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePermissionsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePermissionsMeta' from JSON`,
  );
}

/** @internal */
export const CompanyPagePermissionsResponse$inboundSchema: z.ZodMiniType<
  CompanyPagePermissionsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    universalName: types.string(),
    permissions: z.lazy(() => Permissions$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => CompanyPagePermissionsMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function companyPagePermissionsResponseFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPagePermissionsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPagePermissionsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPagePermissionsResponse' from JSON`,
  );
}

/** @internal */
export type CompanyPageAnalyticsRequest$Outbound = {
  companyId: string;
};

/** @internal */
export const CompanyPageAnalyticsRequest$outboundSchema: z.ZodMiniType<
  CompanyPageAnalyticsRequest$Outbound,
  CompanyPageAnalyticsRequest
> = z.object({
  companyId: z.string(),
});

export function companyPageAnalyticsRequestToJSON(
  companyPageAnalyticsRequest: CompanyPageAnalyticsRequest,
): string {
  return JSON.stringify(
    CompanyPageAnalyticsRequest$outboundSchema.parse(
      companyPageAnalyticsRequest,
    ),
  );
}

/** @internal */
export const FoundedOn$inboundSchema: z.ZodMiniType<FoundedOn, unknown> = z
  .object({
    year: types.number(),
    month: types.optional(types.number()),
    day: types.optional(types.number()),
  });

export function foundedOnFromJSON(
  jsonString: string,
): SafeParseResult<FoundedOn, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FoundedOn$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FoundedOn' from JSON`,
  );
}

/** @internal */
export const Headquarter$inboundSchema: z.ZodMiniType<Headquarter, unknown> = z
  .object({
    city: types.string(),
    country: types.string(),
    line1: types.optional(types.string()),
    postalCode: types.optional(types.string()),
  });

export function headquarterFromJSON(
  jsonString: string,
): SafeParseResult<Headquarter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Headquarter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Headquarter' from JSON`,
  );
}

/** @internal */
export const Analytics$inboundSchema: z.ZodMiniType<Analytics, unknown> = z
  .object({
    name: types.string(),
    universalName: types.string(),
    followerCount: types.nullable(types.number()),
    employeeCount: types.nullable(types.number()),
    visitorsInPastMonthCount: types.nullable(types.number()),
    isActive: types.boolean(),
    foundedOn: types.nullable(z.lazy(() => FoundedOn$inboundSchema)),
    headquarter: types.nullable(z.lazy(() => Headquarter$inboundSchema)),
    description: types.nullable(types.string()),
    tagline: types.nullable(types.string()),
    websiteUrl: types.nullable(types.string()),
    organizationType: types.nullable(types.string()),
    specialities: z.array(types.string()),
    industryUrns: z.array(types.string()),
  });

export function analyticsFromJSON(
  jsonString: string,
): SafeParseResult<Analytics, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Analytics$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Analytics' from JSON`,
  );
}

/** @internal */
export const CompanyPageAnalyticsCredits$inboundSchema: z.ZodMiniType<
  CompanyPageAnalyticsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function companyPageAnalyticsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPageAnalyticsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPageAnalyticsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPageAnalyticsCredits' from JSON`,
  );
}

/** @internal */
export const CompanyPageAnalyticsMeta$inboundSchema: z.ZodMiniType<
  CompanyPageAnalyticsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => CompanyPageAnalyticsCredits$inboundSchema),
});

export function companyPageAnalyticsMetaFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPageAnalyticsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPageAnalyticsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPageAnalyticsMeta' from JSON`,
  );
}

/** @internal */
export const CompanyPageAnalyticsResponse$inboundSchema: z.ZodMiniType<
  CompanyPageAnalyticsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    analytics: z.lazy(() => Analytics$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => CompanyPageAnalyticsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function companyPageAnalyticsResponseFromJSON(
  jsonString: string,
): SafeParseResult<CompanyPageAnalyticsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompanyPageAnalyticsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompanyPageAnalyticsResponse' from JSON`,
  );
}

/** @internal */
export type InboxListRequest$Outbound = {
  nextCursor?: string | undefined;
  count?: number | undefined;
};

/** @internal */
export const InboxListRequest$outboundSchema: z.ZodMiniType<
  InboxListRequest$Outbound,
  InboxListRequest
> = z.object({
  nextCursor: z.optional(z.string()),
  count: z.optional(z.int()),
});

export function inboxListRequestToJSON(
  inboxListRequest: InboxListRequest,
): string {
  return JSON.stringify(
    InboxListRequest$outboundSchema.parse(inboxListRequest),
  );
}

/** @internal */
export const InboxListParticipant$inboundSchema: z.ZodMiniType<
  InboxListParticipant,
  unknown
> = z.object({
  profileUrn: types.string(),
  firstName: types.string(),
  lastName: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  isPremium: types.optional(types.boolean()),
  isVerified: types.optional(types.boolean()),
  memberBadgeType: z.optional(z.nullable(types.string())),
  participantHostUrn: z.optional(z.nullable(types.string())),
});

export function inboxListParticipantFromJSON(
  jsonString: string,
): SafeParseResult<InboxListParticipant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListParticipant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListParticipant' from JSON`,
  );
}

/** @internal */
export const InboxListLastMessage$inboundSchema: z.ZodMiniType<
  InboxListLastMessage,
  unknown
> = z.object({
  text: types.nullable(types.string()),
  deliveredAt: types.number(),
  senderProfileUrn: types.string(),
});

export function inboxListLastMessageFromJSON(
  jsonString: string,
): SafeParseResult<InboxListLastMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListLastMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListLastMessage' from JSON`,
  );
}

/** @internal */
export const InboxListConversation$inboundSchema: z.ZodMiniType<
  InboxListConversation,
  unknown
> = z.object({
  conversationUrn: types.string(),
  conversationUrl: types.string(),
  lastActivityAt: types.number(),
  createdAt: types.number(),
  read: types.boolean(),
  unreadCount: types.number(),
  groupChat: types.boolean(),
  participants: z.array(z.lazy(() => InboxListParticipant$inboundSchema)),
  lastMessage: types.nullable(z.lazy(() => InboxListLastMessage$inboundSchema)),
  lastReadAt: z.optional(z.nullable(types.number())),
  notificationStatus: z.optional(z.nullable(types.string())),
});

export function inboxListConversationFromJSON(
  jsonString: string,
): SafeParseResult<InboxListConversation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListConversation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListConversation' from JSON`,
  );
}

/** @internal */
export const InboxListCredits$inboundSchema: z.ZodMiniType<
  InboxListCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function inboxListCreditsFromJSON(
  jsonString: string,
): SafeParseResult<InboxListCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListCredits' from JSON`,
  );
}

/** @internal */
export const InboxListMeta$inboundSchema: z.ZodMiniType<
  InboxListMeta,
  unknown
> = z.object({
  credits: z.lazy(() => InboxListCredits$inboundSchema),
});

export function inboxListMetaFromJSON(
  jsonString: string,
): SafeParseResult<InboxListMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListMeta' from JSON`,
  );
}

/** @internal */
export const InboxListResponse$inboundSchema: z.ZodMiniType<
  InboxListResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    conversations: z.array(z.lazy(() => InboxListConversation$inboundSchema)),
    nextCursor: types.nullable(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => InboxListMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function inboxListResponseFromJSON(
  jsonString: string,
): SafeParseResult<InboxListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxListResponse' from JSON`,
  );
}

/** @internal */
export type InboxSearchRequest$Outbound = {
  keywords: string;
  nextCursor?: string | undefined;
};

/** @internal */
export const InboxSearchRequest$outboundSchema: z.ZodMiniType<
  InboxSearchRequest$Outbound,
  InboxSearchRequest
> = z.object({
  keywords: z.string(),
  nextCursor: z.optional(z.string()),
});

export function inboxSearchRequestToJSON(
  inboxSearchRequest: InboxSearchRequest,
): string {
  return JSON.stringify(
    InboxSearchRequest$outboundSchema.parse(inboxSearchRequest),
  );
}

/** @internal */
export const InboxSearchParticipant$inboundSchema: z.ZodMiniType<
  InboxSearchParticipant,
  unknown
> = z.object({
  profileUrn: types.string(),
  firstName: types.string(),
  lastName: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  isPremium: types.optional(types.boolean()),
  isVerified: types.optional(types.boolean()),
  memberBadgeType: z.optional(z.nullable(types.string())),
  participantHostUrn: z.optional(z.nullable(types.string())),
});

export function inboxSearchParticipantFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchParticipant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchParticipant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchParticipant' from JSON`,
  );
}

/** @internal */
export const InboxSearchLastMessage$inboundSchema: z.ZodMiniType<
  InboxSearchLastMessage,
  unknown
> = z.object({
  text: types.nullable(types.string()),
  deliveredAt: types.number(),
  senderProfileUrn: types.string(),
});

export function inboxSearchLastMessageFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchLastMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchLastMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchLastMessage' from JSON`,
  );
}

/** @internal */
export const InboxSearchConversation$inboundSchema: z.ZodMiniType<
  InboxSearchConversation,
  unknown
> = z.object({
  conversationUrn: types.string(),
  conversationUrl: types.string(),
  lastActivityAt: types.number(),
  createdAt: types.number(),
  read: types.boolean(),
  unreadCount: types.number(),
  groupChat: types.boolean(),
  participants: z.array(z.lazy(() => InboxSearchParticipant$inboundSchema)),
  lastMessage: types.nullable(
    z.lazy(() => InboxSearchLastMessage$inboundSchema),
  ),
  lastReadAt: z.optional(z.nullable(types.number())),
  notificationStatus: z.optional(z.nullable(types.string())),
});

export function inboxSearchConversationFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchConversation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchConversation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchConversation' from JSON`,
  );
}

/** @internal */
export const InboxSearchCredits$inboundSchema: z.ZodMiniType<
  InboxSearchCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function inboxSearchCreditsFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchCredits' from JSON`,
  );
}

/** @internal */
export const InboxSearchMeta$inboundSchema: z.ZodMiniType<
  InboxSearchMeta,
  unknown
> = z.object({
  credits: z.lazy(() => InboxSearchCredits$inboundSchema),
});

export function inboxSearchMetaFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchMeta' from JSON`,
  );
}

/** @internal */
export const InboxSearchResponse$inboundSchema: z.ZodMiniType<
  InboxSearchResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    conversations: z.array(z.lazy(() => InboxSearchConversation$inboundSchema)),
    nextCursor: types.nullable(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => InboxSearchMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function inboxSearchResponseFromJSON(
  jsonString: string,
): SafeParseResult<InboxSearchResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxSearchResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxSearchResponse' from JSON`,
  );
}

/** @internal */
export type FindConversationRequest$Outbound = {
  profile: string;
  includeMessages?: boolean | undefined;
};

/** @internal */
export const FindConversationRequest$outboundSchema: z.ZodMiniType<
  FindConversationRequest$Outbound,
  FindConversationRequest
> = z.object({
  profile: z.string(),
  includeMessages: z.optional(z.boolean()),
});

export function findConversationRequestToJSON(
  findConversationRequest: FindConversationRequest,
): string {
  return JSON.stringify(
    FindConversationRequest$outboundSchema.parse(findConversationRequest),
  );
}

/** @internal */
export const FindConversationConversation$inboundSchema: z.ZodMiniType<
  FindConversationConversation,
  unknown
> = z.object({
  conversationUrn: types.string(),
});

export function findConversationConversationFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationConversation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationConversation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationConversation' from JSON`,
  );
}

/** @internal */
export const FindConversationSender$inboundSchema: z.ZodMiniType<
  FindConversationSender,
  unknown
> = z.object({
  firstName: types.string(),
  lastName: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
});

export function findConversationSenderFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationSender, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationSender$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationSender' from JSON`,
  );
}

/** @internal */
export const FindConversationType$inboundSchema: z.ZodMiniType<
  FindConversationType,
  unknown
> = openEnums.inboundSchema(FindConversationType);

/** @internal */
export const FindConversationAttachment$inboundSchema: z.ZodMiniType<
  FindConversationAttachment,
  unknown
> = z.object({
  type: FindConversationType$inboundSchema,
  audioUrl: types.optional(types.string()),
  audioDuration: types.optional(types.number()),
  hostUrn: types.optional(types.string()),
  repliedText: types.optional(types.string()),
  repliedSenderName: types.optional(types.string()),
  imageUrl: types.optional(types.string()),
  fileName: types.optional(types.string()),
  fileUrl: types.optional(types.string()),
});

export function findConversationAttachmentFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationAttachment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationAttachment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationAttachment' from JSON`,
  );
}

/** @internal */
export const FindConversationMessage$inboundSchema: z.ZodMiniType<
  FindConversationMessage,
  unknown
> = z.object({
  messageUrn: types.string(),
  text: types.nullable(types.string()),
  deliveredAt: types.number(),
  senderProfileUrn: types.string(),
  sender: z.lazy(() => FindConversationSender$inboundSchema),
  attachments: z.array(z.lazy(() => FindConversationAttachment$inboundSchema)),
  isOutbound: types.boolean(),
});

export function findConversationMessageFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationMessage' from JSON`,
  );
}

/** @internal */
export const FindConversationCredits$inboundSchema: z.ZodMiniType<
  FindConversationCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function findConversationCreditsFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationCredits' from JSON`,
  );
}

/** @internal */
export const FindConversationMeta$inboundSchema: z.ZodMiniType<
  FindConversationMeta,
  unknown
> = z.object({
  credits: z.lazy(() => FindConversationCredits$inboundSchema),
});

export function findConversationMetaFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationMeta' from JSON`,
  );
}

/** @internal */
export const FindConversationResponse$inboundSchema: z.ZodMiniType<
  FindConversationResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    found: types.boolean(),
    conversation: types.nullable(
      z.lazy(() => FindConversationConversation$inboundSchema),
    ),
    messages: types.nullable(
      z.array(z.lazy(() => FindConversationMessage$inboundSchema)),
    ),
    prevCursor: z.optional(z.nullable(types.number())),
    userProfileUrn: types.optional(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => FindConversationMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function findConversationResponseFromJSON(
  jsonString: string,
): SafeParseResult<FindConversationResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FindConversationResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FindConversationResponse' from JSON`,
  );
}

/** @internal */
export type InboxMessagesRequest$Outbound = {
  conversationUrn: string;
  deliveredAt?: number | undefined;
};

/** @internal */
export const InboxMessagesRequest$outboundSchema: z.ZodMiniType<
  InboxMessagesRequest$Outbound,
  InboxMessagesRequest
> = z.object({
  conversationUrn: z.string(),
  deliveredAt: z.optional(z.int()),
});

export function inboxMessagesRequestToJSON(
  inboxMessagesRequest: InboxMessagesRequest,
): string {
  return JSON.stringify(
    InboxMessagesRequest$outboundSchema.parse(inboxMessagesRequest),
  );
}

/** @internal */
export const InboxMessagesSender$inboundSchema: z.ZodMiniType<
  InboxMessagesSender,
  unknown
> = z.object({
  firstName: types.string(),
  lastName: types.string(),
  profileUrl: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  profilePicture: types.nullable(types.string()),
  publicIdentifier: types.nullable(types.string()),
  profileUrn: types.nullable(types.string()),
});

export function inboxMessagesSenderFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesSender, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesSender$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesSender' from JSON`,
  );
}

/** @internal */
export const InboxMessagesType$inboundSchema: z.ZodMiniType<
  InboxMessagesType,
  unknown
> = openEnums.inboundSchema(InboxMessagesType);

/** @internal */
export const InboxMessagesAttachment$inboundSchema: z.ZodMiniType<
  InboxMessagesAttachment,
  unknown
> = z.object({
  type: InboxMessagesType$inboundSchema,
  audioUrl: types.optional(types.string()),
  audioDuration: types.optional(types.number()),
  hostUrn: types.optional(types.string()),
  repliedText: types.optional(types.string()),
  repliedSenderName: types.optional(types.string()),
  imageUrl: types.optional(types.string()),
  fileName: types.optional(types.string()),
  fileUrl: types.optional(types.string()),
});

export function inboxMessagesAttachmentFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesAttachment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesAttachment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesAttachment' from JSON`,
  );
}

/** @internal */
export const InboxMessagesMessage$inboundSchema: z.ZodMiniType<
  InboxMessagesMessage,
  unknown
> = z.object({
  messageUrn: types.string(),
  text: types.nullable(types.string()),
  deliveredAt: types.number(),
  senderProfileUrn: types.string(),
  sender: z.lazy(() => InboxMessagesSender$inboundSchema),
  attachments: z.array(z.lazy(() => InboxMessagesAttachment$inboundSchema)),
  isOutbound: types.boolean(),
});

export function inboxMessagesMessageFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesMessage' from JSON`,
  );
}

/** @internal */
export const InboxMessagesCredits$inboundSchema: z.ZodMiniType<
  InboxMessagesCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function inboxMessagesCreditsFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesCredits' from JSON`,
  );
}

/** @internal */
export const InboxMessagesMeta$inboundSchema: z.ZodMiniType<
  InboxMessagesMeta,
  unknown
> = z.object({
  credits: z.lazy(() => InboxMessagesCredits$inboundSchema),
});

export function inboxMessagesMetaFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesMeta' from JSON`,
  );
}

/** @internal */
export const InboxMessagesResponse$inboundSchema: z.ZodMiniType<
  InboxMessagesResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    messages: z.array(z.lazy(() => InboxMessagesMessage$inboundSchema)),
    prevCursor: types.nullable(types.number()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => InboxMessagesMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function inboxMessagesResponseFromJSON(
  jsonString: string,
): SafeParseResult<InboxMessagesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxMessagesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxMessagesResponse' from JSON`,
  );
}

/** @internal */
export type MarkSeenRequest$Outbound = {
  conversationUrn: string;
};

/** @internal */
export const MarkSeenRequest$outboundSchema: z.ZodMiniType<
  MarkSeenRequest$Outbound,
  MarkSeenRequest
> = z.object({
  conversationUrn: z.string(),
});

export function markSeenRequestToJSON(
  markSeenRequest: MarkSeenRequest,
): string {
  return JSON.stringify(MarkSeenRequest$outboundSchema.parse(markSeenRequest));
}

/** @internal */
export const MarkSeenCredits$inboundSchema: z.ZodMiniType<
  MarkSeenCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function markSeenCreditsFromJSON(
  jsonString: string,
): SafeParseResult<MarkSeenCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkSeenCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkSeenCredits' from JSON`,
  );
}

/** @internal */
export const MarkSeenMeta$inboundSchema: z.ZodMiniType<MarkSeenMeta, unknown> =
  z.object({
    credits: z.lazy(() => MarkSeenCredits$inboundSchema),
  });

export function markSeenMetaFromJSON(
  jsonString: string,
): SafeParseResult<MarkSeenMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkSeenMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkSeenMeta' from JSON`,
  );
}

/** @internal */
export const MarkSeenResponse$inboundSchema: z.ZodMiniType<
  MarkSeenResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => MarkSeenMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function markSeenResponseFromJSON(
  jsonString: string,
): SafeParseResult<MarkSeenResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkSeenResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkSeenResponse' from JSON`,
  );
}

/** @internal */
export type MarkAllReadRequest$Outbound = {};

/** @internal */
export const MarkAllReadRequest$outboundSchema: z.ZodMiniType<
  MarkAllReadRequest$Outbound,
  MarkAllReadRequest
> = z.object({});

export function markAllReadRequestToJSON(
  markAllReadRequest: MarkAllReadRequest,
): string {
  return JSON.stringify(
    MarkAllReadRequest$outboundSchema.parse(markAllReadRequest),
  );
}

/** @internal */
export const MarkAllReadCredits$inboundSchema: z.ZodMiniType<
  MarkAllReadCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function markAllReadCreditsFromJSON(
  jsonString: string,
): SafeParseResult<MarkAllReadCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkAllReadCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkAllReadCredits' from JSON`,
  );
}

/** @internal */
export const MarkAllReadMeta$inboundSchema: z.ZodMiniType<
  MarkAllReadMeta,
  unknown
> = z.object({
  credits: z.lazy(() => MarkAllReadCredits$inboundSchema),
});

export function markAllReadMetaFromJSON(
  jsonString: string,
): SafeParseResult<MarkAllReadMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkAllReadMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkAllReadMeta' from JSON`,
  );
}

/** @internal */
export const MarkAllReadResponse$inboundSchema: z.ZodMiniType<
  MarkAllReadResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => MarkAllReadMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function markAllReadResponseFromJSON(
  jsonString: string,
): SafeParseResult<MarkAllReadResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MarkAllReadResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MarkAllReadResponse' from JSON`,
  );
}

/** @internal */
export const InboxUnreadCountCredits$inboundSchema: z.ZodMiniType<
  InboxUnreadCountCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function inboxUnreadCountCreditsFromJSON(
  jsonString: string,
): SafeParseResult<InboxUnreadCountCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxUnreadCountCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxUnreadCountCredits' from JSON`,
  );
}

/** @internal */
export const InboxUnreadCountMeta$inboundSchema: z.ZodMiniType<
  InboxUnreadCountMeta,
  unknown
> = z.object({
  credits: z.lazy(() => InboxUnreadCountCredits$inboundSchema),
});

export function inboxUnreadCountMetaFromJSON(
  jsonString: string,
): SafeParseResult<InboxUnreadCountMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxUnreadCountMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxUnreadCountMeta' from JSON`,
  );
}

/** @internal */
export const InboxUnreadCountResponse$inboundSchema: z.ZodMiniType<
  InboxUnreadCountResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    unreadCount: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => InboxUnreadCountMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function inboxUnreadCountResponseFromJSON(
  jsonString: string,
): SafeParseResult<InboxUnreadCountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxUnreadCountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxUnreadCountResponse' from JSON`,
  );
}

/** @internal */
export type GetConversationSummaryRequest$Outbound = {
  profile: string;
};

/** @internal */
export const GetConversationSummaryRequest$outboundSchema: z.ZodMiniType<
  GetConversationSummaryRequest$Outbound,
  GetConversationSummaryRequest
> = z.object({
  profile: z.string(),
});

export function getConversationSummaryRequestToJSON(
  getConversationSummaryRequest: GetConversationSummaryRequest,
): string {
  return JSON.stringify(
    GetConversationSummaryRequest$outboundSchema.parse(
      getConversationSummaryRequest,
    ),
  );
}

/** @internal */
export const GetConversationSummaryCredits$inboundSchema: z.ZodMiniType<
  GetConversationSummaryCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getConversationSummaryCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetConversationSummaryCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConversationSummaryCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConversationSummaryCredits' from JSON`,
  );
}

/** @internal */
export const GetConversationSummaryMeta$inboundSchema: z.ZodMiniType<
  GetConversationSummaryMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetConversationSummaryCredits$inboundSchema),
});

export function getConversationSummaryMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetConversationSummaryMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConversationSummaryMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConversationSummaryMeta' from JSON`,
  );
}

/** @internal */
export const GetConversationSummaryResponse$inboundSchema: z.ZodMiniType<
  GetConversationSummaryResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    found: types.boolean(),
    contactId: types.optional(types.string()),
    summary: types.nullable(types.string()),
    summarizedAt: z.optional(z.nullable(types.string())),
    messageCount: z.optional(z.nullable(types.number())),
    conversationUpdatedAt: z.optional(z.nullable(types.string())),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => GetConversationSummaryMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getConversationSummaryResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetConversationSummaryResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetConversationSummaryResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetConversationSummaryResponse' from JSON`,
  );
}

/** @internal */
export type SaveConversationSummaryRequest$Outbound = {
  profile: string;
  summary: string;
};

/** @internal */
export const SaveConversationSummaryRequest$outboundSchema: z.ZodMiniType<
  SaveConversationSummaryRequest$Outbound,
  SaveConversationSummaryRequest
> = z.object({
  profile: z.string(),
  summary: z.string(),
});

export function saveConversationSummaryRequestToJSON(
  saveConversationSummaryRequest: SaveConversationSummaryRequest,
): string {
  return JSON.stringify(
    SaveConversationSummaryRequest$outboundSchema.parse(
      saveConversationSummaryRequest,
    ),
  );
}

/** @internal */
export const SaveConversationSummaryCredits$inboundSchema: z.ZodMiniType<
  SaveConversationSummaryCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function saveConversationSummaryCreditsFromJSON(
  jsonString: string,
): SafeParseResult<SaveConversationSummaryCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaveConversationSummaryCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaveConversationSummaryCredits' from JSON`,
  );
}

/** @internal */
export const SaveConversationSummaryMeta$inboundSchema: z.ZodMiniType<
  SaveConversationSummaryMeta,
  unknown
> = z.object({
  credits: z.lazy(() => SaveConversationSummaryCredits$inboundSchema),
});

export function saveConversationSummaryMetaFromJSON(
  jsonString: string,
): SafeParseResult<SaveConversationSummaryMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaveConversationSummaryMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaveConversationSummaryMeta' from JSON`,
  );
}

/** @internal */
export const SaveConversationSummaryResponse$inboundSchema: z.ZodMiniType<
  SaveConversationSummaryResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    contactId: types.string(),
    summarizedAt: types.string(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => SaveConversationSummaryMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function saveConversationSummaryResponseFromJSON(
  jsonString: string,
): SafeParseResult<SaveConversationSummaryResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaveConversationSummaryResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaveConversationSummaryResponse' from JSON`,
  );
}

/** @internal */
export type PublicProfileRequest$Outbound = {
  profileUrl?: string | undefined;
  slug?: string | undefined;
  country?: string | undefined;
  save: boolean;
};

/** @internal */
export const PublicProfileRequest$outboundSchema: z.ZodMiniType<
  PublicProfileRequest$Outbound,
  PublicProfileRequest
> = z.object({
  profileUrl: z.optional(z.string()),
  slug: z.optional(z.string()),
  country: z.optional(z.string()),
  save: z._default(z.boolean(), true),
});

export function publicProfileRequestToJSON(
  publicProfileRequest: PublicProfileRequest,
): string {
  return JSON.stringify(
    PublicProfileRequest$outboundSchema.parse(publicProfileRequest),
  );
}

/** @internal */
export const PublicProfileSource$inboundSchema: z.ZodMiniEnum<
  typeof PublicProfileSource
> = z.enum(PublicProfileSource);

/** @internal */
export const PublicProfileResponse$inboundSchema: z.ZodMiniType<
  PublicProfileResponse,
  unknown
> = z.object({
  source: PublicProfileSource$inboundSchema,
  completeness: types.string(),
  saved: types.boolean(),
  profile: z.record(z.string(), z.any()),
});

export function publicProfileResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicProfileResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicProfileResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicProfileResponse' from JSON`,
  );
}

/** @internal */
export type PublicFindPeopleTitle$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleTitle$outboundSchema: z.ZodMiniType<
  PublicFindPeopleTitle$Outbound,
  PublicFindPeopleTitle
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleTitleToJSON(
  publicFindPeopleTitle: PublicFindPeopleTitle,
): string {
  return JSON.stringify(
    PublicFindPeopleTitle$outboundSchema.parse(publicFindPeopleTitle),
  );
}

/** @internal */
export type PublicFindPeopleLocation$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleLocation$outboundSchema: z.ZodMiniType<
  PublicFindPeopleLocation$Outbound,
  PublicFindPeopleLocation
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleLocationToJSON(
  publicFindPeopleLocation: PublicFindPeopleLocation,
): string {
  return JSON.stringify(
    PublicFindPeopleLocation$outboundSchema.parse(publicFindPeopleLocation),
  );
}

/** @internal */
export type PublicFindPeopleCompanyUnion$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleCompanyUnion$outboundSchema: z.ZodMiniType<
  PublicFindPeopleCompanyUnion$Outbound,
  PublicFindPeopleCompanyUnion
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleCompanyUnionToJSON(
  publicFindPeopleCompanyUnion: PublicFindPeopleCompanyUnion,
): string {
  return JSON.stringify(
    PublicFindPeopleCompanyUnion$outboundSchema.parse(
      publicFindPeopleCompanyUnion,
    ),
  );
}

/** @internal */
export type PublicFindPeopleIndustry$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleIndustry$outboundSchema: z.ZodMiniType<
  PublicFindPeopleIndustry$Outbound,
  PublicFindPeopleIndustry
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleIndustryToJSON(
  publicFindPeopleIndustry: PublicFindPeopleIndustry,
): string {
  return JSON.stringify(
    PublicFindPeopleIndustry$outboundSchema.parse(publicFindPeopleIndustry),
  );
}

/** @internal */
export type PublicFindPeopleSeniority$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleSeniority$outboundSchema: z.ZodMiniType<
  PublicFindPeopleSeniority$Outbound,
  PublicFindPeopleSeniority
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleSeniorityToJSON(
  publicFindPeopleSeniority: PublicFindPeopleSeniority,
): string {
  return JSON.stringify(
    PublicFindPeopleSeniority$outboundSchema.parse(publicFindPeopleSeniority),
  );
}

/** @internal */
export type PublicFindPeopleFilters$Outbound = {
  title?: string | Array<string> | undefined;
  location?: string | Array<string> | undefined;
  company?: string | Array<string> | undefined;
  industry?: string | Array<string> | undefined;
  seniority?: string | Array<string> | undefined;
  keywords?: string | undefined;
  countryCode?: string | undefined;
};

/** @internal */
export const PublicFindPeopleFilters$outboundSchema: z.ZodMiniType<
  PublicFindPeopleFilters$Outbound,
  PublicFindPeopleFilters
> = z.object({
  title: z.optional(smartUnion([z.string(), z.array(z.string())])),
  location: z.optional(smartUnion([z.string(), z.array(z.string())])),
  company: z.optional(smartUnion([z.string(), z.array(z.string())])),
  industry: z.optional(smartUnion([z.string(), z.array(z.string())])),
  seniority: z.optional(smartUnion([z.string(), z.array(z.string())])),
  keywords: z.optional(z.string()),
  countryCode: z.optional(z.string()),
});

export function publicFindPeopleFiltersToJSON(
  publicFindPeopleFilters: PublicFindPeopleFilters,
): string {
  return JSON.stringify(
    PublicFindPeopleFilters$outboundSchema.parse(publicFindPeopleFilters),
  );
}

/** @internal */
export const Save$outboundSchema: z.ZodMiniEnum<typeof Save> = z.enum(Save);

/** @internal */
export type Chain$Outbound = {
  role: string;
  companiesLimit: number;
};

/** @internal */
export const Chain$outboundSchema: z.ZodMiniType<Chain$Outbound, Chain> = z
  .object({
    role: z.string(),
    companiesLimit: z._default(z.int(), 10),
  });

export function chainToJSON(chain: Chain): string {
  return JSON.stringify(Chain$outboundSchema.parse(chain));
}

/** @internal */
export type CompanySize$Outbound = string | Array<string>;

/** @internal */
export const CompanySize$outboundSchema: z.ZodMiniType<
  CompanySize$Outbound,
  CompanySize
> = smartUnion([z.string(), z.array(z.string())]);

export function companySizeToJSON(companySize: CompanySize): string {
  return JSON.stringify(CompanySize$outboundSchema.parse(companySize));
}

/** @internal */
export type PublicFindPeopleConnectionDegree$Outbound = string | Array<string>;

/** @internal */
export const PublicFindPeopleConnectionDegree$outboundSchema: z.ZodMiniType<
  PublicFindPeopleConnectionDegree$Outbound,
  PublicFindPeopleConnectionDegree
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindPeopleConnectionDegreeToJSON(
  publicFindPeopleConnectionDegree: PublicFindPeopleConnectionDegree,
): string {
  return JSON.stringify(
    PublicFindPeopleConnectionDegree$outboundSchema.parse(
      publicFindPeopleConnectionDegree,
    ),
  );
}

/** @internal */
export type School$Outbound = string | Array<string>;

/** @internal */
export const School$outboundSchema: z.ZodMiniType<School$Outbound, School> =
  smartUnion([z.string(), z.array(z.string())]);

export function schoolToJSON(school: School): string {
  return JSON.stringify(School$outboundSchema.parse(school));
}

/** @internal */
export type PastCompanies$Outbound = string | Array<string>;

/** @internal */
export const PastCompanies$outboundSchema: z.ZodMiniType<
  PastCompanies$Outbound,
  PastCompanies
> = smartUnion([z.string(), z.array(z.string())]);

export function pastCompaniesToJSON(pastCompanies: PastCompanies): string {
  return JSON.stringify(PastCompanies$outboundSchema.parse(pastCompanies));
}

/** @internal */
export type ProfileLanguage$Outbound = string | Array<string>;

/** @internal */
export const ProfileLanguage$outboundSchema: z.ZodMiniType<
  ProfileLanguage$Outbound,
  ProfileLanguage
> = smartUnion([z.string(), z.array(z.string())]);

export function profileLanguageToJSON(
  profileLanguage: ProfileLanguage,
): string {
  return JSON.stringify(ProfileLanguage$outboundSchema.parse(profileLanguage));
}

/** @internal */
export type CompanyHQ$Outbound = string | Array<string>;

/** @internal */
export const CompanyHQ$outboundSchema: z.ZodMiniType<
  CompanyHQ$Outbound,
  CompanyHQ
> = smartUnion([z.string(), z.array(z.string())]);

export function companyHQToJSON(companyHQ: CompanyHQ): string {
  return JSON.stringify(CompanyHQ$outboundSchema.parse(companyHQ));
}

/** @internal */
export type FollowersOf$Outbound = string | Array<string>;

/** @internal */
export const FollowersOf$outboundSchema: z.ZodMiniType<
  FollowersOf$Outbound,
  FollowersOf
> = smartUnion([z.string(), z.array(z.string())]);

export function followersOfToJSON(followersOf: FollowersOf): string {
  return JSON.stringify(FollowersOf$outboundSchema.parse(followersOf));
}

/** @internal */
export type ExcludeProfiles$Outbound = string | Array<string>;

/** @internal */
export const ExcludeProfiles$outboundSchema: z.ZodMiniType<
  ExcludeProfiles$Outbound,
  ExcludeProfiles
> = smartUnion([z.string(), z.array(z.string())]);

export function excludeProfilesToJSON(
  excludeProfiles: ExcludeProfiles,
): string {
  return JSON.stringify(ExcludeProfiles$outboundSchema.parse(excludeProfiles));
}

/** @internal */
export type ExcludeWords$Outbound = string | Array<string>;

/** @internal */
export const ExcludeWords$outboundSchema: z.ZodMiniType<
  ExcludeWords$Outbound,
  ExcludeWords
> = smartUnion([z.string(), z.array(z.string())]);

export function excludeWordsToJSON(excludeWords: ExcludeWords): string {
  return JSON.stringify(ExcludeWords$outboundSchema.parse(excludeWords));
}

/** @internal */
export type PublicFindPeopleRequest$Outbound = {
  filters: PublicFindPeopleFilters$Outbound;
  limit: number;
  save: string;
  chain?: Chain$Outbound | undefined;
  more: boolean;
  sentence?: string | undefined;
  companySize?: string | Array<string> | undefined;
  connectionDegree?: string | Array<string> | undefined;
  verified?: boolean | undefined;
  school?: string | Array<string> | undefined;
  pastCompanies?: string | Array<string> | undefined;
  profileLanguage?: string | Array<string> | undefined;
  companyHQ?: string | Array<string> | undefined;
  followersOf?: string | Array<string> | undefined;
  excludeProfiles?: string | Array<string> | undefined;
  excludeWords?: string | Array<string> | undefined;
  approveLinkedInSpend?: boolean | undefined;
};

/** @internal */
export const PublicFindPeopleRequest$outboundSchema: z.ZodMiniType<
  PublicFindPeopleRequest$Outbound,
  PublicFindPeopleRequest
> = z.object({
  filters: z.lazy(() => PublicFindPeopleFilters$outboundSchema),
  limit: z._default(z.int(), 15),
  save: z._default(Save$outboundSchema, "keep"),
  chain: z.optional(z.lazy(() => Chain$outboundSchema)),
  more: z._default(z.boolean(), false),
  sentence: z.optional(z.string()),
  companySize: z.optional(smartUnion([z.string(), z.array(z.string())])),
  connectionDegree: z.optional(smartUnion([z.string(), z.array(z.string())])),
  verified: z.optional(z.boolean()),
  school: z.optional(smartUnion([z.string(), z.array(z.string())])),
  pastCompanies: z.optional(smartUnion([z.string(), z.array(z.string())])),
  profileLanguage: z.optional(smartUnion([z.string(), z.array(z.string())])),
  companyHQ: z.optional(smartUnion([z.string(), z.array(z.string())])),
  followersOf: z.optional(smartUnion([z.string(), z.array(z.string())])),
  excludeProfiles: z.optional(smartUnion([z.string(), z.array(z.string())])),
  excludeWords: z.optional(smartUnion([z.string(), z.array(z.string())])),
  approveLinkedInSpend: z.optional(z.boolean()),
});

export function publicFindPeopleRequestToJSON(
  publicFindPeopleRequest: PublicFindPeopleRequest,
): string {
  return JSON.stringify(
    PublicFindPeopleRequest$outboundSchema.parse(publicFindPeopleRequest),
  );
}

/** @internal */
export const PublicFindPeopleSource$inboundSchema: z.ZodMiniEnum<
  typeof PublicFindPeopleSource
> = z.enum(PublicFindPeopleSource);

/** @internal */
export const Refused$inboundSchema: z.ZodMiniType<Refused, unknown> = openEnums
  .inboundSchema(Refused);

/** @internal */
export const Exhaustion$inboundSchema: z.ZodMiniType<Exhaustion, unknown> =
  openEnums.inboundSchema(Exhaustion);

/** @internal */
export const AxesTried$inboundSchema: z.ZodMiniType<AxesTried, unknown> = z
  .object({
    axis: types.string(),
    queriesPlanned: types.number(),
    attempted: types.number(),
    yielded: types.number(),
    exhausted: types.boolean(),
  });

export function axesTriedFromJSON(
  jsonString: string,
): SafeParseResult<AxesTried, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AxesTried$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AxesTried' from JSON`,
  );
}

/** @internal */
export const AxesAvailable$inboundSchema: z.ZodMiniType<
  AxesAvailable,
  unknown
> = z.object({
  axis: types.string(),
  queries: types.number(),
});

export function axesAvailableFromJSON(
  jsonString: string,
): SafeParseResult<AxesAvailable, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AxesAvailable$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AxesAvailable' from JSON`,
  );
}

/** @internal */
export const Risk$inboundSchema: z.ZodMiniType<Risk, unknown> = openEnums
  .inboundSchema(Risk);

/** @internal */
export const CountryRiskNote$inboundSchema: z.ZodMiniType<
  CountryRiskNote,
  unknown
> = z.object({
  risk: Risk$inboundSchema,
  reason: types.string(),
});

export function countryRiskNoteFromJSON(
  jsonString: string,
): SafeParseResult<CountryRiskNote, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CountryRiskNote$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CountryRiskNote' from JSON`,
  );
}

/** @internal */
export const Progress$inboundSchema: z.ZodMiniType<Progress, unknown> = z
  .catchall(
    z.object({
      peopleFound: types.optional(types.number()),
      queriesRun: types.optional(types.number()),
    }),
    z.any(),
  );

export function progressFromJSON(
  jsonString: string,
): SafeParseResult<Progress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Progress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Progress' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleCompanyResponse$inboundSchema: z.ZodMiniType<
  PublicFindPeopleCompanyResponse,
  unknown
> = z.catchall(
  z.object({
    name: z.optional(z.nullable(types.string())),
    url: z.optional(z.nullable(types.string())),
  }),
  z.any(),
);

export function publicFindPeopleCompanyResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleCompanyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeopleCompanyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleCompanyResponse' from JSON`,
  );
}

/** @internal */
export const NotSearched$inboundSchema: z.ZodMiniType<NotSearched, unknown> = z
  .object({
    field: types.string(),
    value: types.string(),
    why: types.string(),
    effect: types.string(),
  });

export function notSearchedFromJSON(
  jsonString: string,
): SafeParseResult<NotSearched, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => NotSearched$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'NotSearched' from JSON`,
  );
}

/** @internal */
export const Saved$inboundSchema: z.ZodMiniType<Saved, unknown> = z.object({
  count: types.number(),
  decide: types.optional(types.boolean()),
});

export function savedFromJSON(
  jsonString: string,
): SafeParseResult<Saved, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Saved$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Saved' from JSON`,
  );
}

/** @internal */
export const Ambiguity$inboundSchema: z.ZodMiniType<Ambiguity, unknown> = z
  .object({
    name: types.string(),
    note: types.string(),
  });

export function ambiguityFromJSON(
  jsonString: string,
): SafeParseResult<Ambiguity, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Ambiguity$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Ambiguity' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleKind$inboundSchema: z.ZodMiniEnum<
  typeof PublicFindPeopleKind
> = z.enum(PublicFindPeopleKind);

/** @internal */
export const Because$inboundSchema: z.ZodMiniType<Because, unknown> = z.object({
  field: types.string(),
  value: types.string(),
});

export function becauseFromJSON(
  jsonString: string,
): SafeParseResult<Because, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Because$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Because' from JSON`,
  );
}

/** @internal */
export const Estimate$inboundSchema: z.ZodMiniType<Estimate, unknown> = z
  .object({
    linkedinSearches: types.number(),
  });

export function estimateFromJSON(
  jsonString: string,
): SafeParseResult<Estimate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Estimate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Estimate' from JSON`,
  );
}

/** @internal */
export const Needs$inboundSchema: z.ZodMiniType<Needs, unknown> = z.object({
  kind: PublicFindPeopleKind$inboundSchema,
  because: z.array(z.lazy(() => Because$inboundSchema)),
  estimate: z.lazy(() => Estimate$inboundSchema),
  alternative: types.string(),
});

export function needsFromJSON(
  jsonString: string,
): SafeParseResult<Needs, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Needs$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Needs' from JSON`,
  );
}

/** @internal */
export const Union$inboundSchema: z.ZodMiniType<Union, unknown> = z.object({
  added: types.number(),
  linkedinSearches: types.number(),
});

export function unionFromJSON(
  jsonString: string,
): SafeParseResult<Union, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Union$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Union' from JSON`,
  );
}

/** @internal */
export const ApprovedBy$inboundSchema: z.ZodMiniType<ApprovedBy, unknown> =
  openEnums.inboundSchema(ApprovedBy);

/** @internal */
export const PublicFindPeopleLane$inboundSchema: z.ZodMiniEnum<
  typeof PublicFindPeopleLane
> = z.enum(PublicFindPeopleLane);

/** @internal */
export const PublicFindPeoplePositionStartDate$inboundSchema: z.ZodMiniType<
  PublicFindPeoplePositionStartDate,
  unknown
> = z.object({
  year: types.number(),
});

export function publicFindPeoplePositionStartDateFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeoplePositionStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeoplePositionStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeoplePositionStartDate' from JSON`,
  );
}

/** @internal */
export const PublicFindPeoplePositionEndDate$inboundSchema: z.ZodMiniType<
  PublicFindPeoplePositionEndDate,
  unknown
> = z.object({
  year: types.number(),
});

export function publicFindPeoplePositionEndDateFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeoplePositionEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeoplePositionEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeoplePositionEndDate' from JSON`,
  );
}

/** @internal */
export const PublicFindPeoplePosition$inboundSchema: z.ZodMiniType<
  PublicFindPeoplePosition,
  unknown
> = z.object({
  companyName: types.string(),
  title: types.nullable(types.string()),
  companyUrl: types.nullable(types.string()),
  startDate: types.nullable(
    z.lazy(() => PublicFindPeoplePositionStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => PublicFindPeoplePositionEndDate$inboundSchema),
  ),
  isCurrent: types.boolean(),
});

export function publicFindPeoplePositionFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeoplePosition, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeoplePosition$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeoplePosition' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleEducationStartDate$inboundSchema: z.ZodMiniType<
  PublicFindPeopleEducationStartDate,
  unknown
> = z.object({
  year: types.number(),
});

export function publicFindPeopleEducationStartDateFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleEducationStartDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      PublicFindPeopleEducationStartDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleEducationStartDate' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleEducationEndDate$inboundSchema: z.ZodMiniType<
  PublicFindPeopleEducationEndDate,
  unknown
> = z.object({
  year: types.number(),
});

export function publicFindPeopleEducationEndDateFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleEducationEndDate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeopleEducationEndDate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleEducationEndDate' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleEducation$inboundSchema: z.ZodMiniType<
  PublicFindPeopleEducation,
  unknown
> = z.object({
  schoolName: types.string(),
  schoolUrl: types.nullable(types.string()),
  startDate: types.nullable(
    z.lazy(() => PublicFindPeopleEducationStartDate$inboundSchema),
  ),
  endDate: types.nullable(
    z.lazy(() => PublicFindPeopleEducationEndDate$inboundSchema),
  ),
});

export function publicFindPeopleEducationFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleEducation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeopleEducation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleEducation' from JSON`,
  );
}

/** @internal */
export const RecentPost$inboundSchema: z.ZodMiniType<RecentPost, unknown> = z
  .object({
    url: types.nullable(types.string()),
    text: types.nullable(types.string()),
    likes: types.nullable(types.number()),
    datePublished: types.nullable(types.string()),
  });

export function recentPostFromJSON(
  jsonString: string,
): SafeParseResult<RecentPost, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecentPost$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecentPost' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleContact$inboundSchema: z.ZodMiniType<
  PublicFindPeopleContact,
  unknown
> = z.object({
  name: types.nullable(types.string()),
  headline: types.nullable(types.string()),
  company: types.nullable(types.string()),
  location: types.nullable(types.string()),
  profileUrl: types.nullable(types.string()),
  completeness: types.string(),
  lane: types.optional(PublicFindPeopleLane$inboundSchema),
  summary: types.optional(types.string()),
  connections: types.optional(types.number()),
  imageUrl: types.optional(types.string()),
  publicIdentifier: types.optional(types.string()),
  networkDistance: types.optional(types.string()),
  education: types.optional(types.string()),
  followers: types.optional(types.number()),
  badge: types.optional(types.string()),
  languages: types.optional(z.array(types.string())),
  positions: types.optional(
    z.array(z.lazy(() => PublicFindPeoplePosition$inboundSchema)),
  ),
  educations: types.optional(
    z.array(z.lazy(() => PublicFindPeopleEducation$inboundSchema)),
  ),
  recentPosts: types.optional(z.array(z.lazy(() => RecentPost$inboundSchema))),
});

export function publicFindPeopleContactFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleContact, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeopleContact$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleContact' from JSON`,
  );
}

/** @internal */
export const PublicFindPeopleResponse$inboundSchema: z.ZodMiniType<
  PublicFindPeopleResponse,
  unknown
> = z.object({
  source: PublicFindPeopleSource$inboundSchema,
  provider: types.string(),
  query: types.string(),
  existingCount: types.optional(types.number()),
  askedFor: types.optional(types.number()),
  exhaustionNote: types.optional(types.string()),
  laneNote: types.optional(types.string()),
  refused: types.optional(Refused$inboundSchema),
  notFound: types.optional(z.array(types.string())),
  urlsFound: types.number(),
  newUrls: types.number(),
  hydrated: types.number(),
  searches: types.optional(types.number()),
  searchesFailed: types.optional(types.number()),
  moreAvailable: types.optional(types.boolean()),
  exhaustion: types.optional(Exhaustion$inboundSchema),
  axesTried: types.optional(z.array(z.lazy(() => AxesTried$inboundSchema))),
  axesAvailable: types.optional(
    z.array(z.lazy(() => AxesAvailable$inboundSchema)),
  ),
  countryRiskNote: types.optional(z.lazy(() => CountryRiskNote$inboundSchema)),
  rateLimited: types.optional(types.number()),
  note: types.optional(types.string()),
  stillRunning: types.optional(types.boolean()),
  partial: types.optional(types.boolean()),
  progress: types.optional(z.lazy(() => Progress$inboundSchema)),
  creditsExhausted: types.optional(types.number()),
  creditsUsed: types.optional(types.number()),
  duplicates: types.optional(types.number()),
  queries: types.optional(z.array(types.string())),
  pooled: types.optional(types.number()),
  poolTargeted: types.optional(types.boolean()),
  listTotal: types.optional(types.number()),
  companies: types.optional(
    z.array(z.lazy(() => PublicFindPeopleCompanyResponse$inboundSchema)),
  ),
  notSearched: types.optional(z.array(z.lazy(() => NotSearched$inboundSchema))),
  saved: types.optional(z.lazy(() => Saved$inboundSchema)),
  ambiguity: types.optional(z.array(z.lazy(() => Ambiguity$inboundSchema))),
  needs: types.optional(z.lazy(() => Needs$inboundSchema)),
  honoured: types.optional(z.array(types.string())),
  union: types.optional(z.lazy(() => Union$inboundSchema)),
  approvedBy: types.optional(ApprovedBy$inboundSchema),
  linkedinSearches: types.optional(types.number()),
  excludedCount: types.optional(types.number()),
  contacts: z.array(z.lazy(() => PublicFindPeopleContact$inboundSchema)),
});

export function publicFindPeopleResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicFindPeopleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicFindPeopleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicFindPeopleResponse' from JSON`,
  );
}

/** @internal */
export type PublicFindCompaniesTitle$Outbound = string | Array<string>;

/** @internal */
export const PublicFindCompaniesTitle$outboundSchema: z.ZodMiniType<
  PublicFindCompaniesTitle$Outbound,
  PublicFindCompaniesTitle
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindCompaniesTitleToJSON(
  publicFindCompaniesTitle: PublicFindCompaniesTitle,
): string {
  return JSON.stringify(
    PublicFindCompaniesTitle$outboundSchema.parse(publicFindCompaniesTitle),
  );
}

/** @internal */
export type PublicFindCompaniesLocation$Outbound = string | Array<string>;

/** @internal */
export const PublicFindCompaniesLocation$outboundSchema: z.ZodMiniType<
  PublicFindCompaniesLocation$Outbound,
  PublicFindCompaniesLocation
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindCompaniesLocationToJSON(
  publicFindCompaniesLocation: PublicFindCompaniesLocation,
): string {
  return JSON.stringify(
    PublicFindCompaniesLocation$outboundSchema.parse(
      publicFindCompaniesLocation,
    ),
  );
}

/** @internal */
export type PublicFindCompaniesCompanyUnion$Outbound = string | Array<string>;

/** @internal */
export const PublicFindCompaniesCompanyUnion$outboundSchema: z.ZodMiniType<
  PublicFindCompaniesCompanyUnion$Outbound,
  PublicFindCompaniesCompanyUnion
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindCompaniesCompanyUnionToJSON(
  publicFindCompaniesCompanyUnion: PublicFindCompaniesCompanyUnion,
): string {
  return JSON.stringify(
    PublicFindCompaniesCompanyUnion$outboundSchema.parse(
      publicFindCompaniesCompanyUnion,
    ),
  );
}

/** @internal */
export type PublicFindCompaniesIndustry$Outbound = string | Array<string>;

/** @internal */
export const PublicFindCompaniesIndustry$outboundSchema: z.ZodMiniType<
  PublicFindCompaniesIndustry$Outbound,
  PublicFindCompaniesIndustry
> = smartUnion([z.string(), z.array(z.string())]);

export function publicFindCompaniesIndustryToJSON(
  publicFindCompaniesIndustry: PublicFindCompaniesIndustry,
): string {
  return JSON.stringify(
    PublicFindCompaniesIndustry$outboundSchema.parse(
      publicFindCompaniesIndustry,
    ),
  );
}
