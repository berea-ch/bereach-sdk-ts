/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as discriminatedUnionTypes from "../../types/discriminated-union.js";
import { discriminatedUnion } from "../../types/discriminated-union.js";
import * as openEnums from "../../types/enums.js";
import { ClosedEnum, OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { smartUnion } from "../../types/smart-union.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  ContactsGetByUrlContact,
  ContactsGetByUrlContact$inboundSchema,
  ContactsGetByUrlMeta,
  ContactsGetByUrlMeta$inboundSchema,
} from "./contacts-get-by-url-meta.js";

/**
 * Contact found
 */
export type ContactsGetByUrlResponse = {
  success: true;
  contact: ContactsGetByUrlContact;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ContactsGetByUrlMeta | undefined;
};

export const QueryParamStatus = {
  Draft: "draft",
  Scheduled: "scheduled",
  Sending: "sending",
  Sent: "sent",
  Failed: "failed",
  Cancelled: "cancelled",
} as const;
export type QueryParamStatus = ClosedEnum<typeof QueryParamStatus>;

export type ScheduledMessageListRequest = {
  status?: QueryParamStatus | undefined;
  contactId?: string | undefined;
  campaignSlug?: string | undefined;
  limit?: number | undefined;
  offset?: number | undefined;
};

/**
 * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
 */
export const MessageStatus = {
  Draft: "draft",
  Scheduled: "scheduled",
  Sending: "sending",
  Sent: "sent",
  Failed: "failed",
  Cancelled: "cancelled",
} as const;
/**
 * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
 */
export type MessageStatus = OpenEnum<typeof MessageStatus>;

export type ScheduledMessageListFirstDmCancelled = {
  kind: "cancelled";
  scheduledMessageId: string;
  reason: string | null;
};

export type ScheduledMessageListFirstDmFailed = {
  kind: "failed";
  scheduledMessageId: string;
  reason: string | null;
  canRequeue: true;
};

export type ScheduledMessageListFirstDmSent = {
  kind: "sent";
  scheduledMessageId: string;
  sentAt: string | null;
};

export type ScheduledMessageListFirstDmSending = {
  kind: "sending";
  scheduledMessageId: string;
};

export const FirstDmMessageStatus = {
  Next: "next",
  Waiting: "waiting",
  WaitingAccept: "waiting_accept",
  Accepted: "accepted",
  Window: "window",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
export type FirstDmMessageStatus = OpenEnum<typeof FirstDmMessageStatus>;

export type ScheduledMessageListWhy = {
  status: FirstDmMessageStatus;
  /**
   * Why it has not gone yet, in plain words.
   */
  detail: string;
  /**
   * 1-based place in line when ready; null while set aside.
   */
  position: number | null;
  /**
   * For `window`, when sending hours reopen; for `accepted`, when the pause before the first message lifts.
   */
  opensAt: string | null;
  clearsItself: boolean | null;
  fixUrl: string | null;
};

export type ScheduledMessageListFirstDmQueued = {
  kind: "queued";
  scheduledMessageId: string;
  why: ScheduledMessageListWhy;
};

export type ScheduledMessageListFirstDmHeld = {
  kind: "held";
  scheduledMessageId: string;
  reason: string;
};

export type ScheduledMessageListFirstDmWritten = {
  kind: "written";
  scheduledMessageId: string;
};

export type ScheduledMessageListFirstDmNone = {
  kind: "none";
};

/**
 * Where this person's first message stands, from the one projection every surface reads. Present on list rows.
 */
export type ScheduledMessageListFirstDmUnion =
  | ScheduledMessageListFirstDmNone
  | ScheduledMessageListFirstDmWritten
  | ScheduledMessageListFirstDmHeld
  | ScheduledMessageListFirstDmQueued
  | ScheduledMessageListFirstDmSending
  | ScheduledMessageListFirstDmSent
  | ScheduledMessageListFirstDmFailed
  | ScheduledMessageListFirstDmCancelled
  | discriminatedUnionTypes.Unknown<"kind">;

export const MessageInviteStatus = {
  Next: "next",
  Waiting: "waiting",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
export type MessageInviteStatus = OpenEnum<typeof MessageInviteStatus>;

/**
 * Where this person's connection request stands, from the invitation lane's own projection. Present on connection_request rows.
 */
export type ScheduledMessageListInvite = {
  status: MessageInviteStatus;
  detail: string;
  clearsItself: boolean | null;
  fixUrl: string | null;
};

/**
 * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
 */
export const ScheduledMessageListConnection = {
  Connected: "connected",
  Invited: "invited",
  NotConnected: "not_connected",
} as const;
/**
 * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
 */
export type ScheduledMessageListConnection = OpenEnum<
  typeof ScheduledMessageListConnection
>;

/**
 * Why the run held this message out of the send, and the line it objected to. Present while it is held.
 */
export type ScheduledMessageListHold = {
  reason: string;
  quote?: string | null | undefined;
};

export type ScheduledMessageListMessage = {
  id: string;
  contactId: string;
  /**
   * Contact name (included in list, absent in create)
   */
  contactName?: string | null | undefined;
  /**
   * Contact LinkedIn URL (included in list, absent in create)
   */
  contactLinkedinUrl?: string | null | undefined;
  message: string;
  /**
   * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
   */
  status: MessageStatus;
  /**
   * Legacy. A first message has no send time: the line decides when it goes out. Absent on new rows.
   */
  scheduledSendAt?: string | null | undefined;
  sentAt: string | null;
  campaignSlug: string | null;
  /**
   * Error message if status is 'failed'
   */
  errorMessage?: string | null | undefined;
  createdAt: string;
  contactHeadline?: string | null | undefined;
  contactCompany?: string | null | undefined;
  contactLocation?: string | null | undefined;
  /**
   * Network distance to this contact.
   */
  contactDegree?: string | null | undefined;
  contactProfilePicture?: string | null | undefined;
  contactHotScore?: number | null | undefined;
  /**
   * How many messages and invitations this contact has been sent, across every account you hold, the number that decides whether another is appropriate.
   */
  contactSentDmCount?: number | null | undefined;
  /**
   * Why this contact was judged a fit.
   */
  contactQualificationNotes?: string | null | undefined;
  /**
   * Duplicate of contactQualificationNotes, kept for older callers. Prefer the long name.
   */
  contactQualNotes?: string | null | undefined;
  contactLeadBrief?: string | null | undefined;
  /**
   * Where this person stands in the list: contact, lead, qualified, or rejected.
   */
  contactLifecycleStage?: string | null | undefined;
  /**
   * What this row will send. A connection request carries no body, which is why a queue of them is not a queue of drafts.
   */
  messageType?: string | null | undefined;
  campaignId?: string | null | undefined;
  campaignName?: string | null | undefined;
  updatedAt?: string | null | undefined;
  /**
   * Which connected account will send it.
   */
  linkedinCredentialsId?: string | null | undefined;
  /**
   * Where this person's first message stands, from the one projection every surface reads. Present on list rows.
   */
  firstDm?:
    | ScheduledMessageListFirstDmNone
    | ScheduledMessageListFirstDmWritten
    | ScheduledMessageListFirstDmHeld
    | ScheduledMessageListFirstDmQueued
    | ScheduledMessageListFirstDmSending
    | ScheduledMessageListFirstDmSent
    | ScheduledMessageListFirstDmFailed
    | ScheduledMessageListFirstDmCancelled
    | discriminatedUnionTypes.Unknown<"kind">
    | undefined;
  /**
   * Where this person's connection request stands, from the invitation lane's own projection. Present on connection_request rows.
   */
  invite?: ScheduledMessageListInvite | null | undefined;
  /**
   * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
   */
  connection?: ScheduledMessageListConnection | null | undefined;
  /**
   * Why the run held this message out of the send, and the line it objected to. Present while it is held.
   */
  hold?: ScheduledMessageListHold | null | undefined;
};

export type ScheduledMessageListCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ScheduledMessageListMeta = {
  credits: ScheduledMessageListCredits;
};

/**
 * Messages list
 */
export type ScheduledMessageListResponse = {
  success: true;
  messages: Array<ScheduledMessageListMessage>;
  total: number;
  limit: number;
  offset: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ScheduledMessageListMeta | undefined;
};

/**
 * Default 'draft'. 'scheduled' means approve at once: the message is put in line and goes out once the person is connected, at the account's pace.
 */
export const StatusRequest = {
  Draft: "draft",
  Scheduled: "scheduled",
} as const;
/**
 * Default 'draft'. 'scheduled' means approve at once: the message is put in line and goes out once the person is connected, at the account's pace.
 */
export type StatusRequest = ClosedEnum<typeof StatusRequest>;

export type ScheduledMessageCreateRequest = {
  /**
   * Internal contact ID
   */
  contactId: string;
  /**
   * DM text
   */
  message: string;
  /**
   * Default 'draft'. 'scheduled' means approve at once: the message is put in line and goes out once the person is connected, at the account's pace.
   */
  status?: StatusRequest | undefined;
  /**
   * A first message has no stored send time: the line decides when it goes out. A time within 10 minutes of now is read as an ask to approve and send at once, like status 'scheduled'; anything further out is ignored.
   */
  scheduledSendAt?: string | undefined;
  /**
   * Which list to file the draft under. Omit it: the draft is filed with the person it is for, which is what a chat turn wants.
   */
  campaignSlug?: string | undefined;
};

/**
 * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
 */
export const ScheduledMessageStatus = {
  Draft: "draft",
  Scheduled: "scheduled",
  Sending: "sending",
  Sent: "sent",
  Failed: "failed",
  Cancelled: "cancelled",
} as const;
/**
 * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
 */
export type ScheduledMessageStatus = OpenEnum<typeof ScheduledMessageStatus>;

export type ScheduledMessageCreateFirstDmCancelled = {
  kind: "cancelled";
  scheduledMessageId: string;
  reason: string | null;
};

export type ScheduledMessageCreateFirstDmFailed = {
  kind: "failed";
  scheduledMessageId: string;
  reason: string | null;
  canRequeue: true;
};

export type ScheduledMessageCreateFirstDmSent = {
  kind: "sent";
  scheduledMessageId: string;
  sentAt: string | null;
};

export type ScheduledMessageCreateFirstDmSending = {
  kind: "sending";
  scheduledMessageId: string;
};

export const FirstDmScheduledMessageStatus = {
  Next: "next",
  Waiting: "waiting",
  WaitingAccept: "waiting_accept",
  Accepted: "accepted",
  Window: "window",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
export type FirstDmScheduledMessageStatus = OpenEnum<
  typeof FirstDmScheduledMessageStatus
>;

export type ScheduledMessageCreateWhy = {
  status: FirstDmScheduledMessageStatus;
  /**
   * Why it has not gone yet, in plain words.
   */
  detail: string;
  /**
   * 1-based place in line when ready; null while set aside.
   */
  position: number | null;
  /**
   * For `window`, when sending hours reopen; for `accepted`, when the pause before the first message lifts.
   */
  opensAt: string | null;
  clearsItself: boolean | null;
  fixUrl: string | null;
};

export type ScheduledMessageCreateFirstDmQueued = {
  kind: "queued";
  scheduledMessageId: string;
  why: ScheduledMessageCreateWhy;
};

export type ScheduledMessageCreateFirstDmHeld = {
  kind: "held";
  scheduledMessageId: string;
  reason: string;
};

export type ScheduledMessageCreateFirstDmWritten = {
  kind: "written";
  scheduledMessageId: string;
};

export type ScheduledMessageCreateFirstDmNone = {
  kind: "none";
};

/**
 * Where this person's first message stands, from the one projection every surface reads. Present on list rows.
 */
export type ScheduledMessageCreateFirstDmUnion =
  | ScheduledMessageCreateFirstDmNone
  | ScheduledMessageCreateFirstDmWritten
  | ScheduledMessageCreateFirstDmHeld
  | ScheduledMessageCreateFirstDmQueued
  | ScheduledMessageCreateFirstDmSending
  | ScheduledMessageCreateFirstDmSent
  | ScheduledMessageCreateFirstDmFailed
  | ScheduledMessageCreateFirstDmCancelled
  | discriminatedUnionTypes.Unknown<"kind">;

export const ScheduledMessageInviteStatus = {
  Next: "next",
  Waiting: "waiting",
  Blocked: "blocked",
  NotOnList: "not-on-list",
} as const;
export type ScheduledMessageInviteStatus = OpenEnum<
  typeof ScheduledMessageInviteStatus
>;

/**
 * Where this person's connection request stands, from the invitation lane's own projection. Present on connection_request rows.
 */
export type ScheduledMessageCreateInvite = {
  status: ScheduledMessageInviteStatus;
  detail: string;
  clearsItself: boolean | null;
  fixUrl: string | null;
};

/**
 * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
 */
export const ScheduledMessageCreateConnection = {
  Connected: "connected",
  Invited: "invited",
  NotConnected: "not_connected",
} as const;
/**
 * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
 */
export type ScheduledMessageCreateConnection = OpenEnum<
  typeof ScheduledMessageCreateConnection
>;

/**
 * Why the run held this message out of the send, and the line it objected to. Present while it is held.
 */
export type ScheduledMessageCreateHold = {
  reason: string;
  quote?: string | null | undefined;
};

export type ScheduledMessage = {
  id: string;
  contactId: string;
  /**
   * Contact name (included in list, absent in create)
   */
  contactName?: string | null | undefined;
  /**
   * Contact LinkedIn URL (included in list, absent in create)
   */
  contactLinkedinUrl?: string | null | undefined;
  message: string;
  /**
   * The row's own status. For a first message, `scheduled` is legacy, read as `draft` (see `firstDm`); for a connection request, `scheduled` means approved and in the invitation queue.
   */
  status: ScheduledMessageStatus;
  /**
   * Legacy. A first message has no send time: the line decides when it goes out. Absent on new rows.
   */
  scheduledSendAt?: string | null | undefined;
  sentAt: string | null;
  campaignSlug: string | null;
  /**
   * Error message if status is 'failed'
   */
  errorMessage?: string | null | undefined;
  createdAt: string;
  contactHeadline?: string | null | undefined;
  contactCompany?: string | null | undefined;
  contactLocation?: string | null | undefined;
  /**
   * Network distance to this contact.
   */
  contactDegree?: string | null | undefined;
  contactProfilePicture?: string | null | undefined;
  contactHotScore?: number | null | undefined;
  /**
   * How many messages and invitations this contact has been sent, across every account you hold, the number that decides whether another is appropriate.
   */
  contactSentDmCount?: number | null | undefined;
  /**
   * Why this contact was judged a fit.
   */
  contactQualificationNotes?: string | null | undefined;
  /**
   * Duplicate of contactQualificationNotes, kept for older callers. Prefer the long name.
   */
  contactQualNotes?: string | null | undefined;
  contactLeadBrief?: string | null | undefined;
  /**
   * Where this person stands in the list: contact, lead, qualified, or rejected.
   */
  contactLifecycleStage?: string | null | undefined;
  /**
   * What this row will send. A connection request carries no body, which is why a queue of them is not a queue of drafts.
   */
  messageType?: string | null | undefined;
  campaignId?: string | null | undefined;
  campaignName?: string | null | undefined;
  updatedAt?: string | null | undefined;
  /**
   * Which connected account will send it.
   */
  linkedinCredentialsId?: string | null | undefined;
  /**
   * Where this person's first message stands, from the one projection every surface reads. Present on list rows.
   */
  firstDm?:
    | ScheduledMessageCreateFirstDmNone
    | ScheduledMessageCreateFirstDmWritten
    | ScheduledMessageCreateFirstDmHeld
    | ScheduledMessageCreateFirstDmQueued
    | ScheduledMessageCreateFirstDmSending
    | ScheduledMessageCreateFirstDmSent
    | ScheduledMessageCreateFirstDmFailed
    | ScheduledMessageCreateFirstDmCancelled
    | discriminatedUnionTypes.Unknown<"kind">
    | undefined;
  /**
   * Where this person's connection request stands, from the invitation lane's own projection. Present on connection_request rows.
   */
  invite?: ScheduledMessageCreateInvite | null | undefined;
  /**
   * Where this account stands with them, which decides what approving does: connected means the message goes in line, invited means it waits for an acceptance, not_connected means a connection request goes out first. Null on a connection request row.
   */
  connection?: ScheduledMessageCreateConnection | null | undefined;
  /**
   * Why the run held this message out of the send, and the line it objected to. Present while it is held.
   */
  hold?: ScheduledMessageCreateHold | null | undefined;
};

export type ScheduledMessageCreateCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ScheduledMessageCreateMeta = {
  credits: ScheduledMessageCreateCredits;
};

/**
 * Message created, or an existing one returned instead
 */
export type ScheduledMessageCreateResponse = {
  success: true;
  scheduledMessage: ScheduledMessage;
  contactName?: string | null | undefined;
  /**
   * True when an existing message was returned rather than a new one created.
   */
  duplicate?: boolean | undefined;
  /**
   * True when a recent message to this contact is holding a new one back.
   */
  cooldown?: boolean | undefined;
  /**
   * Why the cooldown applies.
   */
  cooldownReason?: string | undefined;
  /**
   * Present when the requested list matched nothing and the draft was filed with the person instead; names where it landed.
   */
  campaignNote?: string | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ScheduledMessageCreateMeta | undefined;
};

export type DraftScheduleRequest = {
  /**
   * Put every draft of these contacts in line, within ONE list: campaignId or campaignSlug is required with this, or the call is refused. Prefer messageIds, read from the outbox
   */
  contactIds?: Array<string> | undefined;
  /**
   * Put these messages in line
   */
  messageIds?: Array<string> | undefined;
  /**
   * Move these to the front of the line. They still wait for the person to be connected.
   */
  sendNow?: boolean | undefined;
  /**
   * A time within 10 minutes of now is read the same as sendNow; anything later is ignored.
   */
  scheduledSendAt?: string | undefined;
  /**
   * Map of messageId to new content for last-minute edits before approving
   */
  editedMessages?: { [k: string]: string } | undefined;
};

export type DraftScheduleSkipped = {
  contactId: string;
  scheduledMessageId: string;
  reason: string;
};

export type DraftScheduleCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type DraftScheduleMeta = {
  credits: DraftScheduleCredits;
};

/**
 * Messages put in line
 */
export type DraftScheduleResponse = {
  success: true;
  /**
   * Total now in line: messages (queued, put back, or already there) plus connection requests among them, counted in invitationsScheduled below.
   */
  scheduled: number;
  /**
   * Connection requests among the ids, handed to the invitation line.
   */
  invitationsScheduled?: number | undefined;
  /**
   * Messages that entered the line.
   */
  queued: number;
  /**
   * Messages that could not be sent before and are back in line.
   */
  requeued: number;
  /**
   * Messages that were already in line.
   */
  alreadyQueued: number;
  /**
   * Messages held for a read; release the hold first.
   */
  held: number;
  /**
   * Rows the line would not take, each with the reason.
   */
  skipped: Array<DraftScheduleSkipped>;
  /**
   * Invitations put in line for people not connected yet.
   */
  invitesQueued: number;
  /**
   * The connected accounts whose lines gained a message.
   */
  accounts: Array<string>;
  /**
   * Present when a send time was passed: it was ignored, and this says why.
   */
  note?: string | undefined;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: DraftScheduleMeta | undefined;
};

export type ScheduledMessageUpdateRequest = {
  /**
   * Scheduled message id to edit
   */
  messageId: string;
  /**
   * New DM text
   */
  message?: string | undefined;
  /**
   * Accepted and ignored: a first message has no send time.
   */
  scheduledSendAt?: string | null | undefined;
};

export type ScheduledMessageUpdateIneligible = {
  id: string;
  currentStatus: string;
};

export type ScheduledMessageUpdateCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ScheduledMessageUpdateMeta = {
  credits: ScheduledMessageUpdateCredits;
};

/**
 * Message updated
 */
export type ScheduledMessageUpdateResponse = {
  success: true;
  /**
   * How many rows were edited.
   */
  updated: number;
  ineligible: Array<ScheduledMessageUpdateIneligible>;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ScheduledMessageUpdateMeta | undefined;
};

export type ScheduledMessageCancelRequest = {
  /**
   * Cancel specific messages
   */
  messageIds?: Array<string> | undefined;
  /**
   * Cancel everything still waiting for these contacts, within the list named by campaignId or campaignSlug when one is given. Prefer messageIds, read from the outbox
   */
  contactIds?: Array<string> | undefined;
};

export type ScheduledMessageCancelCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ScheduledMessageCancelMeta = {
  credits: ScheduledMessageCancelCredits;
};

/**
 * Messages cancelled
 */
export type ScheduledMessageCancelResponse = {
  success: true;
  cancelled: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ScheduledMessageCancelMeta | undefined;
};

export type ReviewDraftsRequest = {
  /**
   * The list the drafts belong to. Optional: when omitted it is read from the message ids, which must all belong to one list.
   */
  campaignId?: string | undefined;
  /**
   * Message IDs to approve and send
   */
  approved?: Array<string> | undefined;
  /**
   * Message IDs to reject/cancel
   */
  rejected?: Array<string> | undefined;
  /**
   * Map of messageId to new text (applied before approval)
   */
  editedMessages?: { [k: string]: string } | undefined;
};

export type Approved = {
  id: string;
  currentStatus: string | null;
};

export type Rejected = {
  id: string;
  currentStatus: string | null;
};

/**
 * Ids that were not in draft, with the status they were in.
 */
export type ReviewDraftsIneligible = {
  approved: Array<Approved>;
  rejected: Array<Rejected>;
};

export type ReviewDraftsSkipped = {
  contactId: string;
  scheduledMessageId: string;
  reason: string;
};

/**
 * What the line did with the approved first messages. Null when nothing was approved.
 */
export type Line = {
  queued: number;
  requeued: number;
  alreadyQueued: number;
  held: number;
  skipped: Array<ReviewDraftsSkipped>;
  invitesQueued: number;
  accounts: Array<string>;
};

export type LaneBlocked = {
  id: string;
  messageType: string;
  task: string;
};

/**
 * Review applied
 */
export type ReviewDraftsResponse = {
  success: true;
  /**
   * Rows now in line: first messages queued, put back or already there, plus connection requests approved.
   */
  approved: number;
  rejected: number;
  /**
   * Ids that were not in draft, with the status they were in.
   */
  ineligible: ReviewDraftsIneligible;
  /**
   * Connection requests among the approved ids, handed to the invitation line.
   */
  invitationsApproved: number;
  /**
   * What the line did with the approved first messages. Null when nothing was approved.
   */
  line: Line | null;
  /**
   * Rows not approved because their lane is switched off for the list.
   */
  laneBlocked: Array<LaneBlocked>;
};

export type ContextListRequest = {
  /**
   * Filter by scope, prefix-matched: "user" for owner context, "user:icp:" for every saved ICP, "user:playbook:" for every saved playbook.
   */
  scope?: string | undefined;
  /**
   * Filter by type (e.g. "icp", "playbook")
   */
  type?: string | undefined;
  /**
   * Max entries to return (default 20, max 50). Paginate with offset for more.
   */
  limit?: number | undefined;
  /**
   * Pagination offset
   */
  offset?: number | undefined;
  /**
   * Return full content instead of 200-char preview. Use only when retrieving a specific entry (combine with type+scope filters).
   */
  fullContent?: boolean | undefined;
};

export type ContextListEntry = {
  /**
   * Unique entry ID
   */
  id: string;
  type: string;
  label: string | null;
  /**
   * Full content (present when fullContent=true) or a 200-char preview
   */
  content: string;
  /**
   * First 200 chars of content: present when fullContent is not requested
   */
  contentPreview?: string | undefined;
  scope: string;
  updatedAt: string;
};

export type ContextListCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ContextListMeta = {
  credits: ContextListCredits;
};

/**
 * Context entries
 */
export type ContextListResponse = {
  success: true;
  entries: Array<ContextListEntry>;
  /**
   * Total matching entries
   */
  total: number;
  hasMore: boolean;
  /**
   * Where this page started. Add the row count to it for the next page.
   */
  offset: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ContextListMeta | undefined;
};

export type ContextSetRequest = {
  /**
   * One of four closed slots, user-profile, offering, icp or playbook, chosen by what the content means for outreach (global identity or offering, per-chat targeting, or a named first-DM voice), never a free-form custom:* slug.
   */
  type: string;
  /**
   * Markdown content, or the document itself when it is structured.
   */
  content?: string | undefined;
  /**
   * Omit scope and pass label so the server derives and attaches "user:icp:<slug>" or "user:playbook:<slug>" (identity/offering fields keep plain "user", rejected for icp/playbook); to reuse an asset already saved instead of rewriting it, send its existing scope alone with no content.
   */
  scope?: string | undefined;
  /**
   * Display label
   */
  label?: string | undefined;
  /**
   * Internal ticket id from a prior tool result. Omit unless a result supplied it.
   */
  postTicketId?: string | undefined;
};

export type ContextSetEntry = {
  /**
   * Unique entry ID
   */
  id: string;
  type: string;
  label: string | null;
  /**
   * Full content (present when fullContent=true) or a 200-char preview
   */
  content: string;
  /**
   * First 200 chars of content: present when fullContent is not requested
   */
  contentPreview?: string | undefined;
  scope: string;
  updatedAt: string;
};

export type ContextSetCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ContextSetMeta = {
  credits: ContextSetCredits;
};

/**
 * Context saved
 */
export type ContextSetResponse = {
  success: true;
  entry: ContextSetEntry;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ContextSetMeta | undefined;
};

export type ContextDeleteRequest = {
  /**
   * Context type to delete
   */
  type: string;
  /**
   * Scope (defaults to "user")
   */
  scope?: string | undefined;
};

export type ContextDeleteCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type ContextDeleteMeta = {
  credits: ContextDeleteCredits;
};

/**
 * Context deleted
 */
export type ContextDeleteResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: ContextDeleteMeta | undefined;
};

export type EventsFeedRequest = {
  /**
   * ISO timestamp or epoch ms (default: 2 hours ago)
   */
  since?: string | undefined;
  limit?: number | undefined;
};

export const EventsFeedType = {
  TaskCompleted: "task:completed",
  TaskFailed: "task:failed",
  ReplyReceived: "reply:received",
  ConnectionAccepted: "connection:accepted",
  CampaignRateLimited: "campaign:rate_limited",
  CampaignLinkedinExpired: "campaign:linkedin_expired",
  CredentialLlmError: "credential:llm_error",
  CredentialLinkedinLongBroken: "credential:linkedin_long_broken",
  ContactHandover: "contact:handover",
  DraftsPending: "drafts:pending",
  SystemCacheWarning: "system:cache_warning",
} as const;
export type EventsFeedType = OpenEnum<typeof EventsFeedType>;

export type Event = {
  id: string;
  type: EventsFeedType;
  campaignId?: string | undefined;
  campaignName?: string | undefined;
  /**
   * Human-readable one-line summary
   */
  summary: string;
  metadata: { [k: string]: any };
  /**
   * Unix epoch ms
   */
  timestamp: number;
};

/**
 * Events
 */
export type EventsFeedResponse = {
  events: Array<Event>;
  /**
   * Timestamp of most recent event (pass as 'since' for next poll)
   */
  cursor?: number | undefined;
};

export type Context = {
  type: string;
  label: string | null;
  content: string;
  scope: string;
};

export const ActiveAccountStatus = {
  Connected: "connected",
  Expired: "expired",
} as const;
export type ActiveAccountStatus = OpenEnum<typeof ActiveAccountStatus>;

export type ActiveAccount = {
  id: string;
  name: string | null;
  plan: string;
  headline: string | null;
  status: ActiveAccountStatus;
  isUnlimited: boolean;
  /**
   * The workspace's shared credit limit, the same on every account row.
   */
  creditsLimit: number;
  /**
   * Credits the workspace has used this cycle, the same on every account row.
   */
  creditsCount: number;
  simulateWrites: boolean;
  isCurrent: boolean;
};

export const ContextGetAccountStatus = {
  Connected: "connected",
  Expired: "expired",
} as const;
export type ContextGetAccountStatus = OpenEnum<typeof ContextGetAccountStatus>;

export type ContextGetAccount = {
  id: string;
  name: string | null;
  plan: string;
  headline: string | null;
  status: ContextGetAccountStatus;
  isUnlimited: boolean;
  /**
   * The workspace's shared credit limit, the same on every account row.
   */
  creditsLimit: number;
  /**
   * Credits the workspace has used this cycle, the same on every account row.
   */
  creditsCount: number;
  simulateWrites: boolean;
  isCurrent: boolean;
};

export type ContextGetStageCounts = {
  contact: number;
  lead: number;
  qualified: number;
  rejected: number;
  total: number;
};

export type ActiveCampaign = {
  id: string;
  name: string;
  stageCounts: ContextGetStageCounts;
};

export const ContextGetType = {
  TaskCompleted: "task:completed",
  TaskFailed: "task:failed",
  ReplyReceived: "reply:received",
  ConnectionAccepted: "connection:accepted",
  CampaignRateLimited: "campaign:rate_limited",
  CampaignLinkedinExpired: "campaign:linkedin_expired",
  CredentialLlmError: "credential:llm_error",
  CredentialLinkedinLongBroken: "credential:linkedin_long_broken",
  ContactHandover: "contact:handover",
  DraftsPending: "drafts:pending",
  SystemCacheWarning: "system:cache_warning",
} as const;
export type ContextGetType = OpenEnum<typeof ContextGetType>;

export type RecentEvent = {
  id: string;
  type: ContextGetType;
  campaignId?: string | undefined;
  campaignName?: string | undefined;
  /**
   * Human-readable one-line summary
   */
  summary: string;
  metadata: { [k: string]: any };
  /**
   * Unix epoch ms
   */
  timestamp: number;
};

/**
 * Session snapshot
 */
export type ContextGetResponse = {
  /**
   * Credit balance and usage
   */
  credits?: any | null | undefined;
  /**
   * People counts: fit, in the list, not a fit
   */
  pipeline: { [k: string]: number };
  contexts: Array<Context>;
  pendingDrafts: number;
  failedDrafts: number;
  pendingSentInvitations: number;
  activeAccount: ActiveAccount | null;
  accounts: Array<ContextGetAccount>;
  activeCampaigns: Array<ActiveCampaign>;
  sessionMeta?: any | null | undefined;
  onboardingState?: any | null | undefined;
  recentEvents: Array<RecentEvent>;
  /**
   * Set when the LLM circuit breaker has tripped for this credential.
   */
  llmStatus?: string | null | undefined;
  /**
   * Epoch ms the breaker first tripped, so a caller can say how long it has been paused.
   */
  llmFirstTrippedAt?: number | null | undefined;
};

export type GetDmPollingSettingsSettings = {
  dmWebhookUrl: string | null;
  connectionLastPolledAt: string | null;
};

export type GetDmPollingSettingsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type GetDmPollingSettingsMeta = {
  credits: GetDmPollingSettingsCredits;
};

/**
 * Polling settings
 */
export type GetDmPollingSettingsResponse = {
  success: true;
  settings: GetDmPollingSettingsSettings;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: GetDmPollingSettingsMeta | undefined;
};

export type PatchDmPollingSettingsRequest = {
  /**
   * Webhook URL for connection_accepted + conversation_updated events
   */
  dmWebhookUrl?: string | null | undefined;
};

export type PatchDmPollingSettingsSettings = {
  dmWebhookUrl: string | null;
  connectionLastPolledAt: string | null;
};

export type PatchDmPollingSettingsCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type PatchDmPollingSettingsMeta = {
  credits: PatchDmPollingSettingsCredits;
};

/**
 * Settings updated
 */
export type PatchDmPollingSettingsResponse = {
  success: true;
  settings: PatchDmPollingSettingsSettings;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: PatchDmPollingSettingsMeta | undefined;
};

export type DeleteWorkspaceAccountRequest = {
  /**
   * LinkedIn credentials ID
   */
  id: string;
};

export type DeleteWorkspaceAccountCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type DeleteWorkspaceAccountMeta = {
  credits: DeleteWorkspaceAccountCredits;
};

/**
 * Account removed
 */
export type DeleteWorkspaceAccountResponse = {
  success: true;
  message: string;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: DeleteWorkspaceAccountMeta | undefined;
};

export const UpgradeWorkspaceAccountAction = {
  Upgrade: "upgrade",
  Downgrade: "downgrade",
} as const;
export type UpgradeWorkspaceAccountAction = ClosedEnum<
  typeof UpgradeWorkspaceAccountAction
>;

export type UpgradeWorkspaceAccountRequest = {
  /**
   * LinkedIn credentials ID
   */
  credentialsId: string;
  action: UpgradeWorkspaceAccountAction;
};

export type UpgradeWorkspaceAccountCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type UpgradeWorkspaceAccountMeta = {
  credits: UpgradeWorkspaceAccountCredits;
};

/**
 * Plan changed
 */
export type UpgradeWorkspaceAccountResponse = {
  success: true;
  message: string;
  seatsUsed: number;
  seatsIncluded: number;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: UpgradeWorkspaceAccountMeta | undefined;
};

/**
 * "create" to make a new invite, "list" to get existing
 */
export const CreateWorkspaceInviteAction = {
  Create: "create",
  List: "list",
} as const;
/**
 * "create" to make a new invite, "list" to get existing
 */
export type CreateWorkspaceInviteAction = ClosedEnum<
  typeof CreateWorkspaceInviteAction
>;

export type CreateWorkspaceInviteRequest = {
  /**
   * "create" to make a new invite, "list" to get existing
   */
  action: CreateWorkspaceInviteAction;
  /**
   * Invitee email (sends invite email)
   */
  email?: string | undefined;
  /**
   * Invitee name
   */
  name?: string | undefined;
  /**
   * Max times code can be used
   */
  maxUses?: number | undefined;
};

export type CreateWorkspaceInviteInvite2 = {
  id: string;
  email: string | null;
  name: string | null;
  code: string;
  status?: string | undefined;
  maxUses: number;
  useCount: number;
  expiresAt: string | null;
  createdAt: string;
};

export type Workspace = {
  tier: string;
  proSeatsIncluded: number;
  proSeatsUsed: number;
};

export type CreateWorkspaceInviteCredits2 = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CreateWorkspaceInviteMeta2 = {
  credits: CreateWorkspaceInviteCredits2;
};

export type ResponseBody2 = {
  success: true;
  invites: Array<CreateWorkspaceInviteInvite2>;
  workspace: Workspace;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CreateWorkspaceInviteMeta2 | undefined;
};

export type CreateWorkspaceInviteInvite1 = {
  id: string;
  email: string | null;
  name: string | null;
  code: string;
  status?: string | undefined;
  maxUses: number;
  useCount: number;
  expiresAt: string | null;
  createdAt: string;
};

export type CreateWorkspaceInviteCredits1 = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type CreateWorkspaceInviteMeta1 = {
  credits: CreateWorkspaceInviteCredits1;
};

export type ResponseBody1 = {
  success: true;
  invite: CreateWorkspaceInviteInvite1;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: CreateWorkspaceInviteMeta1 | undefined;
};

/**
 * Invite created or listed
 */
export type CreateWorkspaceInviteResponse = ResponseBody2 | ResponseBody1;

export type DeleteWorkspaceInviteRequest = {
  /**
   * Invite ID to delete
   */
  inviteId: string;
};

export type DeleteWorkspaceInviteCredits = {
  /**
   * Credits spent this period.
   */
  current: number;
  /**
   * Period allowance, or null when unlimited.
   */
  limit: number | null;
  /**
   * Allowance left, or null when unlimited.
   */
  remaining: number | null;
  /**
   * Share of the allowance spent, 0 to 100.
   */
  percentage: number;
  isUnlimited: boolean;
};

/**
 * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
 */
export type DeleteWorkspaceInviteMeta = {
  credits: DeleteWorkspaceInviteCredits;
};

/**
 * Invite deleted
 */
export type DeleteWorkspaceInviteResponse = {
  success: true;
  /**
   * Credits consumed by this call. 0 for free endpoints, cached results, duplicates, and for every query that does not touch LinkedIn.
   */
  creditsUsed: number;
  /**
   * Seconds to wait before another call of the same type. 0 means no wait is needed.
   */
  retryAfter: number;
  /**
   * Credit balance carried on every response so a caller never has to ask for it separately. Absent when the caller has no connected account.
   */
  meta?: DeleteWorkspaceInviteMeta | undefined;
};

/** @internal */
export const ContactsGetByUrlResponse$inboundSchema: z.ZodMiniType<
  ContactsGetByUrlResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    contact: ContactsGetByUrlContact$inboundSchema,
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(ContactsGetByUrlMeta$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function contactsGetByUrlResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContactsGetByUrlResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContactsGetByUrlResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContactsGetByUrlResponse' from JSON`,
  );
}

/** @internal */
export const QueryParamStatus$outboundSchema: z.ZodMiniEnum<
  typeof QueryParamStatus
> = z.enum(QueryParamStatus);

/** @internal */
export type ScheduledMessageListRequest$Outbound = {
  status?: string | undefined;
  contactId?: string | undefined;
  campaignSlug?: string | undefined;
  limit: number;
  offset: number;
};

/** @internal */
export const ScheduledMessageListRequest$outboundSchema: z.ZodMiniType<
  ScheduledMessageListRequest$Outbound,
  ScheduledMessageListRequest
> = z.object({
  status: z.optional(QueryParamStatus$outboundSchema),
  contactId: z.optional(z.string()),
  campaignSlug: z.optional(z.string()),
  limit: z._default(z.int(), 50),
  offset: z._default(z.int(), 0),
});

export function scheduledMessageListRequestToJSON(
  scheduledMessageListRequest: ScheduledMessageListRequest,
): string {
  return JSON.stringify(
    ScheduledMessageListRequest$outboundSchema.parse(
      scheduledMessageListRequest,
    ),
  );
}

/** @internal */
export const MessageStatus$inboundSchema: z.ZodMiniType<
  MessageStatus,
  unknown
> = openEnums.inboundSchema(MessageStatus);

/** @internal */
export const ScheduledMessageListFirstDmCancelled$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmCancelled,
  unknown
> = z.object({
  kind: types.literal("cancelled"),
  scheduledMessageId: types.string(),
  reason: types.nullable(types.string()),
});

export function scheduledMessageListFirstDmCancelledFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmCancelled, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageListFirstDmCancelled$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmCancelled' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmFailed$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmFailed,
  unknown
> = z.object({
  kind: types.literal("failed"),
  scheduledMessageId: types.string(),
  reason: types.nullable(types.string()),
  canRequeue: types.literal(true),
});

export function scheduledMessageListFirstDmFailedFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmFailed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmFailed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmFailed' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmSent$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmSent,
  unknown
> = z.object({
  kind: types.literal("sent"),
  scheduledMessageId: types.string(),
  sentAt: types.nullable(types.string()),
});

export function scheduledMessageListFirstDmSentFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmSent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmSent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmSent' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmSending$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmSending,
  unknown
> = z.object({
  kind: types.literal("sending"),
  scheduledMessageId: types.string(),
});

export function scheduledMessageListFirstDmSendingFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmSending, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageListFirstDmSending$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmSending' from JSON`,
  );
}

/** @internal */
export const FirstDmMessageStatus$inboundSchema: z.ZodMiniType<
  FirstDmMessageStatus,
  unknown
> = openEnums.inboundSchema(FirstDmMessageStatus);

/** @internal */
export const ScheduledMessageListWhy$inboundSchema: z.ZodMiniType<
  ScheduledMessageListWhy,
  unknown
> = z.object({
  status: FirstDmMessageStatus$inboundSchema,
  detail: types.string(),
  position: types.nullable(types.number()),
  opensAt: types.nullable(types.string()),
  clearsItself: types.nullable(types.boolean()),
  fixUrl: types.nullable(types.string()),
});

export function scheduledMessageListWhyFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListWhy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListWhy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListWhy' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmQueued$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmQueued,
  unknown
> = z.object({
  kind: types.literal("queued"),
  scheduledMessageId: types.string(),
  why: z.lazy(() => ScheduledMessageListWhy$inboundSchema),
});

export function scheduledMessageListFirstDmQueuedFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmQueued, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmQueued$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmQueued' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmHeld$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmHeld,
  unknown
> = z.object({
  kind: types.literal("held"),
  scheduledMessageId: types.string(),
  reason: types.string(),
});

export function scheduledMessageListFirstDmHeldFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmHeld, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmHeld$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmHeld' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmWritten$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmWritten,
  unknown
> = z.object({
  kind: types.literal("written"),
  scheduledMessageId: types.string(),
});

export function scheduledMessageListFirstDmWrittenFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmWritten, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageListFirstDmWritten$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmWritten' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmNone$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmNone,
  unknown
> = z.object({
  kind: types.literal("none"),
});

export function scheduledMessageListFirstDmNoneFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmNone, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmNone$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmNone' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListFirstDmUnion$inboundSchema: z.ZodMiniType<
  ScheduledMessageListFirstDmUnion,
  unknown
> = discriminatedUnion("kind", {
  none: z.lazy(() => ScheduledMessageListFirstDmNone$inboundSchema),
  written: z.lazy(() => ScheduledMessageListFirstDmWritten$inboundSchema),
  held: z.lazy(() => ScheduledMessageListFirstDmHeld$inboundSchema),
  queued: z.lazy(() => ScheduledMessageListFirstDmQueued$inboundSchema),
  sending: z.lazy(() => ScheduledMessageListFirstDmSending$inboundSchema),
  sent: z.lazy(() => ScheduledMessageListFirstDmSent$inboundSchema),
  failed: z.lazy(() => ScheduledMessageListFirstDmFailed$inboundSchema),
  cancelled: z.lazy(() => ScheduledMessageListFirstDmCancelled$inboundSchema),
});

export function scheduledMessageListFirstDmUnionFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListFirstDmUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListFirstDmUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListFirstDmUnion' from JSON`,
  );
}

/** @internal */
export const MessageInviteStatus$inboundSchema: z.ZodMiniType<
  MessageInviteStatus,
  unknown
> = openEnums.inboundSchema(MessageInviteStatus);

/** @internal */
export const ScheduledMessageListInvite$inboundSchema: z.ZodMiniType<
  ScheduledMessageListInvite,
  unknown
> = z.object({
  status: MessageInviteStatus$inboundSchema,
  detail: types.string(),
  clearsItself: types.nullable(types.boolean()),
  fixUrl: types.nullable(types.string()),
});

export function scheduledMessageListInviteFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListInvite, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListInvite$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListInvite' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListConnection$inboundSchema: z.ZodMiniType<
  ScheduledMessageListConnection,
  unknown
> = openEnums.inboundSchema(ScheduledMessageListConnection);

/** @internal */
export const ScheduledMessageListHold$inboundSchema: z.ZodMiniType<
  ScheduledMessageListHold,
  unknown
> = z.object({
  reason: types.string(),
  quote: z.optional(z.nullable(types.string())),
});

export function scheduledMessageListHoldFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListHold, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListHold$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListHold' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListMessage$inboundSchema: z.ZodMiniType<
  ScheduledMessageListMessage,
  unknown
> = z.object({
  id: types.string(),
  contactId: types.string(),
  contactName: z.optional(z.nullable(types.string())),
  contactLinkedinUrl: z.optional(z.nullable(types.string())),
  message: types.string(),
  status: MessageStatus$inboundSchema,
  scheduledSendAt: z.optional(z.nullable(types.string())),
  sentAt: types.nullable(types.string()),
  campaignSlug: types.nullable(types.string()),
  errorMessage: z.optional(z.nullable(types.string())),
  createdAt: types.string(),
  contactHeadline: z.optional(z.nullable(types.string())),
  contactCompany: z.optional(z.nullable(types.string())),
  contactLocation: z.optional(z.nullable(types.string())),
  contactDegree: z.optional(z.nullable(types.string())),
  contactProfilePicture: z.optional(z.nullable(types.string())),
  contactHotScore: z.optional(z.nullable(types.number())),
  contactSentDmCount: z.optional(z.nullable(types.number())),
  contactQualificationNotes: z.optional(z.nullable(types.string())),
  contactQualNotes: z.optional(z.nullable(types.string())),
  contactLeadBrief: z.optional(z.nullable(types.string())),
  contactLifecycleStage: z.optional(z.nullable(types.string())),
  messageType: z.optional(z.nullable(types.string())),
  campaignId: z.optional(z.nullable(types.string())),
  campaignName: z.optional(z.nullable(types.string())),
  updatedAt: z.optional(z.nullable(types.string())),
  linkedinCredentialsId: z.optional(z.nullable(types.string())),
  firstDm: types.optional(discriminatedUnion("kind", {
    none: z.lazy(() => ScheduledMessageListFirstDmNone$inboundSchema),
    written: z.lazy(() => ScheduledMessageListFirstDmWritten$inboundSchema),
    held: z.lazy(() => ScheduledMessageListFirstDmHeld$inboundSchema),
    queued: z.lazy(() => ScheduledMessageListFirstDmQueued$inboundSchema),
    sending: z.lazy(() => ScheduledMessageListFirstDmSending$inboundSchema),
    sent: z.lazy(() => ScheduledMessageListFirstDmSent$inboundSchema),
    failed: z.lazy(() => ScheduledMessageListFirstDmFailed$inboundSchema),
    cancelled: z.lazy(() => ScheduledMessageListFirstDmCancelled$inboundSchema),
  })),
  invite: z.optional(
    z.nullable(z.lazy(() => ScheduledMessageListInvite$inboundSchema)),
  ),
  connection: z.optional(
    z.nullable(ScheduledMessageListConnection$inboundSchema),
  ),
  hold: z.optional(
    z.nullable(z.lazy(() => ScheduledMessageListHold$inboundSchema)),
  ),
});

export function scheduledMessageListMessageFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListMessage' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListCredits$inboundSchema: z.ZodMiniType<
  ScheduledMessageListCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function scheduledMessageListCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListCredits' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListMeta$inboundSchema: z.ZodMiniType<
  ScheduledMessageListMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ScheduledMessageListCredits$inboundSchema),
});

export function scheduledMessageListMetaFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListMeta' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageListResponse$inboundSchema: z.ZodMiniType<
  ScheduledMessageListResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    messages: z.array(z.lazy(() => ScheduledMessageListMessage$inboundSchema)),
    total: types.number(),
    limit: types.number(),
    offset: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ScheduledMessageListMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function scheduledMessageListResponseFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageListResponse' from JSON`,
  );
}

/** @internal */
export const StatusRequest$outboundSchema: z.ZodMiniEnum<typeof StatusRequest> =
  z.enum(StatusRequest);

/** @internal */
export type ScheduledMessageCreateRequest$Outbound = {
  contactId: string;
  message: string;
  status?: string | undefined;
  scheduledSendAt?: string | undefined;
  campaignSlug?: string | undefined;
};

/** @internal */
export const ScheduledMessageCreateRequest$outboundSchema: z.ZodMiniType<
  ScheduledMessageCreateRequest$Outbound,
  ScheduledMessageCreateRequest
> = z.object({
  contactId: z.string(),
  message: z.string(),
  status: z.optional(StatusRequest$outboundSchema),
  scheduledSendAt: z.optional(z.string()),
  campaignSlug: z.optional(z.string()),
});

export function scheduledMessageCreateRequestToJSON(
  scheduledMessageCreateRequest: ScheduledMessageCreateRequest,
): string {
  return JSON.stringify(
    ScheduledMessageCreateRequest$outboundSchema.parse(
      scheduledMessageCreateRequest,
    ),
  );
}

/** @internal */
export const ScheduledMessageStatus$inboundSchema: z.ZodMiniType<
  ScheduledMessageStatus,
  unknown
> = openEnums.inboundSchema(ScheduledMessageStatus);

/** @internal */
export const ScheduledMessageCreateFirstDmCancelled$inboundSchema:
  z.ZodMiniType<ScheduledMessageCreateFirstDmCancelled, unknown> = z.object({
    kind: types.literal("cancelled"),
    scheduledMessageId: types.string(),
    reason: types.nullable(types.string()),
  });

export function scheduledMessageCreateFirstDmCancelledFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmCancelled, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmCancelled$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmCancelled' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmFailed$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmFailed,
  unknown
> = z.object({
  kind: types.literal("failed"),
  scheduledMessageId: types.string(),
  reason: types.nullable(types.string()),
  canRequeue: types.literal(true),
});

export function scheduledMessageCreateFirstDmFailedFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmFailed, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmFailed$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmFailed' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmSent$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmSent,
  unknown
> = z.object({
  kind: types.literal("sent"),
  scheduledMessageId: types.string(),
  sentAt: types.nullable(types.string()),
});

export function scheduledMessageCreateFirstDmSentFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmSent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateFirstDmSent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmSent' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmSending$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmSending,
  unknown
> = z.object({
  kind: types.literal("sending"),
  scheduledMessageId: types.string(),
});

export function scheduledMessageCreateFirstDmSendingFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmSending, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmSending$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmSending' from JSON`,
  );
}

/** @internal */
export const FirstDmScheduledMessageStatus$inboundSchema: z.ZodMiniType<
  FirstDmScheduledMessageStatus,
  unknown
> = openEnums.inboundSchema(FirstDmScheduledMessageStatus);

/** @internal */
export const ScheduledMessageCreateWhy$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateWhy,
  unknown
> = z.object({
  status: FirstDmScheduledMessageStatus$inboundSchema,
  detail: types.string(),
  position: types.nullable(types.number()),
  opensAt: types.nullable(types.string()),
  clearsItself: types.nullable(types.boolean()),
  fixUrl: types.nullable(types.string()),
});

export function scheduledMessageCreateWhyFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateWhy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateWhy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateWhy' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmQueued$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmQueued,
  unknown
> = z.object({
  kind: types.literal("queued"),
  scheduledMessageId: types.string(),
  why: z.lazy(() => ScheduledMessageCreateWhy$inboundSchema),
});

export function scheduledMessageCreateFirstDmQueuedFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmQueued, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmQueued$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmQueued' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmHeld$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmHeld,
  unknown
> = z.object({
  kind: types.literal("held"),
  scheduledMessageId: types.string(),
  reason: types.string(),
});

export function scheduledMessageCreateFirstDmHeldFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmHeld, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateFirstDmHeld$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmHeld' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmWritten$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmWritten,
  unknown
> = z.object({
  kind: types.literal("written"),
  scheduledMessageId: types.string(),
});

export function scheduledMessageCreateFirstDmWrittenFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmWritten, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmWritten$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmWritten' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmNone$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmNone,
  unknown
> = z.object({
  kind: types.literal("none"),
});

export function scheduledMessageCreateFirstDmNoneFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmNone, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateFirstDmNone$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmNone' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateFirstDmUnion$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateFirstDmUnion,
  unknown
> = discriminatedUnion("kind", {
  none: z.lazy(() => ScheduledMessageCreateFirstDmNone$inboundSchema),
  written: z.lazy(() => ScheduledMessageCreateFirstDmWritten$inboundSchema),
  held: z.lazy(() => ScheduledMessageCreateFirstDmHeld$inboundSchema),
  queued: z.lazy(() => ScheduledMessageCreateFirstDmQueued$inboundSchema),
  sending: z.lazy(() => ScheduledMessageCreateFirstDmSending$inboundSchema),
  sent: z.lazy(() => ScheduledMessageCreateFirstDmSent$inboundSchema),
  failed: z.lazy(() => ScheduledMessageCreateFirstDmFailed$inboundSchema),
  cancelled: z.lazy(() => ScheduledMessageCreateFirstDmCancelled$inboundSchema),
});

export function scheduledMessageCreateFirstDmUnionFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateFirstDmUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ScheduledMessageCreateFirstDmUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateFirstDmUnion' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageInviteStatus$inboundSchema: z.ZodMiniType<
  ScheduledMessageInviteStatus,
  unknown
> = openEnums.inboundSchema(ScheduledMessageInviteStatus);

/** @internal */
export const ScheduledMessageCreateInvite$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateInvite,
  unknown
> = z.object({
  status: ScheduledMessageInviteStatus$inboundSchema,
  detail: types.string(),
  clearsItself: types.nullable(types.boolean()),
  fixUrl: types.nullable(types.string()),
});

export function scheduledMessageCreateInviteFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateInvite, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateInvite$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateInvite' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateConnection$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateConnection,
  unknown
> = openEnums.inboundSchema(ScheduledMessageCreateConnection);

/** @internal */
export const ScheduledMessageCreateHold$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateHold,
  unknown
> = z.object({
  reason: types.string(),
  quote: z.optional(z.nullable(types.string())),
});

export function scheduledMessageCreateHoldFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateHold, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateHold$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateHold' from JSON`,
  );
}

/** @internal */
export const ScheduledMessage$inboundSchema: z.ZodMiniType<
  ScheduledMessage,
  unknown
> = z.object({
  id: types.string(),
  contactId: types.string(),
  contactName: z.optional(z.nullable(types.string())),
  contactLinkedinUrl: z.optional(z.nullable(types.string())),
  message: types.string(),
  status: ScheduledMessageStatus$inboundSchema,
  scheduledSendAt: z.optional(z.nullable(types.string())),
  sentAt: types.nullable(types.string()),
  campaignSlug: types.nullable(types.string()),
  errorMessage: z.optional(z.nullable(types.string())),
  createdAt: types.string(),
  contactHeadline: z.optional(z.nullable(types.string())),
  contactCompany: z.optional(z.nullable(types.string())),
  contactLocation: z.optional(z.nullable(types.string())),
  contactDegree: z.optional(z.nullable(types.string())),
  contactProfilePicture: z.optional(z.nullable(types.string())),
  contactHotScore: z.optional(z.nullable(types.number())),
  contactSentDmCount: z.optional(z.nullable(types.number())),
  contactQualificationNotes: z.optional(z.nullable(types.string())),
  contactQualNotes: z.optional(z.nullable(types.string())),
  contactLeadBrief: z.optional(z.nullable(types.string())),
  contactLifecycleStage: z.optional(z.nullable(types.string())),
  messageType: z.optional(z.nullable(types.string())),
  campaignId: z.optional(z.nullable(types.string())),
  campaignName: z.optional(z.nullable(types.string())),
  updatedAt: z.optional(z.nullable(types.string())),
  linkedinCredentialsId: z.optional(z.nullable(types.string())),
  firstDm: types.optional(discriminatedUnion("kind", {
    none: z.lazy(() => ScheduledMessageCreateFirstDmNone$inboundSchema),
    written: z.lazy(() => ScheduledMessageCreateFirstDmWritten$inboundSchema),
    held: z.lazy(() => ScheduledMessageCreateFirstDmHeld$inboundSchema),
    queued: z.lazy(() => ScheduledMessageCreateFirstDmQueued$inboundSchema),
    sending: z.lazy(() => ScheduledMessageCreateFirstDmSending$inboundSchema),
    sent: z.lazy(() => ScheduledMessageCreateFirstDmSent$inboundSchema),
    failed: z.lazy(() => ScheduledMessageCreateFirstDmFailed$inboundSchema),
    cancelled: z.lazy(() =>
      ScheduledMessageCreateFirstDmCancelled$inboundSchema
    ),
  })),
  invite: z.optional(
    z.nullable(z.lazy(() => ScheduledMessageCreateInvite$inboundSchema)),
  ),
  connection: z.optional(
    z.nullable(ScheduledMessageCreateConnection$inboundSchema),
  ),
  hold: z.optional(
    z.nullable(z.lazy(() => ScheduledMessageCreateHold$inboundSchema)),
  ),
});

export function scheduledMessageFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessage, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessage$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessage' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateCredits$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function scheduledMessageCreateCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateCredits' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateMeta$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ScheduledMessageCreateCredits$inboundSchema),
});

export function scheduledMessageCreateMetaFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateMeta' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCreateResponse$inboundSchema: z.ZodMiniType<
  ScheduledMessageCreateResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    scheduledMessage: z.lazy(() => ScheduledMessage$inboundSchema),
    contactName: z.optional(z.nullable(types.string())),
    duplicate: types.optional(types.boolean()),
    cooldown: types.optional(types.boolean()),
    cooldownReason: types.optional(types.string()),
    campaignNote: types.optional(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => ScheduledMessageCreateMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function scheduledMessageCreateResponseFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCreateResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCreateResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCreateResponse' from JSON`,
  );
}

/** @internal */
export type DraftScheduleRequest$Outbound = {
  contactIds?: Array<string> | undefined;
  messageIds?: Array<string> | undefined;
  sendNow?: boolean | undefined;
  scheduledSendAt?: string | undefined;
  editedMessages?: { [k: string]: string } | undefined;
};

/** @internal */
export const DraftScheduleRequest$outboundSchema: z.ZodMiniType<
  DraftScheduleRequest$Outbound,
  DraftScheduleRequest
> = z.object({
  contactIds: z.optional(z.array(z.string())),
  messageIds: z.optional(z.array(z.string())),
  sendNow: z.optional(z.boolean()),
  scheduledSendAt: z.optional(z.string()),
  editedMessages: z.optional(z.record(z.string(), z.string())),
});

export function draftScheduleRequestToJSON(
  draftScheduleRequest: DraftScheduleRequest,
): string {
  return JSON.stringify(
    DraftScheduleRequest$outboundSchema.parse(draftScheduleRequest),
  );
}

/** @internal */
export const DraftScheduleSkipped$inboundSchema: z.ZodMiniType<
  DraftScheduleSkipped,
  unknown
> = z.object({
  contactId: types.string(),
  scheduledMessageId: types.string(),
  reason: types.string(),
});

export function draftScheduleSkippedFromJSON(
  jsonString: string,
): SafeParseResult<DraftScheduleSkipped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DraftScheduleSkipped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DraftScheduleSkipped' from JSON`,
  );
}

/** @internal */
export const DraftScheduleCredits$inboundSchema: z.ZodMiniType<
  DraftScheduleCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function draftScheduleCreditsFromJSON(
  jsonString: string,
): SafeParseResult<DraftScheduleCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DraftScheduleCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DraftScheduleCredits' from JSON`,
  );
}

/** @internal */
export const DraftScheduleMeta$inboundSchema: z.ZodMiniType<
  DraftScheduleMeta,
  unknown
> = z.object({
  credits: z.lazy(() => DraftScheduleCredits$inboundSchema),
});

export function draftScheduleMetaFromJSON(
  jsonString: string,
): SafeParseResult<DraftScheduleMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DraftScheduleMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DraftScheduleMeta' from JSON`,
  );
}

/** @internal */
export const DraftScheduleResponse$inboundSchema: z.ZodMiniType<
  DraftScheduleResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    scheduled: types.number(),
    invitationsScheduled: types.optional(types.number()),
    queued: types.number(),
    requeued: types.number(),
    alreadyQueued: types.number(),
    held: types.number(),
    skipped: z.array(z.lazy(() => DraftScheduleSkipped$inboundSchema)),
    invitesQueued: types.number(),
    accounts: z.array(types.string()),
    note: types.optional(types.string()),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => DraftScheduleMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function draftScheduleResponseFromJSON(
  jsonString: string,
): SafeParseResult<DraftScheduleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DraftScheduleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DraftScheduleResponse' from JSON`,
  );
}

/** @internal */
export type ScheduledMessageUpdateRequest$Outbound = {
  messageId: string;
  message?: string | undefined;
  scheduledSendAt?: string | null | undefined;
};

/** @internal */
export const ScheduledMessageUpdateRequest$outboundSchema: z.ZodMiniType<
  ScheduledMessageUpdateRequest$Outbound,
  ScheduledMessageUpdateRequest
> = z.object({
  messageId: z.string(),
  message: z.optional(z.string()),
  scheduledSendAt: z.optional(z.nullable(z.string())),
});

export function scheduledMessageUpdateRequestToJSON(
  scheduledMessageUpdateRequest: ScheduledMessageUpdateRequest,
): string {
  return JSON.stringify(
    ScheduledMessageUpdateRequest$outboundSchema.parse(
      scheduledMessageUpdateRequest,
    ),
  );
}

/** @internal */
export const ScheduledMessageUpdateIneligible$inboundSchema: z.ZodMiniType<
  ScheduledMessageUpdateIneligible,
  unknown
> = z.object({
  id: types.string(),
  currentStatus: types.string(),
});

export function scheduledMessageUpdateIneligibleFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageUpdateIneligible, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageUpdateIneligible$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageUpdateIneligible' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageUpdateCredits$inboundSchema: z.ZodMiniType<
  ScheduledMessageUpdateCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function scheduledMessageUpdateCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageUpdateCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageUpdateCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageUpdateCredits' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageUpdateMeta$inboundSchema: z.ZodMiniType<
  ScheduledMessageUpdateMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ScheduledMessageUpdateCredits$inboundSchema),
});

export function scheduledMessageUpdateMetaFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageUpdateMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageUpdateMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageUpdateMeta' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageUpdateResponse$inboundSchema: z.ZodMiniType<
  ScheduledMessageUpdateResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    updated: types.number(),
    ineligible: z.array(
      z.lazy(() => ScheduledMessageUpdateIneligible$inboundSchema),
    ),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => ScheduledMessageUpdateMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function scheduledMessageUpdateResponseFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageUpdateResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageUpdateResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageUpdateResponse' from JSON`,
  );
}

/** @internal */
export type ScheduledMessageCancelRequest$Outbound = {
  messageIds?: Array<string> | undefined;
  contactIds?: Array<string> | undefined;
};

/** @internal */
export const ScheduledMessageCancelRequest$outboundSchema: z.ZodMiniType<
  ScheduledMessageCancelRequest$Outbound,
  ScheduledMessageCancelRequest
> = z.object({
  messageIds: z.optional(z.array(z.string())),
  contactIds: z.optional(z.array(z.string())),
});

export function scheduledMessageCancelRequestToJSON(
  scheduledMessageCancelRequest: ScheduledMessageCancelRequest,
): string {
  return JSON.stringify(
    ScheduledMessageCancelRequest$outboundSchema.parse(
      scheduledMessageCancelRequest,
    ),
  );
}

/** @internal */
export const ScheduledMessageCancelCredits$inboundSchema: z.ZodMiniType<
  ScheduledMessageCancelCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function scheduledMessageCancelCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCancelCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCancelCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCancelCredits' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCancelMeta$inboundSchema: z.ZodMiniType<
  ScheduledMessageCancelMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ScheduledMessageCancelCredits$inboundSchema),
});

export function scheduledMessageCancelMetaFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCancelMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCancelMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCancelMeta' from JSON`,
  );
}

/** @internal */
export const ScheduledMessageCancelResponse$inboundSchema: z.ZodMiniType<
  ScheduledMessageCancelResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    cancelled: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => ScheduledMessageCancelMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function scheduledMessageCancelResponseFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledMessageCancelResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledMessageCancelResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledMessageCancelResponse' from JSON`,
  );
}

/** @internal */
export type ReviewDraftsRequest$Outbound = {
  campaignId?: string | undefined;
  approved?: Array<string> | undefined;
  rejected?: Array<string> | undefined;
  editedMessages?: { [k: string]: string } | undefined;
};

/** @internal */
export const ReviewDraftsRequest$outboundSchema: z.ZodMiniType<
  ReviewDraftsRequest$Outbound,
  ReviewDraftsRequest
> = z.object({
  campaignId: z.optional(z.string()),
  approved: z.optional(z.array(z.string())),
  rejected: z.optional(z.array(z.string())),
  editedMessages: z.optional(z.record(z.string(), z.string())),
});

export function reviewDraftsRequestToJSON(
  reviewDraftsRequest: ReviewDraftsRequest,
): string {
  return JSON.stringify(
    ReviewDraftsRequest$outboundSchema.parse(reviewDraftsRequest),
  );
}

/** @internal */
export const Approved$inboundSchema: z.ZodMiniType<Approved, unknown> = z
  .object({
    id: types.string(),
    currentStatus: types.nullable(types.string()),
  });

export function approvedFromJSON(
  jsonString: string,
): SafeParseResult<Approved, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Approved$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Approved' from JSON`,
  );
}

/** @internal */
export const Rejected$inboundSchema: z.ZodMiniType<Rejected, unknown> = z
  .object({
    id: types.string(),
    currentStatus: types.nullable(types.string()),
  });

export function rejectedFromJSON(
  jsonString: string,
): SafeParseResult<Rejected, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Rejected$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Rejected' from JSON`,
  );
}

/** @internal */
export const ReviewDraftsIneligible$inboundSchema: z.ZodMiniType<
  ReviewDraftsIneligible,
  unknown
> = z.object({
  approved: z.array(z.lazy(() => Approved$inboundSchema)),
  rejected: z.array(z.lazy(() => Rejected$inboundSchema)),
});

export function reviewDraftsIneligibleFromJSON(
  jsonString: string,
): SafeParseResult<ReviewDraftsIneligible, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReviewDraftsIneligible$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReviewDraftsIneligible' from JSON`,
  );
}

/** @internal */
export const ReviewDraftsSkipped$inboundSchema: z.ZodMiniType<
  ReviewDraftsSkipped,
  unknown
> = z.object({
  contactId: types.string(),
  scheduledMessageId: types.string(),
  reason: types.string(),
});

export function reviewDraftsSkippedFromJSON(
  jsonString: string,
): SafeParseResult<ReviewDraftsSkipped, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReviewDraftsSkipped$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReviewDraftsSkipped' from JSON`,
  );
}

/** @internal */
export const Line$inboundSchema: z.ZodMiniType<Line, unknown> = z.object({
  queued: types.number(),
  requeued: types.number(),
  alreadyQueued: types.number(),
  held: types.number(),
  skipped: z.array(z.lazy(() => ReviewDraftsSkipped$inboundSchema)),
  invitesQueued: types.number(),
  accounts: z.array(types.string()),
});

export function lineFromJSON(
  jsonString: string,
): SafeParseResult<Line, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Line$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Line' from JSON`,
  );
}

/** @internal */
export const LaneBlocked$inboundSchema: z.ZodMiniType<LaneBlocked, unknown> = z
  .object({
    id: types.string(),
    messageType: types.string(),
    task: types.string(),
  });

export function laneBlockedFromJSON(
  jsonString: string,
): SafeParseResult<LaneBlocked, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LaneBlocked$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LaneBlocked' from JSON`,
  );
}

/** @internal */
export const ReviewDraftsResponse$inboundSchema: z.ZodMiniType<
  ReviewDraftsResponse,
  unknown
> = z.object({
  success: types.literal(true),
  approved: types.number(),
  rejected: types.number(),
  ineligible: z.lazy(() => ReviewDraftsIneligible$inboundSchema),
  invitationsApproved: types.number(),
  line: types.nullable(z.lazy(() => Line$inboundSchema)),
  laneBlocked: z.array(z.lazy(() => LaneBlocked$inboundSchema)),
});

export function reviewDraftsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ReviewDraftsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ReviewDraftsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ReviewDraftsResponse' from JSON`,
  );
}

/** @internal */
export type ContextListRequest$Outbound = {
  scope?: string | undefined;
  type?: string | undefined;
  limit: number;
  offset: number;
  fullContent: boolean;
};

/** @internal */
export const ContextListRequest$outboundSchema: z.ZodMiniType<
  ContextListRequest$Outbound,
  ContextListRequest
> = z.object({
  scope: z.optional(z.string()),
  type: z.optional(z.string()),
  limit: z._default(z.int(), 20),
  offset: z._default(z.int(), 0),
  fullContent: z._default(z.boolean(), false),
});

export function contextListRequestToJSON(
  contextListRequest: ContextListRequest,
): string {
  return JSON.stringify(
    ContextListRequest$outboundSchema.parse(contextListRequest),
  );
}

/** @internal */
export const ContextListEntry$inboundSchema: z.ZodMiniType<
  ContextListEntry,
  unknown
> = z.object({
  id: types.string(),
  type: types.string(),
  label: types.nullable(types.string()),
  content: types.string(),
  contentPreview: types.optional(types.string()),
  scope: types.string(),
  updatedAt: types.string(),
});

export function contextListEntryFromJSON(
  jsonString: string,
): SafeParseResult<ContextListEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextListEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextListEntry' from JSON`,
  );
}

/** @internal */
export const ContextListCredits$inboundSchema: z.ZodMiniType<
  ContextListCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function contextListCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ContextListCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextListCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextListCredits' from JSON`,
  );
}

/** @internal */
export const ContextListMeta$inboundSchema: z.ZodMiniType<
  ContextListMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ContextListCredits$inboundSchema),
});

export function contextListMetaFromJSON(
  jsonString: string,
): SafeParseResult<ContextListMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextListMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextListMeta' from JSON`,
  );
}

/** @internal */
export const ContextListResponse$inboundSchema: z.ZodMiniType<
  ContextListResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    entries: z.array(z.lazy(() => ContextListEntry$inboundSchema)),
    total: types.number(),
    hasMore: types.boolean(),
    offset: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ContextListMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function contextListResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContextListResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextListResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextListResponse' from JSON`,
  );
}

/** @internal */
export type ContextSetRequest$Outbound = {
  type: string;
  content?: string | undefined;
  scope?: string | undefined;
  label?: string | undefined;
  postTicketId?: string | undefined;
};

/** @internal */
export const ContextSetRequest$outboundSchema: z.ZodMiniType<
  ContextSetRequest$Outbound,
  ContextSetRequest
> = z.object({
  type: z.string(),
  content: z.optional(z.string()),
  scope: z.optional(z.string()),
  label: z.optional(z.string()),
  postTicketId: z.optional(z.string()),
});

export function contextSetRequestToJSON(
  contextSetRequest: ContextSetRequest,
): string {
  return JSON.stringify(
    ContextSetRequest$outboundSchema.parse(contextSetRequest),
  );
}

/** @internal */
export const ContextSetEntry$inboundSchema: z.ZodMiniType<
  ContextSetEntry,
  unknown
> = z.object({
  id: types.string(),
  type: types.string(),
  label: types.nullable(types.string()),
  content: types.string(),
  contentPreview: types.optional(types.string()),
  scope: types.string(),
  updatedAt: types.string(),
});

export function contextSetEntryFromJSON(
  jsonString: string,
): SafeParseResult<ContextSetEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextSetEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextSetEntry' from JSON`,
  );
}

/** @internal */
export const ContextSetCredits$inboundSchema: z.ZodMiniType<
  ContextSetCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function contextSetCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ContextSetCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextSetCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextSetCredits' from JSON`,
  );
}

/** @internal */
export const ContextSetMeta$inboundSchema: z.ZodMiniType<
  ContextSetMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ContextSetCredits$inboundSchema),
});

export function contextSetMetaFromJSON(
  jsonString: string,
): SafeParseResult<ContextSetMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextSetMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextSetMeta' from JSON`,
  );
}

/** @internal */
export const ContextSetResponse$inboundSchema: z.ZodMiniType<
  ContextSetResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    entry: z.lazy(() => ContextSetEntry$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ContextSetMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function contextSetResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContextSetResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextSetResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextSetResponse' from JSON`,
  );
}

/** @internal */
export type ContextDeleteRequest$Outbound = {
  type: string;
  scope?: string | undefined;
};

/** @internal */
export const ContextDeleteRequest$outboundSchema: z.ZodMiniType<
  ContextDeleteRequest$Outbound,
  ContextDeleteRequest
> = z.object({
  type: z.string(),
  scope: z.optional(z.string()),
});

export function contextDeleteRequestToJSON(
  contextDeleteRequest: ContextDeleteRequest,
): string {
  return JSON.stringify(
    ContextDeleteRequest$outboundSchema.parse(contextDeleteRequest),
  );
}

/** @internal */
export const ContextDeleteCredits$inboundSchema: z.ZodMiniType<
  ContextDeleteCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function contextDeleteCreditsFromJSON(
  jsonString: string,
): SafeParseResult<ContextDeleteCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextDeleteCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextDeleteCredits' from JSON`,
  );
}

/** @internal */
export const ContextDeleteMeta$inboundSchema: z.ZodMiniType<
  ContextDeleteMeta,
  unknown
> = z.object({
  credits: z.lazy(() => ContextDeleteCredits$inboundSchema),
});

export function contextDeleteMetaFromJSON(
  jsonString: string,
): SafeParseResult<ContextDeleteMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextDeleteMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextDeleteMeta' from JSON`,
  );
}

/** @internal */
export const ContextDeleteResponse$inboundSchema: z.ZodMiniType<
  ContextDeleteResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => ContextDeleteMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function contextDeleteResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContextDeleteResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextDeleteResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextDeleteResponse' from JSON`,
  );
}

/** @internal */
export type EventsFeedRequest$Outbound = {
  since?: string | undefined;
  limit: number;
};

/** @internal */
export const EventsFeedRequest$outboundSchema: z.ZodMiniType<
  EventsFeedRequest$Outbound,
  EventsFeedRequest
> = z.object({
  since: z.optional(z.string()),
  limit: z._default(z.int(), 20),
});

export function eventsFeedRequestToJSON(
  eventsFeedRequest: EventsFeedRequest,
): string {
  return JSON.stringify(
    EventsFeedRequest$outboundSchema.parse(eventsFeedRequest),
  );
}

/** @internal */
export const EventsFeedType$inboundSchema: z.ZodMiniType<
  EventsFeedType,
  unknown
> = openEnums.inboundSchema(EventsFeedType);

/** @internal */
export const Event$inboundSchema: z.ZodMiniType<Event, unknown> = z.object({
  id: types.string(),
  type: EventsFeedType$inboundSchema,
  campaignId: types.optional(types.string()),
  campaignName: types.optional(types.string()),
  summary: types.string(),
  metadata: z.record(z.string(), z.any()),
  timestamp: types.number(),
});

export function eventFromJSON(
  jsonString: string,
): SafeParseResult<Event, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Event$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Event' from JSON`,
  );
}

/** @internal */
export const EventsFeedResponse$inboundSchema: z.ZodMiniType<
  EventsFeedResponse,
  unknown
> = z.object({
  events: z.array(z.lazy(() => Event$inboundSchema)),
  cursor: types.optional(types.number()),
});

export function eventsFeedResponseFromJSON(
  jsonString: string,
): SafeParseResult<EventsFeedResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EventsFeedResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EventsFeedResponse' from JSON`,
  );
}

/** @internal */
export const Context$inboundSchema: z.ZodMiniType<Context, unknown> = z.object({
  type: types.string(),
  label: types.nullable(types.string()),
  content: types.string(),
  scope: types.string(),
});

export function contextFromJSON(
  jsonString: string,
): SafeParseResult<Context, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Context$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Context' from JSON`,
  );
}

/** @internal */
export const ActiveAccountStatus$inboundSchema: z.ZodMiniType<
  ActiveAccountStatus,
  unknown
> = openEnums.inboundSchema(ActiveAccountStatus);

/** @internal */
export const ActiveAccount$inboundSchema: z.ZodMiniType<
  ActiveAccount,
  unknown
> = z.object({
  id: types.string(),
  name: types.nullable(types.string()),
  plan: types.string(),
  headline: types.nullable(types.string()),
  status: ActiveAccountStatus$inboundSchema,
  isUnlimited: types.boolean(),
  creditsLimit: types.number(),
  creditsCount: types.number(),
  simulateWrites: types.boolean(),
  isCurrent: types.boolean(),
});

export function activeAccountFromJSON(
  jsonString: string,
): SafeParseResult<ActiveAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActiveAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActiveAccount' from JSON`,
  );
}

/** @internal */
export const ContextGetAccountStatus$inboundSchema: z.ZodMiniType<
  ContextGetAccountStatus,
  unknown
> = openEnums.inboundSchema(ContextGetAccountStatus);

/** @internal */
export const ContextGetAccount$inboundSchema: z.ZodMiniType<
  ContextGetAccount,
  unknown
> = z.object({
  id: types.string(),
  name: types.nullable(types.string()),
  plan: types.string(),
  headline: types.nullable(types.string()),
  status: ContextGetAccountStatus$inboundSchema,
  isUnlimited: types.boolean(),
  creditsLimit: types.number(),
  creditsCount: types.number(),
  simulateWrites: types.boolean(),
  isCurrent: types.boolean(),
});

export function contextGetAccountFromJSON(
  jsonString: string,
): SafeParseResult<ContextGetAccount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextGetAccount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextGetAccount' from JSON`,
  );
}

/** @internal */
export const ContextGetStageCounts$inboundSchema: z.ZodMiniType<
  ContextGetStageCounts,
  unknown
> = z.object({
  contact: types.number(),
  lead: types.number(),
  qualified: types.number(),
  rejected: types.number(),
  total: types.number(),
});

export function contextGetStageCountsFromJSON(
  jsonString: string,
): SafeParseResult<ContextGetStageCounts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextGetStageCounts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextGetStageCounts' from JSON`,
  );
}

/** @internal */
export const ActiveCampaign$inboundSchema: z.ZodMiniType<
  ActiveCampaign,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
  stageCounts: z.lazy(() => ContextGetStageCounts$inboundSchema),
});

export function activeCampaignFromJSON(
  jsonString: string,
): SafeParseResult<ActiveCampaign, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActiveCampaign$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActiveCampaign' from JSON`,
  );
}

/** @internal */
export const ContextGetType$inboundSchema: z.ZodMiniType<
  ContextGetType,
  unknown
> = openEnums.inboundSchema(ContextGetType);

/** @internal */
export const RecentEvent$inboundSchema: z.ZodMiniType<RecentEvent, unknown> = z
  .object({
    id: types.string(),
    type: ContextGetType$inboundSchema,
    campaignId: types.optional(types.string()),
    campaignName: types.optional(types.string()),
    summary: types.string(),
    metadata: z.record(z.string(), z.any()),
    timestamp: types.number(),
  });

export function recentEventFromJSON(
  jsonString: string,
): SafeParseResult<RecentEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecentEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecentEvent' from JSON`,
  );
}

/** @internal */
export const ContextGetResponse$inboundSchema: z.ZodMiniType<
  ContextGetResponse,
  unknown
> = z.object({
  credits: z.optional(z.nullable(z.any())),
  pipeline: z.record(z.string(), types.number()),
  contexts: z.array(z.lazy(() => Context$inboundSchema)),
  pendingDrafts: types.number(),
  failedDrafts: types.number(),
  pendingSentInvitations: types.number(),
  activeAccount: types.nullable(z.lazy(() => ActiveAccount$inboundSchema)),
  accounts: z.array(z.lazy(() => ContextGetAccount$inboundSchema)),
  activeCampaigns: z.array(z.lazy(() => ActiveCampaign$inboundSchema)),
  sessionMeta: z.optional(z.nullable(z.any())),
  onboardingState: z.optional(z.nullable(z.any())),
  recentEvents: z.array(z.lazy(() => RecentEvent$inboundSchema)),
  llmStatus: z.optional(z.nullable(types.string())),
  llmFirstTrippedAt: z.optional(z.nullable(types.number())),
});

export function contextGetResponseFromJSON(
  jsonString: string,
): SafeParseResult<ContextGetResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ContextGetResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ContextGetResponse' from JSON`,
  );
}

/** @internal */
export const GetDmPollingSettingsSettings$inboundSchema: z.ZodMiniType<
  GetDmPollingSettingsSettings,
  unknown
> = z.object({
  dmWebhookUrl: types.nullable(types.string()),
  connectionLastPolledAt: types.nullable(types.string()),
});

export function getDmPollingSettingsSettingsFromJSON(
  jsonString: string,
): SafeParseResult<GetDmPollingSettingsSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDmPollingSettingsSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDmPollingSettingsSettings' from JSON`,
  );
}

/** @internal */
export const GetDmPollingSettingsCredits$inboundSchema: z.ZodMiniType<
  GetDmPollingSettingsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function getDmPollingSettingsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<GetDmPollingSettingsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDmPollingSettingsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDmPollingSettingsCredits' from JSON`,
  );
}

/** @internal */
export const GetDmPollingSettingsMeta$inboundSchema: z.ZodMiniType<
  GetDmPollingSettingsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => GetDmPollingSettingsCredits$inboundSchema),
});

export function getDmPollingSettingsMetaFromJSON(
  jsonString: string,
): SafeParseResult<GetDmPollingSettingsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDmPollingSettingsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDmPollingSettingsMeta' from JSON`,
  );
}

/** @internal */
export const GetDmPollingSettingsResponse$inboundSchema: z.ZodMiniType<
  GetDmPollingSettingsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    settings: z.lazy(() => GetDmPollingSettingsSettings$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(z.lazy(() => GetDmPollingSettingsMeta$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function getDmPollingSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetDmPollingSettingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetDmPollingSettingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetDmPollingSettingsResponse' from JSON`,
  );
}

/** @internal */
export type PatchDmPollingSettingsRequest$Outbound = {
  dmWebhookUrl?: string | null | undefined;
};

/** @internal */
export const PatchDmPollingSettingsRequest$outboundSchema: z.ZodMiniType<
  PatchDmPollingSettingsRequest$Outbound,
  PatchDmPollingSettingsRequest
> = z.object({
  dmWebhookUrl: z.optional(z.nullable(z.string())),
});

export function patchDmPollingSettingsRequestToJSON(
  patchDmPollingSettingsRequest: PatchDmPollingSettingsRequest,
): string {
  return JSON.stringify(
    PatchDmPollingSettingsRequest$outboundSchema.parse(
      patchDmPollingSettingsRequest,
    ),
  );
}

/** @internal */
export const PatchDmPollingSettingsSettings$inboundSchema: z.ZodMiniType<
  PatchDmPollingSettingsSettings,
  unknown
> = z.object({
  dmWebhookUrl: types.nullable(types.string()),
  connectionLastPolledAt: types.nullable(types.string()),
});

export function patchDmPollingSettingsSettingsFromJSON(
  jsonString: string,
): SafeParseResult<PatchDmPollingSettingsSettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchDmPollingSettingsSettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchDmPollingSettingsSettings' from JSON`,
  );
}

/** @internal */
export const PatchDmPollingSettingsCredits$inboundSchema: z.ZodMiniType<
  PatchDmPollingSettingsCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function patchDmPollingSettingsCreditsFromJSON(
  jsonString: string,
): SafeParseResult<PatchDmPollingSettingsCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchDmPollingSettingsCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchDmPollingSettingsCredits' from JSON`,
  );
}

/** @internal */
export const PatchDmPollingSettingsMeta$inboundSchema: z.ZodMiniType<
  PatchDmPollingSettingsMeta,
  unknown
> = z.object({
  credits: z.lazy(() => PatchDmPollingSettingsCredits$inboundSchema),
});

export function patchDmPollingSettingsMetaFromJSON(
  jsonString: string,
): SafeParseResult<PatchDmPollingSettingsMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchDmPollingSettingsMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchDmPollingSettingsMeta' from JSON`,
  );
}

/** @internal */
export const PatchDmPollingSettingsResponse$inboundSchema: z.ZodMiniType<
  PatchDmPollingSettingsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    settings: z.lazy(() => PatchDmPollingSettingsSettings$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => PatchDmPollingSettingsMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function patchDmPollingSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<PatchDmPollingSettingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PatchDmPollingSettingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PatchDmPollingSettingsResponse' from JSON`,
  );
}

/** @internal */
export type DeleteWorkspaceAccountRequest$Outbound = {
  id: string;
};

/** @internal */
export const DeleteWorkspaceAccountRequest$outboundSchema: z.ZodMiniType<
  DeleteWorkspaceAccountRequest$Outbound,
  DeleteWorkspaceAccountRequest
> = z.object({
  id: z.string(),
});

export function deleteWorkspaceAccountRequestToJSON(
  deleteWorkspaceAccountRequest: DeleteWorkspaceAccountRequest,
): string {
  return JSON.stringify(
    DeleteWorkspaceAccountRequest$outboundSchema.parse(
      deleteWorkspaceAccountRequest,
    ),
  );
}

/** @internal */
export const DeleteWorkspaceAccountCredits$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceAccountCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function deleteWorkspaceAccountCreditsFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceAccountCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceAccountCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceAccountCredits' from JSON`,
  );
}

/** @internal */
export const DeleteWorkspaceAccountMeta$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceAccountMeta,
  unknown
> = z.object({
  credits: z.lazy(() => DeleteWorkspaceAccountCredits$inboundSchema),
});

export function deleteWorkspaceAccountMetaFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceAccountMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceAccountMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceAccountMeta' from JSON`,
  );
}

/** @internal */
export const DeleteWorkspaceAccountResponse$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceAccountResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => DeleteWorkspaceAccountMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function deleteWorkspaceAccountResponseFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceAccountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceAccountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceAccountResponse' from JSON`,
  );
}

/** @internal */
export const UpgradeWorkspaceAccountAction$outboundSchema: z.ZodMiniEnum<
  typeof UpgradeWorkspaceAccountAction
> = z.enum(UpgradeWorkspaceAccountAction);

/** @internal */
export type UpgradeWorkspaceAccountRequest$Outbound = {
  credentialsId: string;
  action: string;
};

/** @internal */
export const UpgradeWorkspaceAccountRequest$outboundSchema: z.ZodMiniType<
  UpgradeWorkspaceAccountRequest$Outbound,
  UpgradeWorkspaceAccountRequest
> = z.object({
  credentialsId: z.string(),
  action: UpgradeWorkspaceAccountAction$outboundSchema,
});

export function upgradeWorkspaceAccountRequestToJSON(
  upgradeWorkspaceAccountRequest: UpgradeWorkspaceAccountRequest,
): string {
  return JSON.stringify(
    UpgradeWorkspaceAccountRequest$outboundSchema.parse(
      upgradeWorkspaceAccountRequest,
    ),
  );
}

/** @internal */
export const UpgradeWorkspaceAccountCredits$inboundSchema: z.ZodMiniType<
  UpgradeWorkspaceAccountCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function upgradeWorkspaceAccountCreditsFromJSON(
  jsonString: string,
): SafeParseResult<UpgradeWorkspaceAccountCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpgradeWorkspaceAccountCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpgradeWorkspaceAccountCredits' from JSON`,
  );
}

/** @internal */
export const UpgradeWorkspaceAccountMeta$inboundSchema: z.ZodMiniType<
  UpgradeWorkspaceAccountMeta,
  unknown
> = z.object({
  credits: z.lazy(() => UpgradeWorkspaceAccountCredits$inboundSchema),
});

export function upgradeWorkspaceAccountMetaFromJSON(
  jsonString: string,
): SafeParseResult<UpgradeWorkspaceAccountMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpgradeWorkspaceAccountMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpgradeWorkspaceAccountMeta' from JSON`,
  );
}

/** @internal */
export const UpgradeWorkspaceAccountResponse$inboundSchema: z.ZodMiniType<
  UpgradeWorkspaceAccountResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    message: types.string(),
    seatsUsed: types.number(),
    seatsIncluded: types.number(),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => UpgradeWorkspaceAccountMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function upgradeWorkspaceAccountResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpgradeWorkspaceAccountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpgradeWorkspaceAccountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpgradeWorkspaceAccountResponse' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteAction$outboundSchema: z.ZodMiniEnum<
  typeof CreateWorkspaceInviteAction
> = z.enum(CreateWorkspaceInviteAction);

/** @internal */
export type CreateWorkspaceInviteRequest$Outbound = {
  action: string;
  email?: string | undefined;
  name?: string | undefined;
  maxUses: number;
};

/** @internal */
export const CreateWorkspaceInviteRequest$outboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteRequest$Outbound,
  CreateWorkspaceInviteRequest
> = z.object({
  action: CreateWorkspaceInviteAction$outboundSchema,
  email: z.optional(z.string()),
  name: z.optional(z.string()),
  maxUses: z._default(z.int(), 1),
});

export function createWorkspaceInviteRequestToJSON(
  createWorkspaceInviteRequest: CreateWorkspaceInviteRequest,
): string {
  return JSON.stringify(
    CreateWorkspaceInviteRequest$outboundSchema.parse(
      createWorkspaceInviteRequest,
    ),
  );
}

/** @internal */
export const CreateWorkspaceInviteInvite2$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteInvite2,
  unknown
> = z.object({
  id: types.string(),
  email: types.nullable(types.string()),
  name: types.nullable(types.string()),
  code: types.string(),
  status: types.optional(types.string()),
  maxUses: types.number(),
  useCount: types.number(),
  expiresAt: types.nullable(types.string()),
  createdAt: types.string(),
});

export function createWorkspaceInviteInvite2FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteInvite2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteInvite2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteInvite2' from JSON`,
  );
}

/** @internal */
export const Workspace$inboundSchema: z.ZodMiniType<Workspace, unknown> = z
  .object({
    tier: types.string(),
    proSeatsIncluded: types.number(),
    proSeatsUsed: types.number(),
  });

export function workspaceFromJSON(
  jsonString: string,
): SafeParseResult<Workspace, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Workspace$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Workspace' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteCredits2$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteCredits2,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function createWorkspaceInviteCredits2FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteCredits2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteCredits2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteCredits2' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteMeta2$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteMeta2,
  unknown
> = z.object({
  credits: z.lazy(() => CreateWorkspaceInviteCredits2$inboundSchema),
});

export function createWorkspaceInviteMeta2FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteMeta2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteMeta2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteMeta2' from JSON`,
  );
}

/** @internal */
export const ResponseBody2$inboundSchema: z.ZodMiniType<
  ResponseBody2,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    invites: z.array(z.lazy(() => CreateWorkspaceInviteInvite2$inboundSchema)),
    workspace: z.lazy(() => Workspace$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => CreateWorkspaceInviteMeta2$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function responseBody2FromJSON(
  jsonString: string,
): SafeParseResult<ResponseBody2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseBody2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseBody2' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteInvite1$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteInvite1,
  unknown
> = z.object({
  id: types.string(),
  email: types.nullable(types.string()),
  name: types.nullable(types.string()),
  code: types.string(),
  status: types.optional(types.string()),
  maxUses: types.number(),
  useCount: types.number(),
  expiresAt: types.nullable(types.string()),
  createdAt: types.string(),
});

export function createWorkspaceInviteInvite1FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteInvite1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteInvite1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteInvite1' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteCredits1$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteCredits1,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function createWorkspaceInviteCredits1FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteCredits1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteCredits1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteCredits1' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteMeta1$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteMeta1,
  unknown
> = z.object({
  credits: z.lazy(() => CreateWorkspaceInviteCredits1$inboundSchema),
});

export function createWorkspaceInviteMeta1FromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteMeta1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteMeta1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteMeta1' from JSON`,
  );
}

/** @internal */
export const ResponseBody1$inboundSchema: z.ZodMiniType<
  ResponseBody1,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    invite: z.lazy(() => CreateWorkspaceInviteInvite1$inboundSchema),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => CreateWorkspaceInviteMeta1$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function responseBody1FromJSON(
  jsonString: string,
): SafeParseResult<ResponseBody1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseBody1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseBody1' from JSON`,
  );
}

/** @internal */
export const CreateWorkspaceInviteResponse$inboundSchema: z.ZodMiniType<
  CreateWorkspaceInviteResponse,
  unknown
> = smartUnion([
  z.lazy(() => ResponseBody2$inboundSchema),
  z.lazy(() => ResponseBody1$inboundSchema),
]);

export function createWorkspaceInviteResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateWorkspaceInviteResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateWorkspaceInviteResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateWorkspaceInviteResponse' from JSON`,
  );
}

/** @internal */
export type DeleteWorkspaceInviteRequest$Outbound = {
  inviteId: string;
};

/** @internal */
export const DeleteWorkspaceInviteRequest$outboundSchema: z.ZodMiniType<
  DeleteWorkspaceInviteRequest$Outbound,
  DeleteWorkspaceInviteRequest
> = z.object({
  inviteId: z.string(),
});

export function deleteWorkspaceInviteRequestToJSON(
  deleteWorkspaceInviteRequest: DeleteWorkspaceInviteRequest,
): string {
  return JSON.stringify(
    DeleteWorkspaceInviteRequest$outboundSchema.parse(
      deleteWorkspaceInviteRequest,
    ),
  );
}

/** @internal */
export const DeleteWorkspaceInviteCredits$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceInviteCredits,
  unknown
> = z.object({
  current: types.number(),
  limit: types.nullable(types.number()),
  remaining: types.nullable(types.number()),
  percentage: types.number(),
  isUnlimited: types.boolean(),
});

export function deleteWorkspaceInviteCreditsFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceInviteCredits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceInviteCredits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceInviteCredits' from JSON`,
  );
}

/** @internal */
export const DeleteWorkspaceInviteMeta$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceInviteMeta,
  unknown
> = z.object({
  credits: z.lazy(() => DeleteWorkspaceInviteCredits$inboundSchema),
});

export function deleteWorkspaceInviteMetaFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceInviteMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceInviteMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceInviteMeta' from JSON`,
  );
}

/** @internal */
export const DeleteWorkspaceInviteResponse$inboundSchema: z.ZodMiniType<
  DeleteWorkspaceInviteResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.literal(true),
    creditsUsed: types.number(),
    retryAfter: types.number(),
    _meta: types.optional(
      z.lazy(() => DeleteWorkspaceInviteMeta$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_meta": "meta",
    });
  }),
);

export function deleteWorkspaceInviteResponseFromJSON(
  jsonString: string,
): SafeParseResult<DeleteWorkspaceInviteResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeleteWorkspaceInviteResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeleteWorkspaceInviteResponse' from JSON`,
  );
}
