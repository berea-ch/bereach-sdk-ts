/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinChatSearchConversations } from "../../funcs/linkedinChatSearchConversations.js";
import { SearchLinkedInConversationsRequest$zodSchema } from "../../models/searchlinkedinconversationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SearchLinkedInConversationsRequest$zodSchema,
};

export const tool$linkedinChatSearchConversations: ToolDefinition<typeof args> =
  {
    name: "linkedin-chat-search-conversations",
    description: `Search LinkedIn conversations

Search inbox conversations by keyword. Returns matching conversations with participants and last message. 0 credits.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await linkedinChatSearchConversations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
