/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { campaignsFilter } from "../../funcs/campaignsFilter.js";
import { FilterCampaignRequest$zodSchema } from "../../models/filtercampaignop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FilterCampaignRequest$zodSchema,
};

export const tool$campaignsFilter: ToolDefinition<typeof args> = {
  name: "campaigns-filter",
  description: `Check if campaign actions are completed

Check if all provided actionSlugs are completed for a given campaignSlug. Used by n8n workflows to determine if a flow should be skipped.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await campaignsFilter(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
