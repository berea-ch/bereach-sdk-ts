/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinScrapersCollectCommentReplies } from "../../funcs/linkedinScrapersCollectCommentReplies.js";
import { CollectLinkedInCommentRepliesRequest$zodSchema } from "../../models/collectlinkedincommentrepliesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CollectLinkedInCommentRepliesRequest$zodSchema,
};

export const tool$linkedinScrapersCollectCommentReplies: ToolDefinition<
  typeof args
> = {
  name: "linkedin-scrapers-collect-comment-replies",
  description: `Scrape replies to a LinkedIn comment

Returns paginated replies for a specific LinkedIn comment. Use the commentUrn from the comments endpoint response.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinScrapersCollectCommentReplies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
