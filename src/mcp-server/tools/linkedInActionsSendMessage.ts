/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedInActionsSendMessage } from "../../funcs/linkedInActionsSendMessage.js";
import { SendLinkedInMessageRequest$zodSchema } from "../../models/sendlinkedinmessageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SendLinkedInMessageRequest$zodSchema,
};

export const tool$linkedInActionsSendMessage: ToolDefinition<typeof args> = {
  name: "linked-in-actions-send-message",
  description: `Send LinkedIn message

Authenticates the requester, validates LinkedIn credentials, and sends a message to the specified LinkedIn recipient. Rate limited to 80 messages per day per user.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedInActionsSendMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
