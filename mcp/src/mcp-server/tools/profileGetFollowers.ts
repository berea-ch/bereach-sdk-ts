/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { profileGetFollowers } from "../../funcs/profileGetFollowers.js";
import { GetMyLinkedInFollowersRequest$zodSchema } from "../../models/getmylinkedinfollowersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMyLinkedInFollowersRequest$zodSchema.optional(),
};

export const tool$profileGetFollowers: ToolDefinition<typeof args> = {
  name: "profile-get-followers",
  description: `Get authenticated user's LinkedIn followers

Returns a paginated list of the authenticated user's LinkedIn followers. LinkedIn caps visible results at ~1 000. No credits consumed. Requires valid LinkedIn credentials.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await profileGetFollowers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
