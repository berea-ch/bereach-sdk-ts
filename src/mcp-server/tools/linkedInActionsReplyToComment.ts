/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedInActionsReplyToComment } from "../../funcs/linkedInActionsReplyToComment.js";
import { ReplyToLinkedInCommentRequest$zodSchema } from "../../models/replytolinkedincommentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReplyToLinkedInCommentRequest$zodSchema,
};

export const tool$linkedInActionsReplyToComment: ToolDefinition<typeof args> = {
  name: "linked-in-actions-reply-to-comment",
  description: `Reply to a LinkedIn comment

Authenticates the requester, validates LinkedIn credentials, and sends a reply to the specified LinkedIn comment.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedInActionsReplyToComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
