/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinScrapersVisitProfile } from "../../funcs/linkedinScrapersVisitProfile.js";
import { VisitLinkedInProfileRequest$zodSchema } from "../../models/visitlinkedinprofileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VisitLinkedInProfileRequest$zodSchema,
};

export const tool$linkedinScrapersVisitProfile: ToolDefinition<typeof args> = {
  name: "linkedin-scrapers-visit-profile",
  description: `Visit LinkedIn profile and extract contact data

Visit a LinkedIn profile and return contact data. Distance-1 profiles cached 24h. No dedup — always executes. campaignSlug is for tracking only.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinScrapersVisitProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
