/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkedinChatGetMessages } from "../../funcs/linkedinChatGetMessages.js";
import { GetConversationMessagesRequest$zodSchema } from "../../models/getconversationmessagesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetConversationMessagesRequest$zodSchema,
};

export const tool$linkedinChatGetMessages: ToolDefinition<typeof args> = {
  name: "linkedin-chat-get-messages",
  description: `Read messages from a conversation

Fetch messages from a specific LinkedIn conversation. 0 credits.

Pass the full \`conversationUrn\` as returned by \`/chats/linkedin\` or \`/chats/linkedin/search\`. No parsing required — the server handles extraction internally.

Example: \`{ "conversationUrn": "urn:li:msg_conversation:(urn:li:fsd_profile:ACoAAXXX,2-YWUx...)" }\``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await linkedinChatGetMessages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
